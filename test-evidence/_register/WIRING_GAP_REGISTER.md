# WIRING GAP REGISTER

Central register of every wiring/integration gap found during the audit program. Every finding
row must point at real evidence under `test-evidence/` (a saved command transcript, a JSON
response body, a screenshot, a query result file, etc.) and must include a reproduction command
or step sequence a future session can re-run to confirm the finding still holds. Do not add a row
without a corresponding evidence file already committed under `test-evidence/`.

## Severity legend

| Severity | Meaning |
|---|---|
| P0 | Confirmed broken in production / live data path. Blocks a real user-facing flow or silently corrupts/loses data. Fix immediately, no exceptions. |
| P1 | Confirmed broken but scoped/contained (single feature, single org, degrades gracefully, or has a working manual workaround). Fix this cycle. |
| P2 | Real gap but low blast radius (rare code path, admin-only, cosmetic, or affects a deprecated/low-traffic feature). Fix opportunistically. |
| P3 | Unverified suspicion, code-smell, or a gap that needs a product decision before it can be classified as a bug. Track, do not fix blind. |

## Scope tags

| Tag | Meaning |
|---|---|
| CONFIRMED-BROKEN | Live-verified against real data/a real request; the gap reproduces every time. |
| UNVERIFIED | Plausible from a code read, but not yet exercised against real data/a real request. |
| PENDING-SCOPE | Real gap, but whether it's in-scope for this audit program (vs. a separate task) hasn't been decided yet. |
| CONFIRMED-OK | Investigated and found NOT to be a gap — code path verified working as designed. Kept in the register so the question isn't re-asked. |
| RESOLVED | Was CONFIRMED-BROKEN; a fix has been committed and live-reverified against real data/a real request. Kept in the register (not deleted) so the history isn't lost. |

## Findings

| ID | Layer | Severity | Finding | Evidence Path | Reproduction | Scope Tag |
|---|---|---|---|---|---|---|
| WGR-001 | Build/Infra | P3 | `next.config.mjs` `experimental.cpus` build-worker cap (PT-00-002 check): investigated, found PRESENT (`cpus: 1` at `next.config.mjs:35`), with an in-file comment describing the exact multi-worktree memory-thrash / hung-build-past-timeout failure mode this check exists to catch. Not a gap — logged for traceability so this isn't re-checked from scratch. | `test-evidence/pt-00/build-config.txt`, `test-evidence/pt-00/build-proof.txt` | `rm -rf .next && pnpm run build` (or `node scripts/audit/verify-pt00-002.mjs`) — confirms exit 0, no worker-crash/timeout signature in the output. | CONFIRMED-OK |
| WGR-002 | Config/Deploy | P1 | `STANDING_DIRECTIVES.md` DIRECTIVE-019 deploy-verifier prerequisites (`VERCEL_TOKEN`, `VERCEL_PROJECT_ID`) confirmed absent from local `.env.local`. Previously-known, already-documented gap (DIRECTIVE-019's own text already flags this) — `scripts/verify-deployment.ts` and the FORGE `deploy_verify` gate degrade to PENDING/INDETERMINATE (warn, does not block) rather than a real PASS/FAIL without them. This audit re-confirms the absence still holds; it did NOT check whether these are set in Vercel/Railway's own environment (production env vars are outside the local-`.env.local`-only method used here). | `test-evidence/pt-00/env-audit.json` (`summary.p1_findings`, `vars[]` rows for `VERCEL_TOKEN`/`VERCEL_PROJECT_ID`) | `node scripts/audit/verify-pt00-004.mjs` (prints these 2 under `[P1]`) | CONFIRMED-BROKEN |
| WGR-003 | Config | P2 | 13 production-required env vars confirmed absent from local `.env.local` (breaks real production functionality if also unset there, but not the deploy-verify gate specifically — that's WGR-002): `CREDENTIAL_ENCRYPTION_KEY`, `CRON_SECRET`, `INTEGRATION_ENCRYPTION_KEY`, `INTEGRATION_KEY_SECRET`, `PORTAL_ENCRYPT_SECRET`, `RESEND_API_KEY`, `RESEND_WEBHOOK_SECRET`, `SCRAPER_API_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `SUPABASE_URL`, `UNSUBSCRIBE_HMAC_SECRET`, `WORKER_ID`. Two concrete downstream risks worth flagging specifically: (1) `worker/index.ts`'s `validateEnv()` hard `process.exit(1)`s without `SUPABASE_URL` — the worker needs its own `SUPABASE_URL` distinct from the Next.js app's `NEXT_PUBLIC_SUPABASE_URL` (which IS present), so the worker process cannot boot locally even though the web app's Supabase config looks complete; (2) every one of 16 `/api/cron/*` routes permanently 401s without `CRON_SECRET`, regardless of a real Vercel Cron trigger header. This confirms only local-dev absence — production (Vercel/Railway) env vars were not checked by this audit, so this does not by itself indicate a production outage; worth Reid confirming these are genuinely unset in Vercel/Railway production too, not just locally. | `test-evidence/pt-00/env-audit.json` (`summary.p2_findings`) | `node scripts/audit/verify-pt00-004.mjs` (prints these 13 under `[P2]`) | CONFIRMED-BROKEN |
| WGR-004 | Frontend/Page | P0 | `/documents` page: authenticated Playwright navigation timed out after 30000ms during the PT-00-005 smoke sweep (`navigation failed: page.goto: Timeout 30000ms exceeded.`) — a real hang, not a dynamic-route-probe artifact (this path takes no id substitution). Graded P0, not P1: `/documents` is a literal top-level `NAV_ITEMS` sidebar entry (`src/components/layout/nav-items.ts`), not a sub-page — any authenticated user clicking that sidebar item hits a hang, not a page. Root cause not investigated — out of PT-00 scope (PT-00 is baseline evidence collection, not remediation). | `test-evidence/pt-00/smoke-results.json` (`results[]` where `path === "/documents"`), `test-evidence/pt-00/smoke-failures/documents.png` | Authenticated navigation to `/documents` against a local `pnpm dev` server — see `scripts/audit/pt00-005-smoke-suite.mjs` | CONFIRMED-BROKEN |
| WGR-005 | API | P1 | `GET /api/agents/discovery` returns `500 {"error":"Failed to load discovery matches."}` on an authenticated, no-query-param request during the PT-00-005 smoke sweep. P1 not P0: not consumed by any dashboard page found via repo-wide grep — a backing endpoint with no confirmed primary-nav consumer. Root cause not investigated — out of PT-00 scope. | `test-evidence/pt-00/smoke-results.json` (`results[]` where `path === "/api/agents/discovery"`) | `GET /api/agents/discovery` with a valid authenticated session, no query params. Handler: `src/app/api/agents/discovery/route.ts`. | CONFIRMED-BROKEN |
| WGR-006 | API | P1 | `GET /api/consultant/clients` returns `500 {"error":"Failed to load client access grants.","code":"load_failed"}` on an authenticated, no-query-param request during the PT-00-005 smoke sweep. P1 not P0: backs `/settings/white-label`, a sub-settings page, not primary nav. Root cause not investigated — out of PT-00 scope. | `test-evidence/pt-00/smoke-results.json` (`results[]` where `path === "/api/consultant/clients"`) | `GET /api/consultant/clients` with a valid authenticated session, no query params. Handler: `src/app/api/consultant/clients/route.ts`. | CONFIRMED-BROKEN |
| WGR-007 | API | P1 | `GET /api/outreach/sequences` returns `500 {"error":"Failed to load follow-up sequences.","code":"db_error"}` on an authenticated, no-query-param request during the PT-00-005 smoke sweep. This matches a previously-documented, still-unresolved gap: `STATE_OF_THE_BUILD.md`'s 2026-08-13 "outreach consolidation audit" session already found this route queries a `followup_sequences` table confirmed absent in production (`NEEDS REID'S DECISION` — apply dormant migration 083 vs. retire in favor of AG-28's `application_followups`). This finding re-confirms that gap is still live, not a new regression. | `test-evidence/pt-00/smoke-results.json` (`results[]` where `path === "/api/outreach/sequences"`) | `GET /api/outreach/sequences` with a valid authenticated session, no query params. Handler: `src/app/api/outreach/sequences/route.ts`. | CONFIRMED-BROKEN |
| WGR-008 | API | P1 | `GET /api/schoolfunder` returns `500 {"error":"Failed to load students.","code":"load_failed"}` on an authenticated, no-query-param request during the PT-00-005 smoke sweep. P1 not P0: backs the standalone Faith Foundation showcase feature (`BLUEPRINT.md` §1), not core Benavora SaaS navigation. Root cause not investigated — out of PT-00 scope. | `test-evidence/pt-00/smoke-results.json` (`results[]` where `path === "/api/schoolfunder"`) | `GET /api/schoolfunder` with a valid authenticated session, no query params. Handler: `src/app/api/schoolfunder/route.ts`. | CONFIRMED-BROKEN |
| WGR-009 | API | P1 | `GET /api/settings/notifications` returns `500 {"error":"Failed to load notification preferences.","code":"db_error"}` on an authenticated, no-query-param request during the PT-00-005 smoke sweep. P1 not P0: backs `/settings/notifications`, a sub-settings page, not primary nav. Root cause not investigated — out of PT-00 scope. | `test-evidence/pt-00/smoke-results.json` (`results[]` where `path === "/api/settings/notifications"`) | `GET /api/settings/notifications` with a valid authenticated session, no query params. Handler: `src/app/api/settings/notifications/route.ts`. | CONFIRMED-BROKEN |
| WGR-010 | Frontend/Nav | P3 | Route manifest's `orphanRoutes` cross-reference (pages with no incoming link found by scanning `nav-items.ts`) reports 78 of 146 pages as orphaned. Investigated (PT-00-003 session), found NOT to be a defect: `nav-items.ts`'s own header comment states Dashboard/Research/Opportunities/AutoApply/Draft Generator/Donor Discovery live in `Header.tsx`'s top tab bar by design (confirmed against `Header.tsx`'s `TABS` array — covers 6 of the 78); the rest split into equally explainable categories — marketing pages (`/`, `/pricing`, `/how-it-works`, `/for-consultants`, `/privacy`, `/security`, `/terms`), auth pages (`/login`, `/register`, `/forgot-password`, `/reset-password`), `/settings/*` subsections (reached via `SettingsNav`, not the sidebar), and drilldown/action pages reached by a link from a list page rather than the sidebar (`/applications/new`, `/autoapply/[10+ subsections]`, `/donor-discovery/[8 subsections]`, `/funders/new`, `/funders/import`, etc.). `nav-items.ts` was never meant to be the single index of every reachable page — that role belongs to `GlobalSearch`'s separate `feature-index.ts` (per `SESSION_STATE.md`'s 2026-08-14 entry, not re-verified this session). Kept in the register so this list isn't re-investigated cold in a later phase. | `test-evidence/pt-00/route-manifest.json` (`orphanRoutes[]`, 78 entries) | `node scripts/audit/verify-pt00-003.mjs` (prints `orphanRoutes=78`); cross-check any specific path against `src/components/layout/Header.tsx`'s `TABS` array, `SettingsNav`, or a list-page `<Link>`. | CONFIRMED-OK |
| WGR-011 | Frontend/Nav | P3 | Route manifest's `deadNav` cross-reference (nav-items.ts entries pointing at a route that doesn't exist) reports 0 — investigated, found NOT to be a gap. Logged for traceability. | `test-evidence/pt-00/route-manifest.json` (`deadNav: []`) | `node scripts/audit/verify-pt00-003.mjs` (prints `deadNav=0`) | CONFIRMED-OK |
| WGR-012 | Frontend/Page | P1 | `/donor-discovery/prospects/[id]` (`ProspectDetail.tsx`): real, reproducible crash for prospects whose linked directory row has a partial `enrichment` jsonb object. Confirmed live against a real, currently-existing prospect for the Faith Foundation org (`19ec603e-bb4d-4e10-9482-e864dc85f511`, directory row `a5e41547-8819-40ad-981b-dc191b5f26ff`, `enrichment: {ein, ntee_code, asset_amount, giving_total}` only -- missing `giving_focus_areas`/`in_kind_history_signals`/`has_giving_program`/`has_donation_form`/`decision_contacts`). Lines 574/587 read `enrichment.giving_focus_areas.length`/`enrichment.in_kind_history_signals.length` unguarded once `enrichment` itself is truthy, throwing `Cannot read properties of undefined (reading 'length')` and blanking the entire page (`<main>` renders 0 characters, `httpStatus: 200`). Reproduced identically across 4 independent navigation attempts (2 built-in retries, 1 final-recheck pass, 1 standalone confirmation query) with zero build-contention signature present -- a real application bug, not environmental. P1 not P0: a drilldown detail page, not the literal top-level `/donor-discovery` nav entry itself. **RESOLVED commit `d5500cd` (2026-08-19):** `giving_focus_areas`/`in_kind_history_signals`/`decision_contacts` all guarded with `?.length ?? 0` and `?.map`; `DonorProspectExtraction`'s type loosened to mark all three fields optional to match the real backend shape. Re-verified live against `19ec603e-bb4d-4e10-9482-e864dc85f511` (same id this row's own finding used) -- now renders 716 chars of real content, no error boundary. `pnpm run build` exit 0. | `test-evidence/pt-01/render-results.json` (`results[]` where `path === "/donor-discovery/prospects/[id]"`), `test-evidence/pt-01/render-failures/donor_discovery_prospects_id.png` -- pre-fix; `test-evidence/pt-01/wgr-017-fix/wgr-017-fix-verify.json`, `test-evidence/pt-01/wgr-017-fix/after_19ec603e.png` -- post-fix | Authenticated navigation to `/donor-discovery/prospects/19ec603e-bb4d-4e10-9482-e864dc85f511` (real, live prospect for the Faith Foundation org). Root cause: `src/components/donor-discovery/ProspectDetail.tsx` lines 574 and 587 (pre-fix). See `scripts/audit/pt01-002-render-pass.mjs` (original repro), `scripts/audit/wgr017-fix-verify.mjs` (fix verification, commit `d5500cd`). | RESOLVED |
| WGR-013 | Infra/Dev-Environment | P2 | The shared local dev server (`localhost:3000`, plain `next dev`, no `distDir` isolation) was found mid-session with its client hydration bundle (`.next/static/chunks/main-app.js` and others) missing at the filesystem level, caused by a concurrent, unrelated `next build` process in the same checkout (PID 112940, observed still running after 55+ minutes without completing) writing to the same shared `.next/` directory `next dev` reads from. Confirmed live: an initial render-pass sweep against the shared server found 12 of 146 routes failing, every one bearing a build-contention signature (either a server-side `Cannot find module '.../vendor-chunks/@supabase+auth-js@2.108.0.js'` error, or repeated `404`s on core `_next/static/chunks/{main-app.js,app-pages-internals.js,app/(dashboard)/layout.js}` requests); a follow-up check of ~15 more routes stuck on a "Loading..." state found the same signature had since spread to nearly every dashboard route. This is the same multi-worktree `.next` contention failure mode `next.config.mjs`'s `experimental.cpus: 1` comment and WGR-001/`STANDING_DIRECTIVES.md` already document for `next build`-vs-`next build` contention -- this session shows it also applies to `next dev`-vs-`next build` sharing one `.next/`, and that `next dev` does not self-heal from it (still broken 50+ minutes and hundreds of requests after the corruption began; `.next`'s own directory mtime was frozen at the moment of corruption). Worked around for this session only by launching a second, isolated `next dev` instance with `distDir` overridden via a temporary, reverted `next.config.mjs` env-gated addition -- the shared server itself was left running, untouched, still broken, for whatever else may depend on it. | `test-evidence/pt-01/build-contention-investigation-static-chunks.json`, `test-evidence/pt-01/build-contention-investigation-loading-states.json`, `test-evidence/pt-01/render-failures/build-contention-investigation/` (12 screenshots), `test-evidence/pt-01/render-results.json`'s `methodologyNote` field | Run two concurrent `next dev`/`next build` processes against the same un-isolated `.next/` in this checkout under real multi-worktree load (several agent worktrees building at once) and watch `.next/static/chunks/main-app.js` start 404ing on the `next dev` server while it keeps running and never recompiles it. | CONFIRMED-BROKEN |
| WGR-014 | Frontend/Page | P3 | WGR-004's documented `/documents` 30000ms navigation timeout did NOT reproduce in this session's render pass, on either of two independent attempts (an initial dry-run smoke check, and the full 146-route sweep). Rendered cleanly in ~3.7-5.6s both times, real content, no error. Per this audit program's own established convention, a non-reproduction is itself a finding, not silent confirmation the bug is fixed -- root cause was never investigated (out of scope both times this session and in PT-00), so this could be genuine intermittent flakiness or coincidental fast responses both times. Do not treat WGR-004 as resolved on the strength of this entry alone. | `test-evidence/pt-01/render-results.json` (`results[]` where `path === "/documents"`, `httpStatus: 200`, `durationMs: 5572`, `hasRealContent: true`) | Authenticated navigation to `/documents`, repeated across separate sessions/times of day, to establish whether the original PT-00 hang is reliably reproducible, intermittent, or resolved. | UNVERIFIED |
| WGR-015 | Frontend/Nav | P3 | WGR-011's static `deadNav: []` cross-reference (nav-items.ts hrefs vs. the build's route manifest) is now confirmed with LIVE, click-based evidence, not just a static string match: every one of 72 real nav elements across 5 surfaces -- sidebar (`NAV_ITEMS` incl. all 19 children, `DONOR_DISCOVERY_NAV_ITEMS`, `RESOURCES_NAV_ITEMS`, `SETTINGS_NAV_ITEM` -- 42 total), admin/`PLATFORM_NAV_ITEMS` (9), the header tab bar/`TABS` (6), the header avatar/org menu/`MENU_LINKS` (5, a bonus surface beyond the four the task named -- same `Header.tsx` file, same method, cheap extra coverage), and the Settings sub-nav (10, incl. both `ownerOnly` entries, reachable since the authenticated test account -- `info@faithfoundationsf.org` -- is confirmed `profiles.role = "owner"`) -- was located in the real rendered DOM, actually clicked, and resolved to a real rendering page (`resolved_status: "renders"`, zero 404s, zero error boundaries, zero blank renders, zero elements missing from the DOM). No contradiction of WGR-011's static claim found. | `test-evidence/pt-01/nav-resolution.json` (`results[]`, 72 rows, `crossCheckPt00DeadNavClaim.contradictsStaticFinding: false`), `test-evidence/pt-01/nav-resolution-run.log` | `node scripts/audit/pt01-003-nav-resolution.mjs` (requires an authenticated-reachable local dev server on `localhost:3000` or `PT01_BASE_URL`); `node scripts/audit/verify-pt01-003.mjs` to check the evidence file's own shape. | CONFIRMED-OK |
| WGR-016 | Frontend/Page, Audit-Tooling | P3 | PT-01-004's element-wiring classifier initially flagged `/nonprofits`'s "Search" button as CONFIRMED-BROKEN (`form_no_submit_handler`, P0) because it sits inside a `<form>` with no React `onSubmit` prop and no `action` attribute. Investigated against the real source and found NOT a gap: `src/app/(dashboard)/nonprofits/page.tsx` is a plain server component rendering `<form method="GET">` with zero client JS -- clicking Search performs a genuine, correct native browser GET navigation to `/nonprofits?search=...&state=...`, which the server component reads via `searchParams`. This was a false positive in the classifier's own heuristic (it assumed every `<button type="submit">` needs an explicit JS-visible handler to be "wired," which is false for a plain native form -- the browser's default submit-to-current-URL behavior is itself a real, working action). Fixed the same session: `pt01-004-element-wiring.mjs`'s `classifyButtonRow()` now treats `type=submit` inside any `<form>` (with or without a JS handler) as CONFIRMED-OK (`form_native_submit_no_js_handler`), reserving CONFIRMED-BROKEN for `type=submit` with no ancestor `<form>` at all (a case that remains a real, distinct check, unaffected by this fix). Re-crawled `/nonprofits` after the fix and confirmed the corrected classification. | `test-evidence/pt-01/element-graph.json` (`elements[]` row `page === "/nonprofits" && label === "Search"`, `resolved_status: "form_native_submit_no_js_handler"`, `verdict: "CONFIRMED-OK"`), `scripts/audit/pt01-004-element-wiring.mjs`'s `classifyButtonRow()` (fix + inline comment naming this exact case) | `node scripts/audit/pt01-004-element-wiring.mjs` against `/nonprofits`; read `src/app/(dashboard)/nonprofits/page.tsx` lines 171-227 (no `"use client"`, no `onSubmit`, `<form method="GET">`) to confirm the native-submit behavior directly. | CONFIRMED-OK |
| WGR-017 | Frontend/Page | P0 | WGR-012's `/donor-discovery/prospects/[id]` blank-render bug (unguarded `enrichment.giving_focus_areas.length`/`enrichment.in_kind_history_signals.length` in `ProspectDetail.tsx`, originally confirmed against one real prospect) is confirmed, this session, to be reachable via 6 real, visible, clickable links on the primary-nav `/donor-discovery` page itself -- not just a route pattern reused by assumption. The reuse was independently spot-checked rather than trusted: for 5 of the 6 links' distinct real prospect ids (`716a1077-...`, `deb11e9d-...`, `b045b765-...`, `38fa36b4-...`, plus the id shared by both the "View Prospect" and one "Review" link, `28f27b2e-...`), a direct query confirmed every one of their linked `donor_discovery_directory` rows' `enrichment` jsonb carries the identical incomplete shape (`ein, ntee_code, asset_amount, giving_total` only -- missing both fields `ProspectDetail.tsx` reads unguarded), and a live authenticated render of 4 of the 5 confirmed a near-blank main content area (`mainTextLen: 57`, chrome only, matching WGR-012's own symptom exactly); the 5th (`b045b765-...`) hit a navigation abort in the one-off verification script (not independently re-attempted) but shares the identical broken enrichment shape as the other 4, so is graded CONFIRMED-BROKEN on that basis, not blank-render alone. This shows the underlying data-completeness gap is not an isolated single record -- it affects the majority of real prospects sampled -- and that a real, unmodified user flow (open `/donor-discovery`, click a Review link) reaches it directly, raising this from a drilldown-only P1-adjacent concern to a real P0: primary-nav page, visible links, real user path. **RESOLVED commit `d5500cd` (2026-08-19):** same fix as WGR-012 (`giving_focus_areas`/`in_kind_history_signals`/`decision_contacts` guarded with `?.length ?? 0` and `?.map`). This row's own 5 named prospect ids no longer resolve by direct id lookup -- the org's `donor_discovery_prospects` table grew to 133,812 rows since this finding was recorded, past PostgREST's 1000-row page cap -- so the fix was independently re-verified against a freshly live-queried sample instead of trusting the stale id list: 50/50 newly sampled prospects (including WGR-012's original `19ec603e-...` id) still confirmed with the identical incomplete enrichment shape this row describes, and 3 of them (`19ec603e-...`, `16934ed5-...`, `aff22886-...`) authenticated-rendered 700+ chars of real content post-fix, no error boundary -- re-proving both that the data-completeness gap is still live today and that the frontend crash it triggered is fixed. `pnpm run build` exit 0. | `test-evidence/pt-01/element-graph.json` (`elements[]` rows `page === "/donor-discovery" && verdict === "CONFIRMED-BROKEN"`, 6 rows, each `reusedFrom: "test-evidence/pt-01/render-results.json"`), `test-evidence/pt-01/dead-ends/donor_discovery__a__View_Prospect__audit-50.png` and the 5 sibling `donor_discovery__a__Review__audit-*.png` screenshots -- pre-fix; `test-evidence/pt-01/wgr-017-fix/wgr-017-fix-verify.json`, `test-evidence/pt-01/wgr-017-fix/after_*.png` -- post-fix | `node scripts/audit/pt01-004-element-wiring.mjs` against `/donor-discovery` (pre-fix repro); `node scripts/audit/wgr017-fix-verify.mjs` (fix verification, commit `d5500cd` -- resolves fresh candidates live rather than the stale named ids, since those no longer resolve by direct lookup). Root cause: `src/components/donor-discovery/ProspectDetail.tsx` lines 574 and 587 (same as WGR-012, pre-fix). | RESOLVED |
| WGR-018 | Frontend/Page | P3 | Prior request described "Integrations Configure" as reported broken (404) with no matching commit in git history (STATE_OF_THE_BUILD.md 2026-08-18 flagged this claim as unverifiable). Re-verified from scratch this session, live, against a real authenticated Playwright session: the only real "Configure" affordance on /settings/integrations is the State Grant Portals connector card (src/app/(dashboard)/settings/integrations/page.tsx, configurePath="/settings/state-portals" -- confirmed by source read that Grants.gov/ProPublica/SAM.gov cards pass no configurePath at all). Clicked it live -- navigated to /settings/state-portals, rendered real content (Texas TX portal card, "Preview" action, cross-link back to Settings -> Integrations), no error boundary, no 404. A first script attempt initially misreported this as CONFIRMED-BROKEN due to a race condition in the test script itself (querying the DOM immediately after page.goto() before this app's client-side hydration/async data-loading settled) -- root-caused via a standalone debug script and fixed by adding an explicit settle wait before every element query, then re-run to a clean pass. Not a 404, confirmed working today. | test-evidence/pt-01/claimed-fixes-reverify.json (items[] item="Integrations Configure"), test-evidence/pt-01/claimed-fixes/a_integrations_configure.png | node scripts/audit/pt01-005-claimed-fixes-reverify.mjs (requires an authenticated-reachable local dev server on localhost:3000 or PT01_BASE_URL); node scripts/audit/verify-pt01-005.mjs to check the evidence file's own shape. | CONFIRMED-OK |
| WGR-019 | Frontend/Page, API | P3 | Prior request described Grants.gov "Run Now" as reported broken with no matching commit. Re-verified from scratch, live: clicked the real Run Now button on the Grants.gov connector card, capturing the real network response to POST /api/agents/grants-gov (writer-role gated, src/app/api/agents/grants-gov/route.ts) rather than just checking the button exists. Real response: HTTP 200, a genuine JSON body listing real, live grants.gov opportunities (e.g. "GPD Grant Forecast" / VA-GPD-FORECAST-FY2026-FY2028, "Family Unification Program (FUP) Multi-Year NOFO"), not a fabricated/mock payload. Post-click UI state independently confirms the same real result: the card's "Last sync" updated to "3 minutes ago" and the button changed to a "Queued" state with a "View Opportunities ->" link. Genuinely working, not broken. | test-evidence/pt-01/claimed-fixes-reverify.json (items[] item='Grants.gov "Run Now"', detail.responseInfo), test-evidence/pt-01/claimed-fixes/b_grants_gov_run_now.png | node scripts/audit/pt01-005-claimed-fixes-reverify.mjs (real POST triggers a live grants.gov search agent run -- not idempotent-free, but this is the button's own designed, sanctioned action, same as every other live agent-trigger session in this project's history); node scripts/audit/verify-pt01-005.mjs to check the evidence file's own shape. | CONFIRMED-OK |
| WGR-020 | Frontend/Nav | P3 | Prior request described the "Scraping Targets" settings sub-nav link as reported broken with no matching commit. Re-verified from scratch, live: clicked the real "Scraping Targets" tab (href="/settings/scraping") -- navigated correctly, rendered real content (an "Allowed Domains" panel showing "1 domain allowlisted", one real scraping target row for a simpler.grants.gov URL with Active/weekly badges and Run Now/Pause/Delete actions), no error boundary, no blank render, and (after adding an explicit loading-indicator-clear poll to the test script) no leftover "Loading..." spinner state either. Genuinely working today. | test-evidence/pt-01/claimed-fixes-reverify.json (items[] item="Scraping Targets link"), test-evidence/pt-01/claimed-fixes/c_scraping_targets.png | node scripts/audit/pt01-005-claimed-fixes-reverify.mjs; node scripts/audit/verify-pt01-005.mjs | CONFIRMED-OK |
| WGR-021 | Frontend/Page, Storage | P3 | Prior request described the Branding logo upload as reported fixed, but per STATE_OF_THE_BUILD.md this was never confirmed live, only source-read. Re-verified from scratch, live, without uploading anything to production per this task's explicit instruction: navigated to /settings/branding, confirmed a real <input type="file" accept="image/png,image/jpeg,image/webp,image/svg+xml"> is present and rendered (visible "Choose file" control, an existing logo preview already showing), source-confirmed it uploads via supabase.storage.from("org-branding").upload(...) (src/app/(dashboard)/settings/branding/page.tsx), and independently ran a live, read-only storage.listBuckets() call (service-role client, no write) confirming the real target bucket "org-branding" genuinely exists in production (public=true). Both the control's presence and its real storage target are confirmed live, not just source-read. | test-evidence/pt-01/claimed-fixes-reverify.json (items[] item="Branding logo upload", detail.bucketCheck), test-evidence/pt-01/claimed-fixes/d_branding_logo_upload.png | node scripts/audit/pt01-005-claimed-fixes-reverify.mjs (read-only: DOM presence check + storage.listBuckets(), never a file POST); node scripts/audit/verify-pt01-005.mjs | CONFIRMED-OK |
| WGR-022 | Frontend/Nav | P3 | Prior request described the Billing settings sub-nav link as reported fixed, but per STATE_OF_THE_BUILD.md this was never confirmed live, only source-read (src/app/(dashboard)/settings/billing/page.tsx re-exports the real (dashboard)/billing/page.tsx implementation, not a redirect). Re-verified from scratch, live: clicked the real "Billing" tab -- navigated to /settings/billing and rendered real, detailed content (Enterprise plan card, $499/month, real usage metrics -- 25/5,000 daily agent runs, 3 AutoApply submissions this month, 0.634 MB / 50,000 MB storage -- and an honest, non-crashing "Stripe is not configured on this server" informational banner rather than an error). No error boundary, no blank render, no leftover loading spinner after an explicit clear-wait was added to the test script. Resolves correctly today. | test-evidence/pt-01/claimed-fixes-reverify.json (items[] item="Billing nav"), test-evidence/pt-01/claimed-fixes/e_billing_nav.png | node scripts/audit/pt01-005-claimed-fixes-reverify.mjs; node scripts/audit/verify-pt01-005.mjs | CONFIRMED-OK |
| WGR-023 | API/Middleware | P0 | PT-02-002 unauthenticated-rejection sweep (318 API routes) found every cron_secret- and webhook_signature-classified route rejects an unauthenticated call safely (307 redirect to /login, no data exposure -- NOT an auth-bypass), but traced why: src/middleware.ts has no exemption for these paths at all -- only /, /login, /register, /forgot-password, /reset-password, /how-it-works, /pricing, /for-consultants, /security, /privacy, /terms, any /api/auth/* prefix, /invite*, and the exact path /api/users/accept skip its session check (isPublicPath()). Every other path, including all 14 real cron_secret routes (/api/cron/autoapply, /api/cron/campaigns, /api/cron/domain-warmup, /api/cron/draft-automation, /api/cron/draft-queue-check, /api/cron/email-sequences, /api/cron/follow-ups, /api/cron/grantsgov, /api/cron/reminders, /api/cron/research, /api/cron/sales-sends, /api/sources/grantsgov, /api/sources/propublica, /api/sources/samgov) and all 4 webhook_signature routes (/api/admin/webhooks/email-events, /api/admin/webhooks/email-reply, /api/webhooks/resend, /api/webhooks/stripe), requires a valid Supabase session cookie to reach the route handler at all -- confirmed live via fetch/curl, redirect: manual, zero cookies: HTTP 307 Location: /login on every one, before the route file's own CRON_SECRET bearer-token check or Stripe/Resend/Svix signature verification ever executes. Vercel Cron invocations (5 of the 14 are registered in vercel.json: research, grantsgov, reminders, autoapply, domain-warmup) and real Stripe/Resend webhook POSTs are server-to-server calls that never carry a Benavora session cookie, so in production this would block the routes' own intended callers the same way it blocks an anonymous attacker -- meaning nightly autoapply/grants-sync/reminders automation and Stripe/Resend webhook processing (billing status updates, email bounce/open tracking) may not be reachable at all in production, not just unreachable to an attacker. Two further none_detected routes show the identical root cause with an even more direct self-contradiction: /api/platform/bootstrap's own code comment states it is 'intentionally unauthenticated for initial setup, but self-disables after first platform_owner is created' -- yet an unauthenticated call gets the same 307-to-login, meaning the bootstrap flow is not actually reachable without already being logged in as some existing user; /api/unsubscribe (a bearer-token email unsubscribe link meant for a logged-out recipient, already flagged possibleMiddlewareConflict=true by the PT-02-001 static classifier) gets the same 307-to-login instead of the unsubscribe confirmation page. None of this is an auth-bypass (the opposite: middleware is over-broad here, not under-protective) so PT-02-002 records all of these rows as PASS or PASS_DESIGN_MISMATCH, not a P0 finding under this audit's own auth-bypass definition -- registered here separately because the practical impact (webhooks and cron jobs plausibly unreachable by their real callers in production) is a genuine, confirmed-live, production-relevant gap worth a dedicated fix decision (an explicit middleware exemption for these paths, verified instead via their own CRON_SECRET/signature check, the same way /api/users/accept is already exempted and self-verifies via its own token). Production env vars (whether Vercel Cron/Stripe/Resend calls actually get redirected the same way in the deployed environment, not just this local dev server) were not checked -- same local-only-verification caveat as WGR-003. | test-evidence/pt-02/unauth-sweep.json (results[] where mechanism in {cron_secret, webhook_signature} -- all status:307, location:/login; and results[] where path in {/api/platform/bootstrap, /api/unsubscribe} -- verdict PASS_DESIGN_MISMATCH) | node scripts/audit/pt02-002-unauth-sweep.mjs against a local pnpm dev server, then inspect test-evidence/pt-02/unauth-sweep.json; or directly: curl -i http://localhost:3000/api/webhooks/stripe (no cookies, no -L) -- observe 307 Location: /login before any Stripe-signature check runs. Root cause: src/middleware.ts PUBLIC_PATHS/isPublicPath() has no exemption for /api/cron/*, /api/sources/*, /api/webhooks/*, /api/admin/webhooks/*, /api/platform/bootstrap, or /api/unsubscribe. | CONFIRMED-BROKEN |
| WGR-024 | API/AuthZ | P3 | PT-02-003 role-tier enforcement matrix: every admin- and owner-gated API route (49 routes, 76 route+method entries after per-method requireRole() tier extraction, including two entries whose required tier is chosen at runtime rather than a literal string -- /api/autoapply/controls POST/DELETE) was called with real, authenticated sessions at all 4 real role tiers (viewer, writer, admin, owner -- this app has no 'member' tier; writer is the middle tier) against a throwaway local Supabase stack + org + users this audit provisioned itself (never production). 304/304 calls (76 entries x 4 roles) resolved to the expected refuse/permit outcome: every below-tier call got the specific requireRole() 403 {code:'forbidden'} refusal (192/192), and every at-or-above-tier call cleared the role gate (112/112, verified by the absence of the forbidden code -- several then hit real, expected downstream errors from the audit's intentionally minimal local schema, e.g. missing tables, which is a correct 'permitted, failed for an unrelated reason' result, not miscounted as a refusal). Zero under-enforcement findings, zero over-restriction findings. Logged for traceability so this isn't re-asked from scratch. | test-evidence/pt-02/role-matrix.json | Start a local `supabase start` stack + a second `next dev` instance pointed at it via env vars (see scripts/audit/pt02-003-role-matrix.mjs header), then: LOCAL_SUPABASE_URL=... LOCAL_SUPABASE_ANON_KEY=... LOCAL_SUPABASE_SERVICE_ROLE_KEY=... PT02_BASE_URL=http://localhost:3099 node scripts/audit/pt02-003-role-matrix.mjs; or: node scripts/audit/verify-pt02-003.mjs (checks coverage/shape only, does not re-run the sweep) | CONFIRMED-OK |
| WGR-025 | API | P2 | PT-02-004 CRUD cycle: DELETE /api/drafts/queue/{id} is a soft delete (sets status="rejected", rejected_reason="removed_from_queue") but a subsequent GET /api/drafts/queue/{id} still returns 200 with the full item (status now "rejected"), never a 404. A caller relying on "delete then GET 404s" to confirm removal gets a false negative -- the route never signals the item is gone, only that its status field changed. By-design soft-delete, not a crash, so P2 not P0/P1. | test-evidence/pt-02/crud-cycles.json (resources[] where resource==="draft_queue", steps.readAfterDelete) | As a writer-role session: POST /api/drafts/queue {opportunity_id}, then DELETE /api/drafts/queue/{id} (200), then GET /api/drafts/queue/{id} -- returns 200 with item.status="rejected", not 404. Handler: src/app/api/drafts/queue/[id]/route.ts DELETE. | CONFIRMED-BROKEN |
| WGR-026 | API | P2 | PT-02-004 CRUD cycle: DELETE /api/autoapply/profiles/{id} is a soft delete (sets active=false) but a subsequent GET /api/autoapply/profiles/{id} still returns 200 with the full profile (active now false), never a 404. Same class as WGR-025 -- a caller relying on "delete then GET 404s" gets a false negative. By-design soft-delete, not a crash, so P2 not P0/P1. | test-evidence/pt-02/crud-cycles.json (resources[] where resource==="request_profiles", steps.readAfterDelete) | As a writer-role session: POST /api/autoapply/profiles {name, request_type, needs_description}, then DELETE /api/autoapply/profiles/{id} (200), then GET /api/autoapply/profiles/{id} -- returns 200 with profile.active=false, not 404. Handler: src/app/api/autoapply/profiles/[id]/route.ts DELETE. | CONFIRMED-BROKEN |
| WGR-027 | API | P1 | PT-02-004 CRUD cycle: POST /api/email/templates unconditionally 500s ("Failed to create template.", code db_error) for every request, on any org, at writer role. Root cause: the route inserts/selects columns named subject and body, but the live email_templates table (migration 054_email_calendar_integration.sql) defines subject_template and body_template -- there is no subject/body column on this table at all. This is not an edge case: template creation is fully, unconditionally broken via this API for every caller. The PATCH/DELETE handlers in the same file read/select the same nonexistent subject/body columns and would fail identically once a row existed to target. | test-evidence/pt-02/crud-cycles.json (resources[] where resource==="email_templates", steps.create) | As a writer-role session: POST /api/email/templates {name, template_type, subject, body} -- returns 500 {"error":"Failed to create template.","code":"db_error"}. Handler: src/app/api/email/templates/route.ts POST (and PATCH/DELETE reference the same nonexistent columns). Table definition: supabase/migrations/054_email_calendar_integration.sql (subject_template/body_template, not subject/body). | CONFIRMED-BROKEN |
