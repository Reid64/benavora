# WIRING GAP REGISTER

Central register of every wiring/integration gap found during the audit program. Every finding
row must point at real evidence under `test-evidence/` (a saved command transcript, a JSON
response body, a screenshot, a query result file, etc.) and must include a reproduction command
or step sequence a future session can re-run to confirm the finding still holds. Do not add a row
without a corresponding evidence file already committed under `test-evidence/`.

## Severity legend

| Severity | Meaning |
|---|---|
| P0 | Confirmed broken in production / live data path. Blocks a real user-facing flow or silently corrupts/loses data. Fix immediately, no exceptions. |
| P1 | Confirmed broken but scoped/contained (single feature, single org, degrades gracefully, or has a working manual workaround). Fix this cycle. |
| P2 | Real gap but low blast radius (rare code path, admin-only, cosmetic, or affects a deprecated/low-traffic feature). Fix opportunistically. |
| P3 | Unverified suspicion, code-smell, or a gap that needs a product decision before it can be classified as a bug. Track, do not fix blind. |

## Scope tags

| Tag | Meaning |
|---|---|
| CONFIRMED-BROKEN | Live-verified against real data/a real request; the gap reproduces every time. |
| UNVERIFIED | Plausible from a code read, but not yet exercised against real data/a real request. |
| PENDING-SCOPE | Real gap, but whether it's in-scope for this audit program (vs. a separate task) hasn't been decided yet. |
| CONFIRMED-OK | Investigated and found NOT to be a gap — code path verified working as designed. Kept in the register so the question isn't re-asked. |
| RESOLVED | Was CONFIRMED-BROKEN; a fix has been committed and live-reverified against real data/a real request. Kept in the register (not deleted) so the history isn't lost. |
| FIX-APPLIED-PENDING-VERIFICATION | Was CONFIRMED-BROKEN; a fix has been committed and compiles/builds clean, but the required live re-verification (>0 correctly-parsed real records through the fixed function) could not be completed yet — e.g. blocked by an external API rate/quota limit. Not RESOLVED until that live proof exists. |
| FIX-APPLIED-BRANCH-VERIFIED | Was CONFIRMED-BROKEN; a fix has been committed and proven correct against a real database (a Supabase branch or local stack) — legal cases succeed, illegal cases are rejected, evidence captured — but has NOT yet been applied to production (a deliberate, separate step, e.g. bundled into a larger migration-drift remediation batch). Not RESOLVED until applied and re-verified live in production. |

## Findings

| ID | Layer | Severity | Finding | Evidence Path | Reproduction | Scope Tag |
|---|---|---|---|---|---|---|
| WGR-004 | Frontend/Page | P0 | `/documents` page: authenticated Playwright navigation timed out after 30000ms during the PT-00-005 smoke sweep (`navigation failed: page.goto: Timeout 30000ms exceeded.`) — a real hang, not a dynamic-route-probe artifact (this path takes no id substitution). Graded P0, not P1: `/documents` is a literal top-level `NAV_ITEMS` sidebar entry (`src/components/layout/nav-items.ts`), not a sub-page — any authenticated user clicking that sidebar item hits a hang, not a page. Root cause not investigated — out of PT-00 scope (PT-00 is baseline evidence collection, not remediation). | `test-evidence/pt-00/smoke-results.json` (`results[]` where `path === "/documents"`), `test-evidence/pt-00/smoke-failures/documents.png` | Authenticated navigation to `/documents` against a local `pnpm dev` server — see `scripts/audit/pt00-005-smoke-suite.mjs` | CONFIRMED-BROKEN |
| WGR-017 | Frontend/Page | P0 | WGR-012's `/donor-discovery/prospects/[id]` blank-render bug (unguarded `enrichment.giving_focus_areas.length`/`enrichment.in_kind_history_signals.length` in `ProspectDetail.tsx`, originally confirmed against one real prospect) is confirmed, this session, to be reachable via 6 real, visible, clickable links on the primary-nav `/donor-discovery` page itself -- not just a route pattern reused by assumption. The reuse was independently spot-checked rather than trusted: for 5 of the 6 links' distinct real prospect ids (`716a1077-...`, `deb11e9d-...`, `b045b765-...`, `38fa36b4-...`, plus the id shared by both the "View Prospect" and one "Review" link, `28f27b2e-...`), a direct query confirmed every one of their linked `donor_discovery_directory` rows' `enrichment` jsonb carries the identical incomplete shape (`ein, ntee_code, asset_amount, giving_total` only -- missing both fields `ProspectDetail.tsx` reads unguarded), and a live authenticated render of 4 of the 5 confirmed a near-blank main content area (`mainTextLen: 57`, chrome only, matching WGR-012's own symptom exactly); the 5th (`b045b765-...`) hit a navigation abort in the one-off verification script (not independently re-attempted) but shares the identical broken enrichment shape as the other 4, so is graded CONFIRMED-BROKEN on that basis, not blank-render alone. This shows the underlying data-completeness gap is not an isolated single record -- it affects the majority of real prospects sampled -- and that a real, unmodified user flow (open `/donor-discovery`, click a Review link) reaches it directly, raising this from a drilldown-only P1-adjacent concern to a real P0: primary-nav page, visible links, real user path. **RESOLVED commit `d5500cd` (2026-08-19):** same fix as WGR-012 (`giving_focus_areas`/`in_kind_history_signals`/`decision_contacts` guarded with `?.length ?? 0` and `?.map`). This row's own 5 named prospect ids no longer resolve by direct id lookup -- the org's `donor_discovery_prospects` table grew to 133,812 rows since this finding was recorded, past PostgREST's 1000-row page cap -- so the fix was independently re-verified against a freshly live-queried sample instead of trusting the stale id list: 50/50 newly sampled prospects (including WGR-012's original `19ec603e-...` id) still confirmed with the identical incomplete enrichment shape this row describes, and 3 of them (`19ec603e-...`, `16934ed5-...`, `aff22886-...`) authenticated-rendered 700+ chars of real content post-fix, no error boundary -- re-proving both that the data-completeness gap is still live today and that the frontend crash it triggered is fixed. `pnpm run build` exit 0. | `test-evidence/pt-01/element-graph.json` (`elements[]` rows `page === "/donor-discovery" && verdict === "CONFIRMED-BROKEN"`, 6 rows, each `reusedFrom: "test-evidence/pt-01/render-results.json"`), `test-evidence/pt-01/dead-ends/donor_discovery__a__View_Prospect__audit-50.png` and the 5 sibling `donor_discovery__a__Review__audit-*.png` screenshots -- pre-fix; `test-evidence/pt-01/wgr-017-fix/wgr-017-fix-verify.json`, `test-evidence/pt-01/wgr-017-fix/after_*.png` -- post-fix | `node scripts/audit/pt01-004-element-wiring.mjs` against `/donor-discovery` (pre-fix repro); `node scripts/audit/wgr017-fix-verify.mjs` (fix verification, commit `d5500cd` -- resolves fresh candidates live rather than the stale named ids, since those no longer resolve by direct lookup). Root cause: `src/components/donor-discovery/ProspectDetail.tsx` lines 574 and 587 (same as WGR-012, pre-fix). | RESOLVED |
| WGR-023 | API/Middleware | P0 | PT-02-002 unauthenticated-rejection sweep (318 API routes) found every cron_secret- and webhook_signature-classified route rejects an unauthenticated call safely (307 redirect to /login, no data exposure -- NOT an auth-bypass), but traced why: src/middleware.ts has no exemption for these paths at all -- only /, /login, /register, /forgot-password, /reset-password, /how-it-works, /pricing, /for-consultants, /security, /privacy, /terms, any /api/auth/* prefix, /invite*, and the exact path /api/users/accept skip its session check (isPublicPath()). Every other path, including all 14 real cron_secret routes (/api/cron/autoapply, /api/cron/campaigns, /api/cron/domain-warmup, /api/cron/draft-automation, /api/cron/draft-queue-check, /api/cron/email-sequences, /api/cron/follow-ups, /api/cron/grantsgov, /api/cron/reminders, /api/cron/research, /api/cron/sales-sends, /api/sources/grantsgov, /api/sources/propublica, /api/sources/samgov) and all 4 webhook_signature routes (/api/admin/webhooks/email-events, /api/admin/webhooks/email-reply, /api/webhooks/resend, /api/webhooks/stripe), requires a valid Supabase session cookie to reach the route handler at all -- confirmed live via fetch/curl, redirect: manual, zero cookies: HTTP 307 Location: /login on every one, before the route file's own CRON_SECRET bearer-token check or Stripe/Resend/Svix signature verification ever executes. Vercel Cron invocations (5 of the 14 are registered in vercel.json: research, grantsgov, reminders, autoapply, domain-warmup) and real Stripe/Resend webhook POSTs are server-to-server calls that never carry a Benavora session cookie, so in production this would block the routes' own intended callers the same way it blocks an anonymous attacker -- meaning nightly autoapply/grants-sync/reminders automation and Stripe/Resend webhook processing (billing status updates, email bounce/open tracking) may not be reachable at all in production, not just unreachable to an attacker. Two further none_detected routes show the identical root cause with an even more direct self-contradiction: /api/platform/bootstrap's own code comment states it is 'intentionally unauthenticated for initial setup, but self-disables after first platform_owner is created' -- yet an unauthenticated call gets the same 307-to-login, meaning the bootstrap flow is not actually reachable without already being logged in as some existing user; /api/unsubscribe (a bearer-token email unsubscribe link meant for a logged-out recipient, already flagged possibleMiddlewareConflict=true by the PT-02-001 static classifier) gets the same 307-to-login instead of the unsubscribe confirmation page. None of this is an auth-bypass (the opposite: middleware is over-broad here, not under-protective) so PT-02-002 records all of these rows as PASS or PASS_DESIGN_MISMATCH, not a P0 finding under this audit's own auth-bypass definition -- registered here separately because the practical impact (webhooks and cron jobs plausibly unreachable by their real callers in production) is a genuine, confirmed-live, production-relevant gap worth a dedicated fix decision (an explicit middleware exemption for these paths, verified instead via their own CRON_SECRET/signature check, the same way /api/users/accept is already exempted and self-verifies via its own token). Production env vars (whether Vercel Cron/Stripe/Resend calls actually get redirected the same way in the deployed environment, not just this local dev server) were not checked -- same local-only-verification caveat as WGR-003. **Update (PT-14-003, 2026-08-20):** confirmed live in production, closing that exact gap -- two independent live probes against `https://www.benavora.com` itself (not local `pnpm dev`) both reproduce this finding unchanged: (1) the PT-14-002 injection/CSRF sweep, 21 live attempts, see WGR-111; (2) a dedicated PT-14-003 middleware review, 7 further live probes covering 2 of the 5 real `vercel.json` cron targets and both webhook routes, `test-evidence/pt-14/bundle-and-middleware.json`. Every probed route across both passes returned a real `HTTP 307 Location: /login` (with `Server: Vercel` and a distinct `X-Vercel-Id` per request, confirming genuine per-request production responses, not a cached artifact), including with a spoofed `Authorization: Bearer wrongsecret` header, before the route's own `CRON_SECRET`/signature check ever ran. Root cause (`src/middleware.ts` `PUBLIC_PATHS`/`isPublicPath()`) is unchanged in the currently-deployed build. This same pass also directly contradicts WGR-003's separately-recorded "Reid reports cron routes return `401`" -- not yet reconciled, flagged for Reid rather than silently resolved either way. | test-evidence/pt-02/unauth-sweep.json (results[] where mechanism in {cron_secret, webhook_signature} -- all status:307, location:/login; and results[] where path in {/api/platform/bootstrap, /api/unsubscribe} -- verdict PASS_DESIGN_MISMATCH) | node scripts/audit/pt02-002-unauth-sweep.mjs against a local pnpm dev server, then inspect test-evidence/pt-02/unauth-sweep.json; or directly: curl -i http://localhost:3000/api/webhooks/stripe (no cookies, no -L) -- observe 307 Location: /login before any Stripe-signature check runs. Root cause: src/middleware.ts PUBLIC_PATHS/isPublicPath() has no exemption for /api/cron/*, /api/sources/*, /api/webhooks/*, /api/admin/webhooks/*, /api/platform/bootstrap, or /api/unsubscribe. | CONFIRMED-BROKEN |
| WGR-074 | Auth/Admin | P0 | Admin impersonation (POST /api/admin/orgs/[id]/impersonate) is unbounded: the impersonation_org_id cookie it sets is read by ZERO other call sites anywhere in the repo (git grep confirms; it appears only in its own route.ts). The real authorization gate for every owner-scoped admin route, including /admin/orgs/[id] itself, is role-only (profiles.role='owner', hasRequiredRole in role-gate.ts/constants.ts) with no dependency on which org (if any) the caller is currently impersonating -- no such state is ever persisted or checked server-side. An admin who has 'started impersonating' org A can reach org B (or any of the platform's other orgs) via the exact same gate with zero additional restriction, live-confirmed via a local-stack reproduction using the real production RLS policy on organizations for contrast (owner A own-session read of org B correctly returns 0 rows via RLS; the SAME owner A reaches org B fully via the admin/service-role path with 1 row returned, immediately after nominally impersonating org A). Compounded by 'owner' being a per-org role held by 70 real production users today, not a distinct platform-admin population (platform_admins has only 1 row). | test-evidence/pt-05/privileged-access.json (admin_impersonation.bounded_to_org), test-evidence/pt-05/pt05-004-production-investigation.json | node scripts/audit/pt05-004-investigate.mjs (read-only production check); node scripts/audit/pt05-004-privileged-access.mjs (local-stack behavioral reproduction against .pt05-local-stack); git grep -n impersonation_org_id -- src worker scripts (confirms zero read sites outside the setter route). | CONFIRMED-BROKEN |
| WGR-099 | Frontend / Cross-browser | P0 | PT-11-003 (`cross-browser` suite, real execution against the real port-3100 Benavora server, run twice specifically to check persistence per this task's explicit instruction). `e2e/critical-paths.spec.ts` on the WebKit engine: **0 of 5 tests passed in either of 2 independent runs today**, every single failure the identical `TimeoutError: page.waitForURL: Timeout 30000ms exceeded ... waiting for navigation to "**/dashboard"` -- confirming the WebKit post-login navigation race first documented in `CROSSBROWSER_TEST_RESULTS_20260813.md` is still live and fully reproducing, unfixed, with zero exceptions across 10 total WebKit test executions (5 tests x 2 runs). Graded P0, not P1: this blocks the single most fundamental real-user flow (successful login never reaching the dashboard) for an entire browser engine family (WebKit underlies both desktop and mobile Safari) against the real, live application -- not a cosmetic or admin-only gap. Root cause not re-investigated this phase (same as 2026-08-13); most likely candidate per that report remains an in-flight `router.replace("/dashboard")`/`router.refresh()` redirect pair that WebKit resolves differently than Chromium/Firefox. | test-evidence/pt-11/logs/cross-browser-run1.log, test-evidence/pt-11/logs/cross-browser-run2.log (both show `webkit: { "passed": 0, "failed": 5, "total": 5 }` in `suite-results-flaky.json`, and the identical `page.waitForURL` timeout text in the raw Playwright output for all 5 webkit test cases in both files); `CROSSBROWSER_TEST_RESULTS_20260813.md` (repo root, the original 2026-08-13 finding this run reproduces exactly) | `PLAYWRIGHT_BASE_URL=http://localhost:3100 NEXT_PUBLIC_SITE_URL=http://localhost:3100 npx playwright test e2e/critical-paths.spec.ts --project=webkit --reporter=list` (against a real `next dev -p 3100` instance, after real login) -- reproduces 0/5 with the identical timeout on every test. | CONFIRMED-BROKEN |
| WGR-108 | API/SSRF | P0 | POST /api/intelligence/ingest {source:'url', url} (requireRole('writer'), src/app/api/intelligence/ingest/route.ts:61-77) does `fetch(url.trim(), {headers, signal})` on a fully user-supplied URL with zero validation -- no scheme restriction, no private/loopback/link-local IP check, no safeFetch() usage (src/lib/security/safe-fetch.ts, the platform's own SSRF-hardened fetch wrapper, is imported by only 2 files repo-wide: custom-api.ts and custom-scrape.ts -- this route is not one of them). Worse than blind SSRF: the fetched body is extracted via extractSections() and PERSISTED to intelligence_funded_proposals, readable later by any viewer-role user through the Intelligence Library UI -- full response-content exfiltration of whatever the URL resolves to (e.g. cloud metadata, an internal admin API, another internal service), not just a blind fire-and-forget request. Live-reproduced this session: the exact fetch() call copied verbatim from the route, run against a real local loopback listener standing in for an internal service, successfully retrieved and returned the listener's marker payload with zero blocking. **RESOLVED commit `6dd5f32` (2026-08-20):** `fetch()` replaced with `safeFetch()` from a new shared guard (`src/lib/security/ssrf-guard.ts`, with `safe-fetch.ts` refactored to build on it rather than duplicate the IP-range logic — see WGR-109/110 for the same guard applied at the other two sites). A blocked URL now returns `422 {code: "url_blocked"}` instead of proceeding. Live-verified two ways: (1) the guard's own primitive (`assertUrlSafe`), called with real (non-mocked) DNS resolution, blocks all 7 required cases (metadata IP, localhost, loopback/RFC1918 literals, non-http schemes) and allows a real public HTTPS URL — `test-evidence/remediation/ssrf-fix/ssrf-guard-live-verify.json`; (2) `safeFetch()` itself, run against a real local loopback listener standing in for an internal service (the same reproduction shape this row's own pre-fix evidence used), never reaches it (0 hits recorded on the listener) while a real public HTTPS fetch in the same run returns a real 200 with real body content — `test-evidence/remediation/ssrf-fix/safefetch-live-verify.json`. 30 unit tests (`src/__tests__/unit/ssrf-guard.test.ts`) all pass — `test-evidence/remediation/ssrf-fix/unit-test-output.txt`. `pnpm run build` exit 0. | test-evidence/pt-14/injection.json (attempts[] where id==="ssrf-001-intelligence-ingest-raw-fetch"); test-evidence/remediation/ssrf-fix/ssrf-guard-live-verify.json; test-evidence/remediation/ssrf-fix/safefetch-live-verify.json; test-evidence/remediation/ssrf-fix/unit-test-output.txt | node scripts/audit/pt14-002-live-tests.mjs (re-runs the full sweep, including this live local comparative test); direct read of src/app/api/intelligence/ingest/route.ts:53-77 confirms zero URL/host validation before the fetch() call (pre-fix repro against unfixed code). Post-fix: `npx vitest run src/__tests__/unit/ssrf-guard.test.ts`; `npx tsx scripts/audit/ssrf-guard-live-verify.mjs`; `npx tsx scripts/audit/safefetch-live-verify.mjs`. | RESOLVED |
| WGR-109 | Autoapply/SSRF | P0 | WebhookNotifier.notify() (src/lib/autoapply/webhook-notifier.ts:123) does `fetch(config.webhook_url, {method:'POST', headers, body, signal})` on a URL read from webhook_configs.webhook_url with zero validation beyond `new URL(webhook_url)` (a syntactic well-formedness check only -- src/app/api/autoapply/webhooks/route.ts:89, no host/scheme restriction) at creation time (requireRole('admin')). Every real AutoApply queue-processor event (submission_completed, submission_failed, queue_populated, review_needed, agreement_received, captcha_solve_failed) fires this fetch server-side with no safeFetch() protection -- confirmed this is not one of the 2 files repo-wide that import src/lib/security/safe-fetch.ts. An org admin (or a compromised admin session) can point a webhook at http://169.254.169.254/... (cloud metadata), an internal service, or any other address the production server can reach, and the platform's own worker will POST a real outbound request there on every subsequent AutoApply event. Live-reproduced this session: the exact fetch() call copied verbatim, run against a real local loopback listener, successfully delivered the POST with zero blocking. **RESOLVED commit `6dd5f32` (2026-08-20):** `fetch()` replaced with `safeFetch()` (same shared guard as WGR-108/110). `safeFetch()` gained POST-body support (`SafeFetchOptions.body`, auto `Content-Length`, never resent across a redirect hop) since the prior 2 callers were GET-only. A blocked `webhook_url` now logs a specific `[WebhookNotifier] Blocked SSRF-unsafe webhook_url for config <id>` warning and skips that config rather than firing the request — `Promise.allSettled` over configs is unchanged, so one blocked config still can't affect delivery to the org's other configured webhooks. Live-verified: `safeFetch()` (the exact function now used here) run against a real local loopback listener never reaches it (0 hits) while a real public HTTPS call in the same run succeeds — `test-evidence/remediation/ssrf-fix/safefetch-live-verify.json`; the guard's block/allow primitive independently live-verified against real DNS — `test-evidence/remediation/ssrf-fix/ssrf-guard-live-verify.json`. `pnpm run build` exit 0. | test-evidence/pt-14/injection.json (attempts[] where id==="ssrf-002-webhook-notifier-raw-fetch"); test-evidence/remediation/ssrf-fix/safefetch-live-verify.json; test-evidence/remediation/ssrf-fix/ssrf-guard-live-verify.json | node scripts/audit/pt14-002-live-tests.mjs; direct read of src/lib/autoapply/webhook-notifier.ts:123 and src/app/api/autoapply/webhooks/route.ts:81-93 (the only validation on webhook_url is `new URL(webhook_url)`) — pre-fix repro against unfixed code. Post-fix: `npx tsx scripts/audit/safefetch-live-verify.mjs`. | RESOLVED |
| WGR-110 | Autoapply/SSRF | P0 | AutoApply's real submission pipeline (worker/queue-processor.ts:566 reads funders.giving_portal_url, a free-text field with no server-side URL/host validation found anywhere in the funders write path) navigates a real headless Chromium browser to it server-side via src/lib/autoapply/stealth-browser.ts -- confirmed by targeted grep and full file read that this file (and the separate general-purpose scraper's src/lib/scraper/stealth-engine.ts, which navigates to foundation_directory.website/nonprofits.website the same unguarded way) contains zero SSRF-guard pattern (no private-IP check, no safeFetch import, no DNS-lookup validation). Highest-severity of the three SSRF findings by mechanism -- full browser navigation, not just an HTTP fetch, meaning JS execution, screenshot capture, and page-content extraction are all available against whatever the URL resolves to -- but NOT live-reproduced this session (a full Playwright browser launch was judged out of reasonable scope for this pass). Graded on direct static source evidence only; treat as high-confidence, not empirically confirmed the way WGR-108/WGR-109 are. **RESOLVED commit `6dd5f32` (2026-08-20):** call site independently re-confirmed this session (`worker/queue-processor.ts:567`, `const portalUrl = funder.giving_portal_url;`) and found to be worse than originally scoped — beyond the browser navigation (`page.goto(portalUrl!, ...)`, now further downstream in the same function), a second, separate raw `fetch(url, {method:'HEAD', ...})` call on the same unguarded `portalUrl` was found and confirmed at `worker/portal-health.ts` `quickHealthCheck()`, called from this same function before the browser is ever launched. Fixed by validating `portalUrl` with `assertUrlSafe()` (the same shared guard as WGR-108/109, imported from `src/lib/security/ssrf-guard.ts`) immediately after it's read from the `funders` row and before either downstream use; a blocked URL throws `SkipError('portal_url_blocked_ssrf: ...')`, which this pipeline's existing skip-handling already logs (`console.log`) and persists to `submission_queue.error_message` — no new logging plumbing needed. `worker/tsconfig.json` updated to include `src/lib/security/**/*.ts` in its build (it wasn't previously compiled into the worker's `dist/`); `npx tsc -p worker/tsconfig.json --noEmit` and a full `pnpm run build:worker` both pass, `dist/src/lib/security/ssrf-guard.js` confirmed emitted. Not re-graded to "empirically confirmed the way WGR-108/109 are" in the sense of launching a real Playwright session against a malicious `giving_portal_url` (still judged out of reasonable scope, same as the original finding) — the call site and its guard placement are confirmed by direct source read, and the guard's own block/allow behavior (the exact function now gating this call site) is independently live-verified against real DNS resolution and a real local listener in WGR-108/109's evidence. | test-evidence/pt-14/injection.json (attempts[] where id==="ssrf-003-autoapply-portal-url-navigation-static"); test-evidence/remediation/ssrf-fix/ssrf-guard-live-verify.json; test-evidence/remediation/ssrf-fix/safefetch-live-verify.json | Direct read of worker/queue-processor.ts:558-580 (pre-fix, portalUrl = funder.giving_portal_url, no validation) and src/lib/autoapply/stealth-browser.ts (grep for 169.254\|127.0.0.1\|isPrivate\|safeFetch -- zero hits) — pre-fix repro against unfixed code. Post-fix: direct read of worker/queue-processor.ts:567-583 (assertUrlSafe guard) and worker/portal-health.ts (quickHealthCheck, the second confirmed raw-fetch call site this guard also protects); `npx tsc -p worker/tsconfig.json --noEmit`; `pnpm run build:worker`. | RESOLVED |
| WGR-111 | Middleware/Availability | P0 | src/middleware.ts's catch-all matcher requires a valid Supabase session (a real, non-empty session cookie that passes supabase.auth.getUser()) for every path not explicitly listed in PUBLIC_PATHS/isPublicPath(). Neither /api/webhooks/*, /api/admin/webhooks/*, nor /api/cron/* (nor /api/sources/{grantsgov,propublica,samgov}, also cron_secret-gated) are in that allowlist. External callers that will never carry a Supabase session cookie -- Stripe (billing webhooks), Resend (email delivery/reply webhooks), and Vercel's own vercel.json-configured Cron invoker (5 real scheduled jobs: /api/cron/research, /api/cron/grantsgov, /api/cron/reminders, /api/cron/autoapply, /api/cron/domain-warmup) are therefore structurally indistinguishable from an unauthenticated attacker to this middleware. Live-confirmed this session: all 4 tested webhook routes (/api/webhooks/resend, /api/webhooks/stripe, /api/admin/webhooks/email-events, /api/admin/webhooks/email-reply) and all 7 tested cron/source routes returned a real 307 redirect to /login for a cookie-less POST, BEFORE the route's own signature/CRON_SECRET check ever ran -- this is the opposite of a CSRF hole (forged requests genuinely cannot reach the handler), but the same redirect applies to every real external caller too, since neither Stripe, Resend, nor Vercel Cron send a Supabase session cookie. If this holds for real traffic (the middleware has no way to distinguish a forged request from a real one, since both lack a session cookie), real Stripe billing events, real Resend delivery/reply events, and all 5 real Vercel Cron jobs may be silently redirected and never processed in production. NOT confirmed against an actual live Stripe/Resend delivery or a real Vercel Cron firing this session (would need dashboard/delivery-log access to those three external services, out of reach here) -- a real, repeated, direct production observation, not a certainty about external-service behavior. | test-evidence/pt-14/injection.json (attempts[] where id==="csrf-middleware-blocks-external-webhooks-and-cron" and the 11 individual csrf-unsigned-webhook-*/csrf-unauth-cron-* entries) | curl -i -X POST https://www.benavora.com/api/webhooks/stripe (or any of the 10 other listed routes) with no cookie -- observe `307` with `Location: /login`. Cross-check against src/middleware.ts:45-72 (PUBLIC_PATHS / isPublicPath()) and vercel.json's crons[] array. Post-fix: `node scripts/audit/wgr111-live-after.mjs` against production. **RESOLVED commit `031de0b` (2026-08-21):** a pre-audit read every `route.ts` under `src/app/api/cron`, `src/app/api/sources`, `src/app/api/webhooks`, `src/app/api/admin/webhooks` (21 files, `test-evidence/remediation/wgr-111/route-gate-audit.md`) to classify each as SECRET-GATED (18 routes: 11 `CRON_SECRET`-checked cron routes, 3 `CRON_SECRET`-checked source-poll routes, 4 signature-checked webhook routes) or not. `src/middleware.ts` gained an exact-path `SECRET_GATED_PATHS` allowlist (same `isPublicPath()` mechanism `/api/users/accept` already used) covering exactly those 18 routes — not a `/api/cron/*`/`/api/sources/*` prefix, so the 3 sibling routes that are NOT secret-gated (`/api/sources/registry`, `/api/sources/poll` — normal session-authenticated user routes, correctly left alone; `/api/sources/state-portals` — a previously-undocumented gap with NO auth check of its own at all, deliberately left un-exempted rather than silently opened, registered separately as **WGR-154**) keep relying on the middleware's session gate exactly as before. 8 new unit tests (`src/__tests__/unit/middleware-wgr111.test.ts`, `src/__tests__/unit/cron-route-auth.test.ts`) cover the real `middleware()` function's exemption behavior and a real cron route's own `CRON_SECRET` check (401 JSON on missing/wrong secret, 200 on the correct one) — all pass. `pnpm run build` and the full `vitest run` suite both pass (9 pre-existing failures in unrelated live-DB/Playwright/Claude integration tests and one unrelated IRS-990-URL regression test, none touching `middleware.ts` or any file this fix changed, confirmed by `git status`/`grep` cross-check — not a regression from this change). **Live-verified against real production** (`https://www.benavora.com`, deployment `dpl_56bAoAYZfK97EonWBgZxny1ioP2M`): 19/19 completed checks pass — all 14 `CRON_SECRET`-gated routes now return a real `401 {"code":"unauthorized"}` for an unauthenticated request (never a redirect); all 4 webhook routes return a real `500` ("webhook secret not configured") rather than a redirect — not a `401` because `STRIPE_WEBHOOK_SECRET`/`RESEND_WEBHOOK_SECRET` are genuinely absent from Vercel production (re-confirmed live via `vercel env ls production` this same session, matching the already-tracked **WGR-003** gap — not a WGR-111 regression, since the defect this row is about, the redirect, is gone either way); and the WGR-154 control (`state-portals`) still correctly 307s to `/login`, proving the exemption is an exact allowlist, not a broader prefix. **One supplementary check not completed**, noted rather than silently skipped: proving a *valid* `CRON_SECRET` bearer reaches a real 200 — `CRON_SECRET`'s real value is write-only (`vercel env pull` returned empty for it and every other encrypted var checked this session; the Vercel runtime-logs API returned a real `403 Forbidden` for this token/plan) and rotating the live secret to a known value was deliberately avoided as an unauthorized, hard-to-reverse production change. Not blocking RESOLVED status: the finding this row describes is specifically the redirect-before-any-check behavior, which the 19/19 live checks directly and completely disprove; the valid-secret path exercises each route's own pre-existing `CRON_SECRET` comparison logic, unchanged by this fix and already established correct by WGR-023's evidence. Full detail: `test-evidence/remediation/wgr-111/live-after.txt`, `test-evidence/remediation/wgr-111/live-after.json`. | RESOLVED |
| WGR-129 | API/AI-Drafts | P0 | PT-03-002 core journey stage 4 (draft generation): POST /api/ai/draft can return a genuine HTTP 200 with a full, real generated draft while persisting ZERO rows to draft_versions -- confirmed via an isolated diagnostic (test-evidence/pt-03/stage4-draft-persistence-diagnostic.json) that called the real, unmodified route directly inside an authenticated Playwright browser context (bypassing only the UI's own event handlers, not auth or routing): a real 31727-byte grant narrative came back with status 200 in 122365ms, agent_runs recorded status='completed' (agent_type='narrative_drafting', duration_ms=117498, tokens_used=7783 -- a real, successful, well-under-budget Claude call), yet `select count(*) from draft_versions where organization_id=<org>` was 0 both immediately after and on a later re-check. Root cause, confirmed by direct code + schema reads: draft_versions.version_number is NOT NULL with no column default; 009_draft_versions.sql's own design (see its header comment) is that a BEFORE INSERT trigger, trg_set_draft_version_number, assigns it so 'application code never supplies it' -- and generateDraft() (src/lib/drafts/generator.ts, insert at ~line 966-993) indeed never sets it. That trigger was absent from the local Supabase stack this PT-03 phase runs against (pg_trigger for draft_versions returned zero non-internal rows before this session's fix) -- the same class of local-schema gap this session's own pt03-002-core-journey.mjs already found and fixed once for `programs`/`pipeline_history` (see that script's header comment), now fixed a third time for this trigger, applied directly to the local, non-production stack only. But the finding is broader than one local-stack gap: generateDraft()'s draft_versions insert is deliberately best-effort -- on ANY insert error it only console.error()s 'DRAFT VERSION SAVE ERROR' and leaves savedVersion:null, never throwing, so /api/ai/draft (route.ts) returns the same 200 + full draftText/confidenceScore regardless of whether the DB write actually succeeded. The client, src/app/(dashboard)/draft-generator/page.tsx (~line 950, inside handleGenerate()), compounds this: its own comment reads 'The draft was already auto-saved to draft_versions by the API' and it never checks payload.savedVersion for null before calling setDraftText(...) and letting handleGenerateClick() advance the wizard to the Review & Export step (setStep(4)) -- so a savedVersion:null response renders identically to a real save, and a page reload loses the draft completely (no draft_versions row to restore from; the applications.draft_content mirror in generateDraft() at ~line 996-1025 is also a no-op at this point in the journey, since it only fires when an applications row already exists, which it does not yet -- Apply happens in a later journey stage). Separately, a full end-to-end run of the same stage through the real UI (not the isolated diagnostic) hit a second, correlated symptom: an otherwise identical draft-generation call was still status='running' in agent_runs (duration_ms and tokens_used both still null) after this test's own 480s client-side wait elapsed and the harness tore down the dev server -- real, observed latency variance (122s in one case, over 320s and still in flight in another) against the app's own maxDuration=300 budget, which local `next dev` does not enforce but Vercel production does; a kill at that boundary in production would terminate generateDraft() before its own catch block could mark agent_runs 'failed', reproducing the exact stuck-'running'-forever failure mode described (for a prior, already-fixed 60s-limit incident) in route.ts's own maxDuration comment -- recoverable today only via the existing >2-hour 'Clear Stuck Jobs' admin sweep already documented for a different table in WGR-125, and with zero client-side feedback in the interim (draft-generator page.tsx's fetch to /api/ai/draft has no AbortController/timeout of its own). 009_draft_versions.sql is recorded as fully APPLIED in MIGRATION_AUDIT.md's production structural audit, so production's draft_versions table and version_number column almost certainly exist -- but that audit's own stated methodology (CREATE TABLE / ADD COLUMN presence) does not cover trigger objects, so whether production's trg_set_draft_version_number trigger is itself currently live was NOT independently re-verified by this session and is flagged here as a residual gap for a future pass, not assumed either way. Regardless of that open question, the silent-swallow-and-200 pattern is a real, reproducible robustness gap on its own: any future drift that breaks this trigger (a migration rollback, a manual DDL change, a differently-provisioned environment, a transient DB error under load) reproduces total, silent draft loss with zero test coverage catching it and zero user-facing signal, compounded by the client's unconditional trust in a 200 response. **RESOLVED commit `fc8d0fe` (2026-08-20):** root cause confirmed (b) -- a persist that throws (returns a Supabase `{error}`) and was caught-and-ignored, not (a) missing call, (c) schema drift (the live `draft_versions` schema on the local stack matches generator.ts's insert column-for-column, and no PT-06 finding flags this table), or (d) a skip conditional. Fixed in `src/lib/drafts/generator.ts`'s `generateDraft()`: the `draft_versions` insert no longer `console.error()`s and continues with `savedVersion:null` on failure -- it now `throw`s, which the caller (`POST /api/ai/draft`, `route.ts`'s existing try/catch, unchanged) already correctly converts into a `500 {code:"generation_failed"}` and an `agent_runs.status='failed'` row, instead of a `200` claiming success. `GenerateDraftOutput.savedVersion` and `DraftResult.savedVersion` (src/types/ai.ts) tightened from `SavedDraftVersion | null` to `SavedDraftVersion` to make the new invariant (a 200 always carries a real saved version) a compile-time guarantee, not just a runtime one. The one other real caller of `generateDraft()`, `src/lib/drafts/auto-generator.ts` (the AutoApply draft-queue worker), already wraps the call in its own try/catch that marks the queue item failed on any thrown error -- this fix makes that path correct too, for free, with no change needed there. Live-verified against a real dev server + the real local Supabase stack, with two real (non-mocked) Claude API calls, not just a code read: (1) FAILURE case -- `trg_set_draft_version_number` dropped first (reproducing the exact real failure mode this finding documents), a real `POST /api/ai/draft` returned `500`, zero `draft_versions` rows exist for the call, `agent_runs.error_message` reads `"Failed to save draft version: null value in column \"version_number\" ... violates not-null constraint"`, `agent_runs.status='failed'` -- confirming the fix converts the documented silent-swallow into a real, loud failure. (2) SUCCESS case -- trigger restored, a second real `POST /api/ai/draft` returned `200` with a real 3576-character draft; the `draft_versions` row it wrote back (`version_number=1`) was independently read back and its `content` matches the response body's `content` byte-for-byte; `agent_runs.status='completed'`. `pnpm run build` exit 0. The correlated stuck-'running'-forever/maxDuration symptom this finding also describes is unchanged by this fix (out of scope for this pass -- a separate latency/timeout concern, not the persistence-swallow bug) and remains a residual, not re-registered separately here. | test-evidence/pt-03/stage4-draft-persistence-diagnostic.json (isolated root-cause reproduction), test-evidence/pt-03/core-journey.json#stages[3] (draft) and #findings[0] (PT03-002-F01) (full-journey run showing the correlated latency/orphaned-run symptom), test-evidence/pt-03/screenshots/04-draft-*.png; test-evidence/remediation/draft-loss-fix/draft-loss-fix-verify.json (post-fix failure+success proof) | node --import tsx scripts/audit/pt03-002-core-journey.mjs (stage 4) against the local pt05-local-stack; or independently, with a real authenticated session: POST /api/ai/draft {opportunityId,templateType:'grant_narrative'}, then compare `select count(*) from draft_versions where organization_id=<org>` against `select status, duration_ms, tokens_used from agent_runs where organization_id=<org> and agent_type='narrative_drafting' order by created_at desc limit 1` in the same window; `select tgname from pg_trigger where tgrelid='draft_versions'::regclass and not tgisinternal` shows whether trg_set_draft_version_number is present on the target database -- pre-fix repro against unfixed code. Post-fix: `node scripts/audit/draft-loss-fix-verify.mjs` (spins up a real dev server against the local stack, provisions a throwaway org/opportunity/writer session, drops then restores the trigger, and runs both real cases end to end). | RESOLVED |
| WGR-130 | API/Kanban | P0 | PT03-KA-F02 (test-evidence/pt-03/kanban-auth.json, kanban.enforcement_gap_tests[0]): getTransitionRule(discovered, drafting) correctly reports this stage-skip as illegal (allowed=false, reason="Applications can't skip stages. From 'Discovered' you can only advance to 'Eligibility Review'."), but executeTransition() (src/components/applications/pipeline.ts, ~line 526) itself performs no such check -- it is the pipeline's own exported, reusable function, and StageTransitionModal.tsx is the ONLY call site that checks getTransitionRule() before invoking it. Calling executeTransition() directly (as any other caller -- a future UI surface, a script, an API route -- would have to, since the rule check lives in the modal component, not the function) silently persisted the illegal skip: applications.stage was written to 'drafting' and a pipeline_history row was created recording from_stage='discovered', to_stage='drafting' as if it were a normal, legal transition. Confirmed live against a real authenticated org-scoped session, real application row (id=24b7e15f-3a0a-4d26-80b0-bedfff6725b5): write_threw=false, write_error=null, stage_after='drafting', outcome='PERSISTED'. There is no API route for application-stage mutation at all (confirmed by repo-wide search) -- the ONLY server-adjacent code path is this one client-callable function, and it enforces nothing itself. **FIX-APPLIED-BRANCH-VERIFIED commit `9b8982e` (2026-08-20):** new migration `supabase/migrations/141_application_stage_transition_trigger.sql` adds a `BEFORE UPDATE ON applications FOR EACH ROW WHEN (NEW.stage IS DISTINCT FROM OLD.stage)` trigger that transcribes pipeline.ts's own `FORWARD` const + `getTransitionRule()` backward-move rule verbatim as DB-layer data and `RAISE EXCEPTION`s (SQLSTATE `23514`) on any pair that doesn't match — enforced regardless of caller (app code, a future API route, a script, a raw REST call), closing exactly the gap this row describes (executeTransition() itself now cannot persist an illegal skip either, since the trigger sits below it, not just below the modal's pre-check). No admin-override/bypass path exists in the app design (confirmed by repo-wide search) or was added. **Exhaustively verified, not just spot-checked:** all 144 (from,to) pairs from this row's own `kanban.transition_matrix` evidence were replayed against the real trigger — 132/132 real transitions (from ≠ to) match the app's `getTransitionRule()` decision exactly; the only divergence is the 12 same-stage no-op pairs, where the trigger correctly does nothing (nothing changed) while the app reports `allowed:false` for UI-messaging reasons only — not a security gap. Verified on a real Postgres instance (this session's already-running `.pt05-local-stack` local Supabase stack, not a cloud branch — the cloud-branch path named in this task's own instructions was attempted first and found unusable: neither the `supabase` CLI's authenticated session nor the Supabase MCP connector's `list_projects` can see the real production project `vbjplpquqxxfbpazyalt` at all, only two unrelated projects on a different account/org — flagged rather than silently substituted). **NOT yet applied to production** -- deliberately deferred per this task's own instruction, part of the larger migration-drift remediation batch (WGR-041 et al.). `pnpm run build` exit 0 (no application source changed for this fix — DB-layer only). | test-evidence/pt-03/kanban-auth.json#findings[1] (PT03-KA-F02), #kanban.enforcement_gap_tests[0] (executeTransition_direct_bypass), #kanban.journeys (12-stage transition_matrix + 3 full journeys, 80 legal transitions confirmed persisting correctly, establishing this is a targeted bypass of a real, mostly-correct rule set -- not a broken rule set); test-evidence/remediation/statemachine-fix/statemachine-branch-verify.json; test-evidence/remediation/statemachine-fix/statemachine-full-matrix-verify.json | node --import tsx scripts/audit/pt03-005-kanban-auth.mjs against the local pt05-local-stack; or independently, with a real authenticated session: call executeTransition({supabase, applicationId, fromStage:'discovered', toStage:'drafting'}) directly (bypassing StageTransitionModal.tsx's own canConfirm/getTransitionRule() pre-check), then `select stage from applications where id=<id>` and `select from_stage,to_stage from pipeline_history where application_id=<id> order by created_at desc limit 1` -- pre-fix, both reflected the illegal skip as if it were legal. Post-fix: `node scripts/audit/statemachine-fix-branch-verify.mjs` (primary legal/illegal repro); `node scripts/audit/statemachine-fix-full-matrix-verify.mjs` (exhaustive 144-pair parity check) against a real database with migration 141 applied. | FIX-APPLIED-BRANCH-VERIFIED |
| WGR-131 | Data/RLS | P0 | PT03-KA-F03 (test-evidence/pt-03/kanban-auth.json, kanban.enforcement_gap_tests[1]): a raw supabase.from('applications').update({stage:'awarded'}) call -- with NO call to executeTransition() at all, i.e. bypassing WGR-130's already-broken enforcement layer entirely -- against a 'discovered'-stage row, from a real authenticated session that is a genuine member of the row's own organization, persisted with no error. The applications_org_isolation RLS policy (migration 001) scopes rows by organization_id only; it places no constraint on what value 'stage' may be set to, or on which prior stage value a given target is reachable from. There is no CHECK constraint, trigger, or any other database-level mechanism enforcing BEHAVIORAL_CONTRACTS Sec6's transition graph. Confirmed live: real application row (id=16705441-eee5-46a2-b056-3c65eb94b95e), from='discovered', to='awarded' (skipping 10 of the 12 documented stages in one write), supabase_error=null, stage_after='awarded', outcome='PERSISTED'. Any authenticated writer-or-above session in the org (or a bug in any future UI surface that writes this column directly) can set any application straight to any terminal stage from any starting stage with a single REST call. **FIX-APPLIED-BRANCH-VERIFIED commit `9b8982e` (2026-08-20):** same migration/trigger as WGR-130 (`141_application_stage_transition_trigger.sql`) — this row's own exact repro shape (a raw `.update({stage:...})` with no `executeTransition()` call) is the primary case the trigger is designed to close, since RLS/app-code enforcement structurally cannot. Live-reproduced the fix against this row's own exact scenario on a real Postgres instance: a fresh raw PATCH `discovered -> awarded` (skipping 10 of 12 stages, no app code involved) now returns HTTP 400 with the trigger's own `23514` error and an independent re-read confirms `stage` unchanged at `discovered` -- see `test-evidence/remediation/statemachine-fix/statemachine-branch-verify.json`, `illegalTransition` (same application-row pattern as this finding's own `id=16705441-...` repro, a fresh row on the verification stack). A real legal raw PATCH (`discovered -> eligibility_review`) in the same run still succeeds, confirming the fix blocks the exploit without blocking real writes. Verified on `.pt05-local-stack` (a real Postgres instance), not a cloud branch -- see WGR-130 for why the cloud-branch path named in this task's instructions was unusable this session. **NOT yet applied to production** -- deliberately deferred, part of the migration-drift remediation batch. `pnpm run build` exit 0. | test-evidence/pt-03/kanban-auth.json#findings[2] (PT03-KA-F03), #kanban.enforcement_gap_tests[1] (raw_update_bypass); test-evidence/remediation/statemachine-fix/statemachine-branch-verify.json | node --import tsx scripts/audit/pt03-005-kanban-auth.mjs against the local pt05-local-stack; or independently, with a real authenticated org-member session: supabase.from('applications').update({stage:'awarded'}).eq('id', <a discovered-stage row belonging to the caller's own org>) -- pre-fix, the write succeeded with no error regardless of the row's current stage. Post-fix: `node scripts/audit/statemachine-fix-branch-verify.mjs` against a real database with migration 141 applied -- the identical raw-PATCH bypass now returns a real 400 and the row's stage is confirmed unchanged. | FIX-APPLIED-BRANCH-VERIFIED |
| WGR-133 | Auth/PKCE | P0 | PT03-KA-F04 (test-evidence/pt-03/kanban-auth.json, findings[3]; also auth_flows.password_reset, outcome='FAIL'): a real, freshly-issued recovery link (not expired, not reused, real Mailpit-delivered email) was rejected by /reset-password as invalid ('This link is no longer valid'), even though the underlying PKCE code exchange genuinely succeeds server-side. Root-caused by direct, temporary instrumentation of ResetPasswordPageClient.tsx (added then reverted -- confirmed via `git diff` this repo carries no trace of it): its mount useEffect calls supabase.auth.exchangeCodeForSession(code) with no idempotency guard (no ref/flag preventing a repeat call, no AbortController, no cleanup). Because this app has reactStrictMode: true (next.config.mjs), React double-invokes every effect on every mount in `next dev` -- exactly the local/branch execution context this audit program runs against. The two invocations race for the single-use PKCE code_verifier stored in the sb-127-auth-token-code-verifier cookie: a real network response (200, confirmed via response capture) shows the exchange DOES succeed and a real session is briefly established, but the losing invocation's own exchangeCodeForSession call then fails with AuthPKCECodeVerifierMissingError (the verifier was already consumed) and calls setLinkError(true) unconditionally, with no check for whether a sibling invocation already succeeded -- so the user is shown a false link-is-invalid error for a link that was, in fact, exchanged successfully. Reproduced deterministically across three separate diagnostic runs, with the target route pre-warmed to rule out a Next.js dev-server first-compile/Fast-Refresh remount as the cause. Not independently verified against a production build (`next build && next start`, where Strict Mode's double-invoke does not occur) in this session -- registered as a real, currently-reproducing `next dev` defect, not asserted as confirmed-impacting in production. Practical effect observed live: the /reset-password form was never reachable through the UI (the link-invalid error block renders instead), so no new password could be submitted; an independent post-hoc check against GoTrue directly confirmed the old password still works and the intended new password was never accepted -- the password was genuinely never changed. | test-evidence/pt-03/kanban-auth.json#findings[3] (PT03-KA-F04), #auth_flows.password_reset (outcome=FAIL, full step-by-step trace including the independent GoTrue re-verification), test-evidence/pt-03/screenshots/auth-01-forgot-password-sent.png, test-evidence/pt-03/screenshots/auth-02-reset-password-complete.png | node --import tsx scripts/audit/pt03-005-kanban-auth.mjs against the local pt05-local-stack (reactStrictMode: true, next.config.mjs); or independently, under `next dev` only: request a real password reset, follow the real recovery link to /reset-password, and observe that a genuinely valid, first-use link renders 'This link is no longer valid' due to exchangeCodeForSession() being called twice by React's Strict Mode double-invoke of the mount effect in ResetPasswordPageClient.tsx. | CONFIRMED-BROKEN |
| WGR-138 | Integration/Parser | P0 | Grants.gov (src/lib/sources/grantsgov-client.ts searchGrantsGovOpportunities(), the live path behind the daily Vercel Cron /api/cron/grantsgov "0 7 * * *" and /api/sources/grantsgov): the app-coded URL (https://api.grants.gov/grantsws/rest/opportunities/search/v2) returns a real, reproducible HTTP 403 "Missing Authentication Token" (AWS API Gateway error — the path no longer resolves to a live route) on every call, confirmed twice. searchGrantsGovOpportunities() treats any !response.ok as a silent empty array — no error is ever surfaced, so the daily cron has been finding 0 opportunities on every run. The real, current public endpoint (https://api.grants.gov/v1/api/search2, live-confirmed HTTP 200) would not fix this alone: its response wraps hits in data.oppHits, not the top-level oppHits the app reads, and real hits use "title" (not "oppTitle") with no "synopsis" or "awardCeiling" field at all — both fields mapHit() reads for description/amount are confirmed absent from the real schema entirely. Complements WGR-126 (same file, malformed-JSON-crash case from a mocked fetch) — this is the well-formed-but-wrong-shape case: it never crashes, it silently returns nothing, forever. **RESOLVED commit `cde8cd9` (2026-08-20):** `GRANTS_GOV_SEARCH_URL` switched to `https://api.grants.gov/v1/api/search2`; parser now reads `body.data.oppHits` (not top-level) and `hit.title` (not `hit.oppTitle`); `description`/`amount` now honestly map to `null` since search2 list hits carry no synopsis/award-ceiling field (would require a separate per-opportunity detail call, out of scope here — the prior code was silently reading fields that never existed on this endpoint either). Live-verified end-to-end through the real, fixed `searchGrantsGovOpportunities('housing')` function (not a hand-rolled probe): 100 real records returned, 100% shape-valid (`externalId`/`name`/`deadline` populated). Before/after evidence: `test-evidence/remediation/int-fix/wgr-138-grantsgov-live-before.json` (real 403 reproducing the as-coded bug), `test-evidence/remediation/int-fix/wgr-138-grantsgov-live-after.json` (real 100-record success through the fixed function). `pnpm run build` exit 0. | test-evidence/pt-07/data-sources.json#sources.grants_gov; test-evidence/remediation/int-fix/wgr-138-grantsgov-live-before.json; test-evidence/remediation/int-fix/wgr-138-grantsgov-live-after.json | node scripts/audit/pt07-003-data-sources.mjs (re-runs the real live calls); node scripts/audit/verify-pt07-003.mjs (validates the captured evidence); or curl -X POST https://api.grants.gov/grantsws/rest/opportunities/search/v2 -H "Content-Type: application/json" -d '{"keyword":"housing"}' -> 403 (pre-fix repro, path still dead). Post-fix repro: `npx tsx scripts/audit/int-fix-live-after.mjs`. | RESOLVED |
| WGR-139 | Integration/Parser | P0 | SAM.gov opportunities/v2/search (src/lib/sources/samgov-client.ts searchSamGovOpportunities(), the live path behind /api/sources/samgov): as coded, the request omits postedFrom/postedTo. The real, current API rejects this on every call with HTTP 400 "PostedFrom and PostedTo are mandatory" — confirmed live. Same non-crashing, silent failure mode as WGR-138: searchSamGovOpportunities() returns an empty array on !response.ok with no error surfaced, so this integration returns 0 real results on literally every real invocation today. Adding the two mandatory params (verified live, same session) makes the endpoint return real data — the fix is small, but the current live behavior is a total, silent no-op. **FIX-APPLIED-PENDING-VERIFICATION commit `cde8cd9` (2026-08-20):** `searchSamGovOpportunities()` now sends `postedFrom`/`postedTo` (MM/dd/yyyy, trailing 365 days — SAM.gov's own max range) alongside `ptype=o`; the pre-fix 400 reproduces cleanly and is captured live (`wgr-139-samgov-opportunities-live-before.json`). **Not yet live-verified through the fixed function against a >0-record real response** — this session's SAM.gov API key hit its real daily quota (`HTTP 429 "Message throttled out" ... "You can access API after 2026-Aug-21 00:00:00+0000 UTC"`, confirmed via a direct live call, not assumed) partway through remediation, before the post-fix call could be made. `tsc`/`pnpm run build` both pass (code compiles), and the same date-window fix is independently confirmed correct against the real API by this row's own pt-07 evidence (`with_mandatory_date_range_diagnostic`, HTTP 200, 4,274 real records) — but that diagnostic call was made before the fix was committed, against a hand-written probe, not the actual fixed `searchSamGovOpportunities()` function, so it does not substitute for this row's own required post-fix live proof. **RESOLVED, update (2026-08-21):** the 2026-08-21 quota-reset re-run (`npx tsx scripts/audit/int-fix-live-after.mjs`) did NOT return the expected >0 records — it returned `count=0, error=none`, exposing a second, previously-undiscovered bug hiding behind the quota block: `searchSamGovOpportunities()`'s own trailing-365-day `postedFrom`/`postedTo` window is itself rejected by the real API with a fresh, distinct `HTTP 400 {"errorCode":"400","errorMessage":"Date range must be no more than 1 year apart"}` (confirmed via a direct, non-mocked curl of the exact URL the function builds), which the function's existing `if (!response.ok) return []` swallows silently — the same non-throwing failure mode this row's original finding already describes, just triggered by a different cause than the one originally fixed. Root cause: SAM.gov's "1 year" boundary is exclusive of a full 365-day span from "today," not inclusive. Fixed by changing `DEFAULT_DAYS_BACK` from `365` to `364` in `src/lib/sources/samgov-client.ts`. Live-reverified immediately after: a direct curl with the corrected 364-day window returns real `HTTP 200` (`totalRecords:8639`), and a fresh run of the real, unmodified `searchSamGovOpportunities()` function returns `count=100, shapeOk=true, error=none` — `test-evidence/remediation/int-fix/wgr-139-samgov-opportunities-live-after.json`. `npx tsc --noEmit` clean. | test-evidence/pt-07/data-sources.json#sources.sam_gov; test-evidence/remediation/int-fix/wgr-139-samgov-opportunities-live-before.json; test-evidence/remediation/int-fix/wgr-139-samgov-opportunities-live-after.json (post-fix, count=100) | node scripts/audit/pt07-003-data-sources.mjs (re-runs the real live calls); node scripts/audit/verify-pt07-003.mjs (validates the captured evidence); or curl "https://api.sam.gov/opportunities/v2/search?api_key=<key>&ptype=o&limit=5" -> 400 (pre-fix repro, still reproduces against the unfixed request shape). Post-fix verification (confirmed 2026-08-21, real 100-record success): `npx tsx scripts/audit/int-fix-live-after.mjs`. | RESOLVED |
| WGR-142 | Integration/Parser | P0 | SAM.gov Entity Management API v3 (src/lib/donor-discovery/adapters/samgov-adapter.ts searchEntitiesByNaics(), a separate implementation from samgov-client.ts, same api.sam.gov host): the request unconditionally sets an "activeDate" query param. The real, current v3 Entity API rejects this outright with HTTP 400 "The search parameter, activeDate does not exist.", confirmed live (naicsCode=236220, purposeOfRegistrationCode=Z2). Unlike WGR-138/139, this function throws a real SamGovError on !response.ok rather than degrading silently — but the net effect is the same: this donor-discovery corporate-entity-by-NAICS path is fully broken on every real call. **FIX-APPLIED-PENDING-VERIFICATION commit `cde8cd9` (2026-08-20):** `searchEntitiesByNaics()` no longer sets `activeDate` at all (removed from both the request and the `SearchEntitiesOptions` type — the real API doesn't accept it as a parameter under any value); pre-fix 400 reproduced live and captured (`wgr-142-samgov-entity-live-before.json`). **Not yet live-verified through the fixed function against a >0-record real response** — a post-fix call was attempted this session and itself hit `HTTP 429` (SAM.gov's real per-key daily quota, exhausted by this session's own probes, confirmed via direct live call, resets 2026-08-21T00:00:00Z), captured in `wgr-142-samgov-entity-live-after.json` as a real 429/error result, not a >0-record success — so this row stays open per this task's own standard ("a fix that compiles but still returns zero real records is NOT done"). `tsc`/`pnpm run build` both pass. **RESOLVED, update (2026-08-21):** the 2026-08-21 quota-reset re-run returned a fresh, distinct `HTTP 400` (not the prior 429): `{"message":"size is 100","detail":"Size Cannot Exceed 10 Records","errorCode":"SCE"}` — a second, previously-undiscovered bug hiding behind the quota block, unrelated to the `activeDate` bug this row originally fixed. Root cause: the real Entity Management API v3 hard-caps `size` at 10 records, far below the 100 this function requested by default (`SearchEntitiesOptions.size` default) and below the Opportunities API's own, much higher limit — confirmed live via direct curl (`size=100` -> 400 `SCE`; `size=10` -> real 200 with `totalRecords:81915`). Fixed in `src/lib/donor-discovery/adapters/samgov-adapter.ts`: default lowered to 10 and the effective value is now clamped with `Math.min(opts.size ?? 10, 10)` regardless of what a caller passes, so this ceiling can't regress even if a future caller requests more. Live-reverified: a fresh run of the real, unmodified `searchEntitiesByNaics('236220')` function returns `count=10, shapeOk=true, error=none` — `test-evidence/remediation/int-fix/wgr-142-samgov-entity-live-after.json`. `npx tsc --noEmit` clean. | test-evidence/pt-07/data-sources.json#sources.sam_gov.additional_checks.entity_management_v3; test-evidence/remediation/int-fix/wgr-142-samgov-entity-live-before.json; test-evidence/remediation/int-fix/wgr-142-samgov-entity-live-after.json (post-fix, count=10) | node scripts/audit/pt07-003-data-sources.mjs (re-runs the real live calls); node scripts/audit/verify-pt07-003.mjs (validates the captured evidence); or curl "https://api.sam.gov/entity-information/v3/entities?api_key=<key>&purposeOfRegistrationCode=Z2&naicsCode=236220&activeDate=08/20/2026" -> 400 (pre-fix repro, still reproduces against the unfixed request shape). Post-fix verification (confirmed 2026-08-21, real 10-record success): `npx tsx scripts/audit/int-fix-live-after.mjs`. | RESOLVED |
| WGR-143 | Integration/Parser | P0 | SAM.gov Award Notices (src/lib/donor-discovery/adapters/samgov-adapter.ts searchRecentAwardRecipients()/normalizeAwardee(), ptype=a on the same opportunities/v2/search endpoint as WGR-139/140/141, a second, separate consumer of that endpoint): normalizeAwardee() reads raw.awardee?.name directly off each hit, but the real API nests the awardee block one level deeper, at hit.award.awardee — confirmed live: 0 of 5 real sampled Award Notice hits carry a top-level "awardee" field, while 5 of 5 carry it at the real path (hit.award.awardee, with a real {name, location} shape). Since legalName is always undefined at the path the code reads, every hit is silently skipped and searchRecentAwardRecipients() returns zero prospects on every real call, with no error. **FIX-APPLIED-PENDING-VERIFICATION commit `cde8cd9` (2026-08-20):** `normalizeAwardee()` and its `RawAwardOpportunity` type now read `raw.award?.awardee` (nested), not `raw.awardee` (top-level). **Not yet live-verified through the fixed function against a >0-record real response** — a post-fix call was attempted this session and itself hit `HTTP 429` (same SAM.gov daily quota exhaustion as WGR-139/142, confirmed via direct live call, resets 2026-08-21T00:00:00Z), captured in `wgr-143-samgov-award-notices-live-after.json` as a real error result, not a >0-record success. `tsc`/`pnpm run build` both pass; this row's own pt-07 evidence already confirms the corrected field path is right against real data (5/5 real sampled hits carry `award.awardee`, 0/5 carry top-level `awardee`) — but that was a pre-fix structural probe, not a post-fix run of the actual fixed function, so it does not substitute for this row's own required post-fix live proof. **RESOLVED, update (2026-08-21):** the 2026-08-21 quota-reset re-run of the real, unmodified `searchRecentAwardRecipients(90)` function returned `count=678, shapeOk=true, error=none` on the first try — no further fix needed for this row specifically (the `award.awardee` field-path fix already committed in `cde8cd9` was sufficient once the daily quota reset; contrast WGR-139/142, which each hid a second, distinct bug behind the same quota block). Sample of 3 real records (legal_name, hq_address, naics_codes all populated, `directory_id` confirms each was upserted into `donor_discovery_directory`) captured in `test-evidence/remediation/int-fix/wgr-143-samgov-award-notices-live-after.json`. | test-evidence/pt-07/data-sources.json#sources.sam_gov.additional_checks.award_notices_ptype_a; test-evidence/remediation/int-fix/wgr-143-samgov-award-notices-live-before.json; test-evidence/remediation/int-fix/wgr-143-samgov-award-notices-live-after.json (post-fix, count=678) | node scripts/audit/pt07-003-data-sources.mjs (re-runs the real live calls); node scripts/audit/verify-pt07-003.mjs (validates the captured evidence); or curl "https://api.sam.gov/opportunities/v2/search?api_key=<key>&ptype=a&limit=5&postedFrom=05/22/2026&postedTo=08/20/2026" and inspect opportunitiesData[0].award.awardee vs. opportunitiesData[0].awardee (pre-fix repro, still reproduces against the unfixed field path). Post-fix verification (confirmed 2026-08-21, real 678-record success): `npx tsx scripts/audit/int-fix-live-after.mjs`. | RESOLVED |
| WGR-002 | Config/Deploy | P1 | `STANDING_DIRECTIVES.md` DIRECTIVE-019 deploy-verifier prerequisites (`VERCEL_TOKEN`, `VERCEL_PROJECT_ID`) confirmed absent from local `.env.local`. Previously-known, already-documented gap (DIRECTIVE-019's own text already flags this) — `scripts/verify-deployment.ts` and the FORGE `deploy_verify` gate degrade to PENDING/INDETERMINATE (warn, does not block) rather than a real PASS/FAIL without them. This audit re-confirms the absence still holds; it did NOT check whether these are set in Vercel/Railway's own environment (production env vars are outside the local-`.env.local`-only method used here). | `test-evidence/pt-00/env-audit.json` (`summary.p1_findings`, `vars[]` rows for `VERCEL_TOKEN`/`VERCEL_PROJECT_ID`) | `node scripts/audit/verify-pt00-004.mjs` (prints these 2 under `[P1]`) | CONFIRMED-BROKEN |
| WGR-005 | API | P1 | `GET /api/agents/discovery` returns `500 {"error":"Failed to load discovery matches."}` on an authenticated, no-query-param request during the PT-00-005 smoke sweep. P1 not P0: not consumed by any dashboard page found via repo-wide grep — a backing endpoint with no confirmed primary-nav consumer. **Root cause (PT-02-005, live-confirmed):** column-name mismatch. The route queries `.eq("organization_id", ctx.orgId)` against `discovery_matches`, but the live table's real column is `org_id`, not `organization_id`. Traced precisely: the table was first created by `src/supabase/migrations/075_agent_marketplace.sql` with `org_id` — this is the shape that actually landed in production. A later migration (`supabase/migrations/095_discovery_matches.sql`) tried to rename it to `organization_id`, but its `CREATE TABLE IF NOT EXISTS` silently no-op'd against the already-existing table. A still-later RLS-hardening migration (`src/supabase/migrations/119_org_scoped_tables_rls_hardening.sql` line 51) already documents this exact drift ("`discovery_matches: org_id NOT NULL (not organization_id — confirmed live)`") and fixed the RLS policies, but never touched this route's application code. Live-reproduced the exact failure: a query matching the route's shape returns Postgres `42703: column "organization_id" does not exist`, and the equivalent PostgREST REST call returns `HTTP 400 {"code":"42703",...,"message":"column discovery_matches.organization_id does not exist"}`. A second, independent call site with the identical bug (`src/lib/agents/morning-digest.ts` line 38) was found during diagnosis — noted as corroborating evidence, not registered separately. | `test-evidence/pt-00/smoke-results.json` (`results[]` where `path === "/api/agents/discovery"`); `test-evidence/pt-02/pagination-and-500s.json` (`known500s.findings[]` where `wgrId === "WGR-005"`) | `GET /api/agents/discovery` with a valid authenticated session, no query params. Handler: `src/app/api/agents/discovery/route.ts`. Root-cause repro: `node scripts/audit/verify-pt02-005.mjs`, or query `discovery_matches` filtered on `organization_id` directly against `DATABASE_URL` to reproduce the `42703` error. | CONFIRMED-BROKEN |
| WGR-006 | API | P1 | `GET /api/consultant/clients` returns `500 {"error":"Failed to load client access grants.","code":"load_failed"}` on an authenticated, no-query-param request during the PT-00-005 smoke sweep. P1 not P0: backs `/settings/white-label`, a sub-settings page, not primary nav. **Root cause (PT-02-005, live-confirmed):** missing table. The route queries `consultant_client_access` (defined in `supabase/migrations/086_white_label.sql` with columns matching the route's select list exactly), but that migration was never applied to production — the table does not exist live. Confirmed via `to_regclass('public.consultant_client_access')` returning null, and a live REST call returning `HTTP 404 {"code":"PGRST205",...,"message":"Could not find the table 'public.consultant_client_access' in the schema cache"}`. Application code is correct against the migration file on disk; the gap is purely that the migration was never applied. | `test-evidence/pt-00/smoke-results.json` (`results[]` where `path === "/api/consultant/clients"`); `test-evidence/pt-02/pagination-and-500s.json` (`known500s.findings[]` where `wgrId === "WGR-006"`) | `GET /api/consultant/clients` with a valid authenticated session, no query params. Handler: `src/app/api/consultant/clients/route.ts`. Root-cause repro: `SELECT to_regclass('public.consultant_client_access')` against `DATABASE_URL` — returns null. | CONFIRMED-BROKEN |
| WGR-007 | API | P1 | `GET /api/outreach/sequences` returns `500 {"error":"Failed to load follow-up sequences.","code":"db_error"}` on an authenticated, no-query-param request during the PT-00-005 smoke sweep. This matches a previously-documented, still-unresolved gap: `STATE_OF_THE_BUILD.md`'s 2026-08-13 "outreach consolidation audit" session already found this route queries a `followup_sequences` table confirmed absent in production (`NEEDS REID'S DECISION` — apply dormant migration 083 vs. retire in favor of AG-28's `application_followups`). **Re-confirmed live (PT-02-005), still holds, decision still owed, not made here:** `followup_sequences` (`supabase/migrations/083_followup_sequences.sql`, columns match the route exactly) is still absent from production (`to_regclass` null, live REST `HTTP 404 PGRST205`). Checked the alternative path too, since a decision toward it would need it ready: `application_followups` (AG-28's model) is **also** confirmed absent from production today — neither option is a ready-to-flip switch without its own migration-apply step first. This task does not decide or apply either; the product decision remains open. | `test-evidence/pt-00/smoke-results.json` (`results[]` where `path === "/api/outreach/sequences"`); `test-evidence/pt-02/pagination-and-500s.json` (`known500s.findings[]` where `wgrId === "WGR-007"`, `.pendingProductDecision`) | `GET /api/outreach/sequences` with a valid authenticated session, no query params. Handler: `src/app/api/outreach/sequences/route.ts`. Root-cause repro: `SELECT to_regclass('public.followup_sequences'), to_regclass('public.application_followups')` against `DATABASE_URL` — both return null. | CONFIRMED-BROKEN |
| WGR-008 | API | P1 | `GET /api/schoolfunder` returns `500 {"error":"Failed to load students.","code":"load_failed"}` on an authenticated, no-query-param request during the PT-00-005 smoke sweep. P1 not P0: backs the standalone Faith Foundation showcase feature (`BLUEPRINT.md` §1), not core Benavora SaaS navigation. **Root cause (PT-02-005, live-confirmed):** missing table(s). The route queries `schoolfunder_students` (and its two siblings `schoolfunder_volunteer_hours`/`schoolfunder_donations`), all defined in `supabase/migrations/103_schoolfunder.sql` with columns matching the route exactly — none of the three exist in production; that migration was never applied. Matches `STATE_OF_THE_BUILD.md`'s Migration Audit finding verbatim ("SchoolFunder (migration 103) has zero backing tables despite being a confirmed-kept live feature"). Confirmed via `to_regclass()` returning null for all three, and a live REST call returning `HTTP 404 {"code":"PGRST205",...,"message":"Could not find the table 'public.schoolfunder_students' in the schema cache"}`. | `test-evidence/pt-00/smoke-results.json` (`results[]` where `path === "/api/schoolfunder"`); `test-evidence/pt-02/pagination-and-500s.json` (`known500s.findings[]` where `wgrId === "WGR-008"`) | `GET /api/schoolfunder` with a valid authenticated session, no query params. Handler: `src/app/api/schoolfunder/route.ts`. Root-cause repro: `SELECT to_regclass('public.schoolfunder_students')` against `DATABASE_URL` — returns null. | CONFIRMED-BROKEN |
| WGR-009 | API | P1 | `GET /api/settings/notifications` returns `500 {"error":"Failed to load notification preferences.","code":"db_error"}` on an authenticated, no-query-param request during the PT-00-005 smoke sweep. P1 not P0: backs `/settings/notifications`, a sub-settings page, not primary nav. **Root cause (PT-02-005, live-confirmed):** missing table. The route queries `notification_preferences` (defined in `supabase/migrations/087_notification_preferences.sql` with columns matching the route exactly), but that migration was never applied to production. Confirmed via `to_regclass('public.notification_preferences')` returning null, and a live REST call returning `HTTP 404 {"code":"PGRST205",...,"message":"Could not find the table 'public.notification_preferences' in the schema cache"}`. | `test-evidence/pt-00/smoke-results.json` (`results[]` where `path === "/api/settings/notifications"`); `test-evidence/pt-02/pagination-and-500s.json` (`known500s.findings[]` where `wgrId === "WGR-009"`) | `GET /api/settings/notifications` with a valid authenticated session, no query params. Handler: `src/app/api/settings/notifications/route.ts`. Root-cause repro: `SELECT to_regclass('public.notification_preferences')` against `DATABASE_URL` — returns null. | CONFIRMED-BROKEN |
| WGR-012 | Frontend/Page | P1 | `/donor-discovery/prospects/[id]` (`ProspectDetail.tsx`): real, reproducible crash for prospects whose linked directory row has a partial `enrichment` jsonb object. Confirmed live against a real, currently-existing prospect for the Faith Foundation org (`19ec603e-bb4d-4e10-9482-e864dc85f511`, directory row `a5e41547-8819-40ad-981b-dc191b5f26ff`, `enrichment: {ein, ntee_code, asset_amount, giving_total}` only -- missing `giving_focus_areas`/`in_kind_history_signals`/`has_giving_program`/`has_donation_form`/`decision_contacts`). Lines 574/587 read `enrichment.giving_focus_areas.length`/`enrichment.in_kind_history_signals.length` unguarded once `enrichment` itself is truthy, throwing `Cannot read properties of undefined (reading 'length')` and blanking the entire page (`<main>` renders 0 characters, `httpStatus: 200`). Reproduced identically across 4 independent navigation attempts (2 built-in retries, 1 final-recheck pass, 1 standalone confirmation query) with zero build-contention signature present -- a real application bug, not environmental. P1 not P0: a drilldown detail page, not the literal top-level `/donor-discovery` nav entry itself. **RESOLVED commit `d5500cd` (2026-08-19):** `giving_focus_areas`/`in_kind_history_signals`/`decision_contacts` all guarded with `?.length ?? 0` and `?.map`; `DonorProspectExtraction`'s type loosened to mark all three fields optional to match the real backend shape. Re-verified live against `19ec603e-bb4d-4e10-9482-e864dc85f511` (same id this row's own finding used) -- now renders 716 chars of real content, no error boundary. `pnpm run build` exit 0. | `test-evidence/pt-01/render-results.json` (`results[]` where `path === "/donor-discovery/prospects/[id]"`), `test-evidence/pt-01/render-failures/donor_discovery_prospects_id.png` -- pre-fix; `test-evidence/pt-01/wgr-017-fix/wgr-017-fix-verify.json`, `test-evidence/pt-01/wgr-017-fix/after_19ec603e.png` -- post-fix | Authenticated navigation to `/donor-discovery/prospects/19ec603e-bb4d-4e10-9482-e864dc85f511` (real, live prospect for the Faith Foundation org). Root cause: `src/components/donor-discovery/ProspectDetail.tsx` lines 574 and 587 (pre-fix). See `scripts/audit/pt01-002-render-pass.mjs` (original repro), `scripts/audit/wgr017-fix-verify.mjs` (fix verification, commit `d5500cd`). | RESOLVED |
| WGR-027 | API | P1 | PT-02-004 CRUD cycle: POST /api/email/templates unconditionally 500s ("Failed to create template.", code db_error) for every request, on any org, at writer role. Root cause: the route inserts/selects columns named subject and body, but the live email_templates table (migration 054_email_calendar_integration.sql) defines subject_template and body_template -- there is no subject/body column on this table at all. This is not an edge case: template creation is fully, unconditionally broken via this API for every caller. The PATCH/DELETE handlers in the same file read/select the same nonexistent subject/body columns and would fail identically once a row existed to target. | test-evidence/pt-02/crud-cycles.json (resources[] where resource==="email_templates", steps.create) | As a writer-role session: POST /api/email/templates {name, template_type, subject, body} -- returns 500 {"error":"Failed to create template.","code":"db_error"}. Handler: src/app/api/email/templates/route.ts POST (and PATCH/DELETE reference the same nonexistent columns). Table definition: supabase/migrations/054_email_calendar_integration.sql (subject_template/body_template, not subject/body). | CONFIRMED-BROKEN |
| WGR-029 | API | P1 | PT-02-005 pagination audit: `GET /api/donor-discovery/requests/[id]` (`src/app/api/donor-discovery/requests/[id]/route.ts` lines 33-36 and 46-50) issues two unbounded `.select()` calls against `dd_prospect_requests` and `donor_discovery_prospects` -- no `.range()`/`.limit()` on either. Live-reproduced against a real, currently-existing production request (`cf95be82-b66c-48af-a00b-782852b132b5`, org `b1ab7402-dfc2-4712-869f-70ea3566cc1d` -- the real Faith Foundation org, 133,812 real linked prospects, per WGR-017's own already-documented row-count growth): the app's own query for this exact request returns exactly 1000 linked prospect ids (`Content-Range: 0-999/133812`), so `progress.prospect_count` and `progress.stage_breakdown` silently report ~1000 instead of the real 133,812 -- a 99.3% undercount, live, in production, today, for a real request a real user could open right now. P1: scoped to one request-detail endpoint, degrades to a wrong number rather than a crash, but the wrongness is severe (2 orders of magnitude) and currently active, not hypothetical. **RESOLVED commit `5c747ee` (2026-08-20):** the id-collection query was replaced with a PostgREST `!inner` embed (`dd_prospect_requests` filtered directly by `donor_discovery_prospects` join, no client-side id list ever built) so `count`/aggregation reflect every real linked row regardless of count. Live-verified against this row's own real request (`cf95be82-...`, 133,812 real links) via a direct read-only PostgREST call before committing: `Content-Range: 0-2/133812`, matching the true total exactly (previously capped at `0-999/133812`). `tsc --noEmit` and `npm run build` both exit 0. | test-evidence/pt-02/pagination-and-500s.json (`paginationAudit.endpoints[]` where `wgrId === "WGR-029"`; `tableRowCounts.notes`) | `GET /api/donor-discovery/requests/cf95be82-b66c-48af-a00b-782852b132b5` as an authenticated session for org `b1ab7402-dfc2-4712-869f-70ea3566cc1d` -- compare `progress.prospect_count` (reported) against `SELECT count(*) FROM dd_prospect_requests WHERE request_id = 'cf95be82-b66c-48af-a00b-782852b132b5'` (real: 133812) via `DATABASE_URL`. | RESOLVED |
| WGR-031 | API | P1 | PT-02-005 pagination audit: `GET /api/intelligence/proposals` (`src/app/api/intelligence/proposals/route.ts` line 49, and `src/lib/intelligence/proposals-query.ts`'s `computeFilteredStats()` line 318) -- the route's own main paginated results list (line 47, `.range(from,to)`) is correctly paginated and NOT part of this finding, but two sibling queries fired in the same `Promise.all` truncate: `sourcesRes` (`.select("source")`, zero limit) and `computeFilteredStats` (`.limit(5000)`, which PostgREST silently re-caps to 1000 regardless of the app's own higher limit -- see WGR-028). Live table `intelligence_funded_proposals` has 3,489 real rows, already past the 1000 cap, and both truncations are live-reproduced as currently, actively wrong: (1) 2 real, currently-existing sources (`FEDERAL_REGISTER`, `USASPENDING`) are silently absent from the Intelligence Library's filter-dropdown `stats.sources` field returned to every user of this page; (2) the displayed average award amount (`filteredStats`) is $1,010,648 -- computed from the first, arbitrarily-ordered 1000 of 3,489 rows -- versus the real $37,933,899 across all 3,489 rows, a ~37x understatement shown live today. P1: a real, live, feature-facing wrong statistic on a page every user of the Intelligence Library sees, not a crash and not hypothetical. **RESOLVED commit `5c747ee` (2026-08-20):** both truncating queries (`sourcesRes` and `computeFilteredStats`) now page through with `.range()` via the shared `selectAllPages()` helper instead of a single unbounded/re-capped `.limit()` call. Live-verified against the real 3,489-row corpus via a direct read-only PostgREST call before committing: `Content-Range: 0-2/3489`, confirming the paginated query shape reaches the true total rather than stopping at 1000. `tsc --noEmit` and `npm run build` both exit 0. | test-evidence/pt-02/pagination-and-500s.json (`paginationAudit.endpoints[]` where `wgrId === "WGR-031"`) | `GET /api/intelligence/proposals` as any authenticated session -- compare `stats.sources` (missing FEDERAL_REGISTER, USASPENDING) and `filteredStats`'s average award amount ($1,010,648) against `SELECT DISTINCT source FROM intelligence_funded_proposals` and `SELECT round(avg(award_amount)) FROM intelligence_funded_proposals` (real: $37,933,899) via `DATABASE_URL`. | RESOLVED |
| WGR-033 | Worker/Boot | P1 | PT-08 worker boot inventory: worker/enrichment-processor.ts (a complete, real start()/stop()/waitForIdle() processor -- the EA-01..EA-10 corporate-enrichment agent pipeline plus AG-22 PropensityScoringAgent) is never registered at boot. worker/index.ts's main() imports and starts 6 other processors (heartbeat, queueProcessor, ddRequestProcessor, knowledgeIndexerProcessor, confirmationMonitor, scheduler) plus the agent-queue processor, but has no call to enrichment-processor.ts anywhere -- confirmed by a repo-wide grep finding exactly 2 references to the string "enrichment-processor" in the whole *.ts tree: the file's own definition, and one comment-only mention in src/lib/agents/ag-22-propensity-scoring.ts ("worker/enrichment-processor.ts's triggerScoreEngine() is the real call site"), which is documentation, not an import. runEnrichmentBatch() (the function start() calls) has exactly 1 reference in the whole repo -- its own definition -- and scripts/ has zero references either, so there is no on-demand CLI entry point substituting for the missing continuous loop. This is a direct, confirmed doc-vs-code mismatch, not just an absence of evidence: WORKER_ARCHITECTURE_v2.md line 92, the canonical governance doc's own documented boot sequence, states step 6 is "Start enrichment processor loop (continuous, lower priority)" -- no such call exists in the real worker/index.ts. Practical effect: all 10 EA-0X agents and AG-22 (PropensityScoringAgent, confirmed to have this file as its ONLY call site anywhere in the repo) are unreachable via any automatic, continuous, or on-demand production path today -- consistent with, and now freshly re-confirmed for, the gap this project's own governance history (STATE_OF_THE_BUILD.md, multiple sessions since 2026-07-28) already flagged as "not called from worker/index.ts's boot sequence yet." That prior note left open the possibility of a standalone/on-demand trigger; this pass confirms there isn't one -- the only way this code has ever executed is a manually-written, throwaway verification script (see AGENT_VERIFICATION_LOG.md's multiple AG-22 diagnosis entries). | test-evidence/pt-08/boot-inventory.json (processors[] where module === "worker/enrichment-processor.ts"); test-evidence/pt-08/railway-boot-window.json (the complete, real container-boot log window showing every OTHER processor's own "Starting" line firing, with no equivalent line for enrichment-processor anywhere in the deployment's history) | grep -rn "enrichment-processor" --include=*.ts . (excluding node_modules) -- 2 hits, neither an import. grep -n "runEnrichmentBatch" --include=*.ts -r . -- 1 hit, its own definition. grep -n "^##\\|Start enrichment" WORKER_ARCHITECTURE_v2.md -- line 92 documents the missing boot step. Or: node scripts/audit/verify-pt08-001.mjs (lists this under DEFINED-NOT-STARTED). | CONFIRMED-BROKEN |
| WGR-035 | API/Scheduling | P1 | PT-08-002 cron reconciliation: `/api/cron/draft-automation` (daily draft-automation cron -- `DraftQueueEngine.processDeadlineApproaching()` + `DraftAutoGenerator.processQueue()`, unbounded batch) is registered nowhere -- absent from `vercel.json`'s `crons` array and never called from `worker/scheduler.ts` -- despite its own header comment explicitly claiming "Vercel Cron hits GET daily at 06:00 CT (11:00 UTC)" and citing BEHAVIORAL_CONTRACTS §28 / CLAUDE.md §DEPLOYMENT. Distinct from `/api/cron/research` (which IS registered): research's own inline fire-and-forget call only reaches `DraftQueueEngine.processNewOpportunities()` (a different queueing criterion -- newly discovered opportunities, not deadline-approaching ones) and never calls the draft generator at all. Practical effect: opportunities with an approaching deadline are never auto-queued for drafting on that basis, and queued draft items are never bulk-auto-generated -- the only substitute is `POST /api/drafts/queue/trigger`, a manual, writer-role-gated route capped at 3 items per call, whose own header comment ("Uses the same generation code path as the daily cron") shows the code itself still believes a daily cron exists. | test-evidence/pt-08/cron-reconciliation.json (cronApiRouteHandlers.entries[] where path === "/api/cron/draft-automation"; findings[] id WGR-035) | `grep -c "/api/cron/draft-automation" vercel.json` (0 matches); `grep -n "draft-automation\\|runDraftAutomation" worker/scheduler.ts` (0 matches); `node scripts/audit/verify-pt08-002.mjs` | CONFIRMED-BROKEN |
| WGR-036 | API/Scheduling | P1 | PT-08-002 cron reconciliation: `SalesCampaignEngine.processQueuedSends()` (`src/lib/admin/sales-campaign-engine.ts`) -- the only code path in the repo that ever reads and sends `sales_sends` rows with `status='queued'` -- is reachable exclusively via `/api/cron/sales-sends`, which is registered nowhere (absent from `vercel.json`, never called from `worker/scheduler.ts`). Confirmed the producer side is genuinely live and interactive, not hypothetical: `PATCH /api/admin/campaigns/[id]` with `{action:'schedule'}` (`src/app/api/admin/campaigns/[id]/route.ts:108-112`) calls `SalesCampaignEngine.scheduleSends(id)`, which really inserts `sales_sends` rows with `status:'queued'` and a real `scheduled_for` timestamp (`sales-campaign-engine.ts:288`). An admin clicking "Schedule" on a real Sales Outreach campaign today produces rows that sit in `status='queued'` forever, since nothing else in production ever calls `processQueuedSends()`. Highest-confidence finding in this reconciliation pass -- the producer is a real, currently-clickable admin action, not dormant/empty-table speculation. | test-evidence/pt-08/cron-reconciliation.json (cronApiRouteHandlers.entries[] where path === "/api/cron/sales-sends"; findings[] id WGR-036) | `grep -n "scheduleSends\\|processQueuedSends" "src/app/api/admin/campaigns/[id]/route.ts" src/app/api/cron/sales-sends/route.ts src/lib/admin/sales-campaign-engine.ts`; `grep -c "/api/cron/sales-sends" vercel.json` (0 matches); `node scripts/audit/verify-pt08-002.mjs` | CONFIRMED-BROKEN |
| WGR-037 | API/Scheduling | P1 | PT-08-002 cron reconciliation: two-layer gap in the AutoApply 14/30/60-day donation follow-up email system (`autoapply_follow_ups` table, `src/lib/autoapply/follow-up-scheduler.ts`) -- a system distinct from AG-28's `application_followups` sweep (which IS wired, via `worker/scheduler.ts`'s "nightly autonomous pipeline" job calling the differently-named `processFollowups` in `src/worker/jobs/process-followups.ts` -- the near-identical function names across two unrelated systems is itself worth flagging for future readers). (1) `processFollowUps()`, the consumer, is reachable only via `/api/cron/follow-ups`, which is registered nowhere (absent from `vercel.json`, never called from `worker/scheduler.ts`). (2) `scheduleFollowUps()`, the producer that would insert an `autoapply_follow_ups` row on an AutoApply submission, has ZERO call sites anywhere in `src/` -- a grep for the literal call `scheduleFollowUps(` matches only its own definition. Even if `/api/cron/follow-ups` were registered today, it would very likely find nothing to process, since nothing in the live codebase ever schedules a follow-up in the first place -- this 14/30/60-day sequence (documented as code-built in this project's own Phase 3G build history) is fully inert end-to-end. | test-evidence/pt-08/cron-reconciliation.json (cronApiRouteHandlers.entries[] where path === "/api/cron/follow-ups"; findings[] id WGR-037) | `grep -rn "scheduleFollowUps(" --include=*.ts src` (1 hit: its own definition); `grep -c "/api/cron/follow-ups" vercel.json` (0 matches); `grep -n "follow-ups\\|processFollowUps" worker/scheduler.ts` (0 matches -- the nightly job calls the separate `processFollowups`, not this system); `node scripts/audit/verify-pt08-002.mjs` | CONFIRMED-BROKEN |
| WGR-038 | API/Scheduling | P1 | PT-08-002 cron reconciliation: `sequenceEngine.processScheduledSends()` (`src/lib/email/sequence-engine.ts`) -- the sole consumer of scheduled `email_sequence_enrollments` sends -- is reachable only via `/api/cron/email-sequences`, which is registered nowhere (absent from `vercel.json`, never called from `worker/scheduler.ts`). This gap was already publicly documented before this audit (`WORKFLOW_PAGE_PLAN_2026-08-15.md` lines 248-252/287-289: "the cron route that would fire scheduled sends automatically... is not currently registered in vercel.json... Do not claim automatic scheduled mail-merge sends until /api/cron/email-sequences is actually registered"; `NOT_BUILT_MASTER_INVENTORY.md` item 9), but had never been given a WGR register entry with a backing evidence file until this pass. This pass reconfirms the code state is unchanged: a real producer route writes genuine `email_sequence_enrollments` rows, but scheduled sequence steps never fire automatically. | test-evidence/pt-08/cron-reconciliation.json (cronApiRouteHandlers.entries[] where path === "/api/cron/email-sequences"; findings[] id WGR-038); WORKFLOW_PAGE_PLAN_2026-08-15.md lines 248-252, 287-289; NOT_BUILT_MASTER_INVENTORY.md item 9 | `grep -rn "processScheduledSends(" --include=*.ts src` (2 hits: sequence-engine.ts's own definition, cron/email-sequences/route.ts); `grep -c "/api/cron/email-sequences" vercel.json` (0 matches); `node scripts/audit/verify-pt08-002.mjs` | CONFIRMED-BROKEN |
| WGR-040 | Worker/Queue | P1 | PT-08-003 agent_queue lifecycle exercise: `worker/autonomous-orchestrator.ts`'s `processAgentQueue()`/`runQueueItem()` has no per-item timeout wrapper around a claimed job's execution -- grepped the file for `timeout`/`Promise.race`/`setTimeout`; the only match is the `sleep()` helper used for the empty-queue poll backoff (line 151). The claim/retry/max-retries-terminal/completion state machine itself is confirmed CORRECT by this same task's live exercise against a disposable local Postgres database (4 scenarios, all assertions true, `overallVerdict: ALL_SEMANTICS_CORRECT`): a job that throws is correctly bounded by `max_retries` and reaches a real terminal `failed` state that is never reclaimed again, and a poison job that always fails does not permanently block a good job queued behind it (the good job reaches `completed` once the poison job's bounded retries exhaust -- 3 poison-job claim attempts before the good job's first claim, matching `max_retries=3` exactly). This finding is narrower and distinct: a job whose work HANGS (never resolves, never rejects -- e.g. an agent stuck awaiting a dead external call with no timeout of its own) is not bounded by anything visible at the queue-processor layer, since `processAgentQueue()`'s while-loop fully `await`s `runQueueItem()` (which itself fully `await`s `routeQueueItem()`) before claiming the next candidate -- such a job WOULD block every other queued item indefinitely, unlike the fail-fast poison job this task's scenario D exercises. Not independently reproduced (a genuine infinite hang would hang the test run itself) -- recorded as a code-read observation surfaced directly by this task's own live-verification methodology, not a live-verified hang. | `test-evidence/pt-08/queue-semantics.json` (`findings[]` where `id === "WGR-040"`; `scenarios.C_exhaust_retries_max_terminal` and `scenarios.D_poison_job_does_not_block_queue` for the confirmed-correct bounded-retry/isolation behavior this finding is scoped narrower than) | `grep -n "timeout\|Promise.race\|setTimeout" worker/autonomous-orchestrator.ts` (only match: line 151's `sleep()` helper); `node --import tsx scripts/audit/pt08-003-queue-lifecycle.mjs` against a disposable local Postgres DB (see the script's own header for schema/setup) to reproduce the confirmed-correct claim/retry/terminal/completion/isolation behavior this finding sits alongside; `node scripts/audit/verify-pt08-003.mjs` to check the evidence file's own shape. | UNVERIFIED |
| WGR-041 | Data/Infra | P1 | PT-06-002 migration drift map: this project has NO Supabase-CLI migration-tracking table (supabase_migrations.schema_migrations does not exist -- confirmed live, two independent ways: to_regclass() and a direct pg_namespace check). Every migration in this project's history was applied by hand via direct DATABASE_URL/psql (STANDING_DIRECTIVES.md DIRECTIVE-017), never via `supabase db push`/`migration up`, so there is no ledger of which migrations 'ran' or when. Applied status was therefore determined by live object-existence (does every table/column/enum-value a migration file defines actually exist in production right now), the same method MIGRATION_AUDIT.md (2026-07-28/30) used, extended here to cover BOTH known migration directories (src/supabase/migrations, supabase/migrations) instead of just the root tree that prior audit covered. Settled current figure, superseding both the stale 'previously documented 28 of 108 unapplied' premise and MIGRATION_AUDIT.md's own '41 of 112 not applied' figure (both predate this session and predate months of subsequent migration-apply sessions, and neither covered the src/supabase/migrations tree): of 199 total migration files on disk across both directories, 108 are APPLIED-AND-ON-DISK (every defined object confirmed live), 57 are ON-DISK-NOT-APPLIED (drift -- at least one defined table/column/enum-value missing live), 34 are NO_DDL_UNVERIFIABLE (RLS-only/backfill-only/comment-only files with nothing this method can check), and 2 live public-schema tables (corporate_relationships, fundability_deficiencies) are APPLIED-NOT-ON-DISK -- real tables with no CREATE TABLE anywhere on disk, only referenced by later RLS-hardening ALTER TABLE statements; one (fundability_deficiencies) is explicitly documented in a migration's own comment as leftover/superseded schema that was never real. Of the 57 drift entries, an automated grep sweep found 53 with at least one live src/worker/ code reference to the affected table name; 11 of those were individually column-verified this session and registered as their own findings below (WGR-042 through WGR-052). The remaining ~42 are real, confirmed-missing schema with a live table-level code reference each, not yet individually deep-dived to confirm the SPECIFIC missing column (not just the table) is read/written by live code -- tracked in consumer-check.json for a future pass, not silently dropped. | test-evidence/pt-06/migration-drift.json (counts, appliedAndOnDisk, onDiskNotApplied, noDdlUnverifiable, appliedNotOnDisk); test-evidence/pt-06/applied-migrations.json (trackingTableQuery); test-evidence/pt-06/live-schema-snapshot.json (migrationTrackingTableCheck); test-evidence/pt-06/consumer-check.json (per-migration live-consumer grep sweep) | SELECT to_regclass('supabase_migrations.schema_migrations') -- returns null (no tracking table) via DATABASE_URL. node scripts/audit/pt06-002-fetch-live-schema.mjs (re-fetch live schema) then node scripts/audit/pt06-002-migration-drift.mjs (re-run the reconciliation) -- or node scripts/audit/verify-pt06-002.mjs to check the evidence files' own shape and the 4-known-gap cross-reference. | CONFIRMED-BROKEN |
| WGR-042 | API/Agent | P1 | PT-06-002: migration 037_giving_history.sql's funder_giving_history.grant_purpose and .source columns are missing live (real live columns are purpose/recipient_ein/source_filing_url instead -- the table was evidently created out-of-band with a different layout than this checked-in file defines, per MIGRATION_AUDIT.md's 2026-07-30 finding, re-confirmed still current today). Real, live consumers still reference the missing names: src/lib/agents/competitor-intel.ts (selects "recipient_name, amount, grant_purpose, fiscal_year"), src/lib/agents/funder-signal-monitor-agent.ts and src/lib/autoapply/amount-optimizer.ts both reference `.source`. The Competitor Intel Agent's giving-history read fails on the missing grant_purpose column every time it runs. | test-evidence/pt-06/migration-drift.json (onDiskNotApplied[] where filename === "037_giving_history.sql"); test-evidence/pt-06/consumer-check.json (perTable[] where table === "funder_giving_history") | grep -n "grant_purpose\\|funder_giving_history" src/lib/agents/competitor-intel.ts -- confirms the query shape. SELECT column_name FROM information_schema.columns WHERE table_name='funder_giving_history' via DATABASE_URL -- confirms grant_purpose/source absent, live columns are purpose/recipient_ein/source_filing_url. | CONFIRMED-BROKEN |
| WGR-043 | API/Agent | P1 | PT-06-002: migration 038_intelligence_tables.sql's success_probability_scores.data_quality, .created_at, and .updated_at columns are missing live (live columns: id, organization_id, application_id, probability_score, factors, calculated_at). src/lib/agents/success-probability.ts's own upsert ("...data_quality: dataQuality, calculated_at: ..., updated_at: ...", onConflict application_id) references all three; that file's own code explicitly `throw new AgentError("Failed to save probability score.", "write_failed")` when the upsert errors -- this is a hard failure on every real invocation of a live, wired agent, not a silent no-op. src/components/applications/ApplicationDetail.tsx and pipeline.ts also reference `.created_at`/`.updated_at` on reads of this table. Matches MIGRATION_AUDIT.md's 2026-07-30 finding #1, re-confirmed still current today. | test-evidence/pt-06/migration-drift.json (onDiskNotApplied[] where filename === "038_intelligence_tables.sql"); test-evidence/pt-06/consumer-check.json | grep -n "data_quality\\|updated_at" src/lib/agents/success-probability.ts -- confirms the upsert shape and the explicit throw on write failure. SELECT column_name FROM information_schema.columns WHERE table_name='success_probability_scores' via DATABASE_URL -- confirms data_quality/created_at/updated_at absent. | CONFIRMED-BROKEN |
| WGR-044 | API/Worker | P1 | PT-06-002: migration 052_webhook_configs.sql's webhook_configs.type/.is_active/.updated_at columns are missing live -- the table exists but was created by the conflicting sibling 051_submission_intelligence.sql's own `CREATE TABLE IF NOT EXISTS webhook_configs` definition instead (webhook_type/active, no updated_at), so this file's `CREATE TABLE IF NOT EXISTS` silently no-op'd -- confirmed by 051 itself also showing up in this session's drift map (a single missing column, webhook_configs.submission_failed, since 051 and 052 both partially define the same table and neither fully won). Real, live consumers of the 052 shape: src/lib/autoapply/webhook-notifier.ts (`.select('id, type, webhook_url, events').eq('is_active', true)`), src/app/(dashboard)/autoapply/webhooks/page.tsx, src/app/api/autoapply/webhooks/route.ts. WebhookNotifier.notify() queries columns that don't exist and silently returns without sending any webhook, for every org, for every event type, always. Matches MIGRATION_AUDIT.md's 2026-07-30 finding, re-confirmed still current today. | test-evidence/pt-06/migration-drift.json (onDiskNotApplied[] where filename === "052_webhook_configs.sql" and where filename === "051_submission_intelligence.sql"); test-evidence/pt-06/consumer-check.json | grep -n "is_active\\|webhook_url" src/lib/autoapply/webhook-notifier.ts -- confirms the query shape. SELECT column_name FROM information_schema.columns WHERE table_name='webhook_configs' via DATABASE_URL -- confirms type/is_active/updated_at absent. | CONFIRMED-BROKEN |
| WGR-045 | API/Worker | P1 | PT-06-002: migration 052_governance_layer.sql's funder_relationships, queue_controls, submission_usage, and tier_limits tables do not exist live at all -- this is the entire AutoApply governance/risk-gating/usage-tracking/tier-limit layer, unchanged and still absent since MIGRATION_AUDIT.md's 2026-07-30 finding. Confirmed real, live, currently-used consumers: src/lib/autoapply/risk-engine.ts, relationship-manager.ts, follow-up-scheduler.ts (funder_relationships); src/app/api/admin/autoapply-ops/route.ts, src/lib/autoapply/queue-controls.ts, src/lib/drafts/submission-bridge.ts (queue_controls); src/app/(dashboard)/autoapply/usage/UsagePageClient.tsx, src/lib/autoapply/alerting.ts, response-analytics.ts, usage-meter.ts (submission_usage); src/app/api/billing/route.ts, src/app/api/integrations/scraping-targets/route.ts, src/app/api/intelligence/search/route.ts, usage-meter.ts (tier_limits). usage-meter.ts's own tier/allowance checks (checkAllowance/shouldUseOwnKeys, per project history) are structurally inert platform-wide without this migration -- this is not one narrow gap, it is a whole governance subsystem with real UI/API surface that has never worked. | test-evidence/pt-06/migration-drift.json (onDiskNotApplied[] where filename === "052_governance_layer.sql"); test-evidence/pt-06/consumer-check.json (perTable[] for funder_relationships/queue_controls/submission_usage/tier_limits) | SELECT to_regclass('public.funder_relationships'), to_regclass('public.queue_controls'), to_regclass('public.submission_usage'), to_regclass('public.tier_limits') via DATABASE_URL -- all four return null. grep -rn "tier_limits\\|queue_controls" src/lib/autoapply/usage-meter.ts src/lib/autoapply/queue-controls.ts -- confirms real call sites. | CONFIRMED-BROKEN |
| WGR-046 | API | P1 | PT-06-002: migration 060_grantmaker_profiles.sql's intelligence_grantmaker_profiles table is missing 9 of its defined columns live, including foundation_id, name, total_annual_giving, program_priorities, typical_award_range, language_patterns, application_url, last_profiled_at, and profile_data -- a redesigned column set the table never received. Real, live, currently-referenced consumers of the missing column names: src/lib/intelligence/funder-recommender.ts, src/lib/intelligence/grantmaker-profiles.ts, src/lib/intelligence/unified-search.ts, and src/app/(dashboard)/intelligence-library/dashboard/DashboardClient.tsx. Matches MIGRATION_AUDIT.md's 2026-07-30 note ("abandoned redesign") but this session's fresh consumer check shows the redesigned columns ARE referenced by live code today, unlike that prior audit's conclusion -- worth re-flagging as active, not dead. | test-evidence/pt-06/migration-drift.json (onDiskNotApplied[] where filename === "060_grantmaker_profiles.sql"); test-evidence/pt-06/consumer-check.json | grep -n "total_annual_giving\\|program_priorities" src/lib/intelligence/funder-recommender.ts src/lib/intelligence/grantmaker-profiles.ts -- confirms real query shape. SELECT column_name FROM information_schema.columns WHERE table_name='intelligence_grantmaker_profiles' via DATABASE_URL -- confirms the 9 columns absent. | CONFIRMED-BROKEN |
| WGR-047 | API | P1 | PT-06-002: migration 078_forecast_board.sql (src/supabase/migrations tree) defines board_members with org_id/role/committee/term_start/term_end/expertise/active columns -- the LIVE table instead has organization_id/name/title/bio/is_active (from a different, applied migration), so org_id/role/committee/term_start/term_end/expertise/active are all missing. This is the well-documented, previously-flagged stale-migration-078-column-name mismatch bug (AGENT_VERIFICATION_LOG.md/STATE_OF_THE_BUILD.md, multiple 2026-08-07 sessions) -- already fixed in SOME call sites (e.g. relationship-graph-builder-agent.ts) but confirmed STILL live and broken in others: src/app/api/intelligence/relationship-graph/analytics/route.ts's own header comment names `board_members.org_id` directly and its query (`.from("board_members")`) uses it -- this Graph Analytics API route 500s for every org, every call, right now. src/app/(dashboard)/onboarding/page.tsx and src/app/(dashboard)/knowledge-base/edit/page.tsx also reference the missing role/expertise/active columns. | test-evidence/pt-06/migration-drift.json (onDiskNotApplied[] where filename === "078_forecast_board.sql" and directory === "src/supabase/migrations"); test-evidence/pt-06/consumer-check.json | grep -n "board_members\\|org_id" "src/app/api/intelligence/relationship-graph/analytics/route.ts" -- confirms the live query and its own header comment naming the exact broken column. SELECT column_name FROM information_schema.columns WHERE table_name='board_members' via DATABASE_URL -- confirms org_id/role/committee/term_start/term_end/expertise/active absent, organization_id/name/title/bio/is_active present. | CONFIRMED-BROKEN |
| WGR-048 | API | P1 | PT-06-002: migrations 101_twin_auto_populate_log.sql (root) and 102_twin_auto_populate_log.sql (src/supabase/migrations, the parallel-tree duplicate) both define organizational_digital_twins.twin_auto_populate_log -- neither has landed live. Real, live consumer: src/lib/intelligence/twin-auto-populate.ts line 905 writes `twin_auto_populate_log: auditLog` as part of its upsert. That write fails every time the twin auto-populate pipeline runs; the audit-log half of its own output has no column to land in. | test-evidence/pt-06/migration-drift.json (onDiskNotApplied[] where prefixRaw === "101" or "102" and filename matches twin_auto_populate_log); test-evidence/pt-06/consumer-check.json | grep -n "twin_auto_populate_log" src/lib/intelligence/twin-auto-populate.ts -- confirms the real write at line 905. SELECT column_name FROM information_schema.columns WHERE table_name='organizational_digital_twins' AND column_name='twin_auto_populate_log' via DATABASE_URL -- returns zero rows. | CONFIRMED-BROKEN |
| WGR-049 | API/Agent | P1 | PT-06-002: migration 105_applications_metadata_column.sql's applications.metadata column is missing live. Real, live consumers all write/read it directly: src/lib/intelligence/narrative-humanizer.ts ("applications.metadata.humanization_breakdown"), src/lib/agents/draft-generation-agent.ts, src/app/api/drafts/[id]/humanize/route.ts (two real writes: `...(application.metadata ?? {})` merged with new keys), and src/app/api/drafts/[id]/route.ts. The Narrative Humanizer's humanization_score/humanization_breakdown has no column to persist to on every real humanize call. Matches MIGRATION_AUDIT.md's 2026-07-30 finding #8, re-confirmed still current today. | test-evidence/pt-06/migration-drift.json (onDiskNotApplied[] where filename === "105_applications_metadata_column.sql"); test-evidence/pt-06/consumer-check.json | grep -n "application.metadata" "src/app/api/drafts/[id]/humanize/route.ts" -- confirms the real read-merge-write. SELECT column_name FROM information_schema.columns WHERE table_name='applications' AND column_name='metadata' via DATABASE_URL -- returns zero rows. | CONFIRMED-BROKEN |
| WGR-050 | Frontend/Page | P1 | PT-06-002: migration 106_intelligence_library_schema_upgrade.sql adds 20 columns to intelligence_funded_proposals; none exist live. Of those 20, column-specific grep confirms at least 10 are directly read by src/app/(dashboard)/intelligence-library/page.tsx and its src/app/(dashboard)/intelligence-library/dashboard/DashboardClient.tsx: keywords, winning_phrases, persuasive_elements, success_factors, funder_category, ntee_major, ntee_code, geographic_scope, source_type, full_text_search_vector. The Intelligence Library's own winning-phrases/persuasive-elements sections and its filter-by-funder-category UI have no live data to render -- every user of this page sees these sections either empty or erroring, not just degraded. | test-evidence/pt-06/migration-drift.json (onDiskNotApplied[] where filename === "106_intelligence_library_schema_upgrade.sql"); test-evidence/pt-06/consumer-check.json | grep -n "winning_phrases\\|persuasive_elements\\|keywords" "src/app/(dashboard)/intelligence-library/page.tsx" -- confirms real reads. SELECT column_name FROM information_schema.columns WHERE table_name='intelligence_funded_proposals' via DATABASE_URL -- confirms all 20 columns from migration 106 absent. | CONFIRMED-BROKEN |
| WGR-051 | Data/Script | P1 | PT-06-002: migration 100_prospects_contact_fields.sql's prospects.contact_name/.contact_title columns are missing live. Real, live intended consumer confirmed directly: scripts/seed-outreach-prospects.ts's own header comment states "contact_name/contact_title. That migration must be applied before this [script runs]" and its body writes both fields (`contact_name: n.officer_name ?? null`, `contact_title: n.officer_title ?? DEFAULT_CONTACT_TITLE`). Sales Outreach personalization has no named contact for any prospect populated via this real, intended write path. Matches MIGRATION_AUDIT.md's 2026-07-30 finding #9, and directly contradicts project memory benavora-outreach-table-names-collide's claim that this was resolved -- it was written but never actually applied, confirmed still true today. | test-evidence/pt-06/migration-drift.json (onDiskNotApplied[] where filename === "100_prospects_contact_fields.sql"); test-evidence/pt-06/consumer-check.json | grep -n "contact_name\\|contact_title" scripts/seed-outreach-prospects.ts -- confirms the real write path and its own header comment naming this exact gap. SELECT column_name FROM information_schema.columns WHERE table_name='prospects' AND column_name IN ('contact_name','contact_title') via DATABASE_URL -- returns zero rows. | CONFIRMED-BROKEN |
| WGR-052 | API/Frontend | P1 | PT-06-002: migration 054_email_calendar_integration.sql's email_threads and email_messages tables do not exist live (among other tables this file also defines). Real, live consumers of these two tables: src/app/(dashboard)/email/page.tsx, src/app/api/email/threads/route.ts, src/lib/email/gmail-sync.ts, src/lib/email/contact-extractor.ts, src/lib/email/sender.ts, src/lib/email/sequence-engine.ts, src/app/api/email/contacts/route.ts, src/app/api/email/send/route.ts, src/app/api/email/summarize/route.ts, src/lib/agents/email-campaign.ts -- a wide real surface (the entire Email Hub inbox/thread feature, not a peripheral one) that queries two tables the live database has never had. | test-evidence/pt-06/migration-drift.json (onDiskNotApplied[] where filename === "054_email_calendar_integration.sql"); test-evidence/pt-06/consumer-check.json | grep -rln "email_threads\\|email_messages" src/ -- lists the 10 real referencing files. SELECT to_regclass('public.email_threads'), to_regclass('public.email_messages') via DATABASE_URL -- both return null. | CONFIRMED-BROKEN |
| WGR-053 | API/Agent | P1 | PT-06-002: migration 097_deadline_prediction_agent.sql (src/supabase/migrations tree) defines the deadline_predictions table and funders.next_predicted_open_date/.avg_cycle_length_days -- none exist live. Real, live, precisely-named consumer: src/lib/agents/deadline-prediction-agent.ts (AG-25, agentId ag-25-deadline-prediction) inserts into `.from("deadline_predictions")` (line 511) and writes `next_predicted_open_date`/`avg_cycle_length_days` onto funders (lines 787-788) as its two real output writes; src/lib/agents/autonomous-digest-agent.ts also queries `.from("deadline_predictions")` (line 448) for its own digest content, with an explicit error push on failure ("deadline_predictions query failed"). The agent's own header comment (line 48) already documents that this table was 'created from scratch' by this migration -- it has never landed live, so AG-25's core output (predicted next-open dates per funder) has nowhere to persist, and the digest agent's query against it fails on every run. | test-evidence/pt-06/migration-drift.json (onDiskNotApplied[] where filename === "097_deadline_prediction_agent.sql"); test-evidence/pt-06/consumer-check.json (perTable[] where table === "deadline_predictions") | grep -n "deadline_predictions\\|next_predicted_open_date" src/lib/agents/deadline-prediction-agent.ts -- confirms the real insert/update sites and the file's own header comment. SELECT to_regclass('public.deadline_predictions') via DATABASE_URL -- returns null. SELECT column_name FROM information_schema.columns WHERE table_name='funders' AND column_name IN ('next_predicted_open_date','avg_cycle_length_days') -- returns zero rows. | CONFIRMED-BROKEN |
| WGR-054 | API/Agent | P1 | PT-06-003 (code-vs-live-schema cross-reference audit): `applications.funder_id` does not exist live -- the real column is `opportunity_id` only (funder is reachable via `opportunities.funder_id`, one hop away). `src/lib/agents/autonomous-digest-agent.ts:501`'s `collectDraftsPendingReview()` selects `"id, opportunity_id, funder_id, draft_confidence_score, opportunities(name), funders(name)"` directly off `applications` -- the invalid column makes the whole select fail; the caught error is pushed to `errors[]` and the function returns `[]`, so the "drafts pending review" section of the autonomous morning digest silently reports zero pending drafts on every run, regardless of the real count. | `test-evidence/pt-06/schema-mismatch.json` (`runtime_query_code.column_mismatches[]` where `table === "applications"` and `column === "funder_id"`); `test-evidence/pt-06/live-schema.json` (`tables.applications.columns`, confirms no `funder_id`) | `grep -n "funder_id" src/lib/agents/autonomous-digest-agent.ts` around line 501; `SELECT column_name FROM information_schema.columns WHERE table_name='applications'` via `DATABASE_URL` -- confirms `funder_id` absent, `opportunity_id` present. | CONFIRMED-BROKEN |
| WGR-055 | Lib | P1 | PT-06-003: `src/lib/intelligence/rubric-extractor.ts:85`'s `extractRubricFromOpportunity()` selects `'requirements, eligibility_text, description, raw_content, funder_name, title'` off `opportunities` -- 5 of the 6 requested columns don't exist live (real columns: `name` not `title`, `eligibility_requirements` not `eligibility_text`/`requirements`, no `raw_content`, no `funder_name` -- funder name requires a join via `opportunities.funder_id -> funders.name`; only `description` is real). PostgREST rejects the whole select on an unknown column, so `error` is always truthy and the function always returns `null` -- rubric extraction for every opportunity silently never produces a real rubric. | `test-evidence/pt-06/schema-mismatch.json` (`column_mismatches[]` where `table === "opportunities"` and `column` in `title`/`funder_name`/`eligibility_text`/`raw_content`/`requirements`, all citing `src/lib/intelligence/rubric-extractor.ts:85`) | `sed -n '78,95p' src/lib/intelligence/rubric-extractor.ts`; `SELECT column_name FROM information_schema.columns WHERE table_name='opportunities'` via `DATABASE_URL` -- confirms real columns are `name`/`eligibility_requirements`, none of `title`/`funder_name`/`eligibility_text`/`raw_content`/`requirements` exist. | CONFIRMED-BROKEN |
| WGR-056 | Lib | P1 | PT-06-003: `funders.city`/`funders.state` don't exist live (the table has no structured location columns, only free-text `geographic_focus`). `src/lib/autoapply/auto-queue-populator.ts:195` selects `'id, name, category, city, state, giving_portal_url, type'` off `funders` for AutoApply's eligible-funder query -- the invalid columns fail the whole select, so the queue-populator's core "find funders eligible for auto-submission" query breaks for every org. | `test-evidence/pt-06/schema-mismatch.json` (`column_mismatches[]` where `table === "funders"` and `column` in `city`/`state`) | `sed -n '188,199p' src/lib/autoapply/auto-queue-populator.ts`; `SELECT column_name FROM information_schema.columns WHERE table_name='funders'` via `DATABASE_URL` -- confirms no `city`/`state`. | CONFIRMED-BROKEN |
| WGR-057 | Worker | P1 | PT-06-003: `funders.portal_type` doesn't exist live. `worker/autoapply-autonomous-orchestrator.ts` references it 4 times in the same funder-auto-creation function: two `.select('id, portal_type')` calls (lines 261, 271) whose errors are silently discarded (only `{ data }` is destructured, so `data` comes back `undefined`/errored and `existingPortalType` is always `null`); one `.insert({..., portal_type: portalType})` (lines 282-290) that fails outright since the column doesn't exist -- `if (error || !newFunder) return null;` means the ENTIRE new-funder-from-directory-record creation silently fails whenever this path is hit; and one `.update({ portal_type: portalType })` (line 299) whose result isn't even checked. This blocks auto-creating a `funders` row from `foundation_directory`/donor-discovery data whenever a giving-portal URL classification needs to be persisted. | `test-evidence/pt-06/schema-mismatch.json` (`column_mismatches[]` where `table === "funders"` and `column === "portal_type"`, 4 sites all in `worker/autoapply-autonomous-orchestrator.ts`) | `sed -n '250,304p' worker/autoapply-autonomous-orchestrator.ts`; `SELECT column_name FROM information_schema.columns WHERE table_name='funders'` via `DATABASE_URL` -- confirms no `portal_type` (real portal columns are `portal_status`/`portal_login_status`/`portal_review_status`). | CONFIRMED-BROKEN |
| WGR-058 | API | P1 | PT-06-003: `alerts.title`/`alerts.alert_type` don't exist live (real columns: `message`, `type`). `src/app/api/activity/route.ts:85` selects `'id, title, message, alert_type, created_at'` off `alerts` as one of three parallel `Promise.all` queries feeding the Activity feed -- the invalid columns fail that one query, so the alerts section of the activity feed is always empty/errored for every org while the opportunities/other sections still render. | `test-evidence/pt-06/schema-mismatch.json` (`column_mismatches[]` where `table === "alerts"` and `column` in `title`/`alert_type`) | `sed -n '75,89p' src/app/api/activity/route.ts`; `SELECT column_name FROM information_schema.columns WHERE table_name='alerts'` via `DATABASE_URL` -- confirms `message`/`type`, not `title`/`alert_type`. | CONFIRMED-BROKEN |
| WGR-059 | Agent | P1 | PT-06-003: `organizations.service_areas` (plural) doesn't exist live -- only the singular `service_area` does. This is the same class of bug already fixed in `src/lib/agents/donor-intent-monitor-agent.ts` on 2026-08-02 (per `STATE_OF_THE_BUILD.md`), but a sibling call site was never fixed: `src/lib/agents/strategic-advisor-agent.ts:453` (AG-40)'s `loadOrgProfile()` selects `"name, service_area, service_areas"` off `organizations` -- the invalid `service_areas` column fails the whole select, `data` comes back `undefined`, and the function silently falls back to `{ name: "this organization", service_area: null, service_areas: null }` on every real call, meaning AG-40's Strategic Advisor never actually sees the org's real name or service area in its prompts. | `test-evidence/pt-06/schema-mismatch.json` (`column_mismatches[]` where `table === "organizations"` and `column === "service_areas"`) | `sed -n '450,457p' src/lib/agents/strategic-advisor-agent.ts`; `SELECT column_name FROM information_schema.columns WHERE table_name='organizations'` via `DATABASE_URL` -- confirms `service_area` singular only. | CONFIRMED-BROKEN |
| WGR-060 | Lib | P1 | PT-06-003: `intelligence_grantmaker_profiles` has drifted column names across 5 real, live call sites -- code expects `foundation_id`/`name`/`program_priorities`/`total_annual_giving`/`typical_award_range`/`language_patterns`; the live table has `funder_id`/no `name` column/`priorities`/no `total_annual_giving`/split `award_range_min`+`award_range_max`/`typical_language` instead. Confirmed at `src/lib/intelligence/grantmaker-profiles.ts:66,109,182`, `src/lib/intelligence/funder-recommender.ts:41,129-130`, `src/lib/intelligence/unified-search.ts:208`, `src/app/(dashboard)/intelligence-library/dashboard/DashboardClient.tsx:365`. This breaks the Funder Recommender/matching feature (`matchOrgToFunders()`) and the Intelligence Library dashboard's grantmaker-profile reads across the board -- every one of these selects fails on the invalid columns. | `test-evidence/pt-06/schema-mismatch.json` (`column_mismatches[]` where `table === "intelligence_grantmaker_profiles"`) | `sed -n '104,112p' src/lib/intelligence/grantmaker-profiles.ts`; `SELECT column_name FROM information_schema.columns WHERE table_name='intelligence_grantmaker_profiles'` via `DATABASE_URL` -- confirms `funder_id`/`priorities`/`award_range_min`/`award_range_max`/`typical_language`, none of `foundation_id`/`name`/`program_priorities`/`total_annual_giving`/`typical_award_range`/`language_patterns`. | CONFIRMED-BROKEN |
| WGR-064 | Data/Infra | P1 | Tenant FK gap: of 120 live tables carrying an `org_id`/`organization_id` column, 13 have no live FOREIGN KEY constraint referencing `organizations`: `adapter_usage_log`, `agent_configurations`, `autoapply_review_queue`, `board_meeting_packets` (`org_id`), `board_meetings` (`org_id`), `discovery_matches` (`org_id`), `funding_forecasts` (`org_id`), `impact_simulations` (`org_id`), `knowledge_queries` (`org_id`), `opportunity_probability_scores`, `organizational_digital_twins` (`org_id`), `pitch_cache`, `submission_receipts`. Without a live FK, nothing in the database enforces that every value in these columns actually corresponds to a real organization row -- RLS policies on these tables commonly filter on this exact column, so a stray/garbage tenant id would silently bypass the isolation those policies are meant to provide instead of erroring. Fed the PT-05 tenant-isolation phase directly: this was the concrete table list PT-05 tested against, not a general reminder (evidence file's own finding text said so explicitly -- "Feeds PT-05/PT-14"). PT-05-002/PT-05-003 have since live-tested all 13 of these tables (plus 7 others) for cross-tenant SELECT/UPDATE/DELETE/INSERT bypass under the real production RLS policy predicate and found 0 leaks -- see WGR-071 (read) and WGR-073 (write). **This finding itself remains open and unresolved by that result**: RLS enforcement and referential-integrity enforcement are two independent mechanisms, and PT-05 only tested the former. The missing FK here means a malformed/garbage tenant id in these 13 tables' `org_id`/`organization_id` column is still not caught by the database -- e.g. a bug elsewhere in the app writing a non-existent org id would silently succeed and produce an orphaned row RLS can't meaningfully scope (since it wouldn't match any real org's `current_org_id()` and would just become unreadable by anyone, not a leak, but a silent data-integrity failure) -- this is a narrower residual risk than a cross-tenant leak, not the same one, and is why this row's severity/scope tag are unchanged by PT-05's clean result. A separate 64-table list (tables with no `org_id`/`organization_id` column at all) was also produced as the candidate set for human triage on whether each SHOULD have one -- not asserted as a finding by itself, since several are legitimate platform-wide/shared reference tables per `RLS_POLICY_AUDIT.md`/`ANON_GRANT_AUDIT.md`. | `test-evidence/pt-06/integrity.json` (`checks.tenant_fk_gap`, `summary.tenant_fk_gap_findings=13`) | `node scripts/audit/pt06-integrity.mjs`; or cross-reference `information_schema.columns` against `information_schema.table_constraints` for each of the 13 tables above via `DATABASE_URL` | CONFIRMED-BROKEN |
| WGR-065 | Data/Infra | P1 | Unique-index gaps: of 26 identifier-shaped columns checked (name matches an identity-column naming pattern, e.g. `*_id`, `ein`, `email`, `website`), 6 have no unique index AND real live data already contains duplicate values under that column -- meaning even adding a unique constraint today would first require a real dedup pass, not just a DDL statement. Two are P1 (real, large-scale collision): `foundation_directory.website` (151 distinct values repeat across real rows) and `nonprofits.website` (7,318 distinct values repeat across real rows). Four are P2 (small-scale, low-population collision): `funders.website`, `organizations.ein`, `organizations.website`, `profiles.email` (each 1 distinct value repeating across 2-13 rows). Whether any of these six columns is used elsewhere in the app as a join/lookup key that silently assumes uniqueness was not independently confirmed here -- that needs a per-column code read, out of this check's scope. | `test-evidence/pt-06/integrity.json` (`checks.unique_gaps`, `summary.unique_gap_findings=6`) | `node scripts/audit/pt06-integrity.mjs`; or a live `SELECT column, count(*) FROM <table> GROUP BY column HAVING count(*) > 1` against `DATABASE_URL` for any of the 6 named columns | CONFIRMED-BROKEN |
| WGR-067 | Data/Infra | P1 | Null-rate audit on columns real application code reads as a signal, not just a display field: `foundation_directory.email` is NULL on 100% of 133,812 rows and `.contact_emails` is NULL on 100% of rows -- `src/lib/scraper/foundation-scraper.ts`'s enrichment output has never populated either column live, so every outreach/contact surface reading them has zero real data; `.website` is NULL on 38.04% of rows, directly removing `src/lib/donor-discovery/foundation-linkage.ts`'s primary linkage signal for over a third of the table (falls back to RPC name-similarity only). `nonprofits.officer_email` and `.contact_emails` are each NULL on 100% of 1,978,526 rows -- `src/lib/scraper/nonprofit-scraper.ts`'s two primary enrichment outputs have never populated live, at IRS-BMF-import scale; `.website` is NULL on 74.86% of rows (a real, already-app-acknowledged segment per `nonprofits/page.tsx`'s own `.not("website","is",null)` stat card, not a universal requirement -- graded P2 in the source evidence, not P1). `donor_discovery_directory.website` is NULL on 100% of 133,815 rows -- both the primary foundation-linkage signal AND (per WGR-066) the second half of this table's own dedup unique index are disabled for every row today, a compounding effect, not two independent gaps; `.phone` is NULL on 100% of rows (no code path was found that hard-requires it, so this is a coverage gap, not a silent-failure risk, despite sharing the same 100%-null severity classification as the others in this check). | `test-evidence/pt-06/data-quality.json` (`tables.*.null_rate_checks`, each row's `app_requirement` field) | `node scripts/audit/pt06-data-quality.mjs`; or run any of `null_rate_checks[].query` directly against `DATABASE_URL` | CONFIRMED-BROKEN |
| WGR-068 | Data/Infra | P1 | Migration idempotency test, run for real against a disposable local Postgres database (status: RAN, not PENDING/skipped): first-apply pass replayed all 142 files in `supabase/migrations/*.sql` in order -- 121 applied cleanly, 21 failed (almost all `42P01` relation-does-not-exist, i.e. referencing a table/column that only `src/supabase/migrations` creates -- the same cross-directory drift this whole PT-06 phase already documents, not a new defect). An 8-file sample was then re-applied a second time to test idempotency (1 skipped -- can not test re-apply of a file that never applied once): of the 7 actually tested, 6 are NOT idempotent -- 5 `CREATE TABLE` statements with no `IF NOT EXISTS` guard fail loudly with a real error on re-apply (`009_draft_versions.sql`: `42710` type already exists; `015_funder_intelligence.sql`, `016_renewals.sql`, `018_email_activity.sql`, `037_giving_history.sql`: `42P07` relation already exists -- graded P2, since failing loudly on re-apply is a safe, if inconvenient, failure mode). The 6th is the one P1 in this check and materially different in kind: `003_onboarding.sql` is a data backfill with NO guard scoping it to rows that still need it (e.g. no `WHERE onboarding_completed IS NOT true`) -- re-running it against a real seeded row flipped `onboarding_completed` from `false` to `true` with ZERO error, meaning a second run of this migration silently corrupts the state of any row that has legitimately diverged since the first run, rather than failing safely like the 5 `CREATE TABLE` cases. Only 1 of the 7 tested files (`058_backfill_opportunity_deadlines.sql`) is genuinely idempotent -- confirmed via a real establish-then-reapply cycle with an unchanged row count both times. | `test-evidence/pt-06/idempotency.json` (`status="RAN"`, `summary`, `sample_results`, `findings`) | `node scripts/audit/pt06-idempotency.mjs` (spins up and tears down its own disposable database -- see `disposable_database`/`method` fields for the exact protocol) | CONFIRMED-BROKEN |
| WGR-075 | Auth/Admin | P1 | The dedicated impersonation_log audit table (SCHEMA_REGISTRY §55) is unwritable for every real production caller today: its admin_id column has a foreign key to platform_admins(id), but platform_admins has only 1 row and 0 of the platform's 70 real owner-role profiles are present in it (live-queried, read-only, against production). Every real impersonation attempt's impersonation_log insert therefore fails with a 23503 foreign-key violation -- reproduced live against a local-stack copy of the same real FK constraint using a real owner-role profile id exactly the way requireRole("owner")'s gate.userId supplies it in production. The route (src/app/api/admin/orgs/[id]/impersonate/route.ts) never checks the {error} on this insert (the result is not destructured), so the failure is silently swallowed and the caller still receives { ok: true }. impersonation_log has 0 rows in production today, consistent with this. A SEPARATE, generic audit_logs write (logAudit(), no FK problem) DOES succeed per call, confirmed via the same local reproduction, so impersonation is not fully unlogged -- but the specific, purpose-built impersonation audit trail this feature exists to populate is broken for every real user. | test-evidence/pt-05/privileged-access.json (admin_impersonation.audit_logged), test-evidence/pt-05/pt05-004-production-investigation.json | node scripts/audit/pt05-004-investigate.mjs (confirms 0 of 70 owner-role profiles present in platform_admins, impersonation_log row_count=0, live via production DATABASE_URL); node scripts/audit/pt05-004-privileged-access.mjs (reproduces the exact impersonation_log insert against a local FK matching production, observes 23503; reproduces the audit_logs insert, observes success). | CONFIRMED-BROKEN |
| WGR-077 | Autonomous Agents | P1 | PT-09-002 execution proof: AG-05 (ag-05-research family; representative member tested: src/lib/agents/research/corporate-giving.ts, CorporateGivingResearchAgent) ran end-to-end against a real local Supabase target with a real Claude-capable BaseAgent invocation -- a real agent_runs row was written and the run resolved cleanly (data: opportunitiesFound=0, opportunitiesCreated=0, fundersCreated=0) -- but ZERO real business-table rows (opportunities, funders) were written. This is the WIRED-NO-OUTPUT class: the agent looks like it ran successfully (no thrown error, a clean agent_runs completion) but produced no actual output. Plausible root cause per the run's own triggerLog: the agent's real dependency on scraping live Google search results is vulnerable to a CAPTCHA/bot-block or a run with zero qualifying candidate pages. Only 1 of the ~10 files in the ag-05-research family was directly executed (corporate-giving.ts); this finding speaks only to that one member. Several documented siblings (grants_gov_research, simpler_grants_research, state_portal, custom_api_research) already carry their own distinct, previously-documented breakage per test-evidence/pt-09/agent-inventory.json / STATE_OF_THE_BUILD.md's 2026-08-04 entry, not re-verified or exercised by this test. FALSE-PASS CASUALTY: registryPriorStatus for this agent's registered trigger path (WIRED-MANUAL-API, src/app/api/agents/research/route.ts) implies a working manual API surface; this execution proof shows the underlying agent can run clean and still write nothing. | test-evidence/pt-09/batch1-results/AG-05.json, test-evidence/pt-09/execution-batch1.json | node --import tsx scripts/audit/pt09-002-trigger/AG-05.mjs (against the local pt05-local-stack, test-evidence/pt-09/environment.json org) -- compares agent_runs/opportunities/funders row counts before and after via a direct pg connection, independent of the agent's own client. | CONFIRMED-BROKEN |
| WGR-078 | Autonomous Agents | P1 | PT-09-002 execution proof: AG-13 (ag-13-foundation-enrichment, src/lib/scraper/foundation-scraper.ts, enrichSingleFoundation()) ran to completion cleanly (no throw) and returned enriched=false, strategy="none" -- none of the 3 waterfall enrichment strategies patched foundation_directory. Root-caused per-strategy, not left ambiguous: Strategy 1 (IRS 990 XML) was not meaningfully exercised because the local seed row's ein is null (a seed-data limitation, not a code defect -- real BMF-imported foundation_directory rows in production do carry an EIN). Strategy 2 (Google search fallback) WAS exercised for real and hit a genuine, reproducible, non-seed-specific wall on all 3 attempts: "CAPTCHA detected (recaptcha_v2) -- no TWOCAPTCHA_API_KEY configured, skipping page." This is confirmed non-functional as currently deployed whenever Google serves a CAPTCHA challenge, independent of which foundation is being enriched. Strategy 3 (contact-page extraction) never ran since it requires a website, which neither Strategy 1 nor 2 produced. Net: a genuine "ran clean, wrote nothing" result for a real, currently-scheduled production job (foundation-enrichment-weekly, worker/scheduler.ts, Sunday 3AM, gated on ENABLE_SCRAPER=true), with Strategy 2's CAPTCHA/no-solver-key wall as the most actionable, generalizable cause. FALSE-PASS CASUALTY: registryPriorStatus describes this job as WIRED-SCHEDULED-ENV-GATED (i.e. actively scheduled in production); this proof shows a real invocation of its core enrichment function writes nothing whenever the Google-fallback strategy is needed and no CAPTCHA solver key is configured. | test-evidence/pt-09/batch1-results/AG-13.json, test-evidence/pt-09/execution-batch1.json | node --import tsx scripts/audit/pt09-002-trigger/AG-13.mjs (against the local pt05-local-stack, test-evidence/pt-09/environment.json foundationDirectory seed row) -- captures per-strategy console output and before/after foundation_directory row content via a direct pg connection. | CONFIRMED-BROKEN |
| WGR-079 | Autonomous Agents | P1 | PT-09-002 execution proof: AG-14 (ag-14-donor-discovery, worker/dd-request-processor.ts, DdRequestProcessor) -- two real, distinct findings from directly exercising this class against a schema mirrored from real production. (1) dequeue() -- the method the real continuous-poll loop() calls every 15s in production, confirmed STARTED per test-evidence/pt-08/boot-inventory.json -- depends on the RPC donor_discovery_claim_request, which does NOT exist in production (migration 070, which defines it, was never applied). dequeue()'s own error handling (worker/dd-request-processor.ts ~line 268-271) treats this exactly like an empty queue: a server console.error line only, never a database record. A real queued request is left sitting in status='queued' forever with zero visible sign anything is wrong anywhere in the product -- this is the ERROR-SWALLOWED variant of the taxonomy: the real cause is invisible to anyone not tailing server logs at the exact moment of the RPC failure. (2) Bypassing dequeue() and calling processItem() directly on a second probe row reaches further but immediately fails at the taxonomy_ids -> donor_discovery_taxonomy lookup, because that table ALSO does not exist in production (migration 067 is explicitly documented as file-only, never applied). This second failure IS recorded to the DB (the harness replicated loop()'s real catch/update-to-failed behavior) -- see the result's sampleWrittenRow. Net: zero rows landed in donor_discovery_directory or donor_discovery_prospects via either path. In real, unmodified production, this continuous poll loop can never successfully claim or process a single real request, and does so silently. FALSE-PASS CASUALTY: registryPriorStatus describes this as WIRED-EVENT-CONTINUOUS-POLL, CONFIRMED STARTED -- true for the process itself, but the per-item work it exists to do cannot succeed even once against the real, live production schema. | test-evidence/pt-09/batch1-results/AG-14.json, test-evidence/pt-09/execution-batch1.json | node --import tsx scripts/audit/pt09-002-trigger/AG-14.mjs -- inserts a real donor_discovery_requests row against the local pt05-local-stack, calls DdRequestProcessor's real dequeue() and (bracket-accessed) processItem() directly, and independently re-queries donor_discovery_requests/directory/prospects via a direct pg connection. | CONFIRMED-BROKEN |
| WGR-080 | Autonomous Agents | P1 | PT-09-002 execution proof: AG-18 (ag-18-reputation, src/lib/intelligence/reputation-agent.ts, ReputationIntelligenceAgent.runForFunder()) completed cleanly (agent_runs output: success=true, itemsFound=1, itemsProcessed=0, itemsQueued=0, decisions=[], errors=[]) but zero reputation_signals rows were written. Root-caused, not left ambiguous: a follow-up direct call to DuckDuckGo's Instant-Answer API (the exact query searchDuckDuckGo() builds internally) for a very well-known, real, litigation-heavy entity ("Wells Fargo") independently returned RelatedTopics: [] -- 0 results. The response body's own meta fields (production_state: "offline", src_url: "Hello there", description: "testing") indicate DuckDuckGo's free Instant-Answer endpoint is a curated near-empty test/answer index, NOT a general web search API -- it will structurally return 0 results for the overwhelming majority of real risk-keyword queries regardless of which entity is searched. This means checkEntityReputation()'s search step (and therefore this agent's reputation-monitoring capability as a whole) is very likely non-functional against real-world signal detection as currently implemented, independent of Claude classification quality -- Claude was never reached this run since searchDuckDuckGo() returning [] short-circuits before any classification call. FALSE-PASS CASUALTY: registryPriorStatus describes this as WIRED-SCHEDULED-GATED (nightly 2AM pipeline, gated on auto_reputation_enabled); this proof shows the underlying search dependency is structurally near-useless for the feature's actual purpose, so even orgs with the autonomy toggle enabled are unlikely to ever receive a real reputation signal from this path. | test-evidence/pt-09/batch1-results/AG-18.json, test-evidence/pt-09/execution-batch1.json | node --import tsx scripts/audit/pt09-002-trigger/AG-18.mjs (against the local pt05-local-stack, a seeded funder from test-evidence/pt-09/environment.json) -- independently re-verifiable by a direct fetch to https://api.duckduckgo.com/?q=<any real risk-keyword query>&format=json and inspecting RelatedTopics.length. | CONFIRMED-BROKEN |
| WGR-081 | Autonomous Agents | P1 | PT-09-003 execution proof: AG-23/AG-32 canonical-number collision (src/lib/agents/relationship-graph-builder-agent.ts, RelationshipGraphBuilderAgent) is a genuine, confirmed single-class collision, not a naming coincidence. Direct execution shows exactly ONE real class serves both canonical numbers, and its real agent_runs row uses agent_type='ag-32-relationship-graph' only -- a direct count of agent_type='ag-23' anywhere in the database returned 0, meaning AG-23 is a registry-only row with zero backing code and zero real run history under its own literal. FALSE-PASS CASUALTY: registry's WIRED-SCHEDULED status for 'AG-23' is misleading -- any tooling joining agent_registry.agent_id='ag-23' against real agent_runs history will find nothing, ever. | test-evidence/pt-09/batch2-results/AG-23___AG-32.json, test-evidence/pt-09/execution-batch2.json | node --import tsx scripts/audit/pt09-003-trigger/AG-23_AG-32.mjs (against the local pt05-local-stack) -- compares agent_runs counts for agent_type='ag-23' vs 'ag-32-relationship-graph' before/after. | CONFIRMED-BROKEN |
| WGR-082 | Autonomous Agents | P1 | PT-09-003 execution proof: AG-24 (src/app/api/intelligence/outreach/generate/route.ts) is not agent-framework code at all -- confirmed by direct execution and source read: the route makes zero database writes of any kind (before/after sentinel 0/0), calls Claude once, and returns { subject, body, previewCompanyName } directly in the HTTP response for the Corporate Outreach composer UI to display. Its writesTo entry in the registry (email_campaign_sequences/email_sequence_steps/email_sequence_enrollments) is incorrect -- those tables belong to an entirely separate module (src/lib/email/sequence-engine.ts) this route never calls, and do not even exist on the local schema today. FALSE-PASS CASUALTY: registryPriorStatus (WIRED-MANUAL-API) implies persistence this route was never designed to do. | test-evidence/pt-09/batch2-results/AG-24.json, test-evidence/pt-09/execution-batch2.json | node --import tsx scripts/audit/pt09-003-trigger/AG-24.mjs (against the local pt05-local-stack, a seeded corporate prospect) -- captures the route's real response and independently confirms zero writes via a direct pg connection. | CONFIRMED-BROKEN |
| WGR-083 | Autonomous Agents | P1 | PT-09-003 execution proof: AG-25 Deadline Prediction (src/lib/agents/deadline-prediction-agent.ts, ag-25-deadline-prediction) ran to completion cleanly (agent_runs written, errors=[]) but wrote zero deadline_predictions rows. Root cause per the real code and seed data: environment.json's 3 seeded opportunities already carry real deadline values set at seed time, so this agent's own 'only predict when a deadline is missing/ambiguous' gate found nothing to predict for -- a real, grounded null result on this seed data, not independently confirmed as broken against opportunities with genuinely missing/ambiguous deadlines. FALSE-PASS CASUALTY: registryPriorStatus (WIRED-SCHEDULED-GATED+QUEUE) implies real predictions are being generated nightly for gated orgs; this run did not observe that happening. | test-evidence/pt-09/batch2-results/AG-25__Deadline_Prediction__dual-use_number_.json, test-evidence/pt-09/execution-batch2.json | node --import tsx scripts/audit/pt09-003-trigger/AG-25-deadline-prediction.mjs (against the local pt05-local-stack, test-evidence/pt-09/environment.json org). | UNVERIFIED |
| WGR-084 | Autonomous Agents | P1 | PT-09-003 execution proof: AG-26 (src/lib/agents/funding-forecast-agent.ts, ag-26-forecast) ran to completion cleanly (agent_runs written, errors reported empty) but wrote zero funding_forecasts rows against environment.json's seeded org/opportunities/outcomes. FALSE-PASS CASUALTY: registryPriorStatus (WIRED-SCHEDULED, real monthly worker/scheduler.ts job CONFIRMED STARTED+firing live per PT-08) implies a real forecast is produced monthly for every org; this direct invocation shows the underlying forecast-generation logic can run clean and still persist nothing. | test-evidence/pt-09/batch2-results/AG-26.json, test-evidence/pt-09/execution-batch2.json | node --import tsx scripts/audit/pt09-003-trigger/AG-26.mjs (against the local pt05-local-stack, test-evidence/pt-09/environment.json org) -- compares funding_forecasts row counts before/after via a direct pg connection. | CONFIRMED-BROKEN |
| WGR-085 | Autonomous Agents | P1 | PT-09-003 execution proof: AG-30 (src/lib/agents/donor-intent-monitor-agent.ts, ag-30-donor-intent) ran cleanly (agent_runs completed, itemsFound=2, itemsProcessed=2, errors=[]) and made 3 real callClaudeWithWebSearch() calls per prospect (6 total) with zero per-call failures, but wrote zero corporate_intent_signals rows -- output_summary states plainly 'no signal reached the 60/100 intent threshold.' Root cause per the real code: the 2 seeded synthetic company names ('Hill Country Logistics LLC', 'Lonestar Manufacturing Co') have no real CSR/ESG/press-release web footprint to find, so this is a real, grounded null result on synthetic test data rather than a confirmed broken search/scoring pipeline -- but it also independently confirms the historical rotated-API-key block (watchListRef wasBlockedOnRotatedApiKey) does NOT reproduce against the current key (zero per-call errors). FALSE-PASS CASUALTY: registryPriorStatus (WIRED-SCHEDULED-NIGHTLY+QUEUE) implies real intent signals are surfaced nightly; this run produced none. | test-evidence/pt-09/batch2-results/AG-30.json, test-evidence/pt-09/execution-batch2.json | node --import tsx scripts/audit/pt09-003-trigger/AG-30.mjs (against the local pt05-local-stack, test-evidence/pt-09/environment.json's 2 seeded corporateProspects). | UNVERIFIED |
| WGR-086 | Autonomous Agents | P1 | PT-09-003 execution proof, priority suspect (learningAggregatorNowWired_correctsStaleAssumption): AG-36 (src/lib/agents/learning-network-aggregator-agent.ts, ag-36-learning-network) is CONFIRMED wired and, per real evidence from an earlier run captured in this same test pass, does genuinely create real platform_learning_patterns rows with real anonymized narrative content. However, this run's own captured invocation (agent_run_id visible in the evidence file) processed the one remaining un-contributed outcome and only UPDATED 2 existing pattern rows (patternsCreated=0, patternsUpdated=2) -- a real, narrow WIRED-NO-OUTPUT result for this specific run's delta, distinct from the broader (now-corrected) 'orphaned/never-called' finding this suspect item set out to re-test. FALSE-PASS CASUALTY: flagged because the run's own before/after delta on platform_learning_patterns is 0, per the taxonomy's mechanical rule -- see test-evidence/pt-09/execution-batch2.json's suspectDeepDive.learningAggregator for the full resolution and why this is expected idempotent update-only behavior once an org's outcomes have all been contributed once. | test-evidence/pt-09/batch2-results/AG-36.json, test-evidence/pt-09/execution-batch2.json suspectDeepDive.learningAggregator | node --import tsx scripts/audit/pt09-003-trigger/AG-36.mjs (against the local pt05-local-stack, platform-level/SYSTEM_ORG_ID, real awarded outcomes from test-evidence/pt-09/environment.json's org). | CONFIRMED-BROKEN |
| WGR-087 | Autonomous Agents | P1 | PT-09-003 execution proof: AG-40 (src/lib/agents/strategic-advisor-agent.ts, ag-40-strategic-advisor) independently reproduces WIRING_GAP_REGISTER.md WGR-059 live: loadOrgProfile()'s select of 'name, service_area, service_areas' against organizations (which has only the singular service_area column, confirmed via a direct information_schema.columns query) fails PostgREST-side on every call, silently falling back to { name: 'this organization', service_area: null, service_areas: null }. This run additionally wrote zero strategic_recommendations rows (unlike the WGR-059-era run that still produced a degraded-but-real row), a distinct empty-output finding on top of the pre-existing quality defect. FALSE-PASS CASUALTY: registryPriorStatus (WIRED-SCHEDULED-GATED+QUEUE) implies real recommendations are generated nightly; this run produced none, on top of the org-blindness defect. | test-evidence/pt-09/batch2-results/AG-40.json, test-evidence/pt-09/execution-batch2.json | node --import tsx scripts/audit/pt09-003-trigger/AG-40.mjs (against the local pt05-local-stack, test-evidence/pt-09/environment.json org) -- independently re-confirms WGR-059 via a direct information_schema.columns query before invoking the agent. | CONFIRMED-BROKEN |
| WGR-088 | Autonomous Agents | P1 | PT-09-003 execution proof: AG-42 (src/lib/agents/change-monitor-agent.ts, ag-42-change-monitor) ran cleanly (agent_runs completed) but wrote zero corporate_monitoring_events rows. Root cause, directly observed: this agent's real StealthEngine-driven website check attempted to navigate to the 2 seeded corporate_prospects' website URLs (https://lonestar-mfg.example, https://hillcountry-logistics.example) and every attempt failed with net::ERR_NAME_NOT_RESOLVED (3 retries each) -- these are placeholder .example domains from seed data, not real reachable websites, so this is a seed-data limitation for the corporate-prospects half of the scan; the foundation_directory half was not independently isolated in this run. FALSE-PASS CASUALTY: registryPriorStatus (WIRED-SCHEDULED, CONFIRMED STARTED+firing live per PT-08) implies real monitoring happens daily; this run's real network attempts against realistic-shaped seed URLs all failed at DNS resolution before any content diff could occur. | test-evidence/pt-09/batch2-results/AG-42.json, test-evidence/pt-09/execution-batch2.json | node --import tsx scripts/audit/pt09-003-trigger/AG-42.mjs (against the local pt05-local-stack, platform-level/SYSTEM_ORG_ID, real headless-browser navigation attempts to seeded prospect website URLs). | UNVERIFIED |
| WGR-089 | Autonomous Agents | P1 | PT-09-003 execution proof: AG-43 (src/lib/agents/funder-signal-monitor-agent.ts, ag-43-funder-signals, watchListRef ag43ExistsButUnregistered) ran cleanly (agent_runs completed) but wrote zero funder_relationship_signals rows against environment.json's 3 seeded funders -- likely the same class of 'real web search against synthetic test funder names finds nothing real to cite' result AG-30 hit, though not independently isolated per-call in this run. Independently confirms the registry-completeness gap this session's pt09-001 pass first found: this real, working agent class has zero agent_registry row (scripts/seed-agent-registry.ts's ROSTER stops at ag-42-change-monitor), so it is invisible to any registry-driven tooling even though its code runs for real. Flagged as a false-pass casualty per the taxonomy's mechanical WIRED-NO-OUTPUT rule, though there is no registry row for this agent to have made a false claim in the first place -- the caveat here is scoped to the unregistered-agent gap itself, not a misleading registry status. | test-evidence/pt-09/batch2-results/AG-43__beyond_registered_1-42_range___NOT_in_registry_.json, test-evidence/pt-09/execution-batch2.json | node --import tsx scripts/audit/pt09-003-trigger/AG-43.mjs (against the local pt05-local-stack, test-evidence/pt-09/environment.json's 3 seeded funders). | UNVERIFIED |
| WGR-090 | Autonomous Agents | P1 | PT-09-004 consolidation (finding surfaced by PT-09-002 execution proof but not previously given its own register row): AG-20 (src/lib/agents/ea-01-giving-detector.ts, ea01_giving_detector) is TRIGGER-BROKEN, distinct from and additional to WGR-033's "enrichment-processor never started at boot" finding. Direct invocation (bypassing the missing boot wiring entirely) still fails: the real agent_runs row (id 93dbf01b-4d3a-4b5d-a794-f613e20d2ad6, status='failed', confirmed via direct psql read, so the failure is NOT silently swallowed) shows `AgentError: Agent timed out after 60s.` at base-agent.ts:200. Root cause, directly observed in the run's own StealthEngine console output: EA-01 tries 3 candidate paths (/giving, /csr, /community) sequentially, 3 retry attempts each, against a seeded website that fails DNS resolution (the same *.example placeholder-domain class every real dead/moved website would also hit) -- that retry loop alone exceeds BaseAgent's hard 60s per-run ceiling before Claude or any write is ever reached. Directly comparable: AG-21 (EA-08), same corporate-enrichment-shared.ts helper, same class of seed domain, only 2 candidate paths instead of 3, completed well under 60s and produced a real WORKS write. This is a genuine, generalizable robustness defect (a fixed per-candidate retry budget too close to the shared hard timeout), not a harness artifact or seed-data-specific failure -- any real unreachable/slow-to-fail funder or foundation website would reproduce it, blocking even the agent's own documented graceful "no pages found" branch from ever being reached. | test-evidence/pt-09/batch1-results/AG-20.json (triggerLog, errorSurfaced, verdictReasoning) | node --import tsx scripts/audit/pt09-002-trigger/AG-20.mjs (against the local pt05-local-stack, a seeded corporate_prospects row with a non-resolving *.example website) -- reproduces the 60s timeout; compare against AG-21.json's clean completion on the same seed class to see the 2-vs-3-candidate-path delta. | CONFIRMED-BROKEN |
| WGR-093 | Autonomous Agents / Data Integrity | P1 | PT-09-004 consolidation: beyond the 3 agent-number collisions scripts/seed-agent-registry.ts's own header comment already documents as deliberate (AG-23/AG-32 merged, AG-25 dual-use, AG-29 dual-use), this phase found 6 MORE real, undocumented on-disk collisions by cross-referencing every real `super(orgId, "agentId", supabase)` literal in src/lib/agents/*.ts against the live agent_registry table: AG-06 (registry 'ag-06-draft-generator' vs. unregistered budget-builder-agent.ts using literal 'ag-06-budget-builder'), AG-07 (registry 'recursive_learning' vs. unregistered compliance-check-agent.ts using literal 'ag-07-compliance-check'), AG-08 (registry 'ag-08-nofa-parser' vs. unregistered renewal-tracker-agent.ts using literal 'ag-08-renewal-tracker'), AG-09 (registry 'email_parser' vs. unregistered outcome-analyzer-agent.ts using literal 'ag-09-outcome-analyzer'), AG-10 (registry 'ag-10-grant-dna' vs. unregistered document-expiry-agent.ts using literal 'ag-10-document-expiry'), AG-11 (registry 'cold_outreach' vs. unregistered knowledge-gap-agent.ts using literal 'ag-11-knowledge-gap'), AG-12 (registry 'ag-12-autoapply' vs. unregistered search-profile-optimizer-agent.ts using literal 'ag-12-search-optimizer'). worker/autonomous-orchestrator.ts's own header comment independently corroborates every one of these as deliberate code-level labeling, not accidental typos -- these are real, live, scheduled agents running nightly/monthly/weekly under a canonical number the registry has already assigned to a completely different agent. All 6 were live-invoked this phase and all 6 came back WORKS (real writes: renewal-tracker created a real opportunities row, outcome-analyzer patched organizations.analytics, document-expiry logged a real agent_decisions row, knowledge-gap logged 8 category-specific agent_decisions rows, search-profile-optimizer logged real agent_decisions with Claude-generated keyword suggestions, budget-builder patched applications.budget_data) -- so these are functioning agents, not broken ones; the gap is purely that any future tooling joining agent_registry.agent_id against agent_runs.agent_type by canonical number will misattribute or silently drop all 6 agents' real run history. A separate, related, not-yet-disambiguated finding: three separate real budget-generating implementations exist (budget-agent.ts, budget-builder.ts -- both literally agentType 'budget_builder' -- and budget-builder-agent.ts -- 'ag-06-budget-builder') and were not individually resolved this phase. AG-02's own collision partner (EligibilityScoringAgent, literal 'ag-02') is confirmed DEAD CODE (zero production instantiation, only reachable via scripts/ff-agent-test.ts) and was therefore not executed or counted among the 6 live-tested collisions above. | test-evidence/pt-09/agent-inventory.json (watchList[].id === "numberCollisionPairs"); test-evidence/pt-09/batch1-results/AG-06__non-canonical_duplicate_.json, AG-07__non-canonical_duplicate_.json, AG-08__on-disk_collision__unregistered_.json, AG-09__on-disk_collision__unregistered_.json, AG-10__on-disk_collision__unregistered_.json, AG-11__on-disk_collision__unregistered_.json, AG-12__on-disk_collision__unregistered_.json | `grep -rn 'super(' src/lib/agents/*.ts` cross-referenced against a live, read-only `SELECT agent_id FROM agent_registry` -- 6 literals present in code with no matching registry row under that literal, each colliding with a different registered agent's canonical number. Or: node scripts/audit/verify-pt09-001.mjs. | CONFIRMED-BROKEN |
| WGR-095 | Autonomous Agents | P1 | PT-11-003 (AutoApply queue-processor soak test), incidental finding surfaced by a real Railway platform-log pull scoped to the run window (`railway logs --deployment --json --filter "@level:error"`), unrelated to the soak test's own subject: `[AutonomousOrchestrator] AG-38 self-improvement pipeline failed: Failed to start AG-38 platform-level run: null value in column "organization_id" of relation "agent_runs" violates not-null constraint`, fired once at 2026-08-20T09:00:32Z against the real, live, deployed `benavora-worker` Railway service. AG-38 (`self-improvement-agent.ts`) is a platform-level (not per-org) scheduled agent -- per PT-09's own AG-38 verdict (WORKS, `test-evidence/pt-09/PHASE-09-SUMMARY.md` row AG-38) it wrote a real `improvement_proposals`/`agent_performance_metrics` row when invoked directly with an explicit orgId during that phase's execution proof, so this is not the same code path failing the same way twice -- this is the *live scheduled* platform-level run path (no orgId supplied, relies on some internal default/lookup) hitting a real NOT NULL violation on `agent_runs.organization_id` that the direct-invocation path in PT-09 never exercised. Confirmed live and unprompted (not manufactured by this audit) -- it fired on its own schedule during a window this session happened to be watching, not in response to any action this audit took. Not fixed here, out of this audit's scope. | test-evidence/pt-11/suite-results-flaky.json (`suites[].id === "soak-autoapply-queue-processor"`, `.finding` field, quotes the exact Railway log line and timestamp verbatim) | Pull real Railway logs for `benavora-worker` production scoped to any time window: `railway logs --service <benavora-worker-service-id> --json --filter "@level:error"`, grep for `AG-38 self-improvement pipeline failed`; or read `worker/autonomous-orchestrator.ts`'s AG-38 platform-level scheduled-run code path directly to find where it derives (or fails to derive) `organization_id` before calling `startRun()`. | CONFIRMED-BROKEN |
| WGR-100 | Frontend / E2E | P1 | PT-11-003 (`cross-browser` suite, same 2 runs as WGR-099). `e2e/critical-paths.spec.ts`'s "creating an application... adds a row to `/applications/list`" test failed on **both chromium and firefox in both of 2 independent runs today** (only webkit's run-1 failure on this same test class, described separately in WGR-099, is browser-specific timing; this row is the application-creation-flow failure itself, which is not webkit-specific and matches `CROSSBROWSER_TEST_RESULTS_20260813.md`'s original finding exactly). Graded P1, not P0: unlike the WebKit login race, this affects one specific user action (creating an application, then confirming it lands on the list) rather than the entire post-login flow, and only 2 of the 3 tested engines. Root cause not re-investigated this phase (matches the still-open, still-unfixed 2026-08-13 finding). One related, explicitly NON-persisting finding from the same runs, reported honestly as a flake rather than folded into this row: chromium's separate "funder Relationship Builder page loads without 404 or 500" test failed in run 1 and passed in run 2 -- not registered as its own gap, since it did not reproduce. | test-evidence/pt-11/logs/cross-browser-run1.log, test-evidence/pt-11/logs/cross-browser-run2.log (`chromium`/`firefox` counts + per-test failure names in `suite-results-flaky.json`'s `runs[].finding` fields); `CROSSBROWSER_TEST_RESULTS_20260813.md` (repo root, matches this finding exactly) | `PLAYWRIGHT_BASE_URL=http://localhost:3100 NEXT_PUBLIC_SITE_URL=http://localhost:3100 npx playwright test e2e/critical-paths.spec.ts --project=chromium --project=firefox --reporter=list` (against a real `next dev -p 3100` instance) -- reproduces the "adds a row to /applications/list" failure on both engines. | CONFIRMED-BROKEN |
| WGR-101 | Autonomous Agents / AutoApply | P1 | PT-13-001 silent-catch census (census-silent-catches.mjs, real static scan, 2365 catch sites): src/lib/agents/form-filler.ts:253 -- inside the AutoApply real form-filler agent's file-upload field handler, `await page.setInputFiles(combinedSelector, tmpPath).catch(() => undefined)` silently discards any Playwright file-attach failure. The agent proceeds to submit the form as if the file field were successfully populated -- there is no re-check that the input actually holds the file, no retry, no log line, no agent_runs/autoapply_submissions field recording the failure. A real, live grant submission that requires a required-document upload (a common AutoApply case per FEATURE_REGISTRY_v2.md) could be submitted with a silently-missing attachment, with the platform recording it as a normal successful submission. | test-evidence/pt-13/silent-catches.json (findings[] where file==="src/lib/agents/form-filler.ts" && line===253, severity:"P1") | grep -n "setInputFiles" src/lib/agents/form-filler.ts -- confirms the unguarded .catch(() => undefined) at line 253; or re-run node scripts/audit/census-silent-catches.mjs and grep its output for "form-filler.ts.*253". | CONFIRMED-BROKEN |
| WGR-104 | Worker/Boot | P1 | PT-13-002 observability audit (F11): worker/index.ts:144 is the ONE background sub-process in the worker's boot sequence wired so that an unhandled failure does NOT crash the process or mark worker_status.status="error" -- `agentQueueDone = processAgentQueue(supabase).catch((err) => { console.error(...) })` only logs to the console. Contrast with the two handlers immediately above it (process.on("uncaughtException")/process.on("unhandledRejection"), lines 112-128) which both DO set worker_status.status="error" before exiting. If processAgentQueue's internal loop throws past its own catch, the worker keeps sending a healthy 30s heartbeat (an entirely separate interval, unaffected) and /admin/system continues showing the worker "online" while agent_queue processing has silently and permanently stopped -- the platform's own health signal is compatible with a fully dead agent-queue processor. | test-evidence/pt-13/observability.json (part2_errorSurfacing.findings[] id==="F11") | sed -n "108,150p" worker/index.ts -- confirms lines 112-128 (uncaughtException/unhandledRejection) set status="error", while line 144-146 (agentQueueDone) only console.error's. | CONFIRMED-BROKEN |
| WGR-105 | Frontend / Observability | P1 | PT-13-002 observability audit (F20): src/app/api/admin/system/route.ts:71-73 (donor_discovery_requests depth query) filters .eq("status", "pending") -- but donor_discovery_request_status (supabase/migrations/067_donor_discovery_foundation.sql:14-16) is a Postgres ENUM with exactly 6 real values: "queued", "enumerating", "enriching", "scoring", "complete", "failed" -- "pending" has never been a real value (the real create-route, src/app/api/donor-discovery/requests/route.ts:100, writes "queued"). This metric structurally reads 0 forever regardless of real backlog. It directly compounds the already-registered WGR-079 (AG-14's real, live, confirmed-broken stuck-queue bug, where a request sits in "queued" forever with the RPC failure only reaching a server console.error) -- the one dashboard metric built specifically to surface that exact failure mode cannot ever show it. A human diligently checking /admin/system every day would see "donor_discovery_requests: 0" and have no reason to suspect anything is wrong. | test-evidence/pt-13/observability.json (part3_monitoringReality.findings[] id==="F20") | grep -n 'eq("status", "pending")' src/app/api/admin/system/route.ts (line 73) cross-checked against grep -n "donor_discovery_request_status" -A3 supabase/migrations/067_donor_discovery_foundation.sql (real enum values, no "pending"). | CONFIRMED-BROKEN |
| WGR-122 | Middleware/Availability | P1 | PT-10-001 fuzzing POST /api/notifications (the one CRON_SECRET-bearer-gated route deliberately included in the malformed-payload set, exercised with a real bearer token instead of a session cookie) found all 3 cases against it returned a 307 redirect to /login instead of ever reaching the routes own body-validation logic -- the same root-cause middleware gap WGR-111 already registered (src/middleware.ts requires a real Supabase session cookie for any path not in PUBLIC_PATHS, with no carve-out for non-cookie auth mechanisms), but reproduced here for the first time against a route authenticated purely by an Authorization bearer header rather than a cookie-less request, and via 3 different malformed-body shapes (invalid_json, missing_required, wrong_type) all hitting the identical redirect before body validation. Confirms WGR-111s finding is not specific to cookie-less requests -- a request that DOES carry valid non-cookie credentials (a correct CRON_SECRET bearer token) is redirected exactly the same way, meaning this routes real external caller (a cron/automation trigger posting notifications, not a browser session) would be silently redirected to /login in production exactly like the webhook/cron routes WGR-111 already covers. Not a new root cause -- filed as its own row because it is a concretely different route + a different real auth mechanism (bearer vs cookie-less) than any case WGR-111s own evidence covers. | test-evidence/pt-10/malformed-payloads.json (cases[] where route.path=="/api/notifications" and verdict=="FINDING", ids PT10-070,071,072) | node scripts/audit/pt10-001-malformed-payloads.mjs; or directly: curl -i -X POST http://localhost:3299/api/notifications -H "Authorization: Bearer $CRON_SECRET" -H "Content-Type: application/json" -d "{}" against the isolated local dev instance -- observe 307 to /login before any body check runs. Cross-reference src/middleware.ts PUBLIC_PATHS/isPublicPath() (same code WGR-111 cites). | CONFIRMED-BROKEN |
| WGR-124 | Middleware/Availability | P1 | PT-10-002 scenario 1 (db_slow phase, +15000ms latency injected before the fault proxy forwards to the real local Supabase stack, harness-bounded client wait 30000ms): the public path (/) survived the full ~15.2s round trip and still returned its normal 404 response (elapsedMs 15169, matching the injected delay almost exactly) -- but both /dashboard (a real authenticated Server Component page) and /api/notifications (a real API route) never resolved at all, hitting the harness's own 30-second bounded wait and aborting with 'The operation was aborted due to timeout' (elapsedMs ~30003-30006 on both). Root cause, from a direct code read: neither src/lib/supabase/server.ts nor admin.ts configures any fetch-level timeout/AbortSignal on the Supabase client, and no query anywhere in middleware.ts or the dashboard/notifications code paths sets a Postgres statement_timeout or wraps its own await in a bounded race. The two failing targets both make TWO serial network round trips through the slow path per request -- middleware's own getUser() call (which runs on every non-public request) plus the page's/ route's own separate Supabase query -- compounding the injected 15s latency to roughly 30s+, which is consistent with why they cross the harness's bound while the public path (one round trip only) does not. Net effect: a genuinely slow-but-not-fully-down database dependency produces the same practical outcome to a real user as a full outage -- an indefinite hang with no error message, no loading/retry UI, and no bounded fast-fail anywhere in the stack -- rather than a designed degrade path. This is a worse outcome than WGR-123's clean db_down case, which at least resolves quickly. | test-evidence/pt-10/outage-simulation.json#scenarios.scenario1_db_outage.cases[] (phase=="db_slow", target in ["protected_dashboard_with_session","api_notifications"]) | node --import tsx scripts/audit/pt10-002-outage-simulation.mjs (scenario 1 reproduces the db_slow phase deterministically via the fault proxy's own live-reread latency-injection mode); node scripts/audit/verify-pt10-002.mjs. | CONFIRMED-BROKEN |
| WGR-125 | Worker/Queue | P1 | PT-10-002 scenario 2: a real, standalone child process was let claim a real submission_queue row via the exact two-step claim predicate worker/queue-processor.ts's dequeue() uses (confirmed via the child's own CLAIM_WORKER_CLAIMED log line before the kill), then SIGKILLed mid-way through the simulated in-flight-work window, before any terminal status write. A direct service-role re-read after confirming the child had actually exited (not just been signalled) found the row permanently at status='processing', started_at set, completed_at and error_message both still null, with no field drift from the original insert (no partial/corrupt write -- rowCountForTestOrg:1, matches expected, zero unexpected field drift) and confirmed NOT reclaimable via the real dequeue() predicate (status='pending' only). Three code-read findings back this up as a structural gap, not a one-off: (1) dequeue() (lines ~420-459) has no stale-claim timeout -- a row already at status='processing' is invisible to every future dequeue() call, forever, regardless of how long ago it was claimed; (2) no cron/scheduled sweep anywhere in worker/scheduler.ts reaps stale submission_queue rows (repo-wide grep for stuck/stale/reclaim/orphan job-sweep logic touching this table found none); (3) the one existing manual admin remedy, POST /api/admin/system {action:'clear_stuck_jobs'} (src/app/api/admin/system/route.ts lines ~157-189), only clears agent_runs rows stuck at status='running' for over 2 hours and contains zero references to submission_queue -- an admin clicking 'Clear Stuck Jobs' on /admin/system cannot reach this row even if they try. A separate, older AutoApply system (automation_queue / AutomationWorkerAgent) DOES implement a real reapTimedOutItems(), but only on a different table and only when a human/cron hits POST /api/automation/process -- it provides no protection for submission_queue. Real impact: any worker crash, OOM, host eviction, or deploy restart that lands mid-claim leaves a genuine, permanent corrupt half-done state requiring direct DB intervention to recover -- the funder application the row represents silently never gets submitted, retried, or flagged as failed to anyone. | test-evidence/pt-10/outage-simulation.json#scenarios.scenario2_worker_killed_mid_job (behavior, postFailureIntegrityCheck, codeReadNotes, findings[0]=PT10-002-S2-001) | node --import tsx scripts/audit/pt10-002-outage-simulation.mjs (scenario 2 reproduces this deterministically: seed a pending submission_queue row, let a worker claim it, SIGKILL the worker before it writes a terminal status, re-read the row); node scripts/audit/verify-pt10-002.mjs. | CONFIRMED-BROKEN |
| WGR-132 | Frontend/Kanban | P1 | PT03-KA-F01 (test-evidence/pt-03/kanban-auth.json, kanban.enforcement_gap_tests[2]): canMoveToStage() says only owner/admin may move an application to 'submitted', but a 'viewer'-role session's direct executeTransition() call persisted the exact same transition. The role gate is enforced only in StageTransitionModal.tsx's client-side canConfirm check -- executeTransition() itself, and the database (the same applications_org_isolation RLS policy already found role-blind in WGR-131), apply no equivalent check. Confirmed live: real application row (id=3ae4666e-d42d-4da8-81da-d4f00509e6a1), canMoveToStage_viewer=false, canMoveToStage_owner=true (the rule itself is correctly asymmetric), viewer_attempted_write=true, viewer_write_threw=false, viewer_error=null, stage_before='ready_for_review', stage_after='submitted', outcome='PERSISTED_DESPITE_ROLE_RULE'. Rated P1 rather than P0 (unlike WGR-130/131) because the transition itself IS a legal one per the stage graph (ready_for_review -> submitted, compliance_check condition) -- the gap is narrower: which role may perform an otherwise-legal transition, not whether an illegal one can be forced through at all. | test-evidence/pt-03/kanban-auth.json#findings[0] (PT03-KA-F01), #kanban.enforcement_gap_tests[2] (role_gate_bypass), #kanban.journeys[2] (journey C, step 'role-gate bypass: viewer-role session executes ready_for_review -> submitted') | node --import tsx scripts/audit/pt03-005-kanban-auth.mjs against the local pt05-local-stack; or independently: as a real 'viewer'-role session, call executeTransition() directly (bypassing StageTransitionModal.tsx's own canMoveToStage() pre-check) on a 'ready_for_review'-stage application targeting 'submitted' -- the write succeeds despite canMoveToStage(role='viewer', ...) itself correctly returning false for this transition. | CONFIRMED-BROKEN |
| WGR-135 | Business Logic | P1 | PT-04-001/PT-04-004: computeGrantProbability()'s persisted opportunity_probability_scores rows are never recomputed when their underlying input data changes -- a real, live, currently- reproducing staleness bug, not confined to one hand-picked example. Hand-verified via independent reimplementation of the documented formula (src/lib/intelligence/grant-probability-engine.ts) against opportunity 8851652c-... (Texas CDBG Housing, org b1ab7402-...): the row's eligibility_score factor is pinned at the neutral fallback value 0.5 (contribution 15) even though opportunities.eligibility_score is now 72 (non-null, populated 2026-08-19, 17 days after opportunity_probability_scores.computed_at 2026-08-02) -- applying the real, current eligibility_score to the SAME documented formula produces overall_score=47 (contribution 21.6) vs the system's actual persisted/returned overall_score=42, a 5-point discrepancy on the one number src/app/(dashboard)/opportunities/page.tsx's own header comment says the UI reads verbatim and NEVER recomputes. Corroborated independently against the row's own stored key_risks array, which still contains the string 'No eligibility score computed for this opportunity yet.' -- text buildKeyRisks() only emits when eligibility_score IS NULL, directly contradicting the column's current non-null value. Not an isolated instance: a live query of 9 real opportunity_probability_scores rows currently satisfying the engine's own real-data precondition (opportunities.eligibility_score IS NOT NULL) found the SAME pinned-at-0.5-fallback pattern on 3 of 9 (33%) -- all 3 belonging to org b1ab7402 (af4620eb-.../value should be 0.18 not 0.5, 8851652c-.../0.72 not 0.5, a3e45cc5-.../0.05 not 0.5), all computed 2026-07-30 through 2026-08-02, before this org's later eligibility scoring pass populated real values; the other org's 6 sampled rows (bed3e621-...) show no drift, since their stored eligibility_score factor already matches the current column value exactly on all 6. Root cause: computeGrantProbability() has no change-triggered recompute anywhere in the codebase (no DB trigger, no on-write hook from the eligibility-scoring agent, no cron sweep) -- once a row is written it stays permanently stale relative to the real opportunities/organizational_digital_twins/outcomes data unless something re-invokes the function by hand. In the confirmed deep-dive instance the recommendation category itself did not flip (both 'consider'), so this specific case is a wrong displayed number, not yet a wrong recommendation -- but nothing in the formula prevents a larger real-world drift from crossing the apply/consider/skip thresholds (70/40) for other opportunities, which is exactly what this staleness mechanism would silently produce, with no error or staleness indicator surfaced anywhere in the UI. | test-evidence/pt-04/grant-probability.json, test-evidence/pt-04/candidates-raw.json | node scripts/audit/pt04-001-fetch-data.mjs (re-pull the 9 real candidate rows against production, writes candidates-raw.json); node scripts/audit/pt04-001-grant-probability.mjs (independent hand-reimplementation of the formula + diff against the persisted row, writes grant-probability.json); node scripts/audit/verify-pt04-001.mjs (evidence-shape gate). | CONFIRMED-BROKEN |
| WGR-140 | Integration/Parser | P1 | SAM.gov opportunities/v2/search (same file as WGR-139): once a valid date range is supplied, real hits' "description" field is always a URL to a separate GET .../noticedesc endpoint, never literal opportunity text — confirmed on every one of 5 real sampled hits (housing keyword, 2026-08-01..2026-08-20). mapHit()'s `description: toStr(hit.description) \|\| null` stores this URL string directly as the opportunity's description with no crash and no warning — a real, silent data-quality bug distinct from WGR-139's total-failure case (this fires once the mandatory date params are added). | test-evidence/pt-07/data-sources.json#sources.sam_gov.shape_check | node scripts/audit/pt07-003-data-sources.mjs (re-runs the real live calls); node scripts/audit/verify-pt07-003.mjs (validates the captured evidence) | CONFIRMED-BROKEN |
| WGR-145 | Integration | P1 | ScraperAPI proxy rotation (src/lib/scraper/stealth-engine.ts resolveProxy()/StealthEngine.launchContext()): SCRAPER_API_KEY is confirmed absent from this environment's .env.local (consistent with WGR-003, which already flagged it as production-unconfirmed). resolveProxy() returns undefined when the key is unset, and launchContext() only sets launchArgs.proxy when truthy — so with no key, chromium.launch() runs with ZERO proxy configured: every scrape from this environment connects directly from the scraping machine's own IP, not through any rotating pool. Live-confirmed the gateway itself is real and reachable: a CONNECT-tunnel HTTPS request through proxy-server.scraperapi.com:8001 with a deliberately invalid key returns a real, live HTTP 401 "Unauthorized request, please make sure your API key is valid." — proving the mechanism resolveProxy() constructs is genuine and enforces real per-key auth, so a configured key would genuinely route through it. Actual IP rotation (a server-side ScraperAPI account feature, not client logic in this codebase) cannot be verified without a real, funded credential, which does not exist in this environment or (per WGR-003) confirmed to exist in production either. | test-evidence/pt-07/data-sources.json#sources.scraperapi_rotation | node scripts/audit/pt07-003-data-sources.mjs (re-runs the real live calls); node scripts/audit/verify-pt07-003.mjs (validates the captured evidence); or curl -sk -x "http://scraperapi:<key-or-fake>@proxy-server.scraperapi.com:8001" https://httpbin.org/ip. | CONFIRMED-BROKEN |
| WGR-148 | Data/Realtime | P1 | The `supabase_realtime` Postgres publication has ZERO member tables in production -- confirmed live via a direct query of pg_publication_tables (the publication itself exists, it just has no tables attached to it). Cross-referenced against every table this app's own source code subscribes to via `.channel(...).on("postgres_changes", ...)` (src/components/autoapply/{ManualQueue,WorkerStatus,QueueMetrics,ReviewQueue,QueuePanel}.tsx and src/components/command-center/CommandCenterLive.tsx, whose backing snapshot is src/app/api/admin/command-center/route.ts): submission_queue, autoapply_submissions, autoapply_review_queue, worker_status, agent_runs, agent_decisions, applications -- all 7 of 7 subscribed tables are absent from the publication, so Realtime silently never emits a change event for any of them; no error is raised anywhere, the subscribing component simply never receives a push update. This is not a new bug: STATE_OF_THE_BUILD.md's own 2026-08-07 'Command Center Realtime' session entry already documented the identical root cause (publication has zero member tables) for a 3-table subset (agent_runs, agent_decisions, applications) and proposed the one-line fix (`ALTER PUBLICATION supabase_realtime ADD TABLE ...`) -- that fix was never applied; this session's independent, fresh live query (2026-08-20, ~2 weeks later) confirms the same gap still holds today, now covering the full, current set of 7 subscribed tables across both the Command Center and every AutoApply live-status component, none of which were previously registered. Graded P1, not P0: every one of these components was built with a documented 60-second polling safety-net fallback specifically because Realtime's reliability was already known to be uncertain (per that same 2026-08-07 entry's own header comment on QueueMetrics.tsx's precedent), so no page silently shows stale data forever -- it just never updates faster than that fallback interval, which is a real, confirmed, live UX degradation (no instant push anywhere in the app), not a data-loss or blocked-workflow bug. This audit did not independently re-verify that every one of the 7 consumers actually has a working fallback poll (only CommandCenterLive.tsx's was directly cited in the prior session's own text) -- worth confirming before treating the P1 grading as certain for all 7. | test-evidence/pt-07/supabase-state.json#probes.realtime | node scripts/audit/pt07-001-supabase-state-probe.mjs (re-runs the real live checks); node scripts/audit/verify-pt07-001.mjs (validates the captured evidence); or directly: SELECT tablename FROM pg_publication_tables WHERE pubname = 'supabase_realtime' against DATABASE_URL (real response captured this session: zero rows) compared against a grep of src/components/autoapply/*.tsx, src/components/command-center/CommandCenterLive.tsx, and src/app/api/admin/command-center/route.ts for .channel(...).on("postgres_changes", ...) table names. | CONFIRMED-BROKEN |
| WGR-003 | Config | P2 | 13 production-required env vars confirmed absent from local `.env.local` (breaks real production functionality if also unset there, but not the deploy-verify gate specifically — that's WGR-002): `CREDENTIAL_ENCRYPTION_KEY`, `CRON_SECRET`, `INTEGRATION_ENCRYPTION_KEY`, `INTEGRATION_KEY_SECRET`, `PORTAL_ENCRYPT_SECRET`, `RESEND_API_KEY`, `RESEND_WEBHOOK_SECRET`, `SCRAPER_API_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `SUPABASE_URL`, `UNSUBSCRIBE_HMAC_SECRET`, `WORKER_ID`. Two concrete downstream risks worth flagging specifically: (1) `worker/index.ts`'s `validateEnv()` hard `process.exit(1)`s without `SUPABASE_URL` — the worker needs its own `SUPABASE_URL` distinct from the Next.js app's `NEXT_PUBLIC_SUPABASE_URL` (which IS present), so the worker process cannot boot locally even though the web app's Supabase config looks complete; (2) every one of 16 `/api/cron/*` routes permanently 401s without `CRON_SECRET`, regardless of a real Vercel Cron trigger header. This confirms only local-dev absence — production (Vercel/Railway) env vars were not checked by this audit, so this does not by itself indicate a production outage; worth Reid confirming these are genuinely unset in Vercel/Railway production too, not just locally. **Update (PT-02 consolidation, per Reid, not independently re-verified by this audit):** `CRON_SECRET` specifically is confirmed present in Vercel production env vars, resolving that one open question for that one variable — the other 12 vars in this row remain unconfirmed either way. Reid also reports cron routes return `401` (not a redirect) when hit unauthenticated in production. This is Reid's own report only; PT-02-002's own live unauthenticated sweep (against a local dev server, not production) found the opposite for every `cron_secret`-classified route — a `307` redirect to `/login`, before the route's own `CRON_SECRET` check ever runs (full mechanism and evidence: **WGR-023**). The two findings describe different environments (local dev server vs. deployed Vercel production) and are not necessarily in conflict — production's request path may differ from local `pnpm dev` in a way this audit did not test (e.g., middleware behaving differently on that deployment, or a Vercel-Cron-specific request shape this audit didn't replicate) — but that has not been independently confirmed here. Flagging the discrepancy for whoever reviews this next rather than resolving it by assumption. **Update (PT-08 worker boot inventory, 2026-08-19):** for `SUPABASE_URL` specifically, production presence is now confirmed by live evidence, not just Reid's word — `railway logs --deployment` against the live `benavora-worker` Railway service captured the complete container-boot window (`test-evidence/pt-08/railway-boot-window.json`), showing `[Worker] AutoApply Worker started` and every processor's own `Starting` line all firing. `worker/index.ts`'s `validateEnv()` runs synchronously before any of that code executes and hard-exits on a falsy `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`ANTHROPIC_API_KEY`/`WORKER_ID` — reaching those log lines is only possible if all four were truthy at boot. The other 12 vars in this row remain unconfirmed in production either way. | `test-evidence/pt-00/env-audit.json` (`summary.p2_findings`); `test-evidence/pt-02/unauth-sweep.json` (results for `cron_secret`-mechanism routes, all `status: 307`) | `node scripts/audit/verify-pt00-004.mjs` (prints these 13 under `[P2]`); to check the production claim, a real unauthenticated `curl -i --no-location <production-url>/api/cron/reminders` against the deployed Vercel URL, compared against the identical call against a local `pnpm dev` server, would settle whether the two environments genuinely differ. | CONFIRMED-BROKEN |
| WGR-013 | Infra/Dev-Environment | P2 | The shared local dev server (`localhost:3000`, plain `next dev`, no `distDir` isolation) was found mid-session with its client hydration bundle (`.next/static/chunks/main-app.js` and others) missing at the filesystem level, caused by a concurrent, unrelated `next build` process in the same checkout (PID 112940, observed still running after 55+ minutes without completing) writing to the same shared `.next/` directory `next dev` reads from. Confirmed live: an initial render-pass sweep against the shared server found 12 of 146 routes failing, every one bearing a build-contention signature (either a server-side `Cannot find module '.../vendor-chunks/@supabase+auth-js@2.108.0.js'` error, or repeated `404`s on core `_next/static/chunks/{main-app.js,app-pages-internals.js,app/(dashboard)/layout.js}` requests); a follow-up check of ~15 more routes stuck on a "Loading..." state found the same signature had since spread to nearly every dashboard route. This is the same multi-worktree `.next` contention failure mode `next.config.mjs`'s `experimental.cpus: 1` comment and WGR-001/`STANDING_DIRECTIVES.md` already document for `next build`-vs-`next build` contention -- this session shows it also applies to `next dev`-vs-`next build` sharing one `.next/`, and that `next dev` does not self-heal from it (still broken 50+ minutes and hundreds of requests after the corruption began; `.next`'s own directory mtime was frozen at the moment of corruption). Worked around for this session only by launching a second, isolated `next dev` instance with `distDir` overridden via a temporary, reverted `next.config.mjs` env-gated addition -- the shared server itself was left running, untouched, still broken, for whatever else may depend on it. | `test-evidence/pt-01/build-contention-investigation-static-chunks.json`, `test-evidence/pt-01/build-contention-investigation-loading-states.json`, `test-evidence/pt-01/render-failures/build-contention-investigation/` (12 screenshots), `test-evidence/pt-01/render-results.json`'s `methodologyNote` field | Run two concurrent `next dev`/`next build` processes against the same un-isolated `.next/` in this checkout under real multi-worktree load (several agent worktrees building at once) and watch `.next/static/chunks/main-app.js` start 404ing on the `next dev` server while it keeps running and never recompiles it. | CONFIRMED-BROKEN |
| WGR-025 | API | P2 | PT-02-004 CRUD cycle: DELETE /api/drafts/queue/{id} is a soft delete (sets status="rejected", rejected_reason="removed_from_queue") but a subsequent GET /api/drafts/queue/{id} still returns 200 with the full item (status now "rejected"), never a 404. A caller relying on "delete then GET 404s" to confirm removal gets a false negative -- the route never signals the item is gone, only that its status field changed. By-design soft-delete, not a crash, so P2 not P0/P1. | test-evidence/pt-02/crud-cycles.json (resources[] where resource==="draft_queue", steps.readAfterDelete) | As a writer-role session: POST /api/drafts/queue {opportunity_id}, then DELETE /api/drafts/queue/{id} (200), then GET /api/drafts/queue/{id} -- returns 200 with item.status="rejected", not 404. Handler: src/app/api/drafts/queue/[id]/route.ts DELETE. | CONFIRMED-BROKEN |
| WGR-026 | API | P2 | PT-02-004 CRUD cycle: DELETE /api/autoapply/profiles/{id} is a soft delete (sets active=false) but a subsequent GET /api/autoapply/profiles/{id} still returns 200 with the full profile (active now false), never a 404. Same class as WGR-025 -- a caller relying on "delete then GET 404s" gets a false negative. By-design soft-delete, not a crash, so P2 not P0/P1. | test-evidence/pt-02/crud-cycles.json (resources[] where resource==="request_profiles", steps.readAfterDelete) | As a writer-role session: POST /api/autoapply/profiles {name, request_type, needs_description}, then DELETE /api/autoapply/profiles/{id} (200), then GET /api/autoapply/profiles/{id} -- returns 200 with profile.active=false, not 404. Handler: src/app/api/autoapply/profiles/[id]/route.ts DELETE. | CONFIRMED-BROKEN |
| WGR-030 | API | P2 | PT-02-005 pagination audit: `GET /api/donor-discovery/pipeline` (`src/app/api/donor-discovery/pipeline/route.ts` lines 79-84) computes `avgScore` from an unbounded `.select("score").eq("organization_id", organizationId).not("score","is",null)` -- no `.range()`/`.limit()`, no `ORDER BY`. Same truncation mechanism as WGR-029/WGR-031 (live-proven via WGR-028), but graded P2 not P1: the real, live org with 133,812 `donor_discovery_prospects` rows (the same org as WGR-029) currently has ZERO rows with a non-null `score` -- scoring has not run against this batch yet -- so today this query returns 0 rows either way and `avgScore` is `null` in both the truncated and true case. The bug is real and will silently misreport `avgScore` the instant scoring populates more than 1000 of this org's prospects; with no `ORDER BY`, which arbitrary 1000 rows PostgREST returns would not even be deterministic across requests. Latent, not currently producing an observably wrong number, but the mechanism is confirmed, not hypothetical. **RESOLVED commit `5c747ee` (2026-08-20):** the unbounded `avgScore` select now pages through with `.range()` via the new shared `src/lib/supabase/select-all-pages.ts` helper until a short page confirms nothing's left, instead of trusting a single response. Live-verified against the real org's current (0-scored-row) state via a direct read-only PostgREST call: the paginated query shape returns `Content-Range: */0`, matching the real state -- ready to page correctly once scoring populates past 1000 rows. `tsc --noEmit` and `npm run build` both exit 0. | test-evidence/pt-02/pagination-and-500s.json (`paginationAudit.endpoints[]` where `wgrId === "WGR-030"`) | `GET /api/donor-discovery/pipeline` as an authenticated session for org `b1ab7402-dfc2-4712-869f-70ea3566cc1d` -- currently returns `avgScore: null` (0 real scored rows). Re-check after scoring has run against >1000 of this org's prospects; the unbounded query at lines 79-84 will then silently cap at 1000 arbitrary rows. | RESOLVED |
| WGR-032 | API | P2 | PT-02-005 pagination audit: `GET /api/donor-discovery/prospects` (`src/app/api/donor-discovery/prospects/route.ts`) has two internal, unbounded filter-building sub-queries, separate from the route's own correctly-paginated main listing (see WGR-028): the `request_id` branch (lines 81-84, unbounded select against `dd_prospect_requests`) and the `taxonomy_id` branch (lines 98-116, unbounded selects against `donor_discovery_taxonomy` then `donor_discovery_directory`), both used to build a `.in()` id-list filter applied BEFORE the outer `.range()`-bounded page/limit. **`request_id` branch: SILENT_TRUNCATION_CONFIRMED**, same live mechanism/evidence as WGR-029 -- filtering this route by `?request_id=cf95be82-...` (the real 133,812-linked production request) would silently narrow the `.in()` filter to only the first 1000 linked ids, understating the paginated result's own `total` for that request. **`taxonomy_id` branch: SILENT_TRUNCATION_LATENT** -- live-checked and currently non-triggering: 0 of the real 133,815 `donor_discovery_directory` rows have `naics_codes` or `civic_kind` populated at all today, so every `taxonomy_id` filter currently matches 0 rows regardless of this bug; the query shape would truncate the moment enrichment data populates those columns and a taxonomy node matches more than 1000 directory rows. **RESOLVED commit `5c747ee` (2026-08-20):** both branches were rewritten to push the filter into the query itself (PostgREST `!inner` embed + dot-notation filter on the embedded `directory`/`dd_prospect_requests` relation) instead of collecting matching ids client-side first, closing both the `request_id` branch's confirmed truncation and the `taxonomy_id` branch's latent one -- and, for `request_id`, closing a second problem pagination alone could not: at this org's real scale (133,812 links) a fully-paginated id list would itself be too large for a practical `.in()` filter. Live-verified against the real 133,812-linked production request via a direct read-only PostgREST call: `Content-Range: 0-2/133812`, matching the true total exactly. `tsc --noEmit` and `npm run build` both exit 0. | test-evidence/pt-02/pagination-and-500s.json (`paginationAudit.endpoints[]` where `wgrId === "WGR-032"`) | `GET /api/donor-discovery/prospects?request_id=cf95be82-b66c-48af-a00b-782852b132b5` as an authenticated session for org `b1ab7402-dfc2-4712-869f-70ea3566cc1d` -- compare returned `total` against the real 133812 linked prospects. Taxonomy branch: `SELECT count(*) FILTER (WHERE naics_codes IS NOT NULL AND array_length(naics_codes,1)>0), count(*) FILTER (WHERE civic_kind IS NOT NULL) FROM donor_discovery_directory` via `DATABASE_URL` -- currently both 0. | RESOLVED |
| WGR-061 | Lib | P2 | PT-06-003: `knowledge_queries.organization_id` doesn't exist live (real column: `org_id`). `src/lib/intelligence/knowledge-engine.ts:111`'s `queryKnowledgeEngine()` inserts an audit-log row into `knowledge_queries` using `organization_id`; the insert is wrapped in its own try/catch so this never breaks the caller, but it means this table has never actually logged a single real query -- matches a gap already documented narratively in `STATE_OF_THE_BUILD.md`'s 2026-08-07 AG-05 Knowledge Engine session ("queryKnowledgeEngine()'s own audit-log insert into knowledge_queries uses organization_id, but the live column is org_id... this table has never logged a single query in its history") but never previously entered into this register. P2 not P1: it's a silently-swallowed audit-log write, not a user-facing break. | `test-evidence/pt-06/schema-mismatch.json` (`column_mismatches[]` where `table === "knowledge_queries"` and `column === "organization_id"`) | `grep -n "knowledge_queries" src/lib/intelligence/knowledge-engine.ts`; `SELECT column_name FROM information_schema.columns WHERE table_name='knowledge_queries'` via `DATABASE_URL` -- confirms `org_id`, not `organization_id`. | CONFIRMED-BROKEN |
| WGR-066 | Data/Infra | P2 | Duplicate-row-rate audit across the 3 largest anchor tables: exact-match EIN duplication is clean on both tables with a live UNIQUE index on `ein` (`foundation_directory`: 0 duplicate groups across 133,812 rows; `nonprofits`: 0 duplicate groups across 1,978,526 rows -- confirms the index is genuinely enforced, not bypassed). Real duplication shows up on looser, unenforced identity keys instead: `foundation_directory` `lower(name)+state` has 167 duplicate groups / 176 extra rows (0.13%); `nonprofits` `lower(name)+state` has 23,314 duplicate groups / 264,519 extra rows (13.37% -- the single largest raw number in this audit, though a same-name+state collision at ~2M rows is not automatically a defect, since two genuinely distinct nonprofits can share a name and state); `donor_discovery_directory` `lower(legal_name)` has 2,165 duplicate groups / 3,257 extra rows (2.43%) -- and that table's real dedup index (`UNIQUE(lower(legal_name), extract_domain(website))`) is structurally unable to catch duplicates among rows with a null `website`, confirmed via `pg_get_functiondef` that `extract_domain(NULL)` evaluates to NULL and Postgres never treats two NULLs as equal in a unique index -- the identical 2,165/3,257 collision count recurs when restricted to `website IS NULL` rows, direct evidence of that exact blind spot (compounded by WGR-067's 100% `donor_discovery_directory.website` null rate below -- the blind spot applies to the entire table today, not an edge case). A stricter `lower(legal_name)+hq_address` compound key on the same table drops to 42 groups / 45 extra rows (0.03%), a much stronger real-duplicate signal. | `test-evidence/pt-06/data-quality.json` (`tables.foundation_directory`/`nonprofits`/`donor_discovery_directory`.`duplicate_rate_checks`) | `node scripts/audit/pt06-data-quality.mjs`; or run any of `duplicate_rate_checks[].query` directly against `DATABASE_URL` | CONFIRMED-BROKEN |
| WGR-069 | Data/Infra | P2 | 2 live tables (`corporate_relationships`, `fundability_deficiencies`) exist in production but match no `CREATE TABLE` statement in either known migration directory (`supabase/migrations`, `src/supabase/migrations`) -- confirmed by cross-referencing every live table against every migration file's extracted DDL, not assumed from an absence alone. Both are real, populated-schema tables (`corporate_relationships`: 10 columns incl. `org_id`, `source_entity_name`, `target_entity_name`, `warm_introduction_path`; `fundability_deficiencies`: 10 columns incl. `fundability_score_id`, `category`, `is_auto_fixable`). Per `STANDING_DIRECTIVES.md` DIRECTIVE-017, this project has a documented, sanctioned direct-`DATABASE_URL` DDL path outside the migration-file workflow, so this is plausibly legitimate hand-run DDL rather than a tooling bug -- but it means these two tables' schema is NOT reproducible from the committed migration files alone, and any future fresh-database bootstrap (the same disposable-DB protocol WGR-068 used) will silently lack them. Not resolved here -- needs a human decision on whether to backfill a migration file for each, or document them as intentionally hand-managed. | `test-evidence/pt-06/migration-drift.json` (`appliedNotOnDisk.entries`) | `node scripts/audit/pt06-migration-drift.mjs`; or `SELECT to_regclass('public.corporate_relationships'), to_regclass('public.fundability_deficiencies')` against `DATABASE_URL` (both resolve), cross-referenced against a grep for `CREATE TABLE corporate_relationships` / `CREATE TABLE fundability_deficiencies` across both migration directories (zero matches) | PENDING-SCOPE |
| WGR-091 | Autonomous Agents | P2 | PT-09-004 consolidation: AG-31 (ag-31-national-forecast), AG-33 (Partnership Discovery Agent), AG-34 (Personalization Engine) all confirmed to have zero implementation file anywhere in src/lib/agents/*.ts or src/lib/intelligence/*.ts, via a fresh repo-wide grep run this phase (not carried over from an older claim). AG-31 differs from AG-33/AG-34 in one respect worth keeping distinct: AG-31 DOES have a live agent_registry seed row (scripts/seed-agent-registry.ts ROSTER, agent_id 'ag-31-national-forecast') whose own description text explicitly acknowledges no code exists yet -- so the registry is not lying about AG-31, it is honestly tracking a not-yet-built agent. AG-33/AG-34 have neither a registry row nor code -- confirmed absent from both scripts/seed-agent-registry.ts's ROSTER and the live 43-row agent_registry table queried read-only this phase. None of the three are false-pass casualties (no prior status implied they worked); grouped into one row since all three are the same finding class (confirmed zero implementation), each with its own independently-verified evidence file. | test-evidence/pt-09/batch2-results/AG-31.json, AG-33.json, AG-34.json; test-evidence/pt-09/agent-inventory.json (canonicalAgents entries for AG-31/AG-33/AG-34, triggerWiredVerdict NO-CODE / NO-REGISTRY-NO-CODE) | `grep -rniE "national.?forecast\|partnership.?discovery\|personalization.?engine\|ag-31\|ag-33\|ag-34" src/lib/agents/*.ts src/lib/intelligence/*.ts scripts/seed-agent-registry.ts` -- zero implementation-file matches for all three; AG-31 alone matches in the seed script's ROSTER. | CONFIRMED-BROKEN |
| WGR-092 | Autonomous Agents / Testability | P2 | PT-09-004 consolidation: AG-12 (AutoApply -- worker/queue-processor.ts + src/lib/autoapply/{stealth-browser,form-filler-agent,captcha-solver,confirmation-monitor,email-submitter}.ts) has no dry-run/simulation mode anywhere in its real send/submit code path. Confirmed by direct source read and an exhaustive grep for any dry-run/simulate/sandbox/test-mode flag (dryRun, dry_run, DRY_RUN, simulate, sandbox, test.mode) across worker/queue-processor.ts and src/lib/autoapply/email-submitter.ts -- zero matches. Two real, live outbound-effect code paths exist unconditionally: submitViaEmail() sends a real email to a real funder contact address (a real provider messageId is logged, not a simulated one), and FormFillerAgent.fillAndSubmit() drives real Playwright browser automation against a real third-party web portal, gated only by an existing automation_sessions approval row, never by a safe/simulated mode. This means the single riskiest, most consequential automated action in the entire agent roster -- an irreversible real-world side effect against a third party -- is structurally impossible to verify end-to-end without either sending a real email to a real recipient or driving a real browser through a real portal submission. Corroborated by this project's own prior audit history: the 2026-08-13 AutoApply soak test (SOAK_TEST_AUTOAPPLY_RESULTS_20260813.md) deliberately routed all 50 test items around this exact same real-send/real-submit step for the identical reason. Not fixed or worked around this phase -- building a real dry-run capability into email-submitter.ts and form-filler-agent.ts is a remediation task, not something this audit program should do unilaterally while auditing. | test-evidence/pt-09/batch1-results/AG-12.json (triggerLog documents the grep and source-read findings in full, before/after both null since no invocation was attempted) | `grep -rniE "dry.?run\|simulate\|sandbox\|test.?mode" worker/queue-processor.ts src/lib/autoapply/email-submitter.ts` -- zero matches; read worker/queue-processor.ts lines ~829 (submitViaEmail) and ~1308-1319 (FormFillerAgent.fillAndSubmit) directly to confirm both are unconditional real-effect calls. | PENDING-SCOPE |
| WGR-096 | Testing | P2 | PT-11-002 (`unit-src` suite, real execution, first time this specific 13-file set has ever been run to a documented completion in this repo): `src/__tests__/unit/regressions.test.ts`'s "regression: IRS 990 XML fetch using dead S3 URL / wrong fetch method (fetchPage vs fetchRaw)" > "resolves a filing via fetchRaw() on the direct XML URL and never calls fetchPage()" test fails. Expected `website: "https://example.org"`, actual `website: "https://example.org/"` -- a trailing slash is now present in whatever real fetchRaw()/URL-normalization code path this regression test exercises, that was not present when the test was written. This is a genuine, currently-reproducing test-vs-code drift (confirmed via the raw vitest diff output, not inferred), not a flake -- single run this phase, not independently re-run a second time to check persistence. | test-evidence/pt-11/logs/unit-src.log (full vitest diff output + `1 failed \| 132 passed (133)` summary line); test-evidence/pt-11/suite-results-core.json (`suites[].id === "unit-src"`, `.finding` field) | `npx vitest run src/__tests__/unit/regressions.test.ts --reporter=verbose` -- reproduces the exact expected-vs-actual diff on the `fetchRaw()` test. | CONFIRMED-BROKEN |
| WGR-097 | Testing / Frontend | P2 | PT-11-002 (`smoke-playwright-public` suite, real execution against the corrected port-3100 Benavora server -- first time this specific file has ever been run to a documented completion in this repo): `tests/smoke.spec.ts` asserts an H1 heading matching `/Win More Grants/`; the landing page's real, current H1 is "Your mission deserves every dollar available to it." Confirmed this is a genuine stale-copy/stale-test mismatch against the real app, not an environment artifact -- the failure was independently re-verified to be against Benavora's own real content (real nav/logo/CTA visible in the captured page snapshot), not the wrong-server "AFS" page the very first, pre-correction attempt this session hit. Single run this phase, not independently re-run a second time. | test-evidence/pt-11/logs/smoke-playwright-public.log (`1 failed` summary line, full assertion-failure text quoting both the expected regex and the real page's actual H1 text); test-evidence/pt-11/suite-results-core.json (`suites[].id === "smoke-playwright-public"`, `.finding` field) | `PLAYWRIGHT_BASE_URL=http://localhost:3100 npx playwright test tests/smoke.spec.ts --project=public --reporter=list` (against a real `next dev -p 3100` instance) -- reproduces the H1 mismatch. | CONFIRMED-BROKEN |
| WGR-098 | Testing | P2 | PT-11-003 (`visual-regression` suite, real execution -- first time this suite has ever completed a run in this repo; PT-11-001 had already found it affirmatively could not have passed in its prior committed state, since no baseline snapshot directory existed on disk at all). A fresh baseline was generated this session (`--update-snapshots`, 7/7 clean) and the very next comparison run against that same-session baseline failed on the `autoapply` page -- reproduced in BOTH of 2 independent comparison runs (run 1: 115px diff; run 2: 194px diff -- different exact magnitude each time, zero UI code changes between runs, same test). Root cause identified from the run-2 failure's own Playwright page snapshot: the AutoApply page renders a real, live WebSocket-connected "Live Session Viewer" panel (`LiveSessionViewer.tsx`) whose connection-status indicator (connecting/connected/live/offline) is not a `<time>` element, `data-timestamp` attribute, or regex-matched date/relative-time text string -- the only three things `e2e/visual-regression.spec.ts`'s own `timestampMasks()` helper covers. This is a genuine, previously-undocumented gap in the visual-regression suite's own masking coverage, distinct from an actual UI regression on the autoapply page itself (dashboard/opportunities/applications-list/funders all passed cleanly in both runs). Not fixed this phase -- capturing the finding, not remediating the suite, per this audit's scope. | test-evidence/pt-11/logs/visual-regression-run1.log, test-evidence/pt-11/logs/visual-regression-run2.log (both `1 failed \| 6 passed (7)`); test-evidence/pt-11/artifacts/visual-regression/autoapply-run2-failure/ (actual/expected/diff PNGs + error-context.md page snapshot explicitly showing the "Live Session Viewer" heading and a real-time queue-status combobox); test-evidence/pt-11/suite-results-flaky.json (`suites[0].comparison_runs[]`, `.persistence_finding`) | `PLAYWRIGHT_BASE_URL=http://localhost:3100 NEXT_PUBLIC_SITE_URL=http://localhost:3100 npx playwright test e2e/visual-regression.spec.ts --update-snapshots --reporter=list` to generate a fresh baseline, then the identical command without `--update-snapshots` -- reproduces the `autoapply` page failure; read `e2e/visual-regression.spec.ts`'s `timestampMasks()` function directly to confirm it has no selector covering `LiveSessionViewer.tsx`'s connection-status element. | CONFIRMED-BROKEN |
| WGR-102 | Autonomous Agents / AutoApply | P2 | PT-13-001 silent-catch census: src/lib/autoapply/form-filler-agent.ts (a distinct file from WGR-101's src/lib/agents/form-filler.ts -- two separately-named form-filler implementations exist in this codebase) carries 36 swallowed, undocumented catch sites -- the single largest concentration of any file in the entire 2365-site census, more than double the next-highest file. All 36 are marked onRealDataOrAgentPath:true. Sampled directly: several DO carry an explanatory inline comment (e.g. "org lookup failed -- continue without it", "KB lookup failed -- continue without entries", "Claude response unparseable -- continue without filling unmapped fields") describing WHAT happens, but none use the census's stricter "documented-intentional-swallow" vocabulary (best-effort/intentional/non-fatal/etc per DOC_PATTERNS in the census script) explaining WHY silently degrading is safe in each specific case -- a real, middle-ground gap between "no reasoning at all" and "a deliberately justified swallow", not a false positive in the census. This is this repo's single densest concentration of a pattern (agent quietly proceeds on degraded/partial data with no operator-visible trace) that PT-13-002's observability audit (F6/F7) independently found is structurally capable of losing an entire run's completion state the same way. | test-evidence/pt-13/silent-catches.json (findings[] where file==="src/lib/autoapply/form-filler-agent.ts", 36 entries, all swallowed:true documented:false testFile:false onRealDataOrAgentPath:true) | node -e "const f=require('./test-evidence/pt-13/silent-catches.json').findings; console.log(f.filter(x=>x.file==='src/lib/autoapply/form-filler-agent.ts'&&x.swallowed&&!x.testFile&&!x.documented).length)" -- prints 36. | CONFIRMED-BROKEN |
| WGR-103 | Backend / Email | P2 | PT-13-001 silent-catch census: src/lib/email/sequence-engine.ts:230, inside processScheduledSends()'s per-enrollment send loop, a bare `catch { failed++; }` discards the real error object entirely -- only a counter increments, no console.error, no persisted error detail on the email_sequence_enrollments row, no distinguishing which of N enrollments failed or why. Compounds the already-registered WGR-038 (this function's only caller, /api/cron/email-sequences, is not registered in vercel.json/worker/scheduler.ts, so scheduled sends never fire automatically today) -- meaning this gap is currently dormant in production, but would immediately start silently discarding real send-failure reasons the moment WGR-038 is fixed and this code path starts actually running unattended. | test-evidence/pt-13/silent-catches.json (findings[] where file==="src/lib/email/sequence-engine.ts" && line===230, severity:"P2") | grep -n -A2 "catch {" src/lib/email/sequence-engine.ts \| grep -B2 "failed++" -- confirms the bare catch with no logging at line 230. | CONFIRMED-BROKEN |
| WGR-106 | Backend / AutoApply | P2 | PT-13-002 observability audit (F14): src/lib/autoapply/alerting.ts's checkAlerts() (line 21) is a fully-built threshold-alert engine covering worker-offline (>5min stale heartbeat), low success rate (<50% over 1hr), daily cost overrun (>$50), and per-tenant submission-volume anomaly (>3x 30-day average) -- exactly the automated failure-detection layer this platform otherwise lacks. It has zero callers anywhere in the codebase outside its own definition, confirmed both by a repo-wide grep (2 hits total: the function's own definition, and one comment in form-filler-agent.ts:184 explicitly stating it has no caller) and by form-filler-agent.ts's own header comment. Not wired into worker/scheduler.ts, not exposed via any API route, not called from any dashboard component. | test-evidence/pt-13/observability.json (part3_monitoringReality.findings[] id==="F14"; part3_monitoringReality.existsButDeadCode[0]) | grep -rn checkAlerts src worker -- returns exactly 2 lines: the definition and the form-filler-agent.ts comment. | CONFIRMED-BROKEN |
| WGR-107 | Autonomous Agents | P2 | PT-13-002 observability audit (F7): AutonomousAgent.completeRun() (src/lib/agents/autonomous-base.ts:158) and failRun() (line 195) both discard the `error` from their agent_runs .update() call with zero checking and zero console output. This is not a hypothetical risk -- it is the exact code pattern STATE_OF_THE_BUILD.md's 2026-08-03 session documents as having already caused a real production incident: AG-10 (GrantDnaAgent) runs returned success:true internally while the real agent_runs row stayed stuck at status:"running" forever, because migration-added column output_payload had not been applied and the unchecked update silently failed -- the same session separately flags AutonomousDigestAgent and StrategicAdvisorAgent as "likely had every real production run silently stuck at status: running" from the identical pattern. 32 of the canonical Generation-2 agent classes extend AutonomousAgent. The code pattern that caused the prior incident is unchanged in the file as it exists today -- any future column drop, RLS change, or migration gap on agent_runs would reproduce the identical silent-forever-running failure mode with zero code change needed to trigger it. | test-evidence/pt-13/observability.json (part1_runLogCompleteness.findings[] id==="F7") | sed -n "158,204p" src/lib/agents/autonomous-base.ts -- confirms completeRun()/failRun() discard their .update() error with no check; cross-reference STATE_OF_THE_BUILD.md 2026-08-03 AG-10 session entry for the prior real incident this pattern already caused. | CONFIRMED-BROKEN |
| WGR-112 | API/SQLi | P2 | GET /api/email/threads?search= (src/app/api/email/threads/route.ts:38) interpolates the raw, unescaped `search` query param directly into a PostgREST `.or()` filter template literal (`subject.ilike.%${search}%,snippet.ilike.%${search}%`) with no comma/parens stripping -- unlike the other two comparable search endpoints in this codebase (corporate-prospects `q`, which strips [%,]; intelligence/library/search `query`, which runs sanitizeIlikeTerm() stripping [,()%]), both re-confirmed still mitigated this session. Live-reproduced: a payload comma-injecting a `gmail_thread_id.neq.<bogus>` clause into `search` caused the route to return both of two seeded rows despite neither row's subject/snippet containing the search term at all -- proving the injected clause was genuinely honored by PostgREST, not a fluke or a parse error (httpStatus 200, resultCount 2, both seeded ids returned). Scope, precisely: organization_id is a separate, non-injectable top-level .eq() filter (ANDed with the .or() clause as a distinct PostgREST query param) so this specific hole cannot cross the tenant boundary -- confirmed by design and corroborated by PT-05's independent 0/120 cross-tenant leak result. Real impact is a same-org business-logic filter bypass (the caller's own session can retrieve more of their own org's synced_email_threads rows than the search term should match, or trigger a 500 with a differently-shaped malformed payload -- also reproduced this session with an earlier payload variant), not a cross-tenant leak or a classic SQL-injection syntax hole. | test-evidence/pt-14/injection.json (attempts[] where id==="sqli-002-email-threads-or-filter-injection", plus sqli-001/003/004 for baseline and the two mitigated comparison targets) | As a real authenticated (viewer-role) session, GET /api/email/threads?search=zzznomatch99%2Cgmail_thread_id.neq.impossible-value-xyz against an org with at least one real synced_email_threads row -- observe every row in the org returned, not just rows matching 'zzznomatch99'. node scripts/audit/pt14-002-live-tests.mjs reproduces this live against a throwaway org. | CONFIRMED-BROKEN |
| WGR-113 | Email/XSS | P2 | Transactional HTML email templates (src/lib/email/templates/{base-layout,draft-ready,morning-digest,urgent-alert,welcome}.ts) interpolate user-controlled fields (organization display name, digest item titles/details) directly into raw HTML template-literal strings with zero output-escaping anywhere in the directory (grep for escapeHtml\|sanitize across all 5 files: zero hits). First documented in SECURITY_TEST_2026-08-15.md section 2b as a real, unfixed gap; not previously carried into WIRING_GAP_REGISTER.md. Re-confirmed still unfixed this session via a fresh static scan (same result: all 5 files, zero escaping function referenced). Real impact scoped honestly, unchanged from the original finding: not directly exploitable as executing JavaScript in mainstream mail clients (Gmail/Outlook/Apple Mail strip <script> and block inline JS by default), but is genuine uncontrolled HTML injection into a real transactional send -- an attacker who controls an org name or a digest item title reaching one of these templates could inject arbitrary markup (fake buttons/links styled to look legitimate, broken layout, a tracking pixel via <img>). | test-evidence/pt-14/injection.json (attempts[] where id==="xss-004-email-templates-html-injection"), SECURITY_TEST_2026-08-15.md section 2b (original finding) | grep -rn 'escapeHtml\\|sanitize' src/lib/email/templates/ -- zero hits across all 5 files. node scripts/audit/pt14-002-live-tests.mjs re-runs this check as part of the XSS sweep. | CONFIRMED-BROKEN |
| WGR-117 | Storage/Security | P2 | New finding, not in MASTER_BACKLOG's original 6-bucket list: a 7th live bucket, `org-branding`, exists in production storage.buckets today (created after 2026-07-30) with public=true and the identical policy shape as nofa-pdfs -- SELECT open to public (role, no TO clause), INSERT/UPDATE scoped TO authenticated but with no organization_id/owner check in the WITH CHECK clause, meaning any authenticated user from any org can insert or overwrite any object in this bucket, not just their own org's branding assets. Plausibly intentional (branding logos may be intended to render publicly on marketing pages) but, like nofa-pdfs (§1.2 #13), needs the same one-line intentionality confirmation from Reid rather than being assumed safe. | test-evidence/pt-14/rls-anon-audit.json (buckets.org-branding, beyond_scope_findings.new_buckets_not_in_master_backlogs_original_six) | node scripts/audit/pt14-004-anon-rls-storage-audit.mjs -- see buckets['org-branding'].db_policies_applicable_to_this_bucket | PENDING-SCOPE |
| WGR-118 | Data/Security | P2 | Beyond the task's literal read-only scope, but surfaced by the same DB-level grant query: 107 of 184 public-schema tables still carry an unrevoked default PostgreSQL PUBLIC INSERT/UPDATE/DELETE grant to the anon role (same root cause as the already-documented 'Public schema default ACLs auto-grant anon+authenticated full CRUD' pattern). Live-verified this is NOT currently exploitable: for every one of the 107, the applicable RLS write policy's predicate references current_org_id()/auth.uid()/auth.role()='authenticated' -- all of which evaluate to NULL/false for an unauthenticated anon caller (auth.uid() has no JWT sub claim for the anon key), so 0/107 were found with a policy that would actually pass for anon. This is real, live-verified defense-in-depth debt, not a live P0: protection currently depends entirely on every write policy's predicate staying correct, rather than the grant itself being absent (which is how the already-remediated read-side tables in WGR-115 are protected). Recommend revoking the stale INSERT/UPDATE/DELETE grants the same way the 2026-08-06 remediation revoked stale SELECT grants. | test-evidence/pt-14/rls-anon-audit.json (beyond_scope_findings.anon_write_grant_analysis, 107 entries, all policy_appears_to_neutralize_anon=true) | node scripts/audit/pt14-004-anon-rls-storage-audit.mjs -- see beyond_scope_findings.anon_write_grant_analysis | CONFIRMED-BROKEN |
| WGR-119 | Data/Security | P2 | Full public-schema RPC surface enumerated live via pg_proc + has_function_privilege('anon', ..., 'EXECUTE') (NOT live-invoked one by one -- several are real mutating functions, e.g. resume_paused_submission_queue_item, skip_paused_submission_queue_item, reassign_paused_submission_queue_item, increment_usage_tracking, donor_discovery_upsert_directory/_record, donor_discovery_increment_api_spend, and blindly calling them with the anon key would risk corrupting real production state, which enumeration does not require). RESULT: all 47 real functions have EXECUTE granted to anon -- PostgreSQL's default PUBLIC EXECUTE grant, never revoked for any of them, same root cause as WGR-118. 5 are SECURITY DEFINER (current_org_id, is_onboarding_edit_restricted, marketplace_listing_owned_by_org, marketplace_org_has_match_on_listing, register_organization) -- all read-only predicate helpers except register_organization, which was not investigated further this pass. The mutating SECURITY INVOKER functions run AS the anon role if called anonymously, so are subject to the same table-level grants/RLS already checked in WGR-115/118 (e.g. resume_paused_submission_queue_item would fail immediately -- submission_queue has zero anon grants at all) -- but this was confirmed by cross-referencing the underlying tables' own grant state, not by actually invoking any RPC. Recommend revoking anon EXECUTE on every mutating RPC as defense in depth, matching WGR-118's recommendation. | test-evidence/pt-14/rls-anon-audit.json (rpcs, 47 entries) | node scripts/audit/pt14-004-anon-rls-storage-audit.mjs -- see rpcs[] and cross-reference each mutating function's target table in tables{} | CONFIRMED-BROKEN |
| WGR-121 | API/Robustness | P2 | PT-10-001 malformed-payload fuzz (72 cases across 18 real mutation routes, drawn from PT-02s api-routes.json) found 13 cases across 8 distinct routes where malformed-but-plausible input (oversized string, wrong JS type, SQLi/XSS-shaped string, or a garbage non-date string in a field the route never date-validates) produced a real HTTP 500 instead of a clean 4xx: POST /api/drafts/queue (oversized + injection_shaped template_type), POST /api/email/templates (oversized + injection_shaped body), POST /api/financials/budgets (non-numeric total_budget string, garbage period_start string), POST /api/outreach/templates (injection_shaped body), POST /api/autoapply/queue (funder_ids sent as an array of numbers instead of UUID strings), POST /api/donor-discovery/requests (injection_shaped name), POST /api/compliance (garbage due_date string), PATCH /api/knowledge-base (non-numeric annual_budget string -- this one is worse than a generic 500: the response body is the RAW, UNCAUGHT PostgreSQL error text "invalid input syntax for type numeric", leaking the underlying column type/engine detail to the client instead of a wrapped error), POST /api/reports/board (invalid JSON body AND numeric start_date/end_date both 500 despite the routes own request.json().catch(()=>({})) fallback suggesting this was meant to degrade gracefully). Common root cause across most of these: the field in question is only typeof/non-empty checked in the route, never format/type-coerced before being hitting a typed Postgres column (numeric/date/uuid), so a malformed-but-well-typed-JS value reaches the DB layer and throws an unhandled exception the route maps (or fails to map) to a 500. Every case is BEHAVIOR-only (does the app crash or partial-write), matched against a real service-role follow-up read where applicable -- no partial-write/silent-corruption was found among these 13, only clean request failures with a 500 status. Exploitability depth for the injection-shaped payloads specifically is PT-14s job, already covered separately (see WGR-108..113). | test-evidence/pt-10/malformed-payloads.json (cases[] where verdict=="FINDING" and category!="invalid_json" for the redirect cases, ids PT10-004,005,016,017,020,021,033,045,051,055,066,067,068) | node scripts/audit/pt10-001-malformed-payloads.mjs (re-runs the full 72-case fuzz against a local Supabase stack + isolated next dev instance per PT10_BASE_URL); node scripts/audit/verify-pt10-001.mjs re-derives the verdict counts from cases[] and fails on drift. | CONFIRMED-BROKEN |
| WGR-126 | Integration/Parser | P2 | PT-10-002 scenario 3 (malformed third-party response fuzz, monkeypatched global.fetch, never the real internet): grantsgov-client.searchGrantsGovOpportunities throws an uncaught exception on a genuinely malformed Grants.gov API response, with no try/catch protecting the immediate real call site (src/lib/sources/grantsgov-sync.ts syncGrantsGovForOrg(), line ~101, 'for (const keyword of keywords) { const hits = await searchGrantsGovOpportunities(keyword); ... }'). Root cause: mapHit() (grantsgov-client.ts, an unexported helper) does hit.id/hit.oppTitle on every array element with no null guard on the element itself; the outer function only wraps the fetch() call and response.json() in try/catch -- the per-hit mapping loop (lines ~126-129) sits outside both. One real protecting layer exists one hop further up: src/app/api/cron/grantsgov/route.ts wraps EACH org's syncGrantsGovForOrg() call in its own try/catch (line ~50), so a malformed response fails only that one org's entire sync for that run (silently losing every keyword after the failure point, not just the malformed one) -- it does not crash the cron job or any other org's sync. | test-evidence/pt-10/outage-simulation.json#scenarios.scenario3_malformed_third_party_responses.behavior[] (target=="grantsgov-client.searchGrantsGovOpportunities", id="PT10-002-S3-001"), .findings[0] | node --import tsx scripts/audit/pt10-002-outage-simulation.mjs (scenario 3, case PT10-002-S3-001). | CONFIRMED-BROKEN |
| WGR-127 | Integration/Parser | P2 | PT-10-002 scenario 3 (malformed third-party response fuzz): samgov-client.searchSamGovOpportunities throws an uncaught exception on a genuinely malformed SAM.gov API response, with no try/catch protecting the immediate real call site (src/app/api/sources/samgov/route.ts GET() handler, line ~53, 'const hits = await searchSamGovOpportunities();') -- the entire GET() function body was checked directly and has NO try/catch anywhere, only explicit early-return checks for specific known conditions (missing auth, missing orgId, a failed opportunities SELECT). Same structural bug as WGR-126's grantsgov-client -- identical file shape, same missing null guard on array elements in the response-mapping step. Unlike WGR-126, no protecting layer exists anywhere further up: this route is hit directly by an external cron trigger per the file's own header comment ('SYSTEM job... gated solely by CRON_SECRET'), with no per-org looping wrapper in this codebase the way grants.gov's cron route has. Blast radius is narrower (one HTTP request, not a multi-org loop) but the failure mode is worse in kind: an uncaught exception here propagates out of the route handler entirely, so the real caller (Vercel Cron) gets Next.js's own generic framework error response instead of this route's own clean jsonError() JSON shape that every other failure path in this file uses. | test-evidence/pt-10/outage-simulation.json#scenarios.scenario3_malformed_third_party_responses.behavior[] (target=="samgov-client.searchSamGovOpportunities", id="PT10-002-S3-002"), .findings[1] | node --import tsx scripts/audit/pt10-002-outage-simulation.mjs (scenario 3, case PT10-002-S3-002). | CONFIRMED-BROKEN |
| WGR-141 | Integration/Parser | P2 | SAM.gov opportunities/v2/search (same file as WGR-139/140): real search hits never carry an "awardAmount" field at all (confirmed absent from the schema of every one of 5 real sampled hits) — toAmount(hit.awardAmount) always evaluates on undefined and returns null. amount_max is therefore always null for every SAM.gov-sourced opportunity via this path. Not a crash, and toAmount() already handles undefined gracefully by design — logged because it's a silent, permanent data gap someone could otherwise assume is just "most opportunities lack an amount" rather than "this field never populates from this source." | test-evidence/pt-07/data-sources.json#sources.sam_gov.shape_check | node scripts/audit/pt07-003-data-sources.mjs (re-runs the real live calls); node scripts/audit/verify-pt07-003.mjs (validates the captured evidence) | CONFIRMED-BROKEN |
| WGR-146 | Integration | P2 | RESEND_API_KEY is not configured in .env.local or in any Vercel environment (confirmed live via `vercel env ls` — only 12 env vars total exist across the whole project, none Resend-related). src/lib/email/resend-client.ts's sendEmail() — the shared platform-originated transactional email path used by draft-ready/morning-digest/urgent-alert/welcome notifications — was invoked for real with the key deliberately absent (matching the real production state) and returned {success:false, error:'RESEND_API_KEY not configured'}, its documented no-key branch. The platform degrades gracefully (no crash), but zero platform-originated emails can currently be sent in production. src/app/api/webhooks/resend/route.ts's Svix signature verification was read and confirmed correct (fails closed with 500 if RESEND_WEBHOOK_SECRET is unset, HMAC-SHA256 over svix-id.svix-timestamp.body, timingSafeEqual comparison) but is likewise unreachable today since no key/secret exists to produce a real webhook in the first place. | test-evidence/pt-07/comms-billing.json#resend | node --import tsx scripts/audit/pt07-004-comms-billing-probes.mjs (re-runs the real live checks); node scripts/audit/verify-pt07-004.mjs (validates the captured evidence) | PENDING-SCOPE |
| WGR-149 | Data/Infra | P2 | Postgres/PostgREST connection-pool contention under load (PT-12-004, real disposable-branch DB, not production, but a property of the Postgres/PostgREST statement-timeout mechanism itself, not branch-specific): a moderately expensive read-only query (leading-wildcard `ILIKE` sequential scan against `nonprofits.name`, 1.97M rows) that succeeds in 5,096ms when run alone was fired at real concurrency levels 2/5/10/20/40 in the same burst. Every single level produced 100% `statement_timeout_error` (Postgres error `57014`, "canceling statement due to statement timeout") and 0% success -- the query was hard-canceled by Postgres's own `statement_timeout`, not gracefully queued to completion, even at the lowest tested concurrency of 2 simultaneous callers. Observed cutoff was a consistent ~8,072-8,457ms across concurrency 2 through 20; at concurrency=40 wall-clock time roughly doubled to 16,218ms, confirming real connection-wait queueing is happening underneath the fixed-timeout cancellations, not just a flat repeating constant. `pool_exhaustion_error` count was 0 at every level -- the pool itself never refused a connection outright; every failure was the statement-timeout GUC firing on an already-acquired connection once concurrent contention pushed real execution time past it. This is an ungraceful contention failure mode (hard cancel, no retry, no queue-to-completion) for any query of comparable cost run under even mild real-world concurrency, on this infra tier's statement_timeout setting. | test-evidence/pt-12/pool-ratelimit.json#findings.poolStress (`levels[].poolStress.outcomeCounts`, `levels[].poolStress.wallMs`, `levels[].poolStress.errorSamples`) | node scripts/audit/pt12-004-pool-ratelimit.mjs (re-runs the real live burst against the branch, if still live; branch has since been torn down per PT-12-005 -- re-run requires recreating a branch first); or directly: fire 2+ concurrent leading-wildcard ILIKE queries against a large text column via the same Supabase project's PostgREST endpoint and observe `57014` statement_timeout errors under contention where the identical query alone succeeds. | CONFIRMED-BROKEN |
| WGR-153 | API/Cost-Control | P2 | PT-15-002: static-plus-live rate-limiting posture audit of the "expensive" surface (`src/app/api/ai/**` + `src/app/api/agents/**`, 56 route.ts files, Claude/agent-calling, most with `maxDuration>=120`). 23/56 use a real rate limiter (the shared `checkRateLimit()` in `src/lib/utils/rate-limit.ts`, live-confirmed this session to genuinely block after its configured limit and to key correctly per-caller -- 6 calls at limit=3 produced exactly `[true,true,true,false,false,false]`, and a second key was confirmed unaffected by the first key's exhausted limit -- or an equivalent local `isRateLimited()` copy, e.g. `/api/ai/draft`, `/api/ai/budget`). The remaining 30 routes have neither this nor any billing-tier cost throttle (`enforceLimit`/`withUsageCheck`/`checkTierGate`) -- confirmed by direct source grep, not inferred -- while running with `maxDuration` of 60-300s (agent-trigger routes such as `/api/agents/discovery`, `/api/agents/simulate`, `/api/agents/corporate-research`, `/api/agents/foundation-finder`, `/api/agents/disaster`, `/api/agents/custom-api`, `/api/agents/custom-scrape`, plus 23 more -- full list in evidence). Nearly all 30 are still `requireRole`-gated (writer+) or at minimum `requireAuth`/session-checked -- this is not an anonymous-access gap -- but an authenticated writer-role account, a buggy client retry loop, or a compromised session could call any of these repeatedly with zero in-app mechanism slowing them down or capping cost, unlike the 23 routes that do have one. Separately (same audit): this app has no server-side login/signup API route of its own (`src/app/api/auth/` contains only `callback/` and `log-event/`) and `src/middleware.ts` (read in full) contains zero rate-limiting logic of any kind -- sign-in/sign-up go directly from the browser to Supabase's own GoTrue API via the client SDK, so auth-attempt throttling is entirely delegated to Supabase's platform-level rate limits, whose exact configured values for this project were NOT independently verified this session (no read-only CLI/API path was found -- `supabase config push` exists in the installed CLI, v2.102.0, but no `pull`/`get` equivalent). Graded P2, not P1/P0: the gap is real and confirmed but the blast radius is bounded by existing role-gating on nearly every affected route, and the auth-throttling delegation to Supabase is a reasonable, common architecture choice rather than an oversight -- it is the *unverified* Supabase-side config, not an app-level hole, that is the open question there. | `test-evidence/pt-15/readiness-ops.json#rateLimiting` (`detail.liveMechanismTest`, `detail.expensiveEndpointPosture.expensiveAndUnprotected` for the full 30-route list, `detail.authPosture`) | `node scripts/audit/pt15-002-rate-limit-posture.mjs` (re-runs the live `checkRateLimit()` mechanism test and the static route scan); or directly: `grep -L "checkRateLimit\|isRateLimited" src/app/api/ai/**/route.ts src/app/api/agents/**/route.ts` reproduces the unprotected-route list. | CONFIRMED-BROKEN |
| WGR-001 | Build/Infra | P3 | `next.config.mjs` `experimental.cpus` build-worker cap (PT-00-002 check): investigated, found PRESENT (`cpus: 1` at `next.config.mjs:35`), with an in-file comment describing the exact multi-worktree memory-thrash / hung-build-past-timeout failure mode this check exists to catch. Not a gap — logged for traceability so this isn't re-checked from scratch. | `test-evidence/pt-00/build-config.txt`, `test-evidence/pt-00/build-proof.txt` | `rm -rf .next && pnpm run build` (or `node scripts/audit/verify-pt00-002.mjs`) — confirms exit 0, no worker-crash/timeout signature in the output. | CONFIRMED-OK |
| WGR-010 | Frontend/Nav | P3 | Route manifest's `orphanRoutes` cross-reference (pages with no incoming link found by scanning `nav-items.ts`) reports 78 of 146 pages as orphaned. Investigated (PT-00-003 session), found NOT to be a defect: `nav-items.ts`'s own header comment states Dashboard/Research/Opportunities/AutoApply/Draft Generator/Donor Discovery live in `Header.tsx`'s top tab bar by design (confirmed against `Header.tsx`'s `TABS` array — covers 6 of the 78); the rest split into equally explainable categories — marketing pages (`/`, `/pricing`, `/how-it-works`, `/for-consultants`, `/privacy`, `/security`, `/terms`), auth pages (`/login`, `/register`, `/forgot-password`, `/reset-password`), `/settings/*` subsections (reached via `SettingsNav`, not the sidebar), and drilldown/action pages reached by a link from a list page rather than the sidebar (`/applications/new`, `/autoapply/[10+ subsections]`, `/donor-discovery/[8 subsections]`, `/funders/new`, `/funders/import`, etc.). `nav-items.ts` was never meant to be the single index of every reachable page — that role belongs to `GlobalSearch`'s separate `feature-index.ts` (per `SESSION_STATE.md`'s 2026-08-14 entry, not re-verified this session). Kept in the register so this list isn't re-investigated cold in a later phase. | `test-evidence/pt-00/route-manifest.json` (`orphanRoutes[]`, 78 entries) | `node scripts/audit/verify-pt00-003.mjs` (prints `orphanRoutes=78`); cross-check any specific path against `src/components/layout/Header.tsx`'s `TABS` array, `SettingsNav`, or a list-page `<Link>`. | CONFIRMED-OK |
| WGR-011 | Frontend/Nav | P3 | Route manifest's `deadNav` cross-reference (nav-items.ts entries pointing at a route that doesn't exist) reports 0 — investigated, found NOT to be a gap. Logged for traceability. | `test-evidence/pt-00/route-manifest.json` (`deadNav: []`) | `node scripts/audit/verify-pt00-003.mjs` (prints `deadNav=0`) | CONFIRMED-OK |
| WGR-014 | Frontend/Page | P3 | WGR-004's documented `/documents` 30000ms navigation timeout did NOT reproduce in this session's render pass, on either of two independent attempts (an initial dry-run smoke check, and the full 146-route sweep). Rendered cleanly in ~3.7-5.6s both times, real content, no error. Per this audit program's own established convention, a non-reproduction is itself a finding, not silent confirmation the bug is fixed -- root cause was never investigated (out of scope both times this session and in PT-00), so this could be genuine intermittent flakiness or coincidental fast responses both times. Do not treat WGR-004 as resolved on the strength of this entry alone. | `test-evidence/pt-01/render-results.json` (`results[]` where `path === "/documents"`, `httpStatus: 200`, `durationMs: 5572`, `hasRealContent: true`) | Authenticated navigation to `/documents`, repeated across separate sessions/times of day, to establish whether the original PT-00 hang is reliably reproducible, intermittent, or resolved. | UNVERIFIED |
| WGR-015 | Frontend/Nav | P3 | WGR-011's static `deadNav: []` cross-reference (nav-items.ts hrefs vs. the build's route manifest) is now confirmed with LIVE, click-based evidence, not just a static string match: every one of 72 real nav elements across 5 surfaces -- sidebar (`NAV_ITEMS` incl. all 19 children, `DONOR_DISCOVERY_NAV_ITEMS`, `RESOURCES_NAV_ITEMS`, `SETTINGS_NAV_ITEM` -- 42 total), admin/`PLATFORM_NAV_ITEMS` (9), the header tab bar/`TABS` (6), the header avatar/org menu/`MENU_LINKS` (5, a bonus surface beyond the four the task named -- same `Header.tsx` file, same method, cheap extra coverage), and the Settings sub-nav (10, incl. both `ownerOnly` entries, reachable since the authenticated test account -- `info@faithfoundationsf.org` -- is confirmed `profiles.role = "owner"`) -- was located in the real rendered DOM, actually clicked, and resolved to a real rendering page (`resolved_status: "renders"`, zero 404s, zero error boundaries, zero blank renders, zero elements missing from the DOM). No contradiction of WGR-011's static claim found. | `test-evidence/pt-01/nav-resolution.json` (`results[]`, 72 rows, `crossCheckPt00DeadNavClaim.contradictsStaticFinding: false`), `test-evidence/pt-01/nav-resolution-run.log` | `node scripts/audit/pt01-003-nav-resolution.mjs` (requires an authenticated-reachable local dev server on `localhost:3000` or `PT01_BASE_URL`); `node scripts/audit/verify-pt01-003.mjs` to check the evidence file's own shape. | CONFIRMED-OK |
| WGR-016 | Frontend/Page, Audit-Tooling | P3 | PT-01-004's element-wiring classifier initially flagged `/nonprofits`'s "Search" button as CONFIRMED-BROKEN (`form_no_submit_handler`, P0) because it sits inside a `<form>` with no React `onSubmit` prop and no `action` attribute. Investigated against the real source and found NOT a gap: `src/app/(dashboard)/nonprofits/page.tsx` is a plain server component rendering `<form method="GET">` with zero client JS -- clicking Search performs a genuine, correct native browser GET navigation to `/nonprofits?search=...&state=...`, which the server component reads via `searchParams`. This was a false positive in the classifier's own heuristic (it assumed every `<button type="submit">` needs an explicit JS-visible handler to be "wired," which is false for a plain native form -- the browser's default submit-to-current-URL behavior is itself a real, working action). Fixed the same session: `pt01-004-element-wiring.mjs`'s `classifyButtonRow()` now treats `type=submit` inside any `<form>` (with or without a JS handler) as CONFIRMED-OK (`form_native_submit_no_js_handler`), reserving CONFIRMED-BROKEN for `type=submit` with no ancestor `<form>` at all (a case that remains a real, distinct check, unaffected by this fix). Re-crawled `/nonprofits` after the fix and confirmed the corrected classification. | `test-evidence/pt-01/element-graph.json` (`elements[]` row `page === "/nonprofits" && label === "Search"`, `resolved_status: "form_native_submit_no_js_handler"`, `verdict: "CONFIRMED-OK"`), `scripts/audit/pt01-004-element-wiring.mjs`'s `classifyButtonRow()` (fix + inline comment naming this exact case) | `node scripts/audit/pt01-004-element-wiring.mjs` against `/nonprofits`; read `src/app/(dashboard)/nonprofits/page.tsx` lines 171-227 (no `"use client"`, no `onSubmit`, `<form method="GET">`) to confirm the native-submit behavior directly. | CONFIRMED-OK |
| WGR-018 | Frontend/Page | P3 | Prior request described "Integrations Configure" as reported broken (404) with no matching commit in git history (STATE_OF_THE_BUILD.md 2026-08-18 flagged this claim as unverifiable). Re-verified from scratch this session, live, against a real authenticated Playwright session: the only real "Configure" affordance on /settings/integrations is the State Grant Portals connector card (src/app/(dashboard)/settings/integrations/page.tsx, configurePath="/settings/state-portals" -- confirmed by source read that Grants.gov/ProPublica/SAM.gov cards pass no configurePath at all). Clicked it live -- navigated to /settings/state-portals, rendered real content (Texas TX portal card, "Preview" action, cross-link back to Settings -> Integrations), no error boundary, no 404. A first script attempt initially misreported this as CONFIRMED-BROKEN due to a race condition in the test script itself (querying the DOM immediately after page.goto() before this app's client-side hydration/async data-loading settled) -- root-caused via a standalone debug script and fixed by adding an explicit settle wait before every element query, then re-run to a clean pass. Not a 404, confirmed working today. | test-evidence/pt-01/claimed-fixes-reverify.json (items[] item="Integrations Configure"), test-evidence/pt-01/claimed-fixes/a_integrations_configure.png | node scripts/audit/pt01-005-claimed-fixes-reverify.mjs (requires an authenticated-reachable local dev server on localhost:3000 or PT01_BASE_URL); node scripts/audit/verify-pt01-005.mjs to check the evidence file's own shape. | CONFIRMED-OK |
| WGR-019 | Frontend/Page, API | P3 | Prior request described Grants.gov "Run Now" as reported broken with no matching commit. Re-verified from scratch, live: clicked the real Run Now button on the Grants.gov connector card, capturing the real network response to POST /api/agents/grants-gov (writer-role gated, src/app/api/agents/grants-gov/route.ts) rather than just checking the button exists. Real response: HTTP 200, a genuine JSON body listing real, live grants.gov opportunities (e.g. "GPD Grant Forecast" / VA-GPD-FORECAST-FY2026-FY2028, "Family Unification Program (FUP) Multi-Year NOFO"), not a fabricated/mock payload. Post-click UI state independently confirms the same real result: the card's "Last sync" updated to "3 minutes ago" and the button changed to a "Queued" state with a "View Opportunities ->" link. Genuinely working, not broken. | test-evidence/pt-01/claimed-fixes-reverify.json (items[] item='Grants.gov "Run Now"', detail.responseInfo), test-evidence/pt-01/claimed-fixes/b_grants_gov_run_now.png | node scripts/audit/pt01-005-claimed-fixes-reverify.mjs (real POST triggers a live grants.gov search agent run -- not idempotent-free, but this is the button's own designed, sanctioned action, same as every other live agent-trigger session in this project's history); node scripts/audit/verify-pt01-005.mjs to check the evidence file's own shape. | CONFIRMED-OK |
| WGR-020 | Frontend/Nav | P3 | Prior request described the "Scraping Targets" settings sub-nav link as reported broken with no matching commit. Re-verified from scratch, live: clicked the real "Scraping Targets" tab (href="/settings/scraping") -- navigated correctly, rendered real content (an "Allowed Domains" panel showing "1 domain allowlisted", one real scraping target row for a simpler.grants.gov URL with Active/weekly badges and Run Now/Pause/Delete actions), no error boundary, no blank render, and (after adding an explicit loading-indicator-clear poll to the test script) no leftover "Loading..." spinner state either. Genuinely working today. | test-evidence/pt-01/claimed-fixes-reverify.json (items[] item="Scraping Targets link"), test-evidence/pt-01/claimed-fixes/c_scraping_targets.png | node scripts/audit/pt01-005-claimed-fixes-reverify.mjs; node scripts/audit/verify-pt01-005.mjs | CONFIRMED-OK |
| WGR-021 | Frontend/Page, Storage | P3 | Prior request described the Branding logo upload as reported fixed, but per STATE_OF_THE_BUILD.md this was never confirmed live, only source-read. Re-verified from scratch, live, without uploading anything to production per this task's explicit instruction: navigated to /settings/branding, confirmed a real <input type="file" accept="image/png,image/jpeg,image/webp,image/svg+xml"> is present and rendered (visible "Choose file" control, an existing logo preview already showing), source-confirmed it uploads via supabase.storage.from("org-branding").upload(...) (src/app/(dashboard)/settings/branding/page.tsx), and independently ran a live, read-only storage.listBuckets() call (service-role client, no write) confirming the real target bucket "org-branding" genuinely exists in production (public=true). Both the control's presence and its real storage target are confirmed live, not just source-read. | test-evidence/pt-01/claimed-fixes-reverify.json (items[] item="Branding logo upload", detail.bucketCheck), test-evidence/pt-01/claimed-fixes/d_branding_logo_upload.png | node scripts/audit/pt01-005-claimed-fixes-reverify.mjs (read-only: DOM presence check + storage.listBuckets(), never a file POST); node scripts/audit/verify-pt01-005.mjs | CONFIRMED-OK |
| WGR-022 | Frontend/Nav | P3 | Prior request described the Billing settings sub-nav link as reported fixed, but per STATE_OF_THE_BUILD.md this was never confirmed live, only source-read (src/app/(dashboard)/settings/billing/page.tsx re-exports the real (dashboard)/billing/page.tsx implementation, not a redirect). Re-verified from scratch, live: clicked the real "Billing" tab -- navigated to /settings/billing and rendered real, detailed content (Enterprise plan card, $499/month, real usage metrics -- 25/5,000 daily agent runs, 3 AutoApply submissions this month, 0.634 MB / 50,000 MB storage -- and an honest, non-crashing "Stripe is not configured on this server" informational banner rather than an error). No error boundary, no blank render, no leftover loading spinner after an explicit clear-wait was added to the test script. Resolves correctly today. | test-evidence/pt-01/claimed-fixes-reverify.json (items[] item="Billing nav"), test-evidence/pt-01/claimed-fixes/e_billing_nav.png | node scripts/audit/pt01-005-claimed-fixes-reverify.mjs; node scripts/audit/verify-pt01-005.mjs | CONFIRMED-OK |
| WGR-024 | API/AuthZ | P3 | PT-02-003 role-tier enforcement matrix: every admin- and owner-gated API route (49 routes, 76 route+method entries after per-method requireRole() tier extraction, including two entries whose required tier is chosen at runtime rather than a literal string -- /api/autoapply/controls POST/DELETE) was called with real, authenticated sessions at all 4 real role tiers (viewer, writer, admin, owner -- this app has no 'member' tier; writer is the middle tier) against a throwaway local Supabase stack + org + users this audit provisioned itself (never production). 304/304 calls (76 entries x 4 roles) resolved to the expected refuse/permit outcome: every below-tier call got the specific requireRole() 403 {code:'forbidden'} refusal (192/192), and every at-or-above-tier call cleared the role gate (112/112, verified by the absence of the forbidden code -- several then hit real, expected downstream errors from the audit's intentionally minimal local schema, e.g. missing tables, which is a correct 'permitted, failed for an unrelated reason' result, not miscounted as a refusal). Zero under-enforcement findings, zero over-restriction findings. Logged for traceability so this isn't re-asked from scratch. | test-evidence/pt-02/role-matrix.json | Start a local `supabase start` stack + a second `next dev` instance pointed at it via env vars (see scripts/audit/pt02-003-role-matrix.mjs header), then: LOCAL_SUPABASE_URL=... LOCAL_SUPABASE_ANON_KEY=... LOCAL_SUPABASE_SERVICE_ROLE_KEY=... PT02_BASE_URL=http://localhost:3099 node scripts/audit/pt02-003-role-matrix.mjs; or: node scripts/audit/verify-pt02-003.mjs (checks coverage/shape only, does not re-run the sweep) | CONFIRMED-OK |
| WGR-028 | Data/Infra | P3 | PT-02-005 pagination audit: live-verified this Supabase project's PostgREST `db.max_rows` setting silently caps ANY request at 1000 rows -- `GET /rest/v1/nonprofits?select=id&limit=5000` (service-role key, no Range header) returned `HTTP 206 Partial Content`, `Content-Range: 0-999/1978526`, exactly 1000 rows -- regardless of an app-level `.limit()` requesting more, or no `.limit()`/`.range()` at all. No error is raised; a caller that never inspects `count`/`Content-Range` and trusts `data.length` will silently treat a 1000-row slice as complete. This is the mechanism behind every SILENT_TRUNCATION finding below (WGR-029 through WGR-032). Also confirms, positively, that 4 real large-table list endpoints DO paginate correctly and are not affected: `/foundations` (foundation_directory, 133,812 rows, 50/page via `.range()`), `/nonprofits` (nonprofits, 1,978,526 rows, 50/page via `.range()`), `GET /api/intelligence/corporate-prospects` (real page/pageSize/`.range()`, `MAX_PAGE_SIZE=100`, returns total+hasMore), `GET /api/donor-discovery/prospects` main listing path (real page/limit/`.range()`, `MAX_LIMIT=100`, returns total) -- the last of these has two SEPARATE unbounded internal filter-building queries, see WGR-032. `GET /api/intelligence/outreach/prospects` (corporate_prospects) is BOUNDED_NO_TOTAL: hard-capped at `MAX_LIMIT=200` (below the 1000 default, so not this bug) but has no offset/page param and returns no total -- flagged informational, not a truncation finding, since it never claims completeness. Logged so this baseline isn't re-derived from scratch. | test-evidence/pt-02/pagination-and-500s.json (`postgrestDefaultRowCap`, `paginationAudit.endpoints[]` where `verdict === "PAGINATES_CORRECTLY"` or `"BOUNDED_NO_TOTAL"`) | `GET {supabaseUrl}/rest/v1/nonprofits?select=id&limit=5000` with the service-role key, no Range header -- observe `HTTP 206`, `Content-Range: 0-999/1978526`. Or: `node scripts/audit/verify-pt02-005.mjs`. | CONFIRMED-OK |
| WGR-034 | Worker/Boot | P3 | PT-08 worker boot inventory: src/worker/jobs/process-discovery-request.ts exports a real job-handler function (handleProcessDiscoveryRequestJob) and a job-type constant (PROCESS_DISCOVERY_REQUEST_JOB_TYPE = "process_discovery_request") that are referenced nowhere else in the entire repo -- a repo-wide grep for both the constant name and the literal job-type string finds exactly 1 file: the module's own definition. No producer ever enqueues a "process_discovery_request" job and no consumer ever claims one. Distinguished deliberately from WGR-033: this file's OWN header comment states plainly that the real, running consumer of donor_discovery_requests is worker/dd-request-processor.ts's poll loop (confirmed STARTED at boot, per boot-inventory.json), and that this module only "exposes that same pipeline under the src/worker/jobs/* job-payload shape used by the sibling enrich_donor_prospect / score_donor_prospect jobs, without duplicating its logic." So this is genuine orphaned/dead code, but it does NOT back a missing feature -- the capability it would provide is already fully covered by an already-started processor. Filed at P3 (track, not urgent) rather than P1, on that basis. | test-evidence/pt-08/boot-inventory.json (processors[] where module === "src/worker/jobs/process-discovery-request.ts") | grep -rn "process_discovery_request\\|PROCESS_DISCOVERY_REQUEST_JOB_TYPE" --include=*.ts . -- 1 file matches (the module's own definition). Or: node scripts/audit/verify-pt08-001.mjs (lists this under DEFINED-NOT-STARTED). | CONFIRMED-BROKEN |
| WGR-039 | API/Scheduling | P3 | PT-08-002 cron reconciliation: `/api/cron/draft-queue-check` is an orphaned duplicate entry point, same class of finding as WGR-034 (`process-discovery-request.ts`) -- not a functionality gap. Its entire body is one call to `DraftQueueEngine.processNewOpportunities(organizationId)` per org, which is the exact method the registered `/api/cron/research` cron already fire-and-forgets after every successful agent run (`src/app/api/cron/research/route.ts:361-368`). The standalone route is registered nowhere (absent from `vercel.json`, never called from `worker/scheduler.ts`), but the capability it exists to provide already runs automatically via a different, registered path. Logged so a future session doesn't re-flag this as P1 without checking, and as a candidate for deletion or explicit retirement documentation (matching how `/api/cron/campaigns` was handled) rather than being silently forgotten. | test-evidence/pt-08/cron-reconciliation.json (cronApiRouteHandlers.entries[] where path === "/api/cron/draft-queue-check"; findings[] id WGR-039) | `grep -n "processNewOpportunities" src/app/api/cron/research/route.ts src/app/api/cron/draft-queue-check/route.ts` (both call the same method); `node scripts/audit/verify-pt08-002.mjs` | CONFIRMED-OK |
| WGR-062 | Cross-cutting | P3 | PT-06-003 index: the full code-vs-live-schema cross-reference audit (`test-evidence/pt-06/schema-mismatch.json`) found 21 missing tables and 50 column-name mismatches referenced by real (non-test) code. WGR-005 (re-confirmed via this independent method), WGR-047, WGR-049, WGR-053 (already registered via PT-06-002's migration-drift method, cross-corroborated here) and WGR-054 through WGR-061 (newly registered above) account for a verified subset. The remainder has NOT been individually read-verified against source this session -- missing tables: `ab_test_variants`, `compliance_requirements`, `digest_item_log`, `digest_priority_weights`, `funding_sources`, `knowledge_base_entries`, `org_portal_accounts`, `session_recordings`, `stripe_webhook_events` (plus already-known-elsewhere `application_followups`/`consultant_client_access`/`deadline_predictions`/`followup_sequences`/`funder_relationships`/`notification_preferences`/`queue_controls`/`schoolfunder_donations`/`schoolfunder_students`/`schoolfunder_volunteer_hours`/`submission_usage`/`tier_limits`); column mismatches: `agent_performance_metrics.runs_failed`, `autoapply_submissions.response_received_at`/`.variant_id`, `donor_discovery_prospects.scored_at`/`.enrichment_private`, `email_templates.body`/`.subject`, `foundation_profiles.grant_history`, `funder_giving_history.grant_purpose`, `improvement_proposals.affected_agent_id`, `opportunities.probability_score`, `opportunity_probability_scores.org_id`, `org_learning_contributions.anonymized`/`.outcome_id`/`.source_hash`, `organizational_digital_twins.twin_auto_populate_log`, `organizations.analytics`, `pitch_cache.request_type`, `platform_learning_patterns.confidence`/`.weight`, `scraping_targets.updated_at`, `success_probability_scores.data_quality`/`.updated_at`, `webhook_configs.is_active`/`.type`. Flagged here so a future session picks these up from the raw evidence file rather than losing them, instead of registering 50+ rows without individually confirming each against its source file. | `test-evidence/pt-06/schema-mismatch.json` (full `runtime_query_code.missing_tables[]`/`.column_mismatches[]`) | `node scripts/audit/pt06-crossref.mjs` regenerates the evidence file; `node scripts/audit/verify-pt06-003.mjs` confirms it's non-empty and the org_id/organization_id case is present; read each remaining finding's `sites[].file`/`.line` and cross-check against `test-evidence/pt-06/live-schema.json` to individually triage. | UNVERIFIED |
| WGR-063 | Data/Infra | P3 | Foreign key orphan check: PT-06-004 checked all 267 live FK constraints in the public schema for orphaned child rows (a child value with no matching parent row -- the classic case a broken/dropped-then-recreated FK leaves behind). Investigated, found NOT a gap: 0 of 267 constraints have any orphaned row. The same PT-06-004 pass also found 0 tables without a primary key. Logged for traceability so this isn't re-checked from scratch. | `test-evidence/pt-06/integrity.json` (`checks.foreign_keys`, `summary.fk_orphan_findings=0`, `summary.tables_without_pk=0`) | `node scripts/audit/pt06-integrity.mjs` regenerates the evidence file (or a direct re-query of every FK constraint's orphan count via `DATABASE_URL`) | CONFIRMED-OK |
| WGR-070 | Infra/Tooling | P3 | PT-06's own read-only DB access method (direct `DATABASE_URL`/psql per DIRECTIVE-017, used by every check in this phase) was verified safe before any live query ran: connected, set `default_transaction_read_only=on`, confirmed with `SHOW`, ran a real `SELECT 1`, then attempted a real `CREATE TABLE` inside the same session and confirmed Postgres itself rejected it (`25006 read_only_sql_transaction`) -- proving the read-only property is enforced by the database engine, not merely by this audit's own application-level discipline. Not a gap -- logged for traceability so PT-06's evidence-collection method isn't re-verified from scratch by a future session. | `test-evidence/pt-06/connection-proof.txt` | Connect to `DATABASE_URL`, `SET default_transaction_read_only = on`, then attempt any DDL/write statement in the same session and confirm Postgres returns SQLSTATE `25006`. | CONFIRMED-OK |
| WGR-071 | Auth/RLS | P3 | PT-05-002 cross-tenant read audit: authenticated as Org A's real user, attempted to read Org B's known seeded rows across all 120 tenant-scoped tables PT-06 identified (organization_id/org_id column present). 20 tables (the 7 tables PT-05-001 seeded plus all 13 of PT-06's tenant_fk_gap 'missing tenant FK' prime suspects -- integrity.json, check=tenant_fk_gap) were live HTTP-tested with the real production RLS policy predicate reproduced verbatim on a local isolated stack: a real GoTrue JWT for Org A, real requests via both @supabase/supabase-js (API layer) and a raw PostgREST fetch (direct), against Org B's real seeded row id. Result: 0/20 leaked -- every cross-tenant attempt returned 0 rows (HTTP 200, empty result set) on both request paths, while a same-org positive-control read on the identical table correctly returned 1 row, confirming the block is real tenant isolation and not a broken/globally-denying policy. The remaining 100 tables were verified via a read-only inspection of the REAL, live production RLS policy state (pg_class.relrowsecurity + pg_policies qual text, fetched read-only) rather than a live per-row HTTP request: 96/100 carry a correctly org-scoped SELECT/ALL policy referencing the tenant column against a current-org lookup; the other 4 are covered separately in WGR-072. Zero P0 cross-tenant read leaks found across all 120 tables, including all 13 prime suspects flagged for extra scrutiny -- the missing tenant->organizations foreign key PT-06 found on those 13 tables does not correspond to a missing or broken RLS policy; RLS enforcement and referential-integrity enforcement are separate mechanisms here and only the latter is absent. | test-evidence/pt-05/cross-read.json (full per-table results); test-evidence/pt-05/production-rls-policies.json (read-only production RLS ground truth); test-evidence/pt-05/cross-tenant-seed-ids.json (seeded row ids used) | node scripts/audit/pt05-002-fetch-production-rls.mjs && node scripts/audit/pt05-002-provision-and-seed.mjs && node scripts/audit/pt05-002-cross-tenant-read.mjs && node scripts/audit/verify-pt05-002.mjs | CONFIRMED-OK |
| WGR-072 | Auth/RLS | P3 | 4 of the 120 tenant-scoped tables PT-06 identified (ai_usage_log, enrichment_jobs, kb_extended_needs, system_errors) have RLS enabled in production but carry ZERO policies of any command type. This is not a cross-tenant read leak (a policy-less RLS-enabled table denies SELECT to every authenticated/anon role unconditionally, including the row's own owning org -- confirmed via pg_policies returning an empty set for all four, read-only) but it is a genuine, unexplained availability question: either these four tables are intentionally service-role-only (never meant to be read via the authenticated/anon PostgREST path at all, in which case this is expected and not a bug), or a policy that should exist was never written, silently breaking whatever UI/feature reads them via a normal user session. Not investigated further in this pass -- application code was not cross-referenced against these four table names to determine which explanation applies, and no seed data/live HTTP test was run against them (out of PT-05-002's cross-tenant-read scope, which is about Org A vs Org B, not about whether a table is readable at all). | test-evidence/pt-05/production-rls-policies.json (tables.ai_usage_log/enrichment_jobs/kb_extended_needs/system_errors, policies: []); test-evidence/pt-05/cross-read.json (verdict=RLS_ENABLED_NO_SELECT_POLICY_DENY_ALL on all four) | node scripts/audit/pt05-002-fetch-production-rls.mjs, then inspect tables.ai_usage_log.policies / .enrichment_jobs.policies / .kb_extended_needs.policies / .system_errors.policies in the output (all empty arrays) | PENDING-SCOPE |
| WGR-073 | Auth/RLS | P3 | PT-05-003 cross-tenant WRITE audit -- the more dangerous direction than WGR-071's read test: authenticated as Org A's real user, attempted UPDATE, DELETE, and INSERT-tagged-with-Org-B's-org_id against Org B's data across all 120 tenant-scoped tables PT-06 identified. The same 20 tables WGR-071 live-tested (7 PT-05-001 tables + all 13 tenant_fk_gap prime suspects) got a real, live three-way mutation attempt on the local isolated stack with the real production RLS policy predicate reproduced verbatim: a real GoTrue JWT for Org A, real UPDATE/DELETE/INSERT requests via both @supabase/supabase-js (API layer) and a raw PostgREST fetch (direct), against Org B's real seeded row (or, for INSERT, a fabricated row explicitly tagged organization_id/org_id = Org B's real org id, using Org B's real child-object ids where a foreign key was required -- the worst-case "attacker already knows Org B's internal ids" scenario). After every single attempt, Org B's data was independently re-read AS ORG B (a second, separately-authenticated real session) and compared to a captured before-snapshot -- the attacking request's own reported success/failure was never trusted alone. Result: 0/20 P0s across all three operation types (60 live mutation attempts total) -- every UPDATE/DELETE returned 0 rows affected with Org B's re-read data provably unchanged, and every INSERT attempt produced zero new rows visible in Org B's own re-read id list, for all 20 tables including all 13 prime suspects. A same-table positive control (Org A successfully UPDATEing its own row) confirmed the RLS write path is genuinely live, not globally broken, for 17 of 20 tables -- the other 3 (adapter_usage_log, knowledge_queries, submission_receipts) have no UPDATE policy of any kind in production (not even for the owning org), so their cross-tenant UPDATE block is a default-deny-for-everyone artifact rather than a specifically tenant-scoped one; still a correct block, just for a blunter reason, noted rather than silently folded into the same PASS category. The remaining 100 tables were verified via the same read-only production RLS policy inspection WGR-071 used, now checked per-command (INSERT/UPDATE/DELETE separately, not just SELECT/ALL): every table's RLS is enabled, and for every command with at least one applicable policy, the qual/with_check text references the table's own tenant column against a current-org lookup; a handful of tables have no policy at all for a given command (e.g. no DELETE policy), which is a default-deny for that operation platform-wide, not a leak. Zero P0 cross-tenant writes found anywhere. This check was specifically hunting for the worst class of finding in the program (one tenant corrupting or forging another's data) and came back clean across all three operation types and all 120 tables -- graded P3 (same tier as WGR-071's analogous clean read-side result) since a confirmed-clean outcome, not an open gap, is what's being logged here. | test-evidence/pt-05/cross-write.json (full per-table results, all three attempt types, before/after re-reads); test-evidence/pt-05/production-rls-policies.json (read-only production RLS ground truth, reused from WGR-071); test-evidence/pt-05/cross-tenant-seed-ids.json (seeded row ids used as UPDATE/DELETE targets) | node scripts/audit/pt05-003-cross-tenant-write.mjs && node scripts/audit/verify-pt05-003.mjs | CONFIRMED-OK |
| WGR-076 | Auth/RLS | P3 | Demo-account write-protection (migration 138_demo_account_scope.sql, DEMO_ACCOUNT_SCOPE_2026-08-15.md) re-verified live and holds. Beyond PT-06's own column/table-existence-only methodology, this pass directly confirmed via read-only production query that all 3 real functions (is_onboarding_edit_restricted, block_if_onboarding_edit_restricted, block_restricted_organizations_update) and all 6 real triggers (block_restricted_write on knowledge_base/board_members/programs/organizational_digital_twins/documents, block_restricted_organizations_update on organizations) exist live and are wired to the correct functions (joined tgfoid->pg_proc, not just matching trigger names). A live behavioral test against a local reproduction of the real trigger logic (real GoTrue-authenticated sessions, real @supabase/supabase-js writes, real production organizations RLS policy applied for realism) confirmed: a restricted demo profile is blocked (42501) on a protected organizations column, a whole-table-blocked table INSERT, UPDATE, and DELETE; the SAME restricted profile is correctly ALLOWED to update a real branding column (logo_url) NOT on migration 138's protected list, per spec §2.1's carve-out; an unrestricted negative-control profile succeeds on the identical writes; and an independent service-role re-read confirmed every blocked attempt genuinely mutated nothing. Migration 138 is also confirmed present in PT-06's appliedAndOnDisk bucket, not its onDiskNotApplied bucket. | test-evidence/pt-05/privileged-access.json (demo_write_protection), test-evidence/pt-05/pt05-004-production-investigation.json | node scripts/audit/pt05-004-investigate.mjs (read-only production function/trigger check); node scripts/audit/pt05-004-privileged-access.mjs (local-stack behavioral write attempts, 8/8 pass); node scripts/audit/verify-pt05-004.mjs (gate). | CONFIRMED-OK |
| WGR-094 | Autonomous Agents / Governance Docs | P3 | PT-09-004 consolidation: registry/governance metadata drift found across both PT-09-001 (static inventory) and PT-09-002/003 (execution proof), in both directions. (1) 4 agents whose live agent_registry.trigger_type says 'manual' but are actually real, live, scheduled worker/scheduler.ts jobs: AG-25 Disaster Response, AG-29 Fundability Scorer, AG-30 Donor Intent Monitor, AG-35 Community Need Predictor -- the registry understates their real automation (benign direction: they run MORE often than documented, not less). (2) AG-19 (Relationship Builder Agent) is the opposite and more consequential case: its registry trigger_type says 'scheduled', but per multiple prior STATE_OF_THE_BUILD.md sessions and re-confirmed this phase via worker/autonomous-orchestrator.ts's own header comment ('requested RelationshipBuilderAgent -> FunderRelationshipAgent (queue-only, see below)'), it never actually auto-fires -- the real path is a manual UI trigger plus an opt-in, default-OFF platform_config flag (feature.relationship_builder_v2), whose current on/off state for any real org was not checked this phase. (3) FEATURE_REGISTRY_v2.md row #199's claim about AG-18 ('ReputationIntelligenceAgent... never imported or instantiated anywhere outside its own file') is now stale and contradicted by this phase's own direct read of worker/autonomous-orchestrator.ts, which DOES instantiate and call ReputationIntelligenceAgent for funders (~lines 1852-1871) -- a real governance-doc staleness finding, independent of AG-18's own WIRED-NO-OUTPUT verdict (WGR-080). None of these 6 mismatches changes any agent's own functional verdict above -- this row exists so a future session correcting registry/governance-doc text has the full list in one place instead of re-deriving it. | test-evidence/pt-09/agent-inventory.json (summary.registryMetadataStaleCount=4, and canonicalAgents entries for AG-25/AG-29-Fundability/AG-30/AG-35 whose triggerWiredVerdict text includes "registry metadata stale"); test-evidence/pt-09/execution-batch1.json (falsePassCasualties[] where canonicalNumber === "AG-18", registryPriorStatus field) | `node scripts/audit/verify-pt09-001.mjs`; grep `"registry metadata stale"` in test-evidence/pt-09/agent-inventory.json for the 4 trigger_type mismatches; `grep -n "requested RelationshipBuilderAgent" worker/autonomous-orchestrator.ts` for AG-19; `grep -n "ReputationIntelligenceAgent" worker/autonomous-orchestrator.ts` for AG-18 (confirms real instantiation, contradicting FEATURE_REGISTRY_v2.md row #199's stale claim). | CONFIRMED-BROKEN |
| WGR-114 | Cross-cutting | P3 | PT-14's clean results, recorded together rather than as separate rows per the register's own convention of registering a verified-clean outcome (see WGR-071/073/076): (1) CSRF -- 0 of 21 live attempts against production were NOT_BLOCKED (8 no-cookie requireRole-gated routes all 307/401/403; the header-spoof-onboarding attempt confirmed middleware.ts's Headers.set() genuinely overwrites a client-supplied x-organization-id/x-user-id before the route sees it, not merely appends). (2) SQLi -- the two comparison endpoints (corporate-prospects `q`, intelligence/library/search `query`) both correctly neutralize the identical comma-injection payload class that breaks /api/email/threads (WGR-112). (3) XSS -- zero dangerouslySetInnerHTML sink with user-controlled content found repo-wide (the one real hit, HowItWorksClient.tsx, injects only a hardcoded developer-authored CSS string interpolating a fixed brand-token hex constant, confirmed by direct read); a live <script>/onerror payload round-tripped raw and unescaped through both knowledge_base.content and contacts.name (correct -- escaping belongs at render time, not write time) with no render sink found; the one real raw-HTML-building sink in the codebase (src/app/api/unsubscribe/route.ts's escapeHtml()) was executed directly against a live <script>/quote/ampersand payload and correctly neutralized every HTML-meaningful character. (4) SSRF -- the platform's own safeFetch() (src/lib/security/safe-fetch.ts) correctly blocked the same real local loopback listener the two vulnerable sinks (WGR-108/109) successfully reached, confirming the contrast is real (the loopback address itself is reachable in this environment; only the guarded path refuses it). | test-evidence/pt-14/injection.json (full attempts[] array -- csrf-nocookie-*, csrf-header-spoof-onboarding, sqli-001/003/004, xss-001/002/003/005, ssrf-000) | node scripts/audit/pt14-002-live-tests.mjs; node scripts/audit/verify-pt14-001.mjs | CONFIRMED-OK |
| WGR-115 | Data/Security | P3 | MASTER_BACKLOG.md §1.1's 8 named anon-exposed tables (platform_admins, organizational_digital_twins, opportunity_probability_scores, donor_discovery_directory, autoapply_submissions, submission_queue, form_templates, request_profiles) re-checked live, individually, with the real anon key, against production RIGHT NOW -- not re-reading either the 2026-07-30 finding or the later 2026-08-03/08-06 remediation claims. Extended to ALL 184 tables in PT-06's live schema, not just the 8 named ones. RESULT: 0 of 184 tables return real data to an unauthenticated anon-key request. All 8 disputed Tier-1.1 tables resolved CONFIRMED_FIXED_LIVE -- the later remediation claim was correct, MASTER_BACKLOG's July 30 finding is now stale. Verdict breakdown: 77 ANON_BLOCKED_NO_GRANT (base table privilege revoked), 55 ANON_BLOCKED_BY_RLS (grant present, RLS correctly filters to zero rows against a real, verified-nonzero service-role row count), 52 TABLE_EMPTY_INCONCLUSIVE-from-data but each individually resolved via a DB-level grant+RLS+policy fallback check (0 of the 52 flagged as at-risk). | test-evidence/pt-14/rls-anon-audit.json (tables, disputed_items_resolution) | node scripts/audit/pt14-004-anon-rls-storage-audit.mjs; node scripts/audit/verify-pt14-002.mjs | CONFIRMED-OK |
| WGR-116 | Storage/Security | P3 | MASTER_BACKLOG.md §1.2's 5 disputed non-public buckets (session-recordings, org-b1ab7402-..., documents, autoapply-screenshots, org-documents) re-checked live: storage.buckets, pg_policies on storage.objects, and information_schema grants all queried fresh over a read-only DATABASE_URL connection, plus a real anon POST /storage/v1/object/list attempt and (for documents/org-b1ab7402/autoapply-screenshots, which turned out to hold real objects, resolving STORAGE_POLICY_AUDIT.md's own documented '0 objects, can't distinguish locked-down from empty' ambiguity) a real anon GET download attempt against the real object path. RESULT: all 5 are db_policy_verdict=ZERO_POLICY_DEFAULT_DENY (RLS enabled, zero policy applies to public/anon on storage.objects) and anon is genuinely blocked live -- 3 confirmed against real objects (401/400 on download, 0 objects via LIST despite the service-role list finding real content), 2 (session-recordings, org-documents) confirmed safe-by-default-deny via the DB-level check since they hold no objects today to test empirically. nofa-pdfs's read=public/write=unscoped-authenticated pattern (§1.2 #13) is unchanged and was never disputed as a bug, only flagged for a one-line intentionality confirmation from Reid -- still open, not resolved by this audit. | test-evidence/pt-14/rls-anon-audit.json (buckets, disputed_items_resolution) | node scripts/audit/pt14-004-anon-rls-storage-audit.mjs; node scripts/audit/verify-pt14-002.mjs | CONFIRMED-OK |
| WGR-120 | Build/Secrets | P3 | PT-14-003's client-bundle secret scan, recorded as its own row per the register's own convention of registering a verified-clean outcome (see WGR-071/073/076/114) rather than leaving a 0-finding pass undocumented. Scanned every one of 247 real files a fresh `pnpm run build` actually ships to a browser (209 static JS/CSS/JSON chunks, 12 prerendered HTML pages, 26 RSC payloads -- explicitly excluding `.next/server/**/*.js`, which never reaches a client). Three independent passes: (1) known-value -- every non-NEXT_PUBLIC_* secret with a real value in .env.local (SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL, ANTHROPIC_API_KEY, SAM_GOV_API_KEY, OPENAI_API_KEY, GOOGLE_PLACES_API_KEY) searched literally across all 247 files -- zero matches; (2) format-based pattern pass (Stripe secret/restricted keys, PEM private-key blocks, AWS access keys, Resend keys, Postgres connection strings with embedded creds, OpenAI/Anthropic key shapes) -- zero matches; (3) every JWT-shaped token in the bundle (77 found) had its payload decoded and role claim checked -- all 77 are role:"anon" (the real, intentionally-public NEXT_PUBLIC_SUPABASE_ANON_KEY, protected by RLS not secrecy -- corroborated by WGR-115's independent finding that 0/184 tables leak data to this exact key), zero service_role/supabase_admin tokens present anywhere in the shipped bundle. 19 server-only secret names referenced in src/worker but absent from .env.local (CRON_SECRET, STRIPE_SECRET_KEY, RESEND_API_KEY, CREDENTIAL_ENCRYPTION_KEY, etc.) could not be checked by the known-value pass specifically (no local value to search for) -- listed explicitly in bundle-scan.txt rather than silently skipped; the pattern pass still covers several of them by shape. | test-evidence/pt-14/bundle-scan.txt (raw scan output); test-evidence/pt-14/bundle-and-middleware.json (bundleSecretScan, summary.p0Count=0, summary.verdict="PASS") | pnpm run build (fresh, .next deleted first) then node scripts/audit/pt14-003-bundle-secret-scan.mjs; node scripts/audit/verify-pt14-003.mjs (re-derives the scanned-file count from bundle-scan.txt and requires it to match the JSON summary's own claimed count). | CONFIRMED-OK |
| WGR-123 | Middleware/Availability | P3 | PT-10-002 scenario 1 (db_down phase): a hard Supabase outage (connection reset injected via a local fault-injection proxy in front of the real local Supabase stack) was probed against 3 real targets (public /, protected /dashboard with a real authenticated session, and a real API route GET /api/notifications) plus a post-outage recovery check. All 3 degrade cleanly, not a crash: the public path renders unaffected (src/middleware.ts's getUser() call, lines ~117-119, which has zero try/catch anywhere in the function, resolves to a null user via the Supabase client library's own internal handling rather than throwing, so isPublicPath()'s short-circuit still lets the request through); both /dashboard and /api/notifications correctly 307-redirect to /login (same getUser()-returns-null-user path, correctly treated as 'not authenticated' rather than crashing). No raw framework crash, no white-screen/blank body (isBlank:false on every case, looksLikeCrash:false on every db_down case), no partial or malformed HTML in any response body. A recovery probe run immediately after the outage ended confirmed the app was not left in any wedged state -- the same clean 404 response as the pre-outage baseline. One latent risk noted, not itself a live finding: this app has zero React error boundaries anywhere (no app/error.tsx, app/global-error.tsx, or any (dashboard)/error.tsx -- confirmed by a repo-wide search before this scenario ran). The clean degrade observed here works because supabase-js's own client-side handling in getUser() happens to swallow a connection failure into a null-user result rather than throwing; a different failure mode that DOES throw past a Server Component render boundary would have nothing configured anywhere in this app to catch it. | test-evidence/pt-10/outage-simulation.json#scenarios.scenario1_db_outage.cases[] (phase=="db_down"), and .recovery | node --import tsx scripts/audit/pt10-002-outage-simulation.mjs (scenario 1 reproduces the db_down phase deterministically via the fault proxy's connection-reset mode); node scripts/audit/verify-pt10-002.mjs. | CONFIRMED-OK |
| WGR-128 | Integration/Parser | P3 | PT-10-002 scenario 3: 7 of 9 real parser/integration fuzz cases handled genuinely malformed input without an uncaught exception, recorded together per the register's own established convention of logging a clean/mitigated result set as its own row (see WGR-071/073/076/114/120) rather than leaving it undocumented. propublica-990-client.fetchProPublicaFinancials (2 cases) returned cleanly via its own internal try/catch (hasTryCatchAtImmediateCallSite:true -- protection lives inside the function itself, not at the caller). irs990.ts IRS990Source.parseXml (4 cases: empty string, truncated mid-tag, binary garbage, a non-XML HTML error page) is a pure synchronous function with no I/O and returned without throwing on all 4 -- correctly degrades to partial/null fields rather than crashing the batch that calls it. ca-grants-portal-client.fetchCaGrantsPortalFeed (1 case) also returned without throwing, though its own real immediate caller (src/lib/sources/state-portals/ca-grants-portal-sync.ts line ~149) has no try/catch either -- the only real protection found is one further hop up, in scripts/ingest-ca-grants-portal.ts's main(), which wraps the equivalent call in try/catch and a clean fatal()/ process.exit(1) helper. Not a full pass for that one library function -- it remains genuinely unprotected for any other/future caller -- but this specific case did not throw against this specific malformed payload. | test-evidence/pt-10/outage-simulation.json#scenarios.scenario3_malformed_third_party_responses.behavior[] (outcome=="returned_without_throwing", 7 of 9 cases) | node --import tsx scripts/audit/pt10-002-outage-simulation.mjs (scenario 3, cases PT10-002-S3-003 through PT10-002-S3-006); node scripts/audit/verify-pt10-002.mjs. | CONFIRMED-OK |
| WGR-134 | Frontend/AutoApply | P3 | PT03-004-F01 (test-evidence/pt-03/autoapply-donor-journeys.json, findings[0]): src/app/(dashboard)/autoapply/page.tsx's 'QUEUE' mini-panel (~line 634) shows 'Queue is empty.' on any loadQueue() failure (queue.length===0), not just a genuine empty queue -- it never checks queueError, unlike the Session List table ~200 lines below on the same page, which does check and surface it. Reproduced live this session via a real PGRST200 error ('Could not find a relationship between submission_queue and funders') from a genuine local-schema gap (submission_queue had no FK to funders on this stack, unlike the real migration), with a real authenticated session token -- the panel rendered the same 'Queue is empty.' text it would show for a genuinely empty queue, giving no indication anything had gone wrong. Rated P3 (not the higher severity of the kanban-enforcement findings above) since this is a cosmetic error-surfacing gap on a single dashboard widget, not a data-integrity or access-control bypass -- the underlying queue data itself is not lost or corrupted, and the fuller Session List table on the same page does surface the equivalent failure correctly. | test-evidence/pt-03/autoapply-donor-journeys.json#findings[0] (PT03-004-F01) | code read: src/app/(dashboard)/autoapply/page.tsx lines 632-635 (QUEUE panel, checks only queue.length===0) vs lines 835-836 (Session List table, checks queueError explicitly); or live: force loadQueue()'s submission_queue-to-funders embedded-select query to fail (e.g. temporarily drop the FK on a non-production stack) and observe the QUEUE panel renders 'Queue is empty.' with no error indication while the Session List table below it does show an error state. | CONFIRMED-BROKEN |
| WGR-136 | Business Logic | P3 | PT-04-003 pure-function scoring verification: computeConfidence() (src/lib/drafts/generator.ts, draft-narrative confidence score), SubmissionValidator.checkOrgReadiness() (src/lib/autoapply/submission-validator.ts, AutoApply eligibility gate), and computeGrantProbability() (src/lib/intelligence/grant-probability-engine.ts, match/fit probability -- the SAME function WGR-135 finds is never re-triggered against live data; tested here in isolation as a pure function, not against persisted staleness) were each called directly (real, unmodified exported functions, not reimplementations) against 11 total chosen scenarios spanning documented branch boundaries, penalty stacking, neutral-fallback values, and exact recommendation-threshold edges (score===70 apply/consider boundary; single-vs-dual blocker suppression; zero-KB vs base-92 confidence branch; 0-100 clamp under 40 stacked [NEEDS INPUT] markers). All 11/11 scenarios' hand-computed expected output (independently derived from each function's own documented formula, kept structurally separate from the calls to the real code) matched the actual function output exactly -- no wrong threshold, inverted boolean, mis-weighted factor, or off-by-one boundary comparator (specifically checked: the apply threshold uses >=70, not >70, confirmed via an exact-70 boundary case) found in any of the three functions' own internal arithmetic. | test-evidence/pt-04/scoring.json | node scripts/audit/pt04-003-scoring.mjs; node scripts/audit/verify-pt04-003.mjs | CONFIRMED-OK |
| WGR-137 | Business Logic | P3 | PT-04-002 live API verification: GET /api/applications/[id]/reconcile's budget-reconciliation report (total_budget/total_spent/variance/compliance_status) and GET /api/deadlines/check's deadline-reminder-offset logic (30/14/7/3/1-day thresholds, one-way idempotent reminder flags per Contracts §11) were both exercised via real writes and real reads against the live, authenticated Next.js API on the production database, using the dedicated E2E test org -- all seeded rows deleted/reverted afterward, residue check confirmed 0 remaining across grant_budgets, grant_expenses, grant_reconciliation_reports, and the synthetic deadlines. Budget: 4 real budget line items ($26,500) + 3 real expenses ($7,000.75) hand-summed independently (plain arithmetic, no shared code with the route) produced total_budget=26500/total_spent=7000.75/variance=19499.25/compliance_status=under_budget -- the live route's own computed report matched exactly, 0 delta on every field. Deadlines: 6 real scenarios (none-crossed at 45 days out; exact-30-day boundary; two-thresholds-simultaneous at 10 days; due-today-all-5-fire; overdue-partial-with-already-sent-flags-correctly-never-re-firing; one pre-existing real production deadline) covering every documented threshold boundary and the reminder-flags-are-one-way idempotency rule all matched the hand-computed expected reminder set (using date-fns's differenceInCalendarDays directly, not the route's own code) exactly, 6/6. | test-evidence/pt-04/budget-deadline.json | node scripts/audit/pt04-002-budget-deadline.mjs; node scripts/audit/verify-pt04-002.mjs | CONFIRMED-OK |
| WGR-144 | Data/Script | P3 | IRS BMF CSV (src/scripts/import-irs-bmf.ts): the file's own comment ("IRS EO BMF files have NO header row, so we supply the column names") and its importFile() loop (`for await (const line of rl)`, no header-skip logic) both assume every line is data. Live-confirmed against a real, current download (https://www.irs.gov/pub/irs-soi/eo_wy.csv): the file's first line IS a literal header row ("EIN,NAME,ICO,STREET,CITY,STATE,ZIP,GROUP,SUBSECTION,..."). Positionally re-applying BMF_HEADERS to that header row and running the real isFoundation() filter against it confirms it is currently harmless BY COINCIDENCE ONLY: the literal string "FOUNDATION" (in the header row's FOUNDATION column) doesn't match the '02'/'04' codes isFoundation() checks for, so the header row is filtered out before any insert. The stale no-header-row assumption itself is a real drift from the current live file shape, not currently a data-corruption bug. | test-evidence/pt-07/data-sources.json#sources.irs_endpoints.shape_check | node scripts/audit/pt07-003-data-sources.mjs (re-runs the real live calls); node scripts/audit/verify-pt07-003.mjs (validates the captured evidence); or curl https://www.irs.gov/pub/irs-soi/eo_wy.csv \| head -1 -> a literal header row. | CONFIRMED-BROKEN |
| WGR-147 | Integration | P3 | GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET / GOOGLE_REDIRECT_URI are not configured in .env.local or in any Vercel environment (same live `vercel env ls` check as WGR-146). src/lib/integrations/google/auth.ts's getOAuthClient() was called for real with these unset and threw 'Missing GOOGLE_CLIENT_ID' immediately, reproducing the exact failure any real OAuth connect/refresh/calendar-sync attempt would hit today — Google Calendar sync (Behavioral Contracts §20) cannot be authorized in production. A live query of the integrations table (provider='google') found 0 rows across all organizations — no org has ever connected Google Calendar, so this is not currently stranding any real customer's existing connection, but the feature is fully non-functional as shipped. INTEGRATION_ENCRYPTION_KEY (needed to decrypt a stored token, were one to exist) IS present in Vercel production — only the three OAuth app credentials are missing. | test-evidence/pt-07/comms-billing.json#googleCalendar | node --import tsx scripts/audit/pt07-004-comms-billing-probes.mjs (re-runs the real live checks); node scripts/audit/verify-pt07-004.mjs (validates the captured evidence) | PENDING-SCOPE |
| WGR-150 | Perf/Capacity | P3 | Concurrent-user load simulation (PT-12-002, real disposable-branch DB): breaking point for the platform's real weighted request mix (dashboard_load/discovery/pipeline_update/draft_generation) was measured at concurrency=600 (p95=5027ms, p99=5823ms, errorRate=0.00%) -- ABOVE the stated reasonable target of 250 concurrent users for this infra tier, not below it. Zero request errors were observed at ANY tested concurrency level, including the highest tested (1000). Separately, real completed-request throughput plateaus at ~180 req/s starting around concurrency=150 (`throughputPlateauConcurrency: 150`, `maxObservedThroughputRps: 179.7`) -- concurrency above that adds queueing latency without increasing real completed work, making ~180 req/s the practical capacity ceiling for this request mix on this infra tier, well below the 600-concurrency latency-SLA breaking point. Not a defect -- logged as capacity-planning data per task instruction to record findings, not just failures, and to give a future capacity-planning pass (or a re-run against production-tier compute, since this branch is a smaller/free-tier instance than production per PT-12-002's and PT-12-004's own stated caveat) a concrete real baseline to compare against. | test-evidence/pt-12/load-results.json#findings (`degradationOnsetConcurrency: 75`, `breakingPointConcurrency: 600`, `throughputPlateauConcurrency: 150`, `maxObservedThroughputRps: 179.7`, `breakingPointBelowReasonableTarget: false`), test-evidence/pt-12/load-results.txt | node scripts/audit/pt12-002-load-simulation.mjs (requires a live branch matching the `pt12-load-test` shape -- torn down per PT-12-005, would need recreating with `--with-data` first); or `node scripts/audit/verify-pt12-002.mjs` against the already-captured evidence. | CONFIRMED-OK |
| WGR-151 | Perf/Memory | P3 | Sustained soak + memory-growth tracking (PT-12-003, real local Next.js server + real local worker process, 202.2s sustained load, 1,928 real requests, 0 errors, automation confirmed disarmed via 0 `pending` `submission_queue` rows before the run): NO memory leak detected in either process. Next.js: first-quarter avg 53MB -> last-quarter avg 27MB (-48.8%, monotonicity 93.75%, regression slope -213,547 bytes/s -- i.e. shrinking, not growing). Worker: first-quarter avg 42MB -> last-quarter avg 23MB (-44.5%, monotonicity 87.5%, slope -153,778 bytes/s). Both verdicts `flat` (leak classification requires >15% growth AND >60% monotonicity in the growing direction; both processes are negative-growth, nowhere near that bar). Investigated and confirmed NOT a gap -- logged so this question isn't re-asked without cause. | test-evidence/pt-12/soak-memory.json#analysis (`analysis.next`, `analysis.worker`, `findings.overallVerdict: "PASS_NO_LEAK"`), test-evidence/pt-12/soak-memory.txt | node scripts/audit/pt12-003-soak-memory.mjs (requires a live branch matching the `pt12-load-test` shape -- torn down per PT-12-005, would need recreating with `--with-data` first); or `node scripts/audit/verify-pt12-003.mjs` against the already-captured evidence. | CONFIRMED-OK |
| WGR-152 | Data/Infra | P3 | Rate-limiter behavior under real DB contention (PT-12-004, same real disposable-branch DB and same concurrent burst that produced WGR-149's pool-contention failures -- i.e. tested under genuine contention, not an idealized isolated measurement): `worker/rate-limiter.ts`'s `RateLimiter.canSubmitToDomain()` exact filter shape and documented fail-open-on-error semantics were replicated with 10 calls per funder per concurrency level (2/5/10/20/40) against a real "should-block" funder (a funder with a submission in the last 7 days) and a real "should-allow" funder (zero prior submissions) -- 50 block-check calls and 50 allow-check calls total. Result: 0 false-allows (the should-block funder was never incorrectly permitted to submit, even while the same burst was triggering real statement_timeout errors on the pool-stress side per WGR-149) and 0 false-blocks (the should-allow funder was never incorrectly denied). The rate limiter's fail-open design exists in the code but was never actually triggered within this tested contention range -- confirmed sound at the concurrency levels tested (up to 40), not a gap. | test-evidence/pt-12/pool-ratelimit.json#findings.rateLimiterContention (`totalFalseAllows: 0`, `totalFalseBlocks: 0`, `totalBlockCalls: 50`, `totalAllowCalls: 50`) | node scripts/audit/pt12-004-pool-ratelimit.mjs (re-runs the real live burst against the branch, if still live; branch has since been torn down per PT-12-005 -- re-run requires recreating a branch first); or `node scripts/audit/verify-pt12-004.mjs` against the already-captured evidence. | CONFIRMED-OK |
| WGR-154 | API/Middleware | P2 | Found during the WGR-111 route-gate pre-audit (`test-evidence/remediation/wgr-111/route-gate-audit.md`), which read every `route.ts` under `src/app/api/cron`, `src/app/api/sources`, `src/app/api/webhooks`, `src/app/api/admin/webhooks` (21 files) to classify SECRET-GATED vs UNGATED before deciding which get a `src/middleware.ts` exemption. `GET /api/sources/state-portals` (a read-only, on-demand state grant-portal scrape preview, not in `vercel.json`'s `crons[]` and not one of WGR-023/WGR-111's originally-documented 14 real `CRON_SECRET` routes) has ZERO auth check of any kind in the handler itself -- confirmed by a full file read: no `CRON_SECRET` comparison, no `supabase.auth.getUser()` call, no signature check, unlike every one of its 20 sibling files in this same audit. Today this is not externally exploitable: `src/middleware.ts` still requires a valid Supabase session for any path not in `PUBLIC_PATHS` (this route is not listed there), so an anonymous caller is still redirected to `/login` -- but that protection is incidental, not the route's own, and it means ANY authenticated user of ANY role in ANY organization can trigger a live outbound scrape of an arbitrary configured state portal via this org-agnostic, role-agnostic endpoint, which no other route in this audit set permits. Deliberately NOT given a `src/middleware.ts` exemption and NOT given a `CRON_SECRET` check as part of the WGR-111 fix (this task's own instruction for an unsigned third-party webhook is "do not exempt it, list it as a new finding" -- the same conservative treatment is applied here even though this is not a webhook, since adding a `CRON_SECRET` bearer requirement to what is currently a normal in-app, on-demand preview feature would be a product-scope decision, not a mechanical WGR-111 fix). Whether this route should be role-gated (e.g. `requireRole('admin')`), left as-is, or something else is left for a future decision. | test-evidence/remediation/wgr-111/route-gate-audit.md (`src/app/api/sources/*` table, `state-portals` row) | Direct read of `src/app/api/sources/state-portals/route.ts` (entire file, 47 lines) -- `GET()` (lines 23-47) performs zero authentication/authorization check before calling `scrapePortal()`. Cross-check `src/middleware.ts` `PUBLIC_PATHS`/`isPublicPath()` -- `/api/sources/state-portals` is not listed, so today's only protection is the middleware's default session requirement. | CONFIRMED-BROKEN |
