# BENAVORA — Feature Registry v2.0
## Supersedes: Feature_Registry.md v1.0
## Date: August 14, 2026 (last update — row #171 RAG integration unblocked (live path wired + migration 123 confirmed applied), row D7 backup script built+wired+partially verified, row #66 re-attempted with a new, different blocker found; see respective rows for evidence)
## Status: CANONICAL — Updated after every FORGE run and CC session.
## Build tool: FORGE 1.x | Repo: Reid64/benavora | Production: www.benavora.com

**August 7, 2026 update:** every row that was still marked `IN BUILD`/"Tonight" (a stale label —
none of the underlying work was actually done "tonight," some of it dates back to migrations from
weeks ago) was re-verified against real, current, live schema state via a direct `DATABASE_URL`
query, not the row's own prior text — 15 rows total (#79, #135, #136, #140, #152, #156, #157, #158,
#159, #161–165). **Two real exceptions were found, not corrected to BUILT**: `relationship_memory`
(row #98) is confirmed **absent** from production despite its migration file
(`src/supabase/migrations/076_reputation_intelligence.sql`) existing on disk — corrected to
NOT-BUILT, not left as a stale "IN BUILD." And `agent_registry`'s Pillar 17 (Agent Marketplace) has
two separate, more specific gaps than "in build": row #157 (Registry Seed Data) is NOT-BUILT — the
table has **zero rows** live, the "16 agents seeded" claim is false, and the would-be seed content
(`src/lib/agents/agent-registry-seed.ts`, 17 entries) is a never-executed, orphaned array, not a
fallback the API actually uses despite its own header comment's claim; row #159 (Agent Marketplace
UI) is also NOT-BUILT, not BUILT — the `/settings/agents` page that exists at that URL is a real page,
but it's the *different*, already-documented Autonomous Settings Panel (row #213), confirmed by
direct read to call only `/api/autonomous/config`/`/api/autonomous/decisions`, never
`/api/agents/registry`. The schema (#156) and API (#158) for this pillar are both genuinely real —
they were simply never wired to any UI. Separately, `PLANNED` rows #137 (Board Packet Agent, AG-27)
and #141 (Simulation Agent, AG-41) were re-verified BUILT — VERIFIED: both were built (`8943220`,
`0b57861`) and live-verified (`1a14129`, `189e16a`) against the real Faith Foundation org on
2026-08-03, per `AGENT_VERIFICATION_LOG.md`'s "AG-27"/"AG-41" entries — AG-27 correctly scoped a real
test meeting and wrote a real 2/3-real-data packet via its daily 2AM CST pipeline
(`worker/autonomous-orchestrator.ts`, not `worker/scheduler.ts`); AG-41 correctly hand-checked
deterministic math to full decimal precision across all 4 scenario types and confirmed genuine
(non-deduped) idempotency. Both agents' Claude-narrative steps were separately blocked at test time
by the same dead local `ANTHROPIC_API_KEY` documented throughout this doc, since rotated
(`8f3aa06`, 2026-08-04) but not yet re-tested against either agent. Overnight AG-10–42 build+verify
chain (`AGENT_VERIFICATION_LOG.md`, 2026-08-02 through 2026-08-06) also built and live-verified
AG-10, AG-26, AG-29 (canonical), AG-42, and `corporate_prospects`'s creation (2026-08-03) plus the
platform `ANTHROPIC_API_KEY` rotation resolved the two blockers shared by AG-20/21/22/24/30/32 — all
reflected in their respective rows below. Row #227 (ROI Optimization Engine, AG-39) claimed `run()`
"has no production call site" — false; `runRoiOptimizerStep()` has called it from
`worker/autonomous-orchestrator.ts`'s monthly sweep since commit `6ffd4fd` (2026-07-20), ten days
before this row's own "confirmed by repo-wide grep July 19" claim was written — corrected to BUILT —
UNVERIFIED (real call site confirmed, but `roi_insights`/`submission_variables` both confirmed live
with zero rows, so no live execution has actually been recorded yet). Row #471 (UI Redesign Status,
"Opportunities page") was a stale duplicate of row #105 (Probability Badges) claiming `PLANNED` for
an already-BUILT feature (probability badges + sort-by-score both confirmed live in
`opportunities/page.tsx`) — reconciled to cross-reference row #105. Command Center (row #152) is
owner-only, not "owner/admin" as previously worded. See `NOT_BUILT_MASTER_INVENTORY.md` Section 2 for
the parallel, more detailed AG-01–AG-42 accounting this update is consistent with.

**July 30, 2026 update:** every AG-15 through AG-30 row (and the Post-Launch Vision rows that
directly collide with or extend those numbers) has been re-stated using the agent-verification
tiers below, consolidating the live-verification findings recorded across `AGENT_VERIFICATION_LOG.md`'s
q6-q9 passes. Several previously-unqualified "BUILT" claims for autonomous agents were found to be
false or misleading once tested live against production — most commonly, an agent's code is real
and compiles but its first database write fails on a strict `agent_type` enum that was never
extended for it (`AGENTS_v2.md` §1.2), a pattern reproduced live, not inferred, for AG-15, AG-17,
AG-19 (moot — never called), the "AG-30" Donor Intent Monitor, and AG-38. Where a row's real status
turned out to be better than documented (AG-15's deterministic engine, AG-16's Digital Twin builder,
AG-25's Disaster Response agent matching its own spec exactly), that is stated too — this pass is not
one-directional bad news.

**August 2, 2026 update:** the `agent_type` enum gap above was fixed live in production and
re-verified for 6 agents (AG-15, AG-17, AG-19, and the AG-25/28/30 on-disk collisions) — see
`AGENT_VERIFICATION_LOG.md`'s enum-gap entries. Two new schema-drift bugs the fix exposed
(`agent_decisions` missing columns; `organizations.service_areas` querying a nonexistent column)
were found and fixed the same pass. Separately, `AGENTS_v2.md` permanently renumbered its two
genuinely never-built phantom specs that were colliding with real agents: "Impact Simulation
Agent" moved from AG-28 to **AG-41** (AG-28 is now permanently the real Follow-Up Generator, row
#202), and "Change Monitor Agent (CM-01)" moved from AG-30 to **AG-42** (AG-30 is now permanently
the real Donor Intent Monitor, row #218). AG-25 was deliberately left unchanged — its canonical
Disaster Response Agent is real, working code (not phantom like the other two), and its own source
files self-identify as "AG-25" in header comments; see `AGENTS_v2.md` §1.4 for the full reasoning.
Rows #96, #141, #202, #218 below reflect the new numbering. AG-25's own numbering note (row #201)
was strengthened the same day to correct a stale claim: `DeadlinePredictionAgent`
(`"ag-25-deadline-prediction"`) was previously described as unreachable — it's since been enum-fixed
and confirmed live-working, so AG-25 is now explicitly documented as a permanent dual-use number
where *both* halves (Disaster Response and Deadline Prediction) are real and live, not one real
agent and one dead one.

---

## Status Key

| Status | Meaning |
|---|---|
| BUILT | Fully implemented, compile-verified, deployed to production |
| PARTIAL | Core functionality built, gaps documented below |
| IN BUILD | Currently in active FORGE queue or overnight run |
| PLANNED | Scoped and architected, not yet in a queue |
| DEFERRED | Explicitly postponed to a future phase |

**Agent-verification tiers (added July 30, 2026, after `AGENT_VERIFICATION_LOG.md`'s q6-q9 live
verification passes on AG-15 through AG-30):** "BUILT" alone means compile-clean and deployed — it
does not mean anyone confirmed the code actually runs or produces correct output. For every agent
row that log covers, the plain BUILT/PLANNED status below is replaced with one of these three:

| Tier | Meaning |
|---|---|
| BUILT — VERIFIED | Real code, real call site, and its actual runtime behavior against real production data was directly observed this session (live DB read/write, reproduced insert, or hand-checked output) — not just read from source. |
| BUILT — UNVERIFIED | Real code exists, compiles, and has a real call site, but its actual runtime output was not confirmed against live data this session (commonly because a live blocker — missing table, invalid API key, tool-permission denial — prevented it, or because no test was attempted). Treat as "probably does something," not "confirmed correct." |
| BUILT — BLOCKED (VERIFIED) | Real code exists, but a live reproduction this session confirmed it **cannot execute at all** in production today (e.g. an `agent_type` enum insert fails, a required table 404s) — this is stronger than "unverified": the failure itself was directly observed, not inferred. |
| NOT-BUILT | No implementation found anywhere in the repo for this concept, confirmed by direct grep/search this session (or a prior session whose finding was independently re-confirmed) — functionally equivalent to PLANNED, stated explicitly for clarity. |

---

## Phase 1 — Core MVP (Features 1–18) — ALL BUILT

| # | Feature | Description | Status |
|---|---|---|---|
| 1 | Authentication | Email/password login and registration via Supabase Auth. Creates org + profile on signup. Onboarding gate via middleware. | BUILT |
| 2 | Dashboard v2 | Hero banner (Your Task Management Area), FlightPathHUD 6-stage lifecycle cards, Today's Action Items widget, Pipeline, Deadlines, Quick Actions. Two-column layout. | BUILT |
| 3 | Funder CRM | Searchable/sortable funder database. 12 category types. Detail pages with tabs: Overview, Contacts, Opportunities, Applications, Notes. Relationship score display. | BUILT |
| 4 | Contact CRM | Contacts linked to funders. Relationship status (cold/warm/active/champion). Activity log. | BUILT |
| 5 | Opportunities | Opportunity records with eligibility score, probability score, recommendation, keyword tags. Filter by category, deadline, amount, status, probability. | BUILT |
| 6 | Keyword Search | Full-text keyword search across opportunities via opportunity_keywords table. Add/remove tags per opportunity. | BUILT |
| 7 | Application Pipeline | 12-stage kanban board with drag-and-drop. Stage transition rules enforced. Pipeline history logged. Days-in-stage tracking. | BUILT |
| 8 | Document Repository | Drag-and-drop file upload to Supabase Storage. 8 document categories. Expiration warnings. Link docs to applications. | BUILT |
| 9 | Knowledge Base | Organization profile editor, reusable narrative blocks with category tags, standard Q&A answers. Proven narrative badges. | BUILT |
| 10 | AI Draft Generator | Select opportunity + template type. Claude generates draft using KB + proven narratives. Confidence scoring 0-100. Warning banner below 70. | BUILT |
| 11 | Deadline System | Calendar and list views. Color-coded urgency. Auto-created from opportunities. Completion tracking. Predicted deadlines section. | BUILT |
| 12 | Notes System | Polymorphic notes on funders, opportunities, and applications. Timeline display. Author tracking. | BUILT |
| 13 | Outcome Tracking | Record awarded/denied/partial per application. Funder feedback. Narrative snapshot frozen at submission. | BUILT |
| 14 | Recursive Learning | Analyzes awarded applications. Extracts proven narratives. Updates effectiveness scores. Flags winning patterns for reuse. | BUILT |
| 15 | Cold Outreach | Extract contacts from companies without giving pages. Outreach contact table. Convert to funder. | BUILT |
| 16 | Search Profiles | Saved keyword configurations for automated searches. Keywords, categories, geographic scope, amount range, active/paused toggle. | BUILT |
| 17 | Settings | Organization settings. User management with role assignment. Feature flag display. Notification preferences. Agent Marketplace link. | BUILT |
| 18 | RLS Isolation | Row Level Security on all org-scoped tables. Every query scoped by organization_id. Complete tenant data isolation. | BUILT |

---

## Phase 2 — Tier 1-3 Enhancements (Features 19–39) — ALL BUILT

| # | Feature | Tier | Status |
|---|---|---|---|
| 19 | Draft Persistence + Version History | Tier 1 | BUILT |
| 20 | Nav State Preservation | Tier 1 | BUILT |
| 21 | KB Detail Views | Tier 1 | BUILT |
| 22 | AI Humanizer Agent | Tier 1 | BUILT |
| 23 | Grant Source Categorization | Tier 2 | BUILT |
| 24 | Parallel Research Agents | Tier 2 | BUILT |
| 25 | Search Profile Config | Tier 2 | BUILT |
| 26 | Analytics Dashboard | Tier 2 | BUILT |
| 27 | Enhanced Eligibility Scoring | Tier 2 | BUILT |
| 28 | Alerts & Notifications | Tier 2 | BUILT |
| 29 | Multi-Model Consensus | Tier 2 | BUILT |
| 30 | Budget Narrative Generator | Tier 3 | BUILT |
| 31 | Document Assembly Engine | Tier 3 | BUILT |
| 32 | Funder Intelligence | Tier 3 | BUILT |
| 33 | Renewal Tracker | Tier 3 | BUILT |
| 34 | Success Pattern Learning | Tier 3 | BUILT |
| 35 | Compliance Pre-Check | Tier 3 | BUILT |
| 36 | Cold Outreach Sequences | Tier 3 | BUILT — DEPRECATED 2026-08-13, CONSOLIDATION COMPLETE (see addendum below; UI redirected + cron retired, source tables not dropped) |
| 37 | Grant Calendar View | Tier 3 | BUILT |
| 38 | Email Parsing Agent | Tier 3 | BUILT — PARTIAL (see 2026-08-13 addendum below: extract/classify + summarize confirmed working live; Gmail-webhook auto-trigger and auto-reply/respond both confirmed absent) |
| 39 | Board Report Generator | Tier 3 | BUILT |

**2026-08-13 addendum (row #36, Cold Outreach Sequences):** This row's underlying schema
(`email_campaigns`/`campaign_steps`/`campaign_sends`, `supabase/migrations/001_initial_schema.sql`)
is the exact system consolidated in `OUTREACH_CONSOLIDATION_AUDIT.md`'s 2026-08-13 "Outreach ×
Email" pass, per Reid's decision to make the Email system's sequence engine
(`email_campaign_sequences`/`email_sequence_steps`/`email_sequence_enrollments`) the sole
UI-reachable path going forward. `/outreach/campaigns`, `/outreach/campaigns/[id]`, and
`/outreach/sequences` now all `redirect()` to `/email/campaigns` (server components, confirmed by
direct read; `pnpm run build`/`pnpm tsc --noEmit` both clean 2026-08-13). Note
`/outreach/sequences` was a *separate*, never-applied `followup_sequences`-backed page with no
schema relationship to this row (see the audit doc's "system mapping" section) — it is redirected
for UI consistency, not because it was ever the same feature. The one real row pair this row's
schema held (1 `email_campaigns`, 2 `campaign_steps` — confirmed to be E2E test-fixture data, not a
real customer's, per the audit's "correction to this doc's own prior framing" note) was migrated
1:1 into the Email schema; the 1 `campaign_sends` row could not be migrated (event-log vs.
aggregate-state shape mismatch, no equivalent field exists on the target side) and was exported
verbatim to `OUTREACH_ROWS_PRE_CONSOLIDATION_2026-08-13.json` instead of being dropped or guessed
at. **Source tables were NOT dropped or truncated** — marked deprecated via `COMMENT ON TABLE` only
(`scripts/deprecate-outreach-campaign-tables.sql`, applied live and read back to confirm). **Five
real write paths to the deprecated tables remain active this pass**: `POST`/`PUT
/api/agents/campaigns[...]` and the `/api/webhooks/resend` receiver that updates `campaign_sends` on
real `email.opened`/`email.bounced` events — both deliberately left live and unmodified, out of scope
for the cron decision below. **2026-08-13, later same day: Item 5 resolved.** A live `psql` query
confirmed zero orgs have `platform_config.key = 'feature.cold_outreach_email'` set `true` — the
`/api/cron/campaigns` job (every 2 hours, ran `EmailCampaignAgent` for any org with that flag enabled)
had been a scheduled no-op in production. Reid decided to retire it rather than repoint it at the new
schema for a sweep no org uses: the entry was removed from `vercel.json`'s `crons` array (no other
cron entries touched), safely reversible via git; the route/agent code itself was left in place,
unmodified. **This completes the Cold Outreach Sequences consolidation end-to-end** — both the UI
(redirects to `/email/campaigns`) and the backend (no more autonomous writer to the deprecated schema)
are now fully on the Email engine. The two remaining write paths (`/api/agents/campaigns[...]`, the
Resend webhook) were never part of the cron decision and remain live by design — see the audit doc's
"Item 5 — RESOLVED" section for full evidence.

**2026-08-13 addendum (row #38, Email Parsing Agent):** `EMAIL_PARSER_VERIFICATION_2026-08-13.md`
live-tested this row's three distinct capabilities separately rather than accepting the prior
blanket "BUILT," which had no dated verification citation. **(1) EXTRACT/CLASSIFY — CONFIRMED
WORKING.** `EmailParserAgent.run()` (`src/lib/agents/email-parser.ts`) was run live — not
reimplemented — against the real FAITH Foundation org and a real Anthropic API call on two
realistic funder emails: both correctly classified (`award_notification`, `information_request`),
all 5 spec fields (funder name, opportunity reference, action required, urgency, sentiment)
correctly extracted, correct fuzzy `ilike` funder-matching despite non-identical name strings, and
real `email_activity`/`agent_runs` rows confirmed by re-querying the live DB after the run (not
assumed from the return value; test rows deleted after). **(2) SUMMARIZE
(`/api/email/summarize`) — CONFIRMED WORKING, but a genuinely separate system from this row's
classifier**: different tables (`synced_email_threads`/`synced_email_messages`, not
`email_activity`), a different model (`claude-haiku-4-5-20251001` vs. the parser's
`claude-sonnet-4-6`), no classification/extraction/funder-matching, zero shared code path.
Live-tested against a real seeded 3-message thread for the same org — accurate, correctly-scoped
2-4 sentence summary. **(3) RESPOND/REPLY (auto-reply drafting or sending tied to inbound
classification) — CONFIRMED ABSENT.** Repo-wide grep across `src/lib/agents/`,
`src/lib/integrations/google/gmail.ts`, `src/app/api/email/`, and `src/app/api/agents/` found zero
Gmail-drafts-API calls and no route that composes or sends a reply from an inbound classification;
the one near-hit (`src/lib/admin/unsubscribe-agent.ts`) was read in full and confirmed unrelated —
it classifies Sales Outreach cold-email replies for suppression-list management, a different domain
entirely, and only classifies/suppresses, never drafts or sends. This capability is not in
BLUEPRINT.md's §9 spec either — it is new, undesigned feature scope, not a bug. **A real
spec-deviation was also found inside capability (1) itself**: BLUEPRINT.md §9.1's "Gmail API
webhook triggers on new email" is CONFIRMED ABSENT — no webhook/pubsub listener exists anywhere in
the repo, `/api/email/sync` never calls the parser, and the code's own header comments confirm this
is a known, not accidental, gap ("Phase 4 will wire in actual Gmail API integration"). Today the
classifier only runs when a user manually enters email content into the dashboard's
`EmailParserWidget`. **Net: the classification/extraction engine and the separate summarizer are
both real and confirmed working end-to-end; the automatic Gmail-triggered firing and the
auto-reply/respond capability a blanket "BUILT" would imply are both confirmed absent.** See
`EMAIL_PARSER_VERIFICATION_2026-08-13.md` for full evidence and grep commands.

---

## Phase 3 — Browser Automation (Features 40–46) — ALL BUILT

| # | Feature | Status | Notes |
|---|---|---|---|
| 40 | Form Detection | BUILT | Playwright navigates to giving portal URLs. AI identifies form fields. |
| 41 | Auto-Fill Engine | BUILT | humanType() character-by-character field filling. Field confidence scoring. |
| 42 | Challenge Detection | BUILT | CAPTCHAs, MFA, account creation detected. Pauses for human intervention. |
| 43 | Approval Checkpoint | BUILT | Human approval before final submission. Screenshot review. Audit logged. |
| 44 | Portal Credentials | BUILT | Credential vault per funder. Auto-login before form filling. |
| 45 | Submission Verification | BUILT | Captures confirmation page/number. Screenshots in Supabase Storage. |
| 46 | Automation Dashboard | BUILT | Session history, status, diagnostics, retry controls, screenshot review. |

---

## Phase 4 — SaaS Layer (Features 47–52) — ALL BUILT

| # | Feature | Status | Notes |
|---|---|---|---|
| 47 | Stripe Billing | BUILT | 3-tier subscriptions. Checkout, portal, webhooks. Status enforcement. |
| 48 | Usage Limits | BUILT | Per-tier caps enforced at API level. |
| 49 | Onboarding Wizard | BUILT | 7-step guided setup. Progress saved per step. |
| 50 | Audit Logs | BUILT | All user actions tracked. Searchable log viewer. |
| 51 | User Invitations | BUILT | Invite by email with role assignment. Acceptance flow. |
| 52 | UI Theme | BUILT | Elevated Slate design system. FlightPathHUD colored cards. |

---

## Tier 6 — Full Autonomous Operation (Features 53–78)

| # | Feature | Status | Notes |
|---|---|---|---|
| 53 | Grants.gov Client | BUILT | Daily poll via POST API. Auto-creates opportunities with dedup. |
| 54 | SAM.gov Client | BUILT | Weekly poll. src/lib/sources/samgov-client.ts + API route. |
| 55 | ProPublica 990 Mining | BUILT | src/lib/sources/propublica-990-client.ts + batch script. Never run at scale. |
| 56 | State Portal Framework | PARTIAL | **Updated 2026-08-14/15**, per `STATE_PORTAL_SCOPING_2026-08-13.md`'s findings (four uncoordinated implementations, none autonomous/scheduled — see that doc for the full inventory). Two concrete fixes/additions landed this session: (1) **`state_portals` table gap fixed** — the scoping doc's finding that `src/app/(dashboard)/settings/integrations/page.tsx` queried a `state_portals` table that doesn't exist anywhere in either migration tree (silently always rendering "0 Active") is corrected: the query was removed and the card's count now reads `PORTAL_REGISTRY.length` (the same registry `StatePortalResearchAgent`/`state-portal.ts` actually uses for its Run Now button), with `PORTAL_REGISTRY`'s definition extracted out of `state-portal.ts` into a new standalone, dependency-free `src/lib/sources/state-portals/portal-registry.ts` so the client-side settings page can import it without pulling in `state-portal.ts`'s server-only Anthropic/admin-Supabase imports. `pnpm tsc --noEmit` clean. (2) **New California Grants Portal RSS parser built** (`src/lib/sources/state-portals/ca-grants-portal-client.ts` + `ca-grants-portal-sync.ts` + `scripts/ingest-ca-grants-portal.ts`, `pnpm ingest:ca-grants-portal`) — a genuine, standalone 5th implementation per the scoping doc's §3/§5 recommendation, deliberately not reconciled with the existing four. Parses the real WordPress RSS feed at `grants.ca.gov/grants/feed/` (title/link/description/pubDate + `content:encoded`'s Agency/Application-Close-Date `<dl>`) rather than HTML-regex-scraping like the existing `portal-scraper.ts` stub — lower fragility risk per the scoping doc's live-verified finding that CA's general portal was one of only 2 of 17 tested state URLs that returned a real, structured, non-blocked listing. Nonprofit-eligibility signal is cross-referenced against a second source (the HTML archive's `applicant_type-nonprofit` article class, since the feed itself doesn't carry that field — confirmed live during this build) via up to 10 archive pages, respecting Contracts §21's 5s same-domain delay; grants not covered by the fetched archive pages get `nonprofitEligible: null`, never a fabricated `false`. Syncs into `opportunities` (`source: "ca_grants_portal"`) for every org with an active search profile, dedup by URL (matching the `grantsgov-sync.ts`/`federal-grants-poller.ts` convention). **Live-verified this session**: two earlier attempts to run `pnpm ingest:ca-grants-portal` against the real feed were blocked by a tool-permission gate on live network+secret calls before either could execute, but a subsequent attempt in the same session got past the gate and completed a real run: `psql` against the live DB confirms `SELECT source, count(*), min(created_at), max(created_at) FROM opportunities WHERE source = 'ca_grants_portal' GROUP BY source` returns exactly 200 rows, `created_at` 2026-08-15 03:35:26–03:36:03 UTC. Spot-checked 2 of the inserted rows against the live `grants.ca.gov` site: "Elevate Youth California: Cohort 8 Standard Track" (DHCS, `grants.ca.gov/grants/elevate-youth-california-cohort-8-standard-track/`) matches the live page's title/agency; "Division of Boating and Waterways Local Assistance Boat Launching Facility Grant Program FY27" (Dept. of Parks and Recreation, `grants.ca.gov/grants/division-of-boating-and-waterways-local-assistance-boat-launching-facility-grant-program-fy27/`) matches the live page's title and its February 1, 2027 deadline exactly against the DB's `deadline` column — both real, non-fabricated grants. The other three pre-existing implementations (`state-scrapers.ts`, `tdhca-scraper.ts`, `portal-scraper.ts`/`portal-config.ts`) and their own broken/dead URLs (per the scoping doc's §2 live sweep) are unchanged by this pass. |
| 57 | Integration Settings UI | BUILT | /settings/integrations connector cards (Grants.gov, ProPublica, State Portals, SAM.gov "Run Now") now default their required params (keywords/state/ein/query) from real org data instead of posting an empty body that always 400'd. SAM.gov also now reads the org's own encrypted key (integration_keys) before falling back to process.env, per Behavioral Contracts §18. Commit 0232358, July 28 2026. |
| 58 | CSV Import Wizard | BUILT | 3-step wizard at /import. Column mapping. Preview. POST to /api/import/csv. |
| 59 | Custom API Connector | BUILT — VERIFIED | Correction, 2026-08-07: this row was stale — real schema (034/041/124), routes, agent (`CustomApiResearchAgent`), and an 827-line admin settings UI (`/settings/custom-apis`) already existed. **Live-verified 2026-08-08** (`AGENT_VERIFICATION_LOG.md`, queue-37 entry #4): migration 132 (`custom_connector_allowlist`) was not actually applied to production despite being committed — applied live this pass. Adversarial SSRF testing (8/8 private/metadata/loopback/scheme-smuggling targets blocked) confirmed the security boundary was sound from the start, but **two real bugs made the "legitimate fetch succeeds" half of this feature completely non-functional until fixed**: (1) Node ≥18.13's `autoSelectFamily` (Happy Eyeballs) made every real fetch fail with `"Invalid IP address: undefined"`, 100% of the time — fixed by disabling it on the pinned request. (2) truncating an over-`maxBytes` response via `res.destroy()` left the fetch promise permanently unsettled (hung forever) since `"end"` never fires after `destroy()` — fixed by resolving at the truncation point instead of waiting on `"end"`. Both fixed and re-verified: a real allowlisted connector against a real public JSON API now genuinely fetches, maps, and stores 3 real opportunity rows end-to-end. |
| 60 | Custom Scraping Targets | BUILT — VERIFIED (SSRF layer only; see row #59 for the two bugs shared via `safe-fetch.ts`) | Correction, 2026-08-07: this row was stale too — real schema, a working manual-trigger route (`/api/agents/custom-scrape`), a real Claude-extraction agent (`CustomScrapeResearchAgent`), and a 570-line admin settings UI (`/settings/scraping`) already existed. The real gap: `target.url` was fetched with a raw, unvalidated `fetch()` — a genuine SSRF surface (any admin, or anyone who compromises an admin session, could point it at `169.254.169.254`, `localhost`, or an internal `10.x`/`192.168.x` host). Closed this session with `src/lib/security/safe-fetch.ts`, sharing the exact same module (and the same two bugs, see row #59) as the Custom API Connector — DNS-resolves the hostname once, validates every resolved IP against private/loopback/link-local/CGNAT/metadata ranges, then pins the actual TCP connection to that validated IP so a later DNS answer can't steer the connection elsewhere. Redirects are followed manually with the same validation re-run per hop. Layered with a *separate*, admin-only domain allowlist (`custom_connector_allowlist`, migration 132, confirmed applied live 2026-08-08). This agent's own execution path (Claude-extraction) was not independently live-tested 2026-08-08 — that pass tested `CustomApiResearchAgent` end-to-end (no Claude call needed) and `safe-fetch.ts`/`custom-connector-allowlist.ts` directly, both shared by this agent too. Manual-trigger-only, not wired into any autonomous/scheduled pipeline (matches this repo's AG-25/AG-41 precedent). |
| 61 | Automation Queue | BUILT | Worker exists (worker/queue-processor.ts). Priority scoring in worker/batch-scorer.ts, re-run on idle→active transitions: timing, funder match, historical win rate, amount alignment, portal health, deadline proximity (nearest open opportunity per funder), probability score (opportunity_probability_scores, Feature #102, if scored), and organization tier. Lower submission_queue.priority = processed first. |
| 62 | Semi/Autonomous Modes | BUILT | Both modes implemented in AutoApply. |
| 63 | 2Captcha Integration | BUILT | captcha-solver.ts wired into src/lib/autoapply/form-filler-agent.ts — detect/solve/inject for recaptcha v2/v3, hcaptcha, turnstile. Audit logging, screenshot capture, graceful degradation when 2Captcha key is missing. Commit 3e7400b, July 22 2026. |
| 64 | Automation Monitor | BUILT | Real-time queue status. Failure categorization. Screenshot review. |
| 65 | Notification Preferences | BUILT | Per-user event type preferences. In-app + email toggles. |
| 66 | 990-PF Giving History | PARTIAL | `irs990.ts`'s `extractGrantSchedule()` extended with per-recipient Schedule I line items (`enrichment.grant_history`), `foundation-profiler.ts` extended to surface them (pure reader, no duplicated fetch logic), migration adding `foundation_profiles.grant_history` applied live 2026-08-08. **Live-verified 2026-08-08** (`AGENT_VERIFICATION_LOG.md`, queue-37 entry #5): found and documented (not fixed) a pre-existing, unrelated bug — `scripts/enrich-foundations-990.ts` constructs a dead S3 URL for every filing it tries to fetch (the same dead endpoint `foundation-scraper.ts`'s own header already flagged 2026-07-28; this script was never updated to the working batch-ZIP mechanism). Separately found a ~66% zlib decompression failure rate in the pinned `unzipper` package against real, large (84,172-entry, ZIP64) IRS batch archives — a likely risk to the already-live weekly S2 scraper too, not just this feature. On the filings that did decompress, the new extraction code ran cleanly against 36 real 990PF filings and correctly returned zero grants for each (plausible for the small foundations sampled) — but a real, populated Schedule I was not reached within this session, so the positive extraction case remains unconfirmed. **Attempted again 2026-08-14** (`scripts/investigate-990-schedule-i.ts`, a targeted one-off: ranks real `foundation_directory` rows by `asset_amount`/`giving_total` — largest first, since large foundations are far likelier to have a populated Schedule I than the small family foundations the 2026-08-08 sample happened to hit — matches candidate EINs against the live IRS 990 e-file index, then reads only the targeted entries out of the needed batch ZIP(s) via a hand-rolled ZIP64-aware central-directory/local-header parser, avoiding `unzipper` entirely this time). **Real, live run this session**: candidate selection and index-matching both worked correctly — 150 real large-foundation candidates loaded (Lilly Endowment $105.9B, Gates Foundation $78.7B, Ford Foundation $17.5B, etc.), 29 real EINs matched against the live `index_2026.csv` (353,650 rows scanned). **New, previously-undocumented blocker found**: all 10 targeted batch-ZIP downloads failed (`fetch failed` / `terminated`) before a single filing could be extracted — confirmed via a direct `curl -I` this session that these are genuinely large files (one target batch, `2026_TEOS_XML_05A.zip`, reports `Content-Length: 521158638` — 521MB), and no partial file was ever written to the local cache, consistent with the in-process `fetch(...).arrayBuffer()` call failing/timing out on a 500MB+ transfer in this sandbox rather than a server-side rejection. This is a different, newly-identified obstacle than the previously-documented `unzipper` decompression-failure-rate bug — that bug was never even reached this run, since no ZIP data was successfully downloaded to decompress. **The positive-Schedule-I extraction case remains unconfirmed for a third time**, now for a third distinct reason (36-small-foundations sample July session: real zero-grant results but no large foundation reached; 2026-08-08: `unzipper` corrupting large-archive reads; 2026-08-14: large-batch-ZIP downloads failing outright in this sandbox). Streaming the ZIP download instead of buffering the whole file, or running this specific script from a non-sandboxed machine with a stabler connection, are the two most likely fixes — neither attempted this session. Real results saved to `enrichment-output/990-investigation-cache/results-summary.json` (untracked, not committed — investigation output, not a deliverable). **Streaming fix implemented 2026-08-15, retry attempted but still inconclusive — a fourth distinct outcome, not a resolution.** `downloadBatchZip()` was rewritten to stream the response body straight to a temp file on disk via `node:stream/promises` `pipeline()` (never buffering the whole 500MB+ transfer in memory) and the entire ZIP64 parser (`findEOCD`/`parseCentralDirectory`/`extractEntry`) was converted from buffer-slicing to positional (`FileHandle.read()`-based) reads against that file, so only the small byte ranges actually needed (EOCD tail, central directory, each targeted entry) are ever read into memory — the parsing algorithm itself is unchanged. A first re-run this session using the new streaming code got further than the 2026-08-14 attempt (progressed past the point where `fetch(...).arrayBuffer()` previously failed outright, reaching ~100MB streamed into `2026_TEOS_XML_05A.zip.part` per the run log and on-disk partial file) but the process did not complete within the session and left only a partial `.part` file — no filing was extracted, no result updated. **A second, fresh re-run attempt was blocked by a tool-permission gate on live network+secret calls before it could execute at all.** Net effect: the streaming/positional-read code is a real, type-checked (`pnpm tsc --noEmit` clean) fix for the buffering failure mode specifically, and got measurably further than the prior attempt, but **the positive-Schedule-I extraction case remains unconfirmed for a fourth time**, now because the retry itself could not be completed this session (partial download timeout, then a blocked re-attempt) — not because of a newly-found code defect. Say this plainly rather than folding it into the wins above: this is still an open, unresolved verification gap. |
| 67 | Foundation Profile Builder | BUILT | src/lib/intelligence/foundation-profiler.ts + API route. |
| 68 | Success Probability Scoring | BUILT | src/lib/intelligence/success-probability.ts + /api/opportunities/[id]/probability. |
| 69 | Funder Relationship Score | BUILT | src/lib/intelligence/relationship-scorer.ts + API routes. |
| 70 | Competitor Intelligence | BUILT | Agent exists and integrated. |
| 71 | Deadline Prediction | BUILT | src/lib/intelligence/deadline-predictor.ts + API route + UI section. |
| 72 | Application Cloning | BUILT | /api/applications/[id]/clone — AI-adapted narrative for new opportunity. |
| 73 | Semantic Funder Matching | BUILT | src/lib/intelligence/semantic-matcher.ts + /research/match page. |
| 74 | Follow-Up Sequences | BUILT | Table + page + src/worker/jobs/process-followups.ts (276 lines, verified) fully implemented. Commit 2f822b1, July 22 2026. |
| 75 | Financial Reconciliation | BUILT | Budgets, expenses, reconciliation reports. API routes. Financials page. |
| 76 | Compliance Calendar | BUILT | compliance_events table + page + API routes. |
| 77 | Multi-Channel Outreach | BUILT — VERIFIED (ToS-safe draft-plus-human-task model, not automated third-party sending) | **Built and live-verified 2026-08-15.** Deliberately not automated LinkedIn/dialing/mail-carrier API calls — no paid API partnership or ToS-compliant automation path exists for any of the three, so each channel generates a real, personalized Claude draft and logs a real `contact_tasks` row (migration 136, applied live) for a human to send/place/mail manually. LinkedIn (`POST /api/contacts/[id]/outreach/linkedin`, ≤300-char connection note) and Call (`POST /api/contacts/[id]/outreach/call`, 4-6 bullet talking points) follow row #118's personalization pattern (`callClaude()` direct, no BaseAgent) adapted from `corporate_prospects` to the CRM `contacts`/`funders` tables via `src/lib/outreach/contact-context.ts`. Mail (`POST /api/contacts/[id]/outreach/mail`) generates letter-body paragraphs via Claude, renders a letterhead PDF (`src/lib/reports/letter-pdf.tsx`, a new `@react-pdf/renderer` template — row #115's actual "Document Assembly Engine" turned out to be a required-doc ZIP bundler, not a PDF generator, so this reuses the dependency, not that route) using the org's real address/mission fields, and uploads to the `documents` Storage bucket (service-role client, path `{orgId}/outreach-letters/...`, fresh signed URL minted per download since it's stored as `asset_path` not a URL). All three wired into a new "Outreach" card on `src/components/contacts/ContactDetail.tsx` (`ContactOutreachPanel.tsx`) — the real send/draft surface for a specific contact; the `/outreach/templates` page's existing `email`/`linkedin`/`phone_script`/`physical_mail` channel enum (migration 082) is a separate reusable-template library, not per-contact generation, and was left as-is. **Live-verified against a real contact** (`Marcus Whitfield`, created this session against the real `1111 FOUNDATION` funder in the real Faith Foundation org, `b1ab7402-...`/`info@faithfoundationsf.org`, not the abandoned `bed3e621-...` dev-pass duplicate) via real authenticated HTTP calls (admin-generated magic-link session, the documented 2026-08-15 technique) against a live dev server: all 3 endpoints returned real, genuinely personalized content (named the contact, funder, and specific FAITH Foundation program names/KB content — not templated boilerplate), all 3 `contact_tasks` rows confirmed via direct `psql` read, the mail PDF downloaded via its signed URL and confirmed a valid `%PDF` file (7,438 bytes), and `PATCH .../tasks/[taskId]` (mark sent) confirmed working. UI confirmed rendering correctly via a real Playwright screenshot of `/contacts/[id]` under that same session. `pnpm tsc --noEmit` and `pnpm run build` both clean. |
| 78 | White-Label Portal | BUILT | Consultant client access. /settings/white-label. Grant/revoke API. |

---

## Platform Vision — 18 Pillars (Features 79–114+)

### Pillar 1: Philanthropic Intelligence Graph
| # | Feature | Status | Notes |
|---|---|---|---|
| 79 | Graph Database Schema | BUILT — VERIFIED | Reconciled 2026-08-07: `pig_nodes`/`pig_edges` confirmed live via direct `psql`/`DATABASE_URL` this session — `to_regclass()` non-null for both, and a direct `count(*)` found 21 real `pig_nodes` rows and 20 real `pig_edges` rows (not "23" — re-counted precisely, correcting an earlier same-day pass's rounder figure). Real creating migration is `src/supabase/migrations/077_intelligence_graph.sql`, not the "Migration 094 tonight" this row previously claimed (094 in the root `supabase/migrations/` tree is unrelated — `094_agent_registry.sql`, row #156). These are the same tables already read/written by AG-19 (row #100), AG-23/AG-32 (row #220) — this row's stale "IN BUILD" text predated those already-BUILT consumer rows, which is why the doc read as internally inconsistent. |
| 80 | Relationship Discovery Engine | NOT-BUILT | Agent RA-01 = AG-23 (Relationship Mapper Agent), confirmed the same concept under two labels per `AGENT_VERIFICATION_LOG.md` "AG-23" (July 30 2026). No file, class, or route implements RA-01/AG-23 under either name — confirmed by repo-wide grep this session, not just re-reading the doc. The one real, substantive implementation of this concept in the repo is filed under a *different* agent number, AG-32 (`relationship-graph-builder-agent.ts`, see row #220) — that file's own header explicitly identifies itself as "the same agent as AG-23, not a distinct agent." **As of 2026-08-03, row #220's agent is BUILT — VERIFIED and wired into a daily incremental schedule** — cross-reference #220, not this row, for real build status. |
| 81 | Relationship Explorer UI | BUILT | Corrected 2026-08-07: not at "/research/graph" (no such route exists in the real nav) — a real force-directed graph view (hand-rolled SVG Fruchterman-Reingold, no new dependency) was added to the already-BUILT `/intelligence/relationship-graph` page (row #220) as a List/Graph toggle, reading the same `pig_nodes`/`pig_edges` rows the existing card-list view already uses (`RelationshipGraphViz.tsx`, `relationship-graph-shared.tsx`). |
| 82 | Path Finder | BUILT — VERIFIED (algorithm correct; UI unreachable for this org today, same known scope gap as row #81) | `src/lib/intelligence/relationship-graph-pathfinder.ts`'s `findShortestPath()` — real weighted Dijkstra over `pig_nodes`/`pig_edges` (`cost = 1/weight` so it favors strong relationships, degenerating to plain BFS-by-hop-count while every real edge weight is still uniform), wired into `RelationshipGraphViz.tsx` as node-picker + Find Path controls, commit `a8e18ff` (2026-08-07). **Live-verified 2026-08-08** (`AGENT_VERIFICATION_LOG.md`, "Reputation Graph UI (queue-35)"): hand-checked against the real 21-node/20-edge graph — a real 1-hop and 2-hop path both matched raw `pig_edges` exactly, plus 4 correct honest-failure cases (empty graph, disconnected pair, unknown id, same start/end). UI-reachability inherits row #81's gap: this org's 20 real edges are all org-level `asset_compatible`, but the graph view's node-picker only ever populates from board-member-sourced edges (zero for this org, 5 consecutive daily runs) — so the controls never render today, not a defect in the algorithm itself. |

### Pillar 2: AI Opportunity Discovery Engine
| # | Feature | Status | Notes |
|---|---|---|---|
| 83 | Discovery Agent Core (AG-17) | BUILT — VERIFIED | **Fully unblocked as of 2026-08-07.** `src/lib/agents/opportunity-discovery-agent.ts` is a full perceive/decide/execute/observe loop with five strategy branches, not a stub. The `agent_type` enum gap (`"ag-17-discovery"`) was fixed 2026-08-02 and re-verified live (30 new opportunities discovered, 20 chained into eligibility scoring, zero errors). The remaining known bug — `perceiveState()` querying `opportunity_probability_scores` with `org_id` instead of `organization_id`, silently zeroing the average-probability baseline and disabling the `federal_shift` branch — was fixed 2026-08-07 (commit `a310651`) and independently re-verified: old query now errors `column "org_id" does not exist`, fixed query returns 169 real rows for the Faith Foundation org. Migration 101's remaining pieces (`'ag-36-learning-network'` enum value, all 4 `org_autonomous_config` toggle columns) were also applied live the same session, confirmed via `information_schema`. Chain-routing (§1.3's `routeQueueItem()` gap) remains fixed. No known blocker remains on this agent. |
| 84 | Morning Digest | BUILT — UNVERIFIED | src/lib/agents/morning-digest.ts (Generation-1 plain function, wired into the 7AM digest pipeline per row #198's "Autonomous Morning Digest," which is BUILT — that row covers the live `AutonomousDigestAgent`/digest pipeline that actually runs). Not independently re-verified in this pass — no entry for this specific file in `AGENT_VERIFICATION_LOG.md`. |
| 85 | Personalized Match Feed | BUILT | `src/lib/intelligence/match-feed.ts` — deterministic mission/program/geography keyword-affinity scoring against the real `organizational_digital_twins` row, blended with AG-15's `opportunity_probability_scores` where one exists. `/api/intelligence/match-feed` + `/intelligence/match-feed` page, 2026-08-07. No Claude call in the ranking path; personalization-confidence banner surfaced separately from the score. Not independently live-verified against real production data in a browser this session — see STATE_OF_THE_BUILD.md for full build detail. |
| 86 | Discovery Preferences | BUILT | Reuses the existing Search Profile Configuration page (`/search-profiles/configure`, migration 011 columns) rather than a new UI. AG-17 (`opportunity-discovery-agent.ts`) now reads `source_type_filters` (source toggle), `focus_areas`/`populations_served` (query augmentation), and `min_amount`/`max_amount`/`excluded_funders` (result filtering) via `src/lib/agents/research/scheduler.ts`'s shared helpers — previously unconsulted. `categories`/`excluded_categories`/`agent_settings`/`eligibility_filters`/`geographic_scopes` remain unwired for AG-17 specifically, reasoning in the agent file's header. See STATE_OF_THE_BUILD.md 2026-08-07. |

### Pillar 3: Corporate Giving Intelligence
| # | Feature | Status | Notes |
|---|---|---|---|
| 87 | Corporate Prospects Table | BUILT — VERIFIED | **Applied live 2026-08-03** (`107_corporate_prospects.sql` → `108`/`109`/`111` RLS hardening, via `psql`/`DATABASE_URL`). Independently re-confirmed 2026-08-07: table exists, 49 real rows. RLS enabled, `anon`/`authenticated` grants revoked (the migration's own "NO RLS, shared reference data" comment was deliberately not followed — see `AGENT_VERIFICATION_LOG.md`'s corporate_prospects entry for why). |
| 88 | NAICS Consumer UI | BUILT | /donor-discovery/discover + NAICS labels. naics-labels.ts. |
| 89 | Google Places Adapter | BUILT | Existing donor discovery pipeline. |
| 90 | Corporate Enrichment Agents EA-01 to EA-10 (AG-20/AG-21 + 8 more) | BUILT — VERIFIED (EA-01/EA-08); BUILT — UNVERIFIED (EA-02–07/09/10) | **Both total blockers resolved.** `corporate_prospects` created and RLS-hardened 2026-08-03 (row #87); the platform `ANTHROPIC_API_KEY` was rotated 2026-08-06. Re-verified live 2026-08-03: `EA01GivingDetectorAgent.run({prospectId})` and `EA08ExecutiveBiographyAnalyzerAgent.run({prospectId})` both complete cleanly against a real SAM.gov-sourced prospect (correctly took the documented no-website graceful path). **The independent accuracy defect found July 30 is unchanged, still real**: both agents' hardcoded candidate-path lists missed real content for 2-3 of 3 test companies (EA-01 1/9 hits, EA-08 0/6) because `StealthEngine.isPlausibleResponse()` doesn't distinguish a branded 404 from real content, and real pages often live on a different subdomain/path than the 2-3 guesses tried — not fixed, a quality gap distinct from the now-resolved build/wiring blockers. EA-02 through EA-07/EA-09/EA-10 still not individually exercised. **Caveat unchanged**: this orchestrator is not called from `worker/index.ts`'s boot sequence — on-demand only. |
| 91 | Propensity Scoring PS-01 to PS-10 (AG-22) | BUILT — VERIFIED | **Fully unblocked 2026-08-06 — first clean run in this project's history.** `PropensityScoringAgent.run()` completed end-to-end against the real Faith Foundation org and a real prospect: `status: "completed"`, `tokens_used: 4634`, all 9 rubric scores (`PS-01`…`PS-10`) genuinely computed via a live Claude completion (not the prior hand-run transcription) and persisted to `corporate_prospects.scores`. Both named blockers (missing table, dead key) are resolved. The `enrichProspect()` unconditional-`enrichment_completed_at` gating defect noted July 30 is unchanged, still real — not touched by either fix. The `UsageMeter`/BYOK-fallback path noted August 6 is now moot for the platform-key path specifically, since the platform key itself works again; `tier_limits`/`queue_controls`/`submission_usage`/`funder_relationships` (migration `052_governance_layer.sql`) remain unapplied, unrelated to this row's now-resolved blocker. |
| 92 | Corporate Giving DNA | BUILT — VERIFIED | **Built and live-verified 2026-08-15**, correcting a prior stale-PLANNED note (a first attempt, commit `f03ec99` 2026-08-07, existed but was never live-verified — `AGENT_VERIFICATION_LOG.md` "q32-003" found it 404ing in production and 500-crashing in local dev, root cause undiagnosed at the time). This session is a genuine rebuild, not a re-test of the old code: the 2026-08-07 version only had a passive `EnrichmentFindings` renderer for a `giving_dna` column nothing ever populated (0 of 49 real rows had it set, per q32-002's preflight count) — there was no generator. Added `src/lib/intelligence/giving-dna.ts` (`generateGivingDna()`, `buildGivingDnaFacts()`) + `POST /api/intelligence/corporate-prospects/[id]/giving-dna` (`requireRole("writer")`, service-role admin client — `corporate_prospects` has no `organization_id`, same precedent as row #87/#97) + a real "Generate/Regenerate Giving DNA" button wired into the existing prospect profile page (`donor-discovery/outreach/prospects/[id]/page.tsx`). Grounding is structural, not just prompted: `buildGivingDnaFacts()` extracts only real, populated columns/keys (identity, EA-01–EA-10 `enrichment`, AG-22 `scores.PS-01`/`ranking`) into a numbered fact list, the system prompt forbids stating anything not on that list, and the persisted `based_on_fields` records exactly which real fields contributed so the claim is checkable against the row, not just the model's word. **Live-verified against 2 real, contrasting `corporate_prospects` rows** (`scripts/verify-giving-dna.ts`, real service-role client, real Claude call, real writes — kept in `scripts/` as the manual-trigger CLI, same precedent as row #99's verify script): (1) "Greater Than Builders, LLC" — only 4 thin facts on file (industry, city, Google rating, place types) — produced an honest "extremely thin" profile that explicitly named the gaps (no leadership contact, no revenue, no giving history) rather than padding with invented specifics; (2) "GOOD HOUSING CONSTRUCTION LLC" (the one real row in the 49-row pool with populated AG-22 `scores`, per q32-002) — the summary correctly cited the real PS-01 score (40/100) and its real Housing-Compatibility sub-score (62) verbatim from `scores.PS-01.rationale`, and `based_on_fields` exactly matched the 3 real facts extracted (`address_city`, `scores.PS-01`, `scores.ranking`) — independently re-confirmed by re-reading `corporate_prospects.giving_dna` directly after the write. Re-running against the same row a second time produced a new `generated_at` (regeneration confirmed, not a no-op). One real, unrelated data-quality artifact surfaced during testing, not caused by this feature: this row's `address_city` is `"YONGIN-SI"` (South Korea) for a Texas-registered LLC — the generator honestly reported what's on file rather than silently normalizing or hiding it. `pnpm tsc --noEmit` and `pnpm run build` both clean. |
| 93 | AutoApply Routing | BUILT | /api/donor-discovery/prospects/[id]/route-to-autoapply |
| 94 | Email Campaign Routing | BUILT | /api/donor-discovery/prospects/[id]/route-to-email |
| 95 | Relationship Mapper RA-01 (AG-23) | NOT-BUILT | Same finding as row #80 — RA-01 and AG-23 are one concept, confirmed by grep to have zero dedicated implementation under either label. **As of 2026-08-03, row #220's real implementation (AG-32) is BUILT — VERIFIED**, enum-fixed, `board_members` column bug fixed, and wired into a daily incremental schedule. Cross-reference #220 for actual build status; this row/#80 should not be read as "nothing exists for this capability," only "nothing exists under the AG-23/RA-01 label specifically." |
| 96 | Change Monitor CM-01 (**AG-42**, renumbered from AG-30 on 2026-08-02) | BUILT — VERIFIED | **Built and live-verified 2026-08-03** (`AGENT_VERIFICATION_LOG.md` "AG-42"), superseding the prior NOT-BUILT finding — `change-monitor-agent.ts`, migration 113's enum value confirmed live. First-ever real run: `foundation_directory`'s 14 real eligible rows checked, `corporate_prospects` gracefully degraded at the time (now resolved separately, row #87), a synthetic detected-change row correctly produced a real diff/severity/chain-queue entry. Wired into `worker/scheduler.ts`'s daily pipeline. One separate, unrelated bug found downstream: the `'foundation-990-enrichment'` chain target this agent queues into fails 100% of the time on a real Railway env-var-naming mismatch — this agent's own detect+queue responsibility is fully discharged correctly, the break is in the chain target's own wiring. |
| 97 | Corporate Marketplace | BUILT | Prospect search UI + filter engine. `/donor-discovery/marketplace` + `GET /api/intelligence/corporate-prospects`, shipped 2026-08-07. Ownership/employee-count/revenue filters are real but currently match little-to-no live data (0-1 of 49 `corporate_prospects` rows have those fields populated) — see STATE_OF_THE_BUILD.md for the per-filter breakdown. |

### Pillar 4: Autonomous Relationship Builder
| # | Feature | Status | Notes |
|---|---|---|---|
| 98 | Relationship Memory | BUILT — VERIFIED | **Corrected 2026-08-11**: prior NOT-BUILT finding (2026-08-07, based on a `to_regclass()` query) was stale or mistaken — `relationship_memory` is confirmed **present** in production this session via a direct `psql \d relationship_memory`: the table, its primary key, an org-scoped RLS policy, and a foreign key to `organizations` are all live. Its migration file (`src/supabase/migrations/076_reputation_intelligence.sql`) has in fact been applied. Per project memory, the table is real+RLS-scoped but currently empty (no rows written yet by either consumer). This unblocks the table-existence half of AG-19's Phase B warm-intro pathfinder (row #100) and AG-18's `ReputationIntelligenceAgent` class (row #148) — see row #100 for AG-19's remaining (separate) unwired-code blocker. |
| 99 | Signal Monitoring | BUILT — VERIFIED (news + 990 only; LinkedIn deliberately excluded) | **Built and live-verified 2026-08-15.** AG-43 (`src/lib/agents/funder-signal-monitor-agent.ts`, migration `137_funder_signal_monitoring.sql`) reuses AG-30 Donor Intent Monitor's real pattern (row #218: grounded web search + Claude-extracted structured signals + deterministic scoring + 60-day dedup), retargeted from corporate-donor-intent to this org's own `funders` CRM. **LinkedIn scope-cut is deliberate, not silently dropped**: no public API exists for monitoring third-party LinkedIn posts without a paid partner integration this project doesn't have, and direct scraping would violate LinkedIn's ToS — a real legal/ToS risk, consistent with `BEHAVIORAL_CONTRACTS.md` §21/§27. Built two real sources instead: (1) news — 3 web searches per funder, 5 relationship-relevant signal types (leadership_change/board_appointment/funding_priority_announcement/program_expansion/public_recognition), distinct from AG-18 Reputation Intelligence's risk-focused taxonomy; (2) 990_filing — deterministic, non-LLM name+state match against `foundation_directory`'s real ProPublica `enrichment.propublica` data, surfacing the funder's real most-recent assets/revenue/expenses (no per-foundation time series exists in this schema, so this cannot detect a year-over-year *change* the way AG-42 does — stated honestly, not fabricated). HIGH-score signals (>=80) bridge into `relationship_memory` for AG-19 to read, same convention AG-18 already established. **Live-verified against the real Faith Foundation org's real funders** (`AGENT_VERIFICATION_LOG.md`, "AG-43 Funder Signal Monitor"): "1011 FOUNDATION INC" and "1111 FOUNDATION" each produced a real `funder_relationship_signals` row whose 990 figures matched a direct `psql` read of `foundation_directory` byte-for-byte (EIN 912168491: $71,073,222 assets; EIN 461463656: $541,826,488 assets), each correctly triggering a real `alerts` notification and `relationship_memory` bridge write; "Walmart" (no `foundation_directory` match) correctly produced zero signals; dedup confirmed (repeat run updated the existing row, no duplicate); the news half genuinely fired real `web_search` calls every run (real Anthropic token spend) and honestly returned no signal above threshold for these 3 funders this run, rather than being skipped or stubbed. `pnpm tsc --noEmit` — 0 errors. |
| 100 | Relationship Builder | BUILT (flagged, default OFF) | Naming correction, July 30 2026 (see AGENT_VERIFICATION_LOG.md "AG-19"): this row's prior name "Recommendation Engine" and status "designed" are both stale. AGENTS_v2.md's canonical name (`RelationshipBuilderAgent`, `agentId: "ag-19-relationship"`, src/lib/agents/relationship-builder-agent.ts) is the accurate one, verified against real code — it is far more than a recommendation engine: Phase A (deterministic relationship scoring + one Claude-written engagement recommendation into relationship_recommendations, matching this row's original scope) plus a substantial, previously-undocumented Phase B (multi-hop warm-introduction pathfinding over pig_nodes/pig_edges, bounded funder-officer web-search research, priority-ranked action queue, 14-day follow-up deadlines). Its agent_type enum value was added by migration 096 (src/supabase/migrations/), closing the AGENTS_v2.md §1.2 enum gap for this agent specifically — but that migration exists only in the src/supabase/migrations tree, not the parallel root supabase/migrations tree (see project memory `benavora-two-parallel-migrations-directories`), so whether it is actually live in production is unconfirmed. **Updated 2026-08-13, superseding the "never wired" framing below and in row #200**: `worker/autonomous-orchestrator.ts`'s `routeQueueItem()` 'funder_relationship' case now checks an org-scoped `platform_config` flag, `feature.relationship_builder_v2`, before falling back to the Gen-1 `FunderRelationshipAgent` path — when that flag's value is the literal string `'true'` for an org, the event routes to a real `RelationshipBuilderAgent.run('event')` call instead. The flag defaults OFF (no `platform_config` row = fallback to Gen-1) for every org that hasn't explicitly had it set, including Faith Foundation — live-confirmed via `src/__tests__/integration/ag19-relationship-builder-flag.test.ts`, test 4 ("Faith Foundation's real org still has no feature.relationship_builder_v2 row and is unaffected by this entire test"), a real query against `b1ab7402-dfc2-4712-869f-70ea3566cc1d`'s `platform_config` rows returning null. The same suite's test 3 live-ran `RelationshipBuilderAgent.run('event')` end-to-end against a disposable test org with the flag set `true` (via the real `routeQueueItem()` entry point, not a direct class instantiation) and confirmed a real `agent_runs` row (`agent_type: 'ag-19-relationship'`, `status: 'completed'`, `error_message: null`) plus a real, correctly org/funder-scoped write to `funder_relationship_scores` — the class is no longer merely reachable-if-called, it has a real, tested, opt-in production trigger path. An owner/admin-only toggle for this flag now exists at `/settings/agents` ("Relationship Builder v2 (Beta)"), backed by `GET`/`PATCH /api/settings/agents/relationship-builder-v2`. **No org's flag was turned on by this work** — see STATE_OF_THE_BUILD.md/SESSION_STATE.md for the explicit note that enabling this for any production org, including Faith Foundation, is a deliberate decision Reid has not yet made. Same orphaned-code pattern as AG-36 (Learning Network Aggregator) no longer applies — this class now has a real caller, gated behind the flag. **Updated 2026-08-11**: the row #98 table-absence caveat is stale — `relationship_memory` is confirmed **present** in production (table, PK, org-scoped RLS policy, and FK to `organizations` all live), so that blocker on Phase B's warm-intro writes is resolved. |
| 101 | Relationship Builder UI | BUILT | /funders/[id]/relationship (new page, first nested [id]/<subpage> detail route in this repo). Real new API route /api/funders/[id]/relationship-builder wires AG-19 RelationshipBuilderAgent (src/lib/agents/relationship-builder-agent.ts) to a manual UI trigger for the first time — real run("manual") call, real Claude spend, no mock. Existing /api/funders/[id]/relationship (Gen-1 event-sourced score) untouched, shown alongside. Not live-click-through verified this session — code-verified against schema and against the queue-31 preflight's already-live-verified agent behavior only. |

### Pillar 5: Grant Probability Engine
| # | Feature | Status | Notes |
|---|---|---|---|
| 102 | Probability Scoring Engine (AG-15, deterministic engine) | BUILT — VERIFIED | Per `AGENT_VERIFICATION_LOG.md` "AG-15" (July 30 2026): stale "IN BUILD/Tonight" wording corrected. `computeGrantProbability()` (`src/lib/intelligence/grant-probability-engine.ts`) compiles clean and was run live, unmodified, against two real, contrasting opportunities in the real production Faith Foundation org — both scores hand-checked factor-by-factor against the function's own weighting and matched exactly, and both persisted for real via its own upsert into `opportunity_probability_scores` (confirmed by reading the row back). This is the real engine behind the manual/batch call sites; it is **not** the same thing as the blocked autonomous wrapper (see row #196). **Fixed 2026-08-07** (commit `a310651`): `buildKeyRisks()`'s bounds-check ordering bug (`days < 15` checked before `days < 0`, making a lapsed deadline read "under 15 days away" instead of "already passed") was reordered correctly and a regression test added — `pnpm vitest run src/__tests__/unit/grant-probability-engine.test.ts` passes 10/10. Never affected the numeric score, only the user-facing risk text. |
| 103 | Probability API Route | BUILT — VERIFIED | `src/app/api/intelligence/grant-probability/route.ts` confirmed to import and call the real `computeGrantProbability()` directly (not a stub), per `AGENT_VERIFICATION_LOG.md` "AG-15." |
| 104 | Batch Score Runner | BUILT — VERIFIED | `scripts/batch-score-opportunities.ts` confirmed to import and call the real engine directly, per `AGENT_VERIFICATION_LOG.md` "AG-15." |
| 105 | Probability Badges on Opportunities | BUILT — UNVERIFIED | `src/app/(dashboard)/opportunities/page.tsx` confirmed by direct read to reference `opportunity_probability_scores`/`overall_score` — wired to real data, not a placeholder. Read-verified only; no browser/screenshot check was performed this session (per `AGENT_VERIFICATION_LOG.md` "AG-15," explicitly flagged as not visually confirmed). |
| 106 | Factor Breakdown UI | BUILT — VERIFIED | Correction, 2026-08-15: this row was stale — a real `ProbabilityBreakdown` component already existed in `src/app/(dashboard)/opportunities/page.tsx` (commit `cac32f3`, on `main`), a "Score Breakdown" toggle per card that renders confidence/recommendation/estimated ROI/time-to-complete plus a progress bar per real `factors[]` entry (labelled via a literal map of the engine's exact 4 factor-name strings) and the real `key_risks`/`key_strengths` arrays — reading only the already-persisted `opportunity_probability_scores` row (row #102/#105), never recomputing client-side. **Live-verified 2026-08-15**: `scripts/verify-factor-breakdown-ui.ts` independently re-ran the real, unmodified `computeGrantProbability()` against a real Faith Foundation opportunity (`5f0128af-113a-42d2-b7c4-67fe4067a8b7`) and diffed every UI-displayed field (score, confidence, recommendation, key_risks, key_strengths, estimated_roi, time_to_complete, and all 4 factors' weight/value/contribution individually) against the stored row the page actually reads — exact match on every field. `pnpm tsc --noEmit` — 0 errors. Not a browser/screenshot check (no visual regression run), but the data-correctness half this task asked for is confirmed, not inferred. |

### Pillar 6: Organizational Digital Twin
| # | Feature | Status | Notes |
|---|---|---|---|
| 107 | Digital Twin Builder (AG-16) | BUILT — VERIFIED | Per `AGENT_VERIFICATION_LOG.md` "AG-16" (July 30 2026): stale "IN BUILD/Tonight" and stale `AGENTS_v2.md` "PLANNED, none live" framing both corrected — `buildDigitalTwin()` (`src/lib/intelligence/digital-twin-builder.ts`) compiles clean, every column it queries was cross-checked against the real migration DDL (no fabricated schema), and a live read of `organizational_digital_twins` for the real Faith Foundation org confirmed a genuine, data-rich, already-built twin (`twin_completeness_score: 70`, real mission statement, real board members with real bios) — not a stub or seed row. Event-driven (KB save, onboarding, manual API), not schedule-driven — the one real gap is no nightly-sweep entry in `worker/autonomous-orchestrator.ts` (confirmed absent by grep). |
| 108 | Digital Twin API | BUILT — VERIFIED | `/api/intelligence/digital-twin` plus two additional real call sites confirmed this session: `src/app/api/knowledge-base/route.ts` (rebuilds twin on KB write) and `src/app/api/onboarding/complete-setup/route.ts` (builds at onboarding). |
| 109 | Digital Twin Profile Page | BUILT — UNVERIFIED | `/intelligence/twin` — page file exists (per row #110's description of what it shows); not visually/browser-verified this session. |
| 110 | Twin-Powered Draft Generation | BUILT | AG-05 draft-generation-agent.ts reads organizational_digital_twins (migration 094) before generating; applications.twin_powered/twin_completeness recorded per draft. /intelligence/twin shows completeness score + section breakdown; /draft-generator/autonomous shows Twin-Powered badge + low-completeness warning. Not independently re-verified this pass — status unchanged from prior session. |
| 111 | Twin Completeness Score | BUILT — VERIFIED | Confirmed live: the real Faith Foundation org's `organizational_digital_twins` row carries a genuine, non-placeholder `twin_completeness_score: 70`, consistent with `buildKeyStrengths()`'s logic run against this org's real counts. |

### Pillar 7: Autonomous Proposal Factory
| # | Feature | Status | Notes |
|---|---|---|---|
| 112 | Narrative Generator | BUILT | Draft Generator existing feature. |
| 113 | Budget Generator | BUILT | Budget Narrative Generator existing feature. |
| 114 | Logic Model Builder | BUILT | Existing feature. |
| 115 | Document Assembly | BUILT | Document Assembly Engine existing feature. |
| 116 | One-Click Proposal Package | BUILT — VERIFIED | `POST /api/proposals/generate-package` (`a471198`) orchestrates Narrative/Budget/Logic Model concurrently then Document Assembly, with honest per-step `{status: success\|failed}` reporting. Live-verified 2026-08-07 (`AGENT_VERIFICATION_LOG.md` "q33-002") via real authenticated HTTP against the real Faith Foundation org: 3 of 4 steps succeeded with real Claude output and real DB writes (narrative, logic model, document assembly), Budget failed. The partial-failure design itself works correctly. Two real, previously-undocumented production defects found and reproduced twice: (1) every AI-config-reading route queried `platform_config` with no `organization_id` filter despite it being a real per-org table (1,080 rows/107+ orgs) — 95/107 orgs' `ai.model` rows held an invalid, 404ing model string, so Narrative/Budget generation was non-deterministically broken for most orgs; (2) `BudgetAgent` was never given a `timeoutMs` override in `/api/ai/budget/route.ts`, so it inherited `BaseAgent`'s 60s default despite the route's own documented need for up to 300s — reproduced as the actual cause of both real Budget failures found 2026-08-07. **Both fixed 2026-08-11**: an explicit `.eq("organization_id", organizationId)` filter was added to every confirmed `platform_config` read/write call site missing one across the codebase (routes, client pages, and shared lib helpers), and `BudgetAgent`'s instantiation in `/api/ai/budget/route.ts` now passes `timeoutMs: 300000`. Regression test added: `src/__tests__/integration/platform-config-org-scope.test.ts`. `pnpm run build` passes clean. |
| 117 | AutoApply Integration | BUILT | Completed proposals route to AutoApply queue. |

### Pillar 8: Corporate Outreach Factory
| # | Feature | Status | Notes |
|---|---|---|---|
| 118 | Personalized Outreach Generator (AG-24) | BUILT — UNVERIFIED | Per `AGENT_VERIFICATION_LOG.md` "AG-24" (July 30 2026): `AGENTS_v2.md`'s "none found, closest analog is AG-11" claim is wrong, not just stale — real implementation exists at `src/app/api/intelligence/outreach/generate/route.ts` (149 lines), genuinely wired into `/donor-discovery/outreach`'s composer UI. It fell through prior audits because they only grepped `src/lib/agents/`, and this is an API route. Structurally, the personalization is genuine (distinct per-prospect industry/location facts and per-org mission/impact KB content are assembled into the Claude prompt, not a name-templated form letter). **Cannot run end-to-end today**: its first query is against `corporate_prospects`, confirmed still 404ing in production (same blocker as row #90). Actual generated output quality was **not** verified — the local `ANTHROPIC_API_KEY` is invalid (`401`), so no live Claude completion was obtained for this route either. |
| 119 | Outreach Sequence Builder | BUILT | Email campaign builder existing feature. |
| 120 | Corporate Outreach UI | PLANNED | One-click campaign generation per prospect. Phase 2. |

### Pillar 9: Donation Recommendation Marketplace
| # | Feature | Status | Notes |
|---|---|---|---|
| 121 | Marketplace Schema | BUILT — VERIFIED | `marketplace_listings`/`marketplace_matches` (migration 125), applied live 2026-08-07. **2026-08-08**: live adversarial testing found the SELECT RLS policies on both tables were mutually circular (each `EXISTS`-checked the other), causing "infinite recursion detected in policy" on every real session-client query against either table — a 100% failure rate for every real HTTP route (browse, create, request/approve), masked in the original build session because only the service-role-only seed script/matcher were tested. Fixed live via migration 127 (`SECURITY DEFINER` helper functions replacing the circular `EXISTS` subqueries) and re-verified end-to-end: 14/15 automated checks pass, full request→approve and request→decline cycles genuinely complete via real per-org RLS-scoped clients. See `AGENT_VERIFICATION_LOG.md` queue-37 entry #1. |
| 122 | Donor Listing UI | BUILT — VERIFIED | `/marketplace` browse page, inline-hex per Directive 4. Broken by the same RLS recursion bug as row #121 until the 2026-08-08 fix (above) — the page's own `GET` calls would have 500'd for every user, every time. Confirmed working after the fix: an org sees its own listings plus any listing it's been matched to, and nothing else (real RLS-scoped browse narrowing, live-tested with 3 real orgs). |
| 123 | AI Match Engine | PARTIAL — rule-based half BUILT/VERIFIED, AI half PLANNED | `src/lib/marketplace/matcher.ts` — category + geographic-text overlap against `search_profiles`, no Claude call, no confidence score. Live-verified 2026-08-08 against real production data: matched a real listing to both a fresh test org and the real, live Faith Foundation org (which genuinely has an active matching `search_profiles` row) — confirmed correct, not a bug. An actual AI/Claude-scored match engine remains unbuilt. |
| 124 | Request + Approval Flow | BUILT — VERIFIED | `PATCH /api/marketplace/matches/[id]` — request/withdraw/approve/decline state machine, no receipt/payment logic. Broken by the row #121 RLS bug until 2026-08-08 (the route's first line is a `select().single()` on `marketplace_matches`, which recursed). Live-verified after the fix: real per-org actors drove suggested→requested→approved (flips listing to `matched`) and suggested→requested→declined (listing correctly stays `active`) end to end, plus confirmed an unrelated third org can neither read nor update the match row (RLS boundary genuinely enforced). |
| 125 | Donation Receipt Generator | PLANNED | No table, no generation logic. Unchanged — correctly not attempted in the 2026-08-07/08 marketplace MVP passes. |

### Pillar 10: National Disaster Response Engine
| # | Feature | Status | Notes |
|---|---|---|---|
| 126 | FEMA Integration | BUILT — VERIFIED live 2026-08-07, correcting the 2026-07-30 entry | The 2026-07-30 "confirmed real" claim was a code-reading confirmation, not a live one. Live-verifying row #130 this session found `pollFEMADeclarations()`'s hardcoded FEMA URL 404'd on every real call (`disasterDeclarationsSummaries`, lowercase — the real OpenFEMA v2 endpoint requires `DisasterDeclarationsSummaries`, capital D) and `disaster_declarations` did not exist in production at all — meaning this function had never once succeeded live before this session. Both fixed and re-verified: a real poll now inserts real, current FEMA declarations. See `AGENT_VERIFICATION_LOG.md` "Auto-Deploy Response (row #130)". |
| 127 | Emergency Fund Database | BUILT — VERIFIED live 2026-08-07, correcting the 2026-07-30 entry | `disaster_emergency_funds` did not exist in production at all as of 2026-08-07 (confirmed via `information_schema.tables`) — the 2026-07-30 "confirmed by direct read" was a migration-file read, not a live-table check. Table created live this session (migration 079, plus RLS/no-permissive-policy hardening the original migration file lacked). Matching logic re-verified live against a real declaration: correctly returns 0 matches against the now-real-but-still-empty table (honest empty-table result, not a bug). See `AGENT_VERIFICATION_LOG.md` "Auto-Deploy Response (row #130)". |
| 128 | Disaster Response Agent (AG-25) | BUILT — VERIFIED live 2026-08-07 (manual-trigger + now also scheduled, see row #130) | Base finding unchanged (two plain functions, no `agent_runs`/`agent_decisions` row of their own) but "BUILT — VERIFIED" is corrected from a code-read confirmation to a live one: `deployDisasterResponse()` was actually invoked live this session (via the new row #130 pipeline) and produced a real `alerts` row + a real `disaster_declarations.response_deployed` flip. Also confirmed still true: the on-disk literal `"ag-25-deadline-prediction"` belongs to a completely different, unrelated agent (`DeadlinePredictionAgent`). **Now also reachable unattended** — see row #130, a real daily 5:45 AM CST scheduler job now exists (`worker/scheduler.ts`), superseding this row's "manual-trigger only, no schedule" qualifier. |
| 129 | Disaster Response Dashboard | BUILT — UNVERIFIED | `/intelligence/disaster` page confirmed to exist at the expected path; not independently verified whether it calls the route correctly beyond file existence. |
| 130 | Auto-Deploy Response | BUILT — VERIFIED live 2026-08-07 | `worker/autonomous-orchestrator.ts`'s `runDisasterResponsePipeline()`, a real daily 5:45 AM CST `worker/scheduler.ts` job, `org_autonomous_config.auto_deploy_disaster_response` (migration 124, default false), and a real approve-to-deploy branch in `PATCH /api/autonomous/decisions`. Live-verified end-to-end against real, current FEMA disaster data and a real dedicated test org: toggle off → a real `agent_decisions` row (`action_taken: pending_approval`, `required_human_review: true`), zero `alerts`/deploy side effects; toggle on (applied via the same upsert the real API route performs — an actual authenticated HTTP `PATCH` call could not be made this session, see caveat below) → a real `deployDisasterResponse()` call, a real `alerts` row, `disaster_declarations.response_deployed` genuinely flipped. Getting there required fixing 3 real, previously-undocumented blockers found live this session (all now fixed): `org_autonomous_config` missing 6 columns (breaking the *entire* `/api/autonomous/config` route, not just this toggle), `disaster_declarations`/`disaster_emergency_funds` not existing in production at all, and a one-character FEMA URL casing bug that meant `pollFEMADeclarations()` had never once succeeded live — see rows #126–128. **Caveats:** the literal HTTP `PATCH /api/autonomous/config` call and the unattended 5:45 AM scheduled firing were not directly observed this session (sandbox blocked local dev-server startup for the HTTP test; no fast-forward mechanism existed for the schedule) — wiring for both was confirmed by direct code read and, for the pipeline logic itself, by manual invocation of the real, unmodified function. Full detail: `AGENT_VERIFICATION_LOG.md` "Auto-Deploy Response (row #130)". |

### Pillar 11: Predictive Funding Forecast
| # | Feature | Status | Notes |
|---|---|---|---|
| 131 | Forecast Schema | BUILT — VERIFIED | Updated 2026-08-03: `funding_forecasts` (migration 078, RLS added migration 105) now has a real producer (row #132) and a `UNIQUE(org_id, forecast_date, forecast_period)` constraint (migration 110, `src/supabase/migrations/`), added as part of AG-26's own build task per its spec's explicit idempotency requirement — confirmed live via `pg_constraint` before and after. `strategic-advisor-agent.ts` (AG-40) remains the one confirmed downstream consumer. |
| 132 | Forecast Agent (AG-26) | BUILT — VERIFIED | **Live-execution-tested 2026-08-03** (`AGENT_VERIFICATION_LOG.md` "AG-26"), superseding the "not yet tested" note below it. Every dimension confirmed against real production data: both `90_day`/`12_month` rows write in one run; neutral-fallback math for partially-scored windows matches a hand-computed reproduction to full decimal precision; the zero-opportunity-org branch writes an honest `$0` row with explicit `null` confidence, not a skip; idempotency confirmed via byte-identical `id`/`created_at` across two runs. `AG-40`'s (Strategic Advisor) real cross-agent read of this output was also directly exercised and confirmed. New on-demand trigger route added 2026-08-07: `POST /api/reports/forecast` (`GET` for the read side), independent of `runFundingForecastMonthlyPipeline()`'s 1st-of-month cron gate. **Narrative-synthesis open item re-tested live 2026-08-07, resolved differently than expected**: the dead-platform-key cause is confirmed gone (raw Anthropic API call and an isolated `callClaude()` call both succeed on the current key). But two real, fresh runs against the real org still wrote empty `key_risks`/`key_opportunities`/`recommended_actions` — root-caused by direct reproduction, not guessed: `generateNarratives()`'s Claude call hits `stopReason: "max_tokens"` (capped at `NARRATIVE_MAX_TOKENS = 900` in `funding-forecast-agent.ts`) given this org's now-larger real pipeline (77-79 open opportunities vs. 42-44 on 2026-08-03), truncating the JSON response mid-string; the resulting `JSON.parse` failure is caught by the same try/catch that also used to catch the dead-key error, so both failure modes produce an identical "(narrative synthesis unavailable this run.)" methodology suffix with no way to distinguish them from the stored row alone. **Not fixed in this pass** (out of this task's scope) — flagging precisely for a future session: raise `NARRATIVE_MAX_TOKENS` or trim the per-period opportunity slice in `buildNarrativePrompt()`. |
| 133 | Forecast Dashboard | BUILT (code) — NOT DEPLOYED | **Live-verified 2026-08-07** (`AGENT_VERIFICATION_LOG.md` "AG-26 / Forecast Dashboard"): both `src/app/(dashboard)/reports/forecast/page.tsx` and `src/app/api/reports/forecast/route.ts` are real, correctly placed/named (matching the live `reports/roi`/`.../simulate` sibling convention exactly), compile clean (`pnpm tsc --noEmit`), and are on `main`/pushed to `origin/main` — but both return a genuine, build-manifest-level `404` in production (`x-matched-path: /404`, `x-next-error-status: 404`) under a real authenticated session for the real Faith Foundation org owner, while sibling pages (`/dashboard`, `/reports/roi`, `/reports/simulate`) all return real `200`s in the same session. Root cause is a pending `vercel --prod` deploy, not a code defect — the identical failure shape already documented for row #160 (Agent Log Viewer). The real `funding_forecasts` data underneath is confirmed current (4 real rows, 2026-08-03 and 2026-08-07, per row #132) but **could not be confirmed to actually render** — the page never loads. Do not upgrade to `BUILT — VERIFIED` until a session with working deploy access ships `main` and this exact check (page load, numbers matching the DB, honest-narrative-fallback rendering, and a real "Run Forecast" button click) is re-run. |
| 134 | Market Trend Intelligence | BUILT — VERIFIED (data computation), deploy/browser-render unconfirmed | **Scope correction, 2026-08-07 (commit `c9b001f`):** built as an opportunity-volume-over-existing-data view (`/api/intelligence/trends` bucketed by month/category/source_type from this org's own `opportunities`, plus a secondary `intelligence_funded_proposals` by-award-year panel on `/reports/funding-summary`), not this row's original "federal budget + foundation trend analysis" concept — stated explicitly in the route's own header comment, not silently narrowed. **Live-verified 2026-08-07** (`AGENT_VERIFICATION_LOG.md` "Market Trend Intelligence"): the route's real, unmodified bucketing logic run against the real 219-opportunity Faith Foundation org matches an independent hand-run SQL `count(*) ... group by` exactly for all 3 real months on file, including the category/source_type breakdowns; the empty-state path was independently confirmed against a real 0-opportunity org. **Not confirmed:** the actual HTTP route call or browser-rendered chart — local dev-server startup was blocked in-session (sandbox permission), and production's deployment status for this commit is unconfirmed (no `x-matched-path` check performed, matching row #133's own documented gap pattern for undeployed-but-correct code). Re-verify with a page load + `x-matched-path` check before upgrading further. |

### Pillar 12: AI Board Advisor
| # | Feature | Status | Notes |
|---|---|---|---|
| 135 | Board Members Schema | BUILT — VERIFIED | Reconciled 2026-08-07: `board_members` is not new — it has existed since Phase 1 (`supabase/migrations/001_initial_schema.sql`) and carries 8 real rows live (3 for Faith Foundation alone — Reid Whitesides, Pastor Juan Valdez, Scott Ellis — confirmed in AG-27's 2026-08-03 pre-flight check). `board_meetings` is the genuinely new table this row describes, added by `src/supabase/migrations/078_forecast_board.sql` (RLS added migration 105) — confirmed live via direct `to_regclass()` this session; AG-27's live-verification pass independently confirmed its column set matches consuming code exactly and exercised a real insert/read/delete cycle against it. |
| 136 | Meeting Packets Schema | BUILT — VERIFIED | Reconciled 2026-08-07: `board_meeting_packets` (also `src/supabase/migrations/078_forecast_board.sql`, RLS migration 105) confirmed live via direct `to_regclass()` this session; `UNIQUE(meeting_id)` constraint added by AG-27's own migration (`src/supabase/migrations/111_ag27_board_packet.sql`), confirmed present via `pg_constraint` in AG-27's 2026-08-03 live-verification pass alongside a real end-to-end packet write/read (see row #137). Current live row count is 0 — reflects that AG-27's synthetic test meeting/packet were deleted after verification per this project's standing cleanup convention, not that the table has never been exercised. |
| 137 | Board Packet Agent | BUILT — VERIFIED (Claude discussion-item synthesis separately blocked at test time by the since-fixed dead API key) | **Built and live-verified 2026-08-03** (commit `8943220` build, commit `1a14129` verify, `AGENT_VERIFICATION_LOG.md` "AG-27"), superseding PLANNED. `src/lib/agents/board-packet-agent.ts`, class `BoardPacketAgent extends AutonomousAgent`, `agentId: "ag-27-board-packet"` — confirmed live in the `agent_type` enum this session (migration 111). **Correcting a citation**: wired into `worker/autonomous-orchestrator.ts` (not `worker/scheduler.ts`) two ways, confirmed by direct read this session — a dedicated daily-schedule primary trigger (`runBoardPacketDailyPipeline()`, 2AM CST sweep, scoping `board_meetings` with `status='scheduled'` in the `[today, today+2]` window and no existing packet) and an `agent_queue` event-chained safety-net case (`'ag-27-board-packet'`) for short-notice meetings, plus a manual trigger route (`/api/autonomous/board-packet-trigger`). Live-verification ran the real, unmodified daily pipeline against a real (temporary, deliberately edge-of-window) test meeting for the real Faith Foundation org: correctly scoped, correctly wrote a packet with 2/3 sections real data (pipeline summary: 42 real open opportunities; financial snapshot: real `annual_budget`/staff counts) and 1/3 a genuine, correctly-detected first-meeting/no-outcomes fallback (not fabricated) — cross-checked against `agent_decisions.action_payload`'s own self-reported tally. One genuine defect found and documented, not fixed: `buildPipelineSummary()`'s deadline filter has no lower bound (`lte(deadline, windowEnd)` only), so already-past-deadline opportunities can appear in the pipeline summary. Claude-generated `recommendedDiscussionItems` could not be verified — blocked at test time by the same dead local `ANTHROPIC_API_KEY` documented elsewhere in this doc, root-caused via a direct isolated Anthropic API call, not inferred; not yet re-tested against the platform key rotated in commit `8f3aa06` (2026-08-04). |
| 138 | Board Member Portal | BUILT (honest Phase 1 scope, not a self-service login portal) | /board/[id] + GET /api/board/[id], 2026-08-07. Reachable by an already-authenticated org staff user (viewer role+), not a board member logging in as themselves — board_members has no auth-identity column (no user_id/profile_id/login-token) and the live user_role enum has no board_member role, so real board-member self-service login is a separate, larger follow-on project, not built here. Shows one board_members row's profile plus ALL of that org's board_meeting_packets (org-scoped via board_meeting_packets.org_id), not per-meeting-attendee-scoped, since no invite/attendee join table linking a board_members.id to a specific board_meetings.id exists anywhere in the schema. Renders AG-27's real packet_content shape (pipelineSummary/outcomesSinceLastMeeting/financialSnapshot/recommendedDiscussionItems). Linked from the board members list at /knowledge-base/profile. Also fixed in this pass: /api/intelligence/relationship-graph/route.ts was querying board_members with `.eq("org_id", ...)` — board_members' real column is organization_id, not org_id (org_id only exists on the newer board_meetings/board_meeting_packets tables) — this silently zeroed every org-scoped connection read and 404'd every edge-ownership check; fixed to organization_id. |
| 139 | Plain Language Financials | BUILT | Real `plainLanguageFinancials` key on `board_meeting_packets.packet_content` (board-packet-agent.ts, AG-27): aggregates real `grant_budgets`/`grant_expenses`/`grant_reconciliation_reports` (organization_id-scoped, migrations 084/089), Claude-narrated into 2-4 jargon-free sentences with a `groundedFacts` array tracing every sentence to a real aggregate, same discipline as the packet's `groundedIn` discussion items. Rendered as a new section on the existing packet card at `/board/[id]` (q34-002) — not a new page/route. Degrades to an honest "no financial data on file yet" note (no Claude call) when the org has zero real budget/expense/reconciliation rows. Distinct from and additional to `financialSnapshot` (organizations.annual_budget), which is unchanged. |

### Pillar 13: Community Impact Simulator
| # | Feature | Status | Notes |
|---|---|---|---|
| 140 | Simulation Schema | BUILT — VERIFIED | Reconciled 2026-08-07: `impact_simulations` confirmed live via direct schema query; actively written by AG-41's real 2026-08-03 scenario runs. |
| 141 | Simulation Agent | BUILT — VERIFIED (manual-trigger only, no schedule) | **Built and live-verified 2026-08-03** (`AGENT_VERIFICATION_LOG.md` "AG-41"), superseding PLANNED. Agent **AG-41** (renumbered from AG-28 on 2026-08-02; AG-28 is now permanently AG-28 Autonomous Follow-Up Generator, row #202; see `AGENTS_v2.md` §1.4). `src/lib/agents/impact-simulation-agent.ts`, class `ImpactSimulationAgent extends AutonomousAgent`, `agentId: "ag-41-impact-simulation"` (confirmed live in the `agent_type` enum this session, migration 112). Reachable only via `POST /api/agents/simulate` (`requireRole("writer")`) — confirmed by repo-wide grep this session, zero references anywhere in `worker/` — same manual-trigger-only pattern as AG-25's Disaster Response half (row #128), not a scheduled agent. The 4 real scenario invocations (`budget_cut` ×2, `program_expansion`, `gain_funder`) against the real Faith Foundation org hand-checked deterministic math to full decimal precision against real `funding_forecasts`/`organizations` data (exact match both times), confirmed genuine idempotency (2 identical-input runs produced 2 distinct rows, not deduped — no UNIQUE constraint on `impact_simulations`, see row #140), and confirmed `gain_funder`'s confidence is correctly hardcoded `"low"`/40, correctly force-triggering `required_human_review: true` via the shared base-class hard limit. Narrative synthesis (`keyRisks`/`keyOpportunities`/`exposedPrograms`) degraded gracefully rather than fabricating at test time on 2026-08-03, blocked by the same dead local `ANTHROPIC_API_KEY` documented elsewhere in this doc. **Re-tested live 2026-08-07 against the rotated platform key** (`AGENT_VERIFICATION_LOG.md`, second "AG-41" entry): a new real `lose_funder` invocation (the one scenario type never exercised 2026-08-03, against a real funder with zero outcomes/pipeline, correctly `$0` deterministic impact) produced a genuinely populated `keyRisks`/`keyOpportunities`/`narrative` — no `narrativeUnavailable` fallback — independently confirmed by two isolated `POST /v1/messages` calls to the real Anthropic API (a 404 on a retired model, distinguishing "key valid" from "key invalid," then a clean 200 completion on the real `DEFAULT_MODEL`). **Narrative synthesis is no longer blocked; all 4 `SCENARIO_TYPES` now have live-confirmed coverage.** Not to be confused with the real, different, already-BUILT AG-37 Simulation Agent in `AGENTS_v2.md`'s Phase 2-5 section (row #224). |
| 142 | Simulator UI | BUILT — VERIFIED | `/intelligence/simulate` built 2026-08-07, **live-verified the same day** (`AGENT_VERIFICATION_LOG.md`, "AG-41 / Simulator UI" entry) through a real authenticated browser session (Playwright + a genuine Supabase admin-issued magic-link session for the real Faith Foundation owner, no password read/changed) against the real Next.js dev server. All 4 scenario types were run through actual UI interactions — funder dropdown (`lose_funder`), number inputs (`gain_funder`, `program_expansion`), percentage slider (`budget_cut`) — each producing a real, network-captured `POST /api/agents/simulate` call (200, real response body) rather than a mock. Every rendered field (deterministic impact, confidence badge, `baselineUsed`, key risks/opportunities, real Claude-generated narrative, `exposedPrograms` for `budget_cut` naming the real on-file "Down Payment Assistance Program" KB entry) was cross-checked byte-for-byte against a direct `impact_simulations`/`agent_decisions`/`agent_runs` database read and matched exactly. `gain_funder`'s low-confidence/human-review callout renders visibly and was confirmed consistent with (though not directly sourced from) the database's independently-forced `agent_decisions.required_human_review: true`. The pre-rotation `narrativeUnavailable` degraded state was also confirmed still rendering correctly on an old (2026-08-03) row, alongside real narrative text on the 4 new rows — both UI states observed side by side, not inferred. Talks to `POST /api/agents/simulate` (AG-41) exclusively — never `/api/reports/simulate` (the different, already-BUILT AG-37 page at `/reports/simulate`, row #224). `pnpm tsc --noEmit` — 0 errors. |

### Pillar 14: Funding Gap Analyzer
| # | Feature | Status | Notes |
|---|---|---|---|
| 143 | Eligibility Gap Detection | BUILT | Eligibility scoring existing feature. |
| 144 | Narrative Gap Analysis | BUILT | Per-opportunity KB completeness scoring vs a specific funder's own listed requirements, at `GET /api/opportunities/[id]/narrative-gap-analysis` (`src/lib/intelligence/narrative-gap-analysis.ts`). Extends AG-11 (Knowledge Gap Agent, row #211) rather than duplicating it — reuses AG-11's own `knowledge_base` presence query (extracted to `src/lib/agents/knowledge-base-completeness.ts`) but scores against a per-opportunity relevant-category subset (best-effort keyword match on `opportunities.required_documents`/`eligibility_requirements`, falling back to all 10 standard categories when nothing narrows it — no real per-funder requirement schema exists in the schema to key off instead) rather than AG-11's org-wide, always-all-10-categories weekly sweep. Request-scoped read, no `agent_runs`/`agent_decisions` writes. |
| 145 | Geographic Gap Detection | BUILT — VERIFIED (1 confirmed false-positive defect) | Portfolio-wide scan of this org's open opportunities (`src/lib/intelligence/geographic-gap-analysis.ts`, capped at 30 soonest-deadline first) flagging funders/opportunities whose stated geographic text doesn't overlap the org's own `organizations.service_area`. Text-based/best-effort by necessity — neither `funders.geographic_focus` nor `opportunities.geographic_restrictions` is structured geospatial data in this schema, both are free text — so this is bidirectional keyword/substring overlap with a national-keyword allowlist ("nationwide," "no restriction," etc.), not a verified geographic determination; the API's `methodology` field and the UI both say so explicitly. Deliberately reads `organizations.service_area` (singular text) directly rather than `organizational_digital_twins.service_areas` (plural array) — confirmed live in `digital-twin-builder.ts` that the twin's array is *derived* from this same column split on commas, not an independent richer source, matching the precedent AG-30 already set. Live-verified 2026-08-07 against the real Faith Foundation org (`AGENT_VERIFICATION_LOG.md` "q33-003/004"): of 2 real mismatches found, 1 is a confirmed false positive — `NATIONAL_KEYWORDS` doesn't cover "50 states" (without "all") or "domestic," so a real, plainly-nationwide funder ("Domestic (50 states, DC, and US territories)") gets flagged as a Texas mismatch. Not fixed as part of the verification pass. |
| 146 | Gap Recommendations | BUILT | Synthesis/display layer (`src/lib/intelligence/gap-recommendations.ts`, `GET /api/intelligence/gap-analysis`, `/intelligence/gap-analysis` page) combining row #144's per-opportunity Narrative Gap Analysis with row #145's Geographic Gap Detection into one concrete next action per flagged gap — e.g. `Add a "impact" Knowledge Base entry — specific outcome numbers and beneficiary counts...` for a narrative gap, or a named-funder eligibility-verification warning for a geographic mismatch. Not a new autonomous agent — no `agent_runs`/`agent_decisions` writes, request-scoped read only, mirroring AG-11's concrete-not-vague suggestion style with static (not Claude-generated) per-category tips since this is a UI display layer, not an autonomous decision needing its own reasoning trail. Narrative half capped at 15 opportunities (N+1 query cost per opportunity) vs. row #145's 30-cap geographic scan; both caps and any truncation are stated in the UI, never silent. |

### Pillar 15: Reputation Intelligence
| # | Feature | Status | Notes |
|---|---|---|---|
| 147 | Reputation Signal Schema | BUILT — VERIFIED | `reputation_signals` + `reputation_alerts` confirmed real and actively written by the live nightly path, per `AGENT_VERIFICATION_LOG.md` "AG-18" (July 30 2026). |
| 148 | Reputation Agent (AG-18) | BUILT — VERIFIED (plain-function path only) | Per `AGENT_VERIFICATION_LOG.md` "AG-18": the plain function `checkEntityReputation()` (`src/lib/intelligence/reputation-agent.ts`) is confirmed live and unchanged — nightly `runReputationStep()` (sampled to 5 funders/night) inserts `reputation_alerts` and escalates critical/high severity to an immediate alert; no `agent_decisions`/`relationship_memory` write on this path. **A second, undocumented implementation was found in the same file**: `ReputationIntelligenceAgent extends AutonomousAgent` (`agentId: "ag-18-reputation"`), which does log decisions and writes `relationship_memory` for HIGH/CRITICAL signals — but repo-wide grep confirms it is never instantiated anywhere, real but orphaned code (see row #199 for the registry-row correction this caused). Also confirmed: the manual `agent_queue` case `'reputation'` produces materially different, weaker behavior (no alert row, no notification, no org-scoping) than the nightly sweep — same function, different downstream effect depending on trigger path. |
| 149 | Reputation API | BUILT — VERIFIED | `/api/intelligence/reputation` confirmed as one of exactly three real call sites of `checkEntityReputation()`. |
| 150 | Reputation Monitor UI | BUILT — UNVERIFIED | `/intelligence/reputation` page — not independently visually verified this session. |
| 151 | Auto-Monitor on Add | BUILT — VERIFIED | All 5 real production funder-insert call sites (`FunderForm.tsx`, `api/funders/import` bulk CSV, `foundations/page.tsx`'s single/bulk import, `intelligence/recommendations/page.tsx`'s `handleAdd`) now enqueue a real `agent_queue` row via `enrollInReputationMonitoring()` / the new `POST /api/funders/enroll-monitoring` route (org-derived, cross-org-id-forgery-checked, capped at 25/request), commit `11dd99e` (2026-08-07). Also fixed the weak `'reputation'` queue case to route funder-scoped events through `ReputationIntelligenceAgent.runForFunder()` (full alert/notification/`relationship_memory` parity with the nightly sweep) instead of a bare no-audit-trail check. **Live-verified 2026-08-08** (`AGENT_VERIFICATION_LOG.md`, "Reputation Graph UI (queue-35)"): real `funders` inserts mimicking both the manual and bulk-import payload shapes produced real `agent_queue` rows the live worker picked up and completed in ~30s, confirmed via real `agent_runs` rows (`agent_type: 'ag-18-reputation'`) — both insert paths work end-to-end, not just compile-clean. |

### Pillar 16: Executive Command Center
| # | Feature | Status | Notes |
|---|---|---|---|
| 152 | Command Center Page | BUILT — UNVERIFIED | **Corrected 2026-08-12** (`DATED_VERIFICATION_2026-08-12.md`): the "(599 lines)" citation and "using `createAdminClient()`" (stated as happening inside this file) are both stale — `wc -l "src/app/(dashboard)/command-center/page.tsx"` returns **111**, not 599; `grep -n "createAdminClient" "src/app/(dashboard)/command-center/page.tsx"` returns zero matches. `git log --oneline -- "src/app/(dashboard)/command-center/page.tsx"` shows the file was rewritten in commit `1fc87fa` (2026-08-07) into a thin 111-line shell that delegates data-fetching to `src/lib/command-center/snapshot.ts` (`createAdminClient()` confirmed present there, lines 7/43) and rendering to `CommandCenterLive.tsx` (see row #153's real-time findings). The functional substance is still accurate, re-confirmed live this session: `grep -n 'checkPermission(user.id, "owner"'` still matches line 39, and all 7 nav-rail links (`/admin/orgs`, `/admin/system`, `/admin/monitor`, `/admin/audit-log`, `/admin/sales-outreach`, `/admin/autoapply-ops`, `/admin/improvements`) are still present verbatim. — *Prior text, reconciled 2026-08-07, now stale on the specifics above:* `src/app/(dashboard)/command-center/page.tsx` (599 lines) confirmed real by direct read — a genuine `checkPermission(user.id, "owner", supabase)` gate with redirect (`/dashboard?notice=owner_required`), **owner-only, not "owner/admin"** (correcting this row's own prior wording) — using `createAdminClient()` for real cross-org queries (same precedent as `/admin`), with a real nav rail linking to the 7 admin routes above. Not a stub. Not browser/screenshot-verified this session. |
| 153 | Real-Time Panel Updates | BUILT — BLOCKED (VERIFIED) | Real `postgres_changes` subscriptions wired in `CommandCenterLive.tsx` against `agent_runs`/`agent_decisions`/`applications` (owner-gated Command Center, row #152), commit `1fc87fa` (2026-08-07), with a 60s safety-net poll as a fallback. **Live-verified 2026-08-07** (`AGENT_VERIFICATION_LOG.md`, "AI Board Advisor / Command Center (queue-34)") that it does **not currently work in production**: the `supabase_realtime` publication has **zero member tables database-wide**, confirmed via `pg_publication_tables` — a subscribed channel received zero events after a real `agent_runs` insert during a live 20-second listen window. Only the 60s poll actually refreshes this page today. One-line fix identified, not yet applied: `ALTER PUBLICATION supabase_realtime ADD TABLE agent_runs, agent_decisions, applications;`. Code is correct; production is not receiving realtime events until that DDL runs. |
| 154 | Configurable Panel Layout | BUILT | Native HTML5 drag-and-drop reorder of the Command Center's 5 real sections (stat row, AI Pipeline Status, Data Intelligence Status, Most Active Orgs, Recent Agent Runs table), persisted per-owner on `profiles.command_center_layout` (migration 131, jsonb) via `/api/command-center/layout` GET/PUT. All 5 live in one CSS grid so default order still renders the original 3-panel side-by-side row. |
| 155 | TV/Projector Mode | BUILT | Real `element.requestFullscreen()` toggle (not CSS-only) on the Command Center's panel-content wrapper, with a `fullscreenchange` listener so exiting via Escape stays in sync. Larger type + fewer columns/columns-hidden in the stat row and table; header/Admin Quick Actions excluded for free since Fullscreen API only renders the target subtree. |

### Pillar 17: Agent Marketplace
| # | Feature | Status | Notes |
|---|---|---|---|
| 156 | Agent Registry Schema | BUILT — VERIFIED | **Corrected 2026-08-12** (`DATED_VERIFICATION_2026-08-12.md`): "both currently empty, 0 rows each" is stale — `SELECT count(*) FROM agent_registry;` / `SELECT count(*) FROM agent_configurations;` both return **43** live, populated by a real seed script (`scripts/seed-agent-registry.ts`, commits `69578b4`/`b1a91dd`, post-dating the 2026-08-07 reconciliation below) — see row #157's corrected status. Table-existence claim otherwise unchanged. — *Prior text, reconciled 2026-08-07, now stale on the row count:* `agent_registry`/`agent_configurations` confirmed live via direct `to_regclass()` this session (`supabase/migrations/094_agent_registry.sql`, root tree — this is the real 094; the src tree's own, different `094` migration is unrelated, see row #79's correction). Both tables exist. |
| 157 | Registry Seed Data | BUILT — VERIFIED | **Corrected 2026-08-12** (`DATED_VERIFICATION_2026-08-12.md`): the NOT-BUILT finding below is stale. Live `SELECT count(*) FROM agent_registry;` returns **43** real rows, not zero — populated by a real seed script (`scripts/seed-agent-registry.ts`) that did not exist at the time of the 2026-08-07 correction; `git log --oneline -- scripts/seed-agent-registry.ts` shows the seeding commits `69578b4`/`b1a91dd`, both post-dating that correction. Independently re-confirmed via `AGENT_VERIFICATION_LOG.md`'s "Agent Marketplace + Agent Log Viewer (q27-001/002/003)" entry, which read the 43 real rows back through a real authenticated `GET /api/agents/registry` call against production. — *Prior text, correction dated 2026-08-07, now stale:* **Correction, 2026-08-07: the "16 agents seeded" claim is false, and the gap is more specific than "never run."** Direct live `count(*)`: `agent_registry` has zero rows. The seed content exists only as a static, never-executed TypeScript array, `src/lib/agents/agent-registry-seed.ts` (17 entries). No seed script targets this table either. |
| 158 | Registry API | BUILT — VERIFIED | **Corrected 2026-08-12** (`DATED_VERIFICATION_2026-08-12.md`): "returns real but currently-empty data" / "`{ agents: [] }` for every org" is stale, directly following from row #157's own corrected status — `agent_registry`/`agent_configurations` now hold 43 real rows each (live `count(*)`, re-confirmed this session), so the route genuinely returns real per-agent data today, not an empty array. Route code itself not re-read this pass beyond the counts, since the counts alone settle the claim; independently re-confirmed via a real authenticated HTTP call in `AGENT_VERIFICATION_LOG.md`'s q27 entry. — *Prior text, reconciled 2026-08-07, now stale:* `/api/agents/registry` (`GET`) confirmed by direct read to query the real `agent_registry`/`agent_configurations` tables (viewer-role gated, org id server-derived) — not a stub. Given row #157, it currently returns `{ agents: [] }` for every org in production, which is real, correct behavior given the live data state, not a code defect. |
| 159 | Agent Marketplace UI | BUILT — VERIFIED | **Corrected 2026-08-12** (`DATED_VERIFICATION_2026-08-12.md`): the "not browser/screenshot-verified this session — deferred to q27-004" caveat below is stale — q27 has since run. `AGENT_VERIFICATION_LOG.md`'s "Agent Marketplace + Agent Log Viewer (q27-001/002/003)" entry used a real, GoTrue-validated Supabase session (no password read or changed) driving a real Playwright/Chromium browser against production: the page rendered 43 real agent cards (confirmed by both DOM query and a visually-inspected screenshot), a real toggle click persisted through a full page reload and an independent service-role re-query, and two previously-undocumented schema-drift bugs (`agent_configurations.organization_id` missing live; `agent_registry.avg_tokens_per_run` missing live — both broke `GET`/`PATCH /api/agents/registry(/configure)` for every org until fixed) were found and fixed live in the same pass. — *Prior text, built 2026-08-07, wiring/build details unchanged:* **Built 2026-08-07**, closing the prior NOT-BUILT correction below (kept for history). Real page at `src/app/(dashboard)/agents/marketplace/page.tsx` (`/agents/marketplace`), distinct from `/settings/agents` (row #213, the unrelated Autonomous Settings Panel). Confirmed by direct read: calls `GET /api/agents/registry` on mount and renders every returned agent (name, `agent_id`, description, plan badge, trigger-type + cron badge, inactive badge, last-run relative time, run count); enable/disable toggle wired to `POST /api/agents/registry/configure` with optimistic update + rollback on failure. Distinct loading/401/403/500/empty states, not happy-path only. Real nav entry added to `src/components/layout/nav-items.ts` (`Bot` icon, reused from the Platform admin section) — reachable through the UI, not URL-only. `pnpm tsc --noEmit` clean on both edited files. — *Earlier prior text, kept for history:* **Correction, 2026-08-07: the prior "BUILT, 775-line page confirmed present" conclusion for this row conflated two different features at the same URL.** `src/app/(dashboard)/settings/agents/page.tsx` (775 lines) is real, but by direct read it is entirely the already-and-correctly-documented **Autonomous Settings Panel (row #213)** — it calls only `/api/autonomous/config` and `/api/autonomous/decisions`, confirmed by grep of every `fetch(` call in the file. Zero references to `/api/agents/registry` or `AGENT_REGISTRY_SEED` anywhere in it. Repo-wide grep for "Agent Marketplace" and for any client-side caller of `/api/agents/registry` this session found zero matches outside the API route itself and its sibling configure route — no page anywhere rendered this concept at that time. The schema (#156) and API (#158) for this pillar are real. |
| 160 | Agent Log Viewer | BUILT — VERIFIED | **Corrected 2026-08-15**: the PLANNED status below was stale — this row was already built 2026-08-07 (commit `2ed3983`, the same q27 session that shipped row #159), reachable via the "View run history →" link on every card in row #159's Agent Marketplace. `src/app/(dashboard)/agents/marketplace/[agentId]/page.tsx` + `GET /api/agents/registry/[agentId]/runs` (`requireRole("viewer")`, organization_id server-derived, `.eq("agent_type", agentId)` against the real `agent_runs` table, cursor-on-`created_at` pagination). One real gap found and fixed this session: the API supported `cursor`/`limit` query params but the page never used them — it rendered only the first 50 rows with no way to see older runs. Added a "Load More" control (same cursor pattern as the existing Decision Log UI, row #214). `pnpm tsc --noEmit` and `pnpm run build` both clean. **Live-verified 2026-08-15** via a real authenticated Playwright session (admin-issued magic link for `info@faithfoundationsf.org`, no password read/changed) against a real local dev server (no mocks): loaded `/agents/marketplace/ag-17-discovery` and confirmed all 5 rendered rows (id, status, items_found/processed, error_message) matched a direct `psql` read of `agent_runs WHERE agent_type='ag-17-discovery' AND organization_id='b1ab7402-...'` exactly, same order, byte-for-byte on the JSON summary/error fields. Pagination separately live-verified against AG-02 Eligibility Scoring (124 real runs, same org): first page returned exactly 50 rows with "Load More" visible, clicking it issued a real cursor-qualified request and appended 50 more (100 rendered total) with zero id overlap between pages. |

### Pillar 18: Funding Knowledge Engine
| # | Feature | Status | Notes |
|---|---|---|---|
| 161 | pgvector Extension | BUILT — VERIFIED | Reconciled 2026-08-07: `SELECT extname, extversion FROM pg_extension WHERE extname = 'vector'` confirmed live this session — `vector 0.8.0` installed. |
| 162 | Knowledge Patterns Table | BUILT — VERIFIED | Reconciled 2026-08-07: `knowledge_patterns` confirmed live via direct `count(*)` this session — 33 real rows (funder-specific and general narrative/timing/competitive patterns with genuine `success_rate`/`sample_count`/`confidence` values, e.g. a real HUD Continuum-of-Care scoring-rubric pattern, a real logic-model-format preference pattern), populated by `scripts/seed-knowledge-patterns.ts` (confirmed present — unlike row #157's orphaned agent-registry seed). **Genuine finding**: this table has **no `embedding` column** (confirmed via `information_schema.columns`) — despite pgvector being installed platform-wide (#161), this specific table is not itself vector-searchable; see #163. |
| 163 | Knowledge Engine Core | BUILT — VERIFIED | **Corrected 2026-08-12** (`DATED_VERIFICATION_2026-08-12.md`): the "Minor defect found" display bug below is fixed — commit `08fa5c2` changed the interpolation from `` `${top.success_rate}% success rate}` `` to `` `${Math.round(top.success_rate * 100)}% success rate}` ``. Re-ran the real, unmodified `queryKnowledgeEngine('housing grant program success', orgId, supabase)` live this session (`node --import tsx`) against the real Faith Foundation org: still returns 10 real, relevant patterns, and the insight string now reads `"(73% success rate)"`, not `"(0.73% success rate)"`. Core retrieval claim (10 real patterns, real synthesized insight, keyword/`ILIKE`-based not vector-based) unchanged and reconfirmed. — *Prior text, reconciled 2026-08-07, defect since fixed:* Reconciled 2026-08-07: `src/lib/intelligence/knowledge-engine.ts`'s `queryKnowledgeEngine()` was run live this session (`node --import tsx`, real query "housing grant program success" against the real Faith Foundation org) and returned 10 real, genuinely relevant patterns (logic-model formatting, need-statement sourcing, HUD/HHS timing rules, etc.) plus a synthesized insight string — not a stub. **Finding**: this is keyword/`ILIKE` retrieval over `knowledge_patterns` + `intelligence_funded_proposals`, not vector/semantic search, per the file's own header comment ("Full vector search will replace this once `intelligence_funded_proposals.embedding` is populated") — pgvector being installed does not mean this engine uses it yet. **Minor defect found**: the top-match insight string has a real display bug — `success_rate` is stored as a 0–1 fraction (e.g. `0.73`) but the insight text interpolates it directly as `"(0.73% success rate)"` instead of `× 100`; should read "73%." |
| 164 | Knowledge Query API | BUILT — VERIFIED | Reconciled 2026-08-07: `/api/intelligence/knowledge-query` (`POST`) confirmed by direct read to be a thin wrapper (auth check, org-id-from-header, body validation) that calls `queryKnowledgeEngine()` directly with no separate logic — exercised indirectly via row #163's live library call. |
| 165 | Knowledge Engine UI | BUILT — VERIFIED (wiring confirmed, not browser-tested) | Reconciled 2026-08-07: `/intelligence/knowledge` confirmed by direct read to call `fetch("/api/intelligence/knowledge-query", ...)` — genuinely wired to the real route, not a placeholder. Not browser/screenshot-verified this session. |
| 166 | NIH RePORTER Ingestion | BUILT | scripts/ingest-nih-reporter.ts. Script exists, never run at scale. |
| 167 | NSF Awards Ingestion | BUILT | scripts/ingest-nsf-awards.ts. Script exists, never run at scale. |
| 168 | Federal Register Ingestion | BUILT — VERIFIED | **Run for real 2026-08-14/15**, per `INTELLIGENCE_LIBRARY_NIGHTS_SCOPE_2026-08-13.md`'s Session A recommendation. Found and fixed a real bug on the first attempt: all 3 hardcoded `AGENCIES` slugs (`department-of-housing-and-urban-development` etc.) don't match the Federal Register API's real slug format (`housing-and-urban-development-department` etc.) — confirmed via the live `/api/v1/agencies.json` list — so every request 400'd (`{"errors":{"agencies":"invalid value"}}`) and the script had, in fact, never successfully executed even once before this session, not just "never at scale." Corrected to the real slugs, re-run clean: 1,000 documents fetched across 10 pages, 1 genuine NOFA-titled row inserted (live `psql` count against `intelligence_funded_proposals WHERE source='FEDERAL_REGISTER'` confirms 1 row) — the low yield is real and expected, not a bug: most NOTICE-type HUD/HHS/DOJ documents in this window simply aren't titled as a NOFA/NOFO/Notice-of-Funding/Grant-Opportunity, which the script correctly filters on. |
| 169 | SAMHSA/HRSA Ingestion | BUILT — VERIFIED | **Run for real 2026-08-14/15**, per `INTELLIGENCE_LIBRARY_NIGHTS_SCOPE_2026-08-13.md`'s Session A recommendation. Found and fixed a real bug: `award_amount` is `numeric(12,2)` (migration `048_grant_intelligence.sql`, max ~$9.99B), but page 1 (sorted by amount desc) is dominated by real multi-billion-dollar HHS Medicaid entitlement awards (e.g. one real row at $14,250,962,864) — the whole page-1 batch insert (100 rows) failed on numeric overflow. A second, compounding bug masked the cause: the script's `fail()` helper only reads `.message` off `instanceof Error`, but Supabase's `PostgrestError` isn't one, so the real error printed as `[object Object]`. Both fixed: `toRow()` now nulls `award_amount` (keeping the rest of the row) when the value exceeds the column's range instead of failing the batch, and `fail()` now unwraps `.message` or falls back to a JSON dump. Re-run clean: 1,000 awards fetched across 10 pages, 319 real rows inserted, 0 failures (live `psql` count against `intelligence_funded_proposals WHERE source='USASPENDING'` confirms 319 rows). |
| 170 | Embedding Indexer Agent (AG-29, canonical) | BUILT — VERIFIED | **Built and live-verified 2026-08-03** (`AGENT_VERIFICATION_LOG.md` "AG-29"), superseding the prior "no dedicated indexer agent exists" finding. `src/lib/agents/knowledge-indexer-agent.ts`, class `KnowledgeIndexerAgent`, `agentId: "ag-29-knowledge-indexer"`, wraps the already-proven `embeddings.ts` library in a real `AutonomousAgent` subclass. Generated real embeddings for all 3 real pending `outcomes` rows; confirmed idempotent (structurally gated, can't double-call OpenAI); confirmed its no-real-content skip path correctly excludes all 133,812 `foundation_directory` rows lacking real text. **Found genuinely, continuously running in production already** — the real deployed Railway worker was independently confirmed polling this agent every ~60-70s, unprompted. **Do not confuse with the differently-scoped "AG-29" in the Post-Launch Vision section (Fundability Scorer, row #217) — that remains a real, unrelated, separately-built agent colliding on the same number.** |
| 171 | RAG Integration in Draft Generator | BUILT — UNVERIFIED (both 2026-08-07 blockers resolved this session, no live end-to-end call made) | **Updated 2026-08-14.** Both gaps the 2026-08-07 pass flagged are now closed: (1) `generateDraft()` (`src/lib/drafts/generator.ts`) — the actual live path the UI/`/api/ai/draft` route calls, confirmed by grep to be a real call site alongside `roi-optimizer-agent.ts`/`auto-generator.ts` — now calls `queryKnowledgeEngine()` in the same non-blocking `Promise.all([...]).catch()` group as the existing `retrieveIntelligence`/`retrieveRubric`/`retrieveLogicModel` calls, and renders matched patterns into the prompt as a clearly-labeled "KNOWLEDGE ENGINE PATTERNS" block, separate from org-voice (KB/proven-narrative/intelligence-library) sections so a human fact-checker can tell a cross-org match from this org's own records. `KnowledgeSource.kind` (`src/types/ai.ts`) extended with a new `"knowledge_engine"` variant so matched patterns are cited in `sources`/`draft_knowledge_sources`. (2) Migration `123_knowledge_engine_draft_integration.sql`'s `applications.knowledge_patterns_applied` column — confirmed live this session via a direct `psql`/`DATABASE_URL` query against `information_schema.columns` (returned exactly 1 matching row) — is now applied in production, so the write this code performs is no longer guaranteed to fail. The write itself remains best-effort (logged on error via `appUpdateError`, never throws), matching the surrounding draft-version/application-sync block's existing discipline. **Not upgraded to VERIFIED**: no live `generateDraft()` call was made this session (would require a real opportunity, org, and Claude completion) to directly observe a populated `knowledge_patterns_applied` row — only the code path and the column's live existence were confirmed independently, not the full runtime chain together. `DraftGenerationAgent`'s own separate, still-real gap (no queue case/nightly wiring/API route — an autonomous-trigger problem, unrelated to this fix, which is about the manual/live UI path) is unchanged, still real. `pnpm tsc --noEmit` and `pnpm run build` both clean with these changes in the tree. |

---

### Autonomous Agent Infrastructure
| # | Feature | Status | Notes |
|---|---|---|---|
| 187 | Autonomous Infrastructure Schema | BUILT | autonomous_triggers, agent_queue, agent_decisions, org_autonomous_config. Migration this session. |
| 188 | AutonomousAgent Base Class | BUILT | src/lib/agents/autonomous-base.ts. AUTONOMOUS_HARD_LIMITS. Decision logging. Chain support. |
| 189 | Autonomous Orchestrator (Railway) | BUILT | worker/autonomous-orchestrator.ts. 2AM nightly per-org pipeline. |
| 190 | Agent Queue Processor | BUILT | Continuous poll. Priority ordering. Retry logic with max_retries. |
| 191 | Autonomous Config API | BUILT | GET/PATCH /api/autonomous/config |
| 192 | Decision Log API | BUILT | GET/PATCH /api/autonomous/decisions |
| 193 | Queue Management API | BUILT | GET/DELETE /api/autonomous/queue |
| 194 | Manual Trigger API | BUILT | POST /api/autonomous/trigger |

### Autonomous Agents (18 agents upgraded)
| # | Feature | Status | Notes |
|---|---|---|---|
| 195 | AG-17 Autonomous Discovery | BUILT — VERIFIED | **Fully unblocked, see row #83 for full detail.** Enum gap fixed 2026-08-02 (30 opportunities discovered, 20 chained live); `org_id` column bug fixed 2026-08-07. No known blocker remains. |
| 196 | AG-15 Autonomous Probability Scoring | BUILT — BLOCKED | **Enum gap fixed 2026-08-02** — this row's own "fails on the first line of every `run()`" text is now stale; `ProbabilityScoringAgent` completes a real run (`status: completed`) with zero enum errors. **Re-verified live 2026-08-03**: every per-opportunity scoring call instead fails on the (at-the-time) dead platform `ANTHROPIC_API_KEY` (401) — 0 of 20 real candidates scored. **Not independently re-tested since the 2026-08-06 platform key rotation** that unblocked AG-22's identical failure mode (row #91) — very likely also fixed now, flagged as a quick, high-confidence re-verification rather than assumed. Do not confuse with the deterministic engine `computeGrantProbability()` (row #102), genuinely BUILT — VERIFIED and unaffected by either blocker. |
| 197 | AG-05 Autonomous Draft Generation | BUILT | Auto-drafts above threshold. pending_review=true. Never submits. |
| 198 | Autonomous Morning Digest | BUILT | 7AM AI briefing of overnight activity. |
| 199 | AG-18 Autonomous Reputation Intelligence | BUILT | Severity classification. Instant CRITICAL alerts. Correction, July 30 2026 (see AGENT_VERIFICATION_LOG.md "AG-18"): "Auto memory entries" does not describe the live path. That behavior (writing relationship_memory rows for HIGH/CRITICAL signals) exists only in a separate, undocumented `ReputationIntelligenceAgent` class (src/lib/intelligence/reputation-agent.ts, `agentId: "ag-18-reputation"`) that is never imported or instantiated anywhere outside its own file — the actual nightly path (worker/autonomous-orchestrator.ts's `runReputationStep()`) calls the plain `checkEntityReputation()` function directly and writes only reputation_signals + reputation_alerts + a critical-severity `alerts` row, with no relationship_memory write. Severity classification and instant CRITICAL alerts are accurate for the live path. |
| 200 | AG-19 Autonomous Relationship Builder | BUILT — WIRED (opt-in flag, default OFF) | Corrected July 30 2026, see `AGENT_VERIFICATION_LOG.md` "AG-19" and row #100. The real `RelationshipBuilderAgent` (Phase A scoring/recommendations + a substantial, previously-undocumented Phase B multi-hop warm-introduction pathfinder over `pig_nodes`/`pig_edges`) is real, 1,174+ lines, far more built than "nightly scoring + momentum" describes. **Updated 2026-08-13: it is no longer "never wired."** `worker/autonomous-orchestrator.ts`'s `routeQueueItem()` 'funder_relationship' case now reads an org-scoped `platform_config` flag, `feature.relationship_builder_v2` — when set to the literal string `'true'` for an org, that org's funder-relationship events route to a real `RelationshipBuilderAgent.run('event')` call; every other org (no row, or any value other than `'true'`) still falls through to the unrelated, simpler Generation-1 `FunderRelationshipAgent` path exactly as before. This is wired behind an opt-in flag, default OFF for every existing org including Faith Foundation — not a global cutover. Live-tested end-to-end via `src/__tests__/integration/ag19-relationship-builder-flag.test.ts` against the real, unmodified `routeQueueItem()`: flag unset routes to Gen-1 (confirmed via the real `agent_runs.agent_type` discriminator); flag `'true'` routes to Gen-2 and completes a real run with a real `funder_relationship_scores` write; Faith Foundation's real org confirmed to have no flag row and be unaffected. An owner/admin-only toggle exists at `/settings/agents`, backed by `GET`/`PATCH /api/settings/agents/relationship-builder-v2`. What runs nightly (the event-delta scorer, for every org without the flag set) is unchanged from before. Enabling this flag for any specific org — including Faith Foundation — was not done as part of this work and remains Reid's decision; see STATE_OF_THE_BUILD.md/SESSION_STATE.md. |
| 201 | AG-25 Autonomous Deadline Prediction | BUILT — VERIFIED | Pattern detection. Auto-creates projected opportunities at 90-day horizon. **Permanent dual-use number, both halves now confirmed real and live (not a bug to fix)** — this row's on-disk `agent_type` literal `"ag-25-deadline-prediction"` belongs to `DeadlinePredictionAgent`, unrelated to the Disaster Response Agent also numbered AG-25 elsewhere in this document (row #128). Searching `agent_runs`/`agent_queue` for "AG-25" surfaces this row's deadline-prediction activity, never disaster declarations. **Enum gap fixed and independently re-verified live 2026-08-02** (`AGENT_VERIFICATION_LOG.md`): this agent now completes a real run cleanly (`status: completed`, `itemsFound: 15`, zero errors) — no longer just a naming-collision note, genuinely confirmed working. See `AGENTS_v2.md` §1.4/§5 (AG-25 numbering note) for the full explanation of why this number is deliberately never renumbered, unlike AG-28/AG-30. |
| 202 | AG-28 Autonomous Follow-Up Generator | BUILT | Event-driven on stage transitions. Never sends directly. **AG-28 confirmed as this agent's permanent, sole number 2026-08-02** — the conflicting phantom "Impact Simulation Agent" that used to also claim AG-28 (row #141) was renumbered to AG-41; do not reuse AG-28 for anything else. Enum-gap-fixed and re-verified live 2026-08-02 (`AGENT_VERIFICATION_LOG.md`): completes a real run with zero `agent_type` enum errors. |
| 203 | AG-02 Autonomous Eligibility Scoring | BUILT | Auto-scores new discoveries. Chains qualified opps to AG-15. |
| 204 | AG-03 Autonomous Deadline Extraction | BUILT | Creates deadline records for all new opportunities. |
| 205 | AG-07 Autonomous Compliance Check | BUILT | Event-driven on ready_for_review. Blocks non-compliant applications. |
| 206 | AG-04 Autonomous Fit Analysis | BUILT | Fires at eligibility >= 70. Creates discovered-stage application. |
| 207 | AG-06 Autonomous Budget Builder | BUILT | Event-driven on drafting stage. Generates budget for human review. |
| 208 | AG-08 Renewal Tracker | BUILT | Monthly. Auto-creates renewal opportunity records for recurring grants. |
| 209 | AG-09 Outcome Analyzer | BUILT | Weekly + event-driven. Updates analytics and proven narrative status. |
| 210 | AG-10 Document Expiry Monitor | BUILT | Nightly. 30-day expiry notifications. |
| 211 | AG-11 Knowledge Gap Detector | BUILT | Weekly. Identifies missing KB categories with specific fill-in prompts. |
| 212 | AG-12 Search Profile Optimizer | BUILT | Monthly. Performance analysis and keyword improvement suggestions. |

### Autonomous UI
| # | Feature | Status | Notes |
|---|---|---|---|
| 213 | Autonomous Settings Panel | BUILT | /settings/agents — per-org toggle controls + threshold slider. |
| 214 | Decision Log UI | BUILT | Timeline view, approve/reject interface, pagination. |
| 215 | Dashboard 24h Activity Feed | BUILT | Live autonomous activity panel on main dashboard. |
| 216 | Autonomous Draft Review Page | BUILT | /draft-generator/autonomous — pending review queue. |

### Post-Launch Vision (Phases 2-5)
| # | Feature | Status | Notes |
|---|---|---|---|
| 217 | Fundability Intelligence Score | BUILT — UNVERIFIED | Phase 2. `FundabilityScorerAgent` (`agentId: "ag-29-fundability"`), `/api/intelligence/fundability`, opportunity detail panel. **Numbering-collision note, confirmed per `AGENT_VERIFICATION_LOG.md` "AG-29"**: this agent's `ag-29-fundability` literal collides with, but is a completely different agent from, the canonical AG-29 = Knowledge Engine Indexer Agent (row #170, embeddings/pgvector). Do not conflate the two when auditing "is AG-29 built" — this row's agent does real Claude text analysis; row #170's does not exist as an autonomous agent at all. Not independently re-verified for functional correctness this pass. |
| 218 | AI Donor Intent Engine (**AG-30, confirmed permanent 2026-08-02**) | BUILT — VERIFIED | `donor-intent-monitor-agent.ts` (`agentId: "ag-30-donor-intent"`) + `/api/intelligence/donor-intent` + page. Enum gap fixed 2026-08-02; `service_areas`→`service_area` column bug fixed the same day. **`corporate_prospects` blocker resolved 2026-08-03** (row #87) — re-verified live: `DonorIntentMonitorAgent.run('manual')` now completes a full real run against the real Faith Foundation org (`status: completed`, `items_found: 1, items_processed: 1`). Downstream per-signal Claude calls hit the (at-the-time) dead platform key and were caught into `errors[]` without failing the run — likely also resolved by the 2026-08-06 key rotation, not independently re-tested since. **AG-30 is now this agent's permanent, sole number.** |
| 219 | AutoApply Full Autonomous Mode | BUILT | Phase 2. Nightly batch queuer (migration 092, worker/autoapply-autonomous-orchestrator.ts) + /autoapply/controls Autonomous Mode panel + /autoapply Autonomous Queue section. |
| 220 | Corporate Relationship Graph | BUILT — VERIFIED | Phase 3. AG-32 (src/lib/agents/relationship-graph-builder-agent.ts) + /api/intelligence/relationship-graph + /intelligence/relationship-graph UI. Reads pig_nodes/pig_edges, not a corporate_relationships table (none exists — see agent file header). **Fully unblocked as of 2026-08-03**: enum gap closed; a `board_members` column-naming bug (query assumed `role`/`expertise`/`org_id`/`active`, real columns are `title`/`organization_id`/`is_active`) found and fixed; `corporate_prospects` blocker resolved (row #87); wired into a daily incremental schedule (`runRelationshipGraphIncrementalPipeline()` in `worker/scheduler.ts`). Full real run confirmed: `items_found: 23, items_processed: 23`, all 3 real board members processed, `assetCompatibleMatches: 20`. This is the real, substantive implementation of the RA-01/AG-23 "Relationship Mapper" concept (rows #80, #95), built under a different number by explicit design choice — the file's own header states this outright. Rows #80/#95 should cross-reference this row rather than stand alone as NOT-BUILT. |
| 221 | Donor Personalization Engine | BUILT — VERIFIED (scoped-down MVP, not visitor-detection) | Phase 3. Preflight 2026-08-07 confirmed zero real visitor-type signal exists anywhere in this repo — built an org-configurable content-variant toggle on `outreach_templates` instead (`outreach_template_variants`, migration 126). **Live-verified 2026-08-08**: partial-unique-active-index and deactivate/activate switching both confirmed live with real rows; `ON DELETE CASCADE` confirmed. One precision: the toggle persists correctly, but `/outreach/templates`' own template card always renders the base template's `subject`/`body` — activating a variant does not change the page's own visible content, only which pill is highlighted (the variant text is reachable only via a hover tooltip). No visitor detection, session fingerprinting, or send-pipeline wiring — unchanged, correctly out of scope. |
| 222 | Community Need Prediction | BUILT | Phase 3. AG-35 agent + /api/intelligence/community-need + /intelligence/community-need UI. |
| 223 | Global Learning Network | BUILT | AG-36 aggregator writes platform_learning_patterns (migration 083); draft-generation-agent.ts now queries it pre-draft, injects matched patterns into the Claude prompt, tracks applications.platform_patterns_applied (migration 084), and boosts confidence up to +20 for high-confidence patterns. /intelligence/learning-network dashboard (stats + pattern table) reads it via GET /api/intelligence/learning-network. Org-side NTEE matching not possible yet -- organizations has no ntee_code column, so matching is funder_category + platform-wide (ntee_code IS NULL) patterns only. AG-36 itself (src/lib/agents/learning-network-aggregator-agent.ts, 905 lines, class LearningNetworkAggregatorAgent) is real code but is never imported or called anywhere in src/ or worker/ -- confirmed by repo-wide grep July 19, 2026, see AGENTS_v2.md AG-36 spec. The write path this row describes has no live trigger; platform_learning_patterns is currently populated only by whatever seeded it previously, not by this agent running. |
| 224 | Predictive Fundraising Simulator | BUILT | Phase 4. Scenario builder + 3-year projection UI at /reports/simulate, backed by /api/reports/simulate (AG-37 SimulationAgent). |
| 225 | Autonomous Continuous Improvement Engine | BUILT — UNVERIFIED | Phase 4. AG-38 (src/lib/agents/self-improvement-agent.ts) + /admin/improvements review UI + agent_performance_metrics dashboard. **Enum gap confirmed closed 2026-08-07**: `'ag-38-self-improvement'` is now present in the live `agent_type` enum (65 total values, direct query) — the `22P02` failure this row previously described no longer reproduces at the schema level. Downgraded from BLOCKED to UNVERIFIED, not promoted to VERIFIED, because no live execution of this agent's actual `run()` was attempted this pass — only the enum precondition was re-checked. Zero rows still exist in `agent_runs`/`improvement_proposals`/`agent_performance_metrics` for this agent as of the last count; a fresh live run would be needed to confirm it now completes and produces real output, not just that the enum no longer blocks the first insert. |
| 226 | Community Resource Graph | BUILT — VERIFIED (ranked keyword+geo list, not graph traversal) | Phase 4. Preflight 2026-08-07 confirmed AG-35's real output (`community_need_signals`) has no lat/lng and no structured location join, and `pig_nodes`/`pig_edges` is an unrelated graph — built `resource-matcher.ts` as a Jaccard keyword + geo-text-overlap ranked list against real `funders`/`opportunities`/`programs` instead. **Live-verified 2026-08-08** against the real Faith Foundation org's real data (a realistic eviction-data signal genuinely matched its real "Emergency Bridge Housing"/"Veterans Path Home" programs and multiple real housing_grant opportunities, each with an honest nonzero score and reason) and confirmed a genuinely empty state (zero matches, no padding) for an org with zero real resources. No defects found. |
| 227 | ROI Optimization Engine | BUILT — UNVERIFIED | Phase 5. /reports/roi dashboard + /api/reports/roi + AG-39 `roi_insights`/`submission_variables` aggregation. **Correction, 2026-08-07: the "run() has no production call site" claim is false.** `RoiOptimizerAgent.trackSubmissionVariables()` is called from `/api/autonomous/track-submission/route.ts` (unchanged, telemetry half). Independently re-grepped `worker/autonomous-orchestrator.ts`: `runRoiOptimizerStep()` imports and calls `RoiOptimizerAgent.run('schedule')`, and **has done so since commit `6ffd4fd` (2026-07-20)** — ten days before this row's own "confirmed by repo-wide grep July 19" claim was written, meaning the claim was already stale the day it was written. Wired into the same monthly gate (`isFirstOfMonthChicago()`) as AG-26 (row #132), the exact cadence pattern this task asked to confirm/match. Downgraded from BUILT to BUILT — UNVERIFIED rather than VERIFIED, since no live execution of `run()` against real data has been recorded — only the call-site wiring was confirmed, not the runtime output: independently re-confirmed via direct `psql` this session, both `roi_insights` and `submission_variables` exist live but currently hold **zero rows each** (the monthly gate has not fired since this wiring landed, and no submission has yet been tracked through `/api/autonomous/track-submission` either). |
| 228 | AI Strategic Advisor | BUILT | Phase 5. Command center at /intelligence/strategic-advisor, backed by /api/intelligence/strategic-advisor (AG-40 StrategicAdvisorAgent). Dashboard widget + nav badge wired. |
| 229 | Global Feature Search (command palette) | BUILT — UNVERIFIED (browser-level) | **New, 2026-08-14.** `src/components/layout/GlobalSearch.tsx` (net-new component, net-new persistent header search input — the header previously had no search box at all) + `src/lib/search/feature-index.ts` (net-new, hand-curated ~90-entry route index), wired into `Header.tsx`/`DashboardShell.tsx`. Ctrl+K/Cmd+K opens a modal command palette; a persistent header input also opens it on focus/type. Fuzzy-matches label/keywords/description via `fuse.js` (new dependency, `package.json`). Every index entry carries the `requiredRole` actually enforced by that route's own code (per the file's own header comment — not guessed from nav visibility), and `hasRequiredRole()` (the same role-hierarchy helper already used elsewhere in the app, `src/lib/utils/constants.ts`) filters the index **before** Fuse ever sees a role-gated entry, so a search can't reveal the existence of an owner-only page (e.g. Billing, Command Center, `/admin/*`) to a sub-owner session. `role` is passed down from `DashboardShell`'s own session-derived prop (not request-body-supplied), the same prop already gating other role-restricted UI in that component. `pnpm tsc --noEmit` — 0 errors. `pnpm run build` — clean, all routes compiled. **Not independently verified this session**: an attempted live Playwright run (login as the `beta1@benavora-test.com` admin-tier account, open the palette, search "billing"/"command center" and confirm 0 results, search "funders" and confirm a real navigation) was blocked by this session's sandbox denying the `npx playwright test` invocation outright — the same class of sandbox permission gate documented elsewhere in this doc (e.g. rows #130, #134). This session's verification was code-level only (full read of both new files and the two diffed files, confirmed real wiring, no placeholders). **Genuinely unconfirmed**: that the fuzzy search actually renders and matches in a live browser, and that role-based filtering behaves as coded against a real second (non-owner) account — flag for a future session with working local dev-server/Playwright access to close with a real browser run. |

---

## Data Pipeline Features (Separate from UI Features)

| # | Feature | Status | Notes |
|---|---|---|---|
| D1 | IRS BMF Full Import | BUILT | pnpm ingest:bmf. nonprofits table has 1,978,526 records live (verified via check-enrichment-detailed.ts, July 22 2026). |
| D2 | IRS 990 Stream Parser | PARTIAL — EIN column bug fixed and live-verified 2026-08-14 | The "last FORGE queue" fix this row referred to (commit `b07b7ea`, 2026-07-16) added header-lookup with positional fallbacks to `scripts/enrich-foundations-990.ts`'s `streamIndexRows()`, but the fallback constants (`EIN_FALLBACK_IDX=1`, `OBJECT_ID_FALLBACK_IDX=7`, `XML_BATCH_ID_FALLBACK_IDX=8`) were themselves off by one column against the real IRS index CSV — confirmed live 2026-08-14 by fetching the actual `index_2025.csv`/`index_2026.csv` files (`https://apps.irs.gov/pub/epostcard/990/xml/<year>/index_<year>.csv`): real header order is `RETURN_ID,FILING_TYPE,EIN,TAX_PERIOD,SUB_DATE,TAXPAYER_NAME,RETURN_TYPE,DLN,OBJECT_ID,XML_BATCH_ID`, so EIN is at index 2 (not 1), OBJECT_ID at 8 (not 7), XML_BATCH_ID at 9 (not 8). Normal header-name lookup already worked correctly on live data (no trailing-CR issue reproduced against the current files), so this bug was dormant — but if header lookup ever failed for any reason, the fallback would silently read `FILING_TYPE`/`DLN`/`OBJECT_ID` instead, producing zero valid EINs for the entire run. Fixed by correcting the three constants to 2/8/9; the column-parsing logic was also extracted into two pure, exported functions (`parseIndexHeaders`/`parseIndexRow`) so it's unit-testable without a live Supabase connection (the script's Supabase client init was made lazy, gated behind a new `isMain` guard, matching the existing `backup-enrichment-output.ts` pattern). Verified three ways: (1) a new regression test in `src/__tests__/unit/regressions.test.ts` using real captured header/row lines from the live 2026 index — confirmed to fail against the pre-fix constants and pass against the fix; (2) a real, ad hoc re-run of the fixed functions against 5,000 consecutive real rows streamed live from `index_2026.csv`, both via the normal header path and a simulated header-lookup-failure path — 5,000/5,000 valid 9-digit EINs on both paths, zero mismatches between them; (3) `tsc --noEmit` clean. Still PARTIAL, not BUILT: this is a fix to the index-row-parsing bug only — the script's XML-fetch step still constructs the dead `s3.amazonaws.com/irs-form-990` fallback URL (see row #66's 2026-08-08 finding; the working mechanism is `foundation-scraper.ts`'s batch-ZIP `buildEinIndex`/`tryIrs990`, which this script does not use), and it has still never been run against the full `foundation_directory` at scale. |
| D3 | ProPublica Batch Enrichment | BUILT | Script exists. Never run against full 133K foundation records. |
| D4 | 298K Prospect CSV Import | NOT-BUILT | **Re-confirmed 2026-08-15**, fourth consecutive session, full detail in `D4_PROSPECT_IMPORT_INVESTIGATION_2026-08-15.md`. `D:\` confirmed not merely permission-blocked but genuinely not mounted on this machine at all — a direct `fs.readdirSync()` sweep of every drive letter C–Z with the sandbox explicitly disabled found only `C:\`. No file matching a ~298K-row dataset exists anywhere else reachable either: every CSV in the repo tree was enumerated (largest is `exports/foundation-directory-full.csv` at 133,813 lines, not a match), and every plausible existing table was cross-checked against real live row counts via `DATABASE_URL` — `nonprofits` (1,978,526, Directive 2 Phase A already done at a different scale), `foundation_directory` (133,812), `donor_discovery_directory`/`donor_discovery_prospects` (133,815/133,812, internal reshapes of `foundation_directory`, not an external import), `corporate_prospects` (49), `prospects`/`prospect_lists` (0/0, real target of `seed-outreach-prospects.ts` but sourced from `nonprofits`, not a CSV) — none match. `scripts/import-prospects.ts` still does not exist, confirmed a 4th independent time. Deliberately not built blind against a guessed column schema this session, per the BMF ingest script's own "scrambled columns" precedent (`scripts/ingest-irs-bmf-full.ts`) for what happens when an import script is written against assumed shape with nothing real to validate against. **Genuine, unchanged blocker**: needs Reid to either place the real file somewhere this sandbox can reach, or confirm its real column schema sight-unseen. Correction, 2026-08-07/08 (prior sessions, `AGENT_VERIFICATION_LOG.md` queue-37 entry #6): the "script exists, never run" note was wrong — `scripts/import-prospects.ts` does not exist anywhere in the repo. |
| D5 | Intelligence Library Corpus | PARTIAL | **Corrected 2026-08-15** — the "11 NIH proposals loaded" figure was stale (flagged but not fixed by `INTELLIGENCE_LIBRARY_NIGHTS_SCOPE_2026-08-13.md`, which found the real `ingest-nih-reporter.ts` script had already delivered 2,442 rows on 2026-07-26, five weeks after the 11-row figure was written). Live `psql` count against `intelligence_funded_proposals` by `source`, 2026-08-15: `NIH_REPORTER` 2,442, `NSF_AWARDS` 614, `USASPENDING` 319 (rows #166/167/169 — SAMHSA/HRSA lane), `INTELLIGENCE_LIBRARY_SEED` 80 (Claude-authored, not sourced ingestion), old `NIH` seed 11, `USDA`/`HUD`/`NIH_NIAID`/`DOJ_OJP` 5 each (synthetic 2026-07-18 seed batch, not live pulls), `PROPUBLICA_990` 2, `FEDERAL_REGISTER` 1 (row #168, just run — low yield is real/expected, most Federal Register NOTICE docs aren't NOFA-titled). **Real total: ~3,376 sourced rows** (excluding the 80 synthetic/seed rows), already past Directive 3's 2,000+ target on raw volume but concentrated in 3 of the 15 Directive-3 sources (NIH RePORTER, NSF, USASpending/SAMHSA-HRSA) — Federal Register's real yield is genuinely small, not a bug. All 4 of rows #166-169's scripts have now actually been run at least once (previously only 2 of 4 had); 9-11 of Directive 3's 15 sources still have zero ingestion code at all (unbuilt web scrapers — see `INTELLIGENCE_LIBRARY_NIGHTS_SCOPE_2026-08-13.md` §4 for the full per-source build gap, deliberately scoped to future one-source-per-session work, not a single unattended run). Nights 2-7's *application-feature* build items (rubrics, need-statement DB, budget/eval libraries, grantmaker intelligence, narrative/DNA scoring, cross-library polish, per `INTELLIGENCE_BUILD_ROADMAP.md`) remain not run — this row is scoped to data-ingestion volume only. |
| D6 | Foundation Website Scraper | BUILT | Superseded by S1/S2 below (StealthEngine + foundation-scraper.ts), not the older 54K-URL-list concept this row originally described. As of July 28 2026: the IRS 990 XML fetch bug (dead S3 fallback + browser-rendered XML viewer instead of a raw fetch, commit 52dd3ce) is fixed, and a real run tonight is confirmed parsing at an 8/10 success rate. Still not run at the full 133,812-record foundation_directory scale — see Directive 1 in STANDING_DIRECTIVES.md. |
| D7 | DATAOCEAN Backup | MOOT — Reid confirmed 2026-08-14 the underlying data no longer needs a backup path | `scripts/backup-enrichment-output.ts` (`pnpm backup:enrichment`) copies `enrichment-output/` recursively to a dated subfolder (`D:\dataocean\enrichment-backups\<YYYY-MM-DD>\`, override via `DATAOCEAN_BACKUP_ROOT`), never throws (returns `{ok:false, reason}` on a missing/unreachable drive so a dev-machine-only D:\ dependency can never block the Railway worker, which has none). Wired as the first line of `main()` in all 6 real enrichment-output writers: `enrich-foundations-990.ts`, `enrich-foundations-web.ts`, `enrich-nonprofits-propublica.ts`, `import-teos-local.ts`, `ingest-irs-bmf-full.ts`, `run-foundation-scraper.ts` — confirmed by direct diff of each file this session, not assumed. **Live-tested twice 2026-08-14**: (1) against the real default `D:\` root — confirmed unreachable from that session's sandbox (`fs.accessSync("D:\\")` → `ENOENT`); correctly returned `{ok:false}` and logged a warning rather than throwing; (2) against a local absolute-path stand-in — genuinely copied 10 real files / 4.87MB from the live `enrichment-output/` into a dated subfolder, confirmed present on disk after the call. One real, minor, unfixed bug from that session: `path.parse(BACKUP_ROOT).root` returns an empty string (not the drive letter) when `BACKUP_ROOT` is relative, misreporting "not reachable" for an otherwise-writable relative target — harmless today since the real default is always absolute. **Reclassified same day, later session**: Reid confirmed directly that the `enrichment-output/` data this script exists to protect is old, was already scraped once, and is already ingested into the live app — so a fresh D:\ backup of it is not operationally needed. This is no longer an open CRITICAL/unresolved risk; the unverified real-`D:\`-write question above is now moot rather than outstanding. The script itself is left in place (harmless, already wired into all 6 writers) in case a future dataset warrants the same backup path again — it just isn't blocking or urgent. |

---

## Scraper Features (Directive 1 — Stealth Enrichment Engine)

| # | Feature | Description | Status |
|---|---|---|---|
| S1 | Stealth Engine Core | src/lib/scraper/stealth-engine.ts — shared Playwright/Chromium engine used by both scrapers below: header consistency (realistic per-request header sets), cookie jar persistence across navigations, honeypot-field avoidance, and response verification (confirms the page actually returned the expected content before treating a fetch as successful). | BUILT |
| S2 | Foundation Enrichment Scraper | src/lib/scraper/foundation-scraper.ts — StealthEngine-based waterfall (homepage fetch -> contact-page discovery -> extraction) against foundation_directory. Wired into worker/scheduler.ts's `foundation-enrichment-weekly` job (Sunday 3AM CST, gated behind `ENABLE_SCRAPER=true`) and surfaced on the dashboard via S5. **Pagination bug fixed 2026-08-14/15**: `loadEinsMissingWebsite()` requested pages of 5000 via `.range()`, but PostgREST caps every response at 1000 rows regardless of the requested range (`db.max_rows`) — the old `if (data.length < PAGE) break` end-of-data check was true on the very first page, silently scoping the entire EIN index to an arbitrary 1000 of 114,037+ real candidates on every run, including this live weekly scraper (same root cause independently documented against the US6 template, row below, on 2026-08-14). Fixed by adding `.order("id", { ascending: true })` for stable pagination and advancing `from` by the actual `data.length` returned each page instead of the requested `PAGE` size, looping until `data.length === 0`. **Not live-verified this session**: a fresh confirmation run (`pnpm scrape:foundations-v2`, matching the US6 template's own reused code path) was blocked by a tool-permission gate on live network+secret calls before it could execute — the fix is code-verified (correct loop semantics, `pnpm tsc --noEmit` clean) but not yet observed against production data. | BUILT |
| S3 | Nonprofit Contact Scraper | src/lib/scraper/nonprofit-scraper.ts — StealthEngine sibling to foundation-scraper.ts, targeting `nonprofits WHERE website IS NOT NULL AND contact_emails IS NULL` (~6,066 rows as of 2026-07-27). Writes contact_emails/officer_email/phone, COALESCE-style so it never clobbers other enrichment passes. **Updated July 28 2026:** now also wired into worker/scheduler.ts as `nonprofit-enrichment-weekly` (Sunday 4AM CST, staggered 1hr after foundation-enrichment-weekly, same `ENABLE_SCRAPER` gate), commit 899567f — no longer CLI-only, though `scripts/run-nonprofit-scraper.ts` remains available for manual runs too. | BUILT |
| S4 | Scraper Railway Worker Job | worker/scheduler.ts — two weekly jobs, both gated behind `ENABLE_SCRAPER`: `foundation-enrichment-weekly` (Sunday 3AM CST, Foundation Enrichment Scraper / S2) and `nonprofit-enrichment-weekly` (Sunday 4AM CST, Nonprofit Contact Scraper / S3, added July 28 2026 per commit 899567f). Both scrapers now have scheduler integration. | BUILT |
| S5 | Scraper Status API | /api/scraper/status — GET route (viewer-role gated) reporting live `foundation_directory` enrichment counts/rate computed from the DB, plus best-effort last-run stats from a local `enrichment-output/scraper-stats.json` file (null when unavailable) and the next scheduled Sunday-3AM-CST run time. Foundation-directory-scoped only, matching S2/S4. | BUILT |

---

## Universal Scraper (UNIVERSAL_SCRAPER_PRD.md, build steps uscraper-001 through 007 — July 28 2026)

Ground-up replacement architecture per `UNIVERSAL_SCRAPER_PRD.md`: keyword + schema in, structured JSON out, regardless of source format. Distinct from the Directive-1 scrapers above (S1-S5), which remain in place as-is (not superseded). Status calls below are strict about what "verified against real data" actually covered this session — see `STATE_OF_THE_BUILD.md`'s uscraper session entries for full detail per step.

| # | Feature | Description | Status |
|---|---|---|---|
| US1 | scrape_jobs/scrape_results schema | `supabase/migrations/110_scrape_jobs_universal_scraper.sql` (commit a3378c5) — generic job/result tables per PRD §3.4, service-role-only RLS, no `organization_id` (platform infra, not per-org). **Confirmed live 2026-08-14**: both US6 and US7 template runs this session inserted and finalized real `scrape_jobs`/`scrape_results` rows (`job-store.ts` logged `REAL DB WRITE` on every insert, never fell back to the mock-JSON path) — the DDL-credential gap that blocked this through 2026-08-07 is resolved; migration 110 is applied in production. | BUILT — VERIFIED |
| US2 | Elite stealth stack install + smoke test | Installed `camoufox-js`, `rebrowser-patches`, `fingerprint-generator`, `ghost-cursor` per PRD §3.2 (commit edad095). `camoufox-js` **confirmed non-functional on this machine**: segfaults inside its unconditional `sampleWebGL()` call, root-caused to `better-sqlite3`'s native binary crashing on `new Database()` under Node 20.20.2 (camoufox-js declares `node >=22`). `rebrowser-patches` + `ghost-cursor` layered on the existing Playwright `stealth-engine.ts` confirmed working as the real fallback stack — this is what US3 actually built on. | PARTIAL — primary engine (camoufox-js) blocked/non-functional; working fallback stack identified and used downstream |
| US3 | UniversalFetcher browser layer | `src/lib/scraper-v2/universal-fetcher.ts` (commit f4a455c) — Playwright/Chromium + puppeteer-extra-plugin-stealth + fingerprint-generator + ghost-cursor + Crawlee SessionPool (the US2 fallback stack, not camoufox-js). **Verified live** against 3 real, varied targets (apnews.com, kingarthurbaking.com, irs.gov) via `scripts/test-universal-fetcher.ts` — all 3 fetched successfully, real content lengths 153K-2.3M chars, no cherry-picking. `pnpm tsc --noEmit` — 0 errors. | BUILT — verified against real fetches |
| US4 | Schema-flexible extraction layer | `src/lib/scraper-v2/extractor.ts` + `src/lib/scraper-v2/discovery.ts` (commit 0916efd) — Readability/jsdom strips page chrome, Claude forced via `tool_choice` to report only fields it can genuinely find in the caller's schema, missing fields explicitly nulled rather than fabricated. **Verified live** against 2 real pages (kingarthurbaking.com, crema-coffee.com found via `discoverUrls()`) with a business_name/phone/address schema — every non-null field returned was grepped back against the raw fetched HTML and confirmed present verbatim; absent fields came back null. jsdom pinned to 25.0.1 (30.x crashes on Node 20). **Fixed 2026-08-14**: `buildPrompt()` now explicitly biases contact-field extraction toward organizational contact patterns (info@/contact@-style, footer/"Contact Us" context) and deprioritizes byline/reporter/quoted-source attribution — a heuristic, not a hard exclusion — closing the real entity-misattribution defect documented on row US7 below. Live-verified against the exact real failing page (Anderson Center Services Inc's republished news article) and the exact real wwvremc.com/ezramedical.org success-case pages: the misattribution is gone (now returns honest `null`) and both real success cases are unchanged. See `STATE_OF_THE_BUILD.md`'s matching 2026-08-14 session for full before/after evidence. | BUILT — verified against real pages, real HTML cross-check; byline-misattribution prompt fix live-verified 2026-08-14 |
| US5 | Full pipeline wiring + CLI | `scripts/run-universal-scraper.ts` (commit 03a49cb, `pnpm scrape:universal`) wires discovery -> fetch -> extract into a CLI writing to scrape_jobs/scrape_results. **Verified end-to-end**: `--keyword "vegan bakeries Austin" --schema '{"name":"string","address":"string","website":"string"}' --limit 5` — 5 real URLs discovered via DuckDuckGo (Google/Bing blocked that run), 3 fetched+extracted with genuine non-null fields, 2 blocked (Yelp 403). Since migration 110 (US1) isn't live yet, all 6 records (1 job + 5 results) were written as mock JSON under `scrape-output/` (gitignored, confirmed present on disk) rather than the DB — the script logs this loudly on every mock write, never silently. Real DB writes start automatically once US1 is applied, no code change needed. | BUILT — verified end-to-end, DB writes pending US1 |
| US6 | Foundation-990 job template | `src/lib/scraper-v2/templates/foundation-990-template.ts` + `src/lib/scraper-v2/job-store.ts` + `scripts/run-foundation-990-template.ts` (`pnpm scrape:foundations-v2`). **Correction, 2026-08-14: this row was stale on two counts.** These files were already committed on 2026-07-30 (commit `5ee9528`), not "untracked/uncommitted" as previously stated — and they were already run for real that same day (see `STATE_OF_THE_BUILD.md`'s July 30 "uscraper-007 live-verification" session entry), not "never run." **Re-run live 2026-08-14** against 25 real `foundation_directory` candidates: real `scrape_jobs` row + 25 real `scrape_results` rows written (confirmed `REAL DB WRITE` on every insert, US1 now live), a real IRS 990 batch-ZIP index download matched 378 EINs — but **0 of 25 enriched**, same root cause as July 30 and still unfixed: `foundation-scraper.ts`'s `loadEinsMissingWebsite()` requests pages of 5000 via `.range()`, but PostgREST caps every response at 1000 rows regardless of the requested range (`db.max_rows`), so its `data.length < PAGE` end-of-data check is true on the very first page and the EIN index silently scopes to an arbitrary 1000 of 114,037+ real candidates — this specific 25-candidate sample never overlapped that window. This is a real, pre-existing bug in the *reused* `foundation-scraper.ts` code (also used by the live weekly S2 scraper), not a defect in this template's own logic — left unfixed here since it's shared production code outside this task's scope (one-line fix identified: change the loop's break condition from `data.length < PAGE` to `data.length === 0`). `pnpm tsc --noEmit` — 0 errors. **Fixed 2026-08-14/15** (see row S2 above): `loadEinsMissingWebsite()` now orders by `id` and advances `from` by the actual returned `data.length` each page, looping until `data.length === 0`, so it no longer stops after the first PostgREST-capped 1000-row page. **Not re-run live this session** — a confirmation pass against this template was blocked by a tool-permission gate on live network+secret calls; the fix is code-verified only (`pnpm tsc --noEmit` clean), the "0 of 25 enriched" real-run result above still stands as the last actual observed run. | BUILT — VERIFIED (template mechanics correct and real-DB-write-confirmed as of 2026-08-14); shared pagination bug fixed 2026-08-14/15 but not yet re-confirmed with a live run |
| US7 | Nonprofit-contact job template | `src/lib/scraper-v2/templates/nonprofit-contact-template.ts` + `scripts/run-nonprofit-contact-template.ts` (`pnpm scrape:nonprofits-v2`). **Correction, 2026-08-14: same stale-premise issue as US6** — already committed 2026-07-30 (`5ee9528`, which also fixed a real query-timeout bug: `.order("id")` against the 1.97M-row `nonprofits` table timed out, fixed by dropping the order clause) and already run for real that day (blocked then on the since-rotated stale `ANTHROPIC_API_KEY`). **Re-run live 2026-08-14, twice:** first a 25-candidate run (manually stopped after candidate 4 stalled in a slow same-domain crawl, ~30+ min with no progress — resource contention from other concurrent sessions on this machine is the likely cause, not confirmed as a template defect) produced a real, cross-verified write: Beacon of Downtown Houston's phone `(713) 220-9737`, extracted via Claude from `https://www.beaconhomeless.org/copy-of-matching-your-gift`, confirmed present verbatim in the real fetched HTML (`<a href="tel:(713) 220-9737">`) and genuinely written to `nonprofits.phone`. **Found and fixed one real bug this session**: 19,379 of the candidate pool (real live count) carry a placeholder-shaped `website` value (`"N/A"`, `"SEE SCHEDULE O"`, etc. — free text from IRS filings, not a real domain) that wasn't caught by the existing `.not("website", "is", null)` filter — one such row wasted 4 real fetch attempts (`ERR_NAME_NOT_RESOLVED` against a mis-parsed `https://n/A`, then `Invalid URL` on the literal string) before falling through harmlessly. Fixed with a `looksLikeRealDomain()` guard (requires a `.`, no whitespace) before any fetch is attempted; re-verified live against an 8-candidate clean batch: 8/8 processed, 3/8 correctly instant-skipped as placeholder domains (including a messier real case with embedded spaces, `"WWW. FAITHCOMESBYHEARING. COM"`, that the naive check wouldn't have caught), 3/8 real nonprofits updated with genuine extracted contact data (`wwvremc.com` → `info@wwvremc.com`; `ezramedical.org` → phone `718.686.7600`, confirmed present verbatim via a direct Playwright-rendered-HTML cross-check; `tcunion.org` → `giffordk@TCUnion.org`). `pnpm tsc --noEmit` — 0 errors (full project). **Found, NOT fixed — a real, previously-undocumented extraction-accuracy defect**: Anderson Center Services Inc's `contact_emails`/`officer_email` got written to `jferro@poughkeepsiejournal.com` ("John Ferro"), confirmed via direct Playwright-rendered-HTML cross-check to be a real newspaper reporter's byline (`John Ferro: 845-437-4816; jferro@poughkeepsiejournal.com; Twitter: @PoJoEnviro`) at the bottom of a syndicated news article hosted on the org's own `/news/` page — not a hallucination (the text is genuinely on the page, Playwright-rendered; a plain non-JS `curl` fetch of the same URL misses it entirely since the site is a JS-rendered Wix build) but a real entity-misattribution: `extractStructured()`'s schema/prompt has no way to distinguish "this organization's own official contact" from "any person's contact info that happens to appear on the page," including reporter bylines and quoted sources on the org's own site. Domain-scoping the candidate URL doesn't prevent this, since orgs commonly republish external press coverage on their own domain. Same class of accuracy gap as the already-documented EA-01/EA-08 defect (row #90) — flagged here rather than silently fixed at the time, since a real fix needs prompt/heuristic changes to the shared `extractStructured()` extraction layer (US4). **Fixed 2026-08-14** (separate follow-up session, see US4 row above and `STATE_OF_THE_BUILD.md`): `extractStructured()`'s prompt now deprioritizes byline/reporter/quoted-source contact info in favor of organizational contact patterns, as a heuristic rather than a hard rule. Re-ran the real, unmodified extractor against the exact real failing URL (`.../news/anderson-center-graduates-largest-class-ever/`, confirmed via direct `scrape_results` query) — now correctly returns `{email: null, phone: null, contact_name: null}` instead of the reporter's info. Re-ran against the exact real URLs behind this row's own `wwvremc.com`/`ezramedical.org` success cases (also pulled from `scrape_results`) and confirmed both still extract correctly, no regression. | BUILT — VERIFIED (real writes confirmed correct via raw-HTML cross-check; one real bug found+fixed 2026-08-14; the byline-misattribution accuracy defect found the same day was also fixed and live-regression-tested against the real failing case plus 2 real success cases) |

**Net status, corrected 2026-08-14:** all 3 general-purpose pipeline layers (US3/US4/US5) remain proven against real, varied live targets. US1 (schema) is now confirmed live — migration 110 is applied, real (non-mock) `scrape_jobs`/`scrape_results` writes are happening. US6 and US7 were **not** actually "never run" as this section claimed through 2026-08-11 — both were committed and first run for real on 2026-07-30 (`STATE_OF_THE_BUILD.md`), and both were re-run for real again today with fresh evidence above: US6's mechanics are sound but its effective yield is capped near-zero by a pre-existing pagination bug in reused code (unfixed, shared with the live S2 scraper); US7 is genuinely working end-to-end (real writes, cross-verified against raw page content) but carries a real, unfixed accuracy risk (byline/journalist-contact misattribution) worth weighing before treating its output as trustworthy donor-contact data at scale. US2 (camoufox-js) remains blocked/non-functional as previously documented.

---

## Testing Features

| # | Feature | Status | Notes |
|---|---|---|---|
| T1 | Jest Unit Tests | BUILT | src/__tests__/unit/ — success probability, semantic matcher, board report. |
| T2 | Smoke Tests | BUILT | src/__tests__/smoke/ — 5 critical API routes. |
| T3 | GitHub Actions Daily Workflow | BUILT | .github/workflows/daily-tests.yml — 11PM CST (5AM UTC). |
| T4 | E2E Tests | BUILT | **2026-08-13**: `e2e/critical-paths.spec.ts` confirmed committed (`git log`: `acb16ff`, further touched in `b46e135`) and on disk — real Playwright specs covering login and core flows, not a stub. Passed real compile/build/test gates in a FORGE queue earlier today. Already relied on as pre-existing by row T8's own cross-browser run (`test:e2e:all-browsers` runs this exact spec across chromium/firefox/webkit). No dated live-run citation of its own for this row specifically — see T8 for the actual pass/fail numbers from running it. |
| T5 | Visual Regression Tests | BUILT | **2026-08-13**: `e2e/visual-regression.spec.ts` confirmed committed (`git log`: `acb16ff`) and on disk — real Playwright screenshot-vs-baseline spec, not a stub. Passed real compile/build/test gates in a FORGE queue earlier today. No dated live-run citation for this row yet (no session has reported a pass/fail count or baseline-diff result for this specific spec) — flag for a future session to actually execute it and upgrade to BUILT — VERIFIED with real results. |
| T6 | DB Migration Tests | BUILT | `scripts/check-migration-idempotency.ts` (`pnpm check:migrations`) — real static analysis of every top-level SQL statement in both migration directories, plus a live re-run spot-check (`BEGIN`/`ROLLBACK`) against production. Ran 2026-08-08: root `supabase/migrations/` — 135 files, 958 DDL statements classified, **377 non-idempotent** (51 files with ≥1 issue); `src/supabase/migrations/` — 57 files, 309 DDL statements classified, **76 non-idempotent** (9 files with ≥1 issue). Not "every migration is idempotent" — real, substantial gaps found (mostly bare `CREATE POLICY`/`CREATE INDEX`/`CREATE TYPE ... AS ENUM` with no guard). Live spot-check (5 files/directory, confirmed-applied via real schema query, transaction-rolled-back) found zero unexpected errors — every non-idempotent statement failed cleanly with an "already exists"-class error, never corrupting data. Full detail in `MIGRATION_IDEMPOTENCY_AUDIT.md`. |
| T7 | Soak Tests | BUILT — VERIFIED (genuinely partial run — see notes) | Real soak test run 2026-08-08 against `pnpm scrape:nonprofits` (`src/lib/scraper/nonprofit-scraper.ts`) — ~14 min live run, real Supabase writes, real Chromium/network calls. Found a real, previously-undocumented bug: 100% of the 90 records attempted failed (0 enriched) because ~95% of real `nonprofits.website` values lack an `http(s)://` scheme prefix, so every `page.goto()` throws before any real fetch; no rate-limiting/CAPTCHA/crash/memory issue observed. See `SOAK_TEST_RESULTS.md`. **2026-08-13**: a second, separate soak test exercised the AutoApply queue processor (`worker/queue-processor.ts`), the real deployed Railway worker — 50 real `submission_queue` rows enqueued across 3 disposable test orgs, monitored live for the full 130-minute safety-cap window. **Honest result: the run did not fully drain** — 0/50 items reached a terminal status before the cap was hit, real numbers, not a full pass. Root cause identified, not a hang: `worker/queue-processor.ts:410` calls `waitBetweenSubmissions()` unconditionally after every item regardless of outcome, sleeping a random 60-120s each time — real throughput is capped at ~0.6-1.0 items/min, so a full 50-item drain was always going to need 50-100+ minutes even with zero errors, making the 130-min cap being hit expected, not evidence of a defect. Zero errors, zero rate-limit/CAPTCHA/crash/memory issues observed across the full window; all 3 disposable test orgs cleaned up and confirmed deleted. Real browser-automation code path (StealthBrowser/CAPTCHA/proxy) was deliberately not exercised — all 50 items took the `no_portal_or_email` fast-skip path by design, to avoid firing real automated submissions at real funder portals. See `SOAK_TEST_AUTOAPPLY_RESULTS_20260813.md` for full detail. |
| T8 | Cross-Browser Tests | BUILT — VERIFIED (partial pass — see notes) | **2026-08-13**: added `test:e2e:all-browsers` (`package.json`) running `e2e/critical-paths.spec.ts` across `--project=chromium --project=firefox --project=webkit` (new projects in `playwright.config.ts`) — the first real Firefox/WebKit run in this repo's history (previously Chromium-only per `TESTING_v2.md` §7). Fixed 4 real blockers to get a genuine (non-fabricated) run: Firefox/WebKit browser binaries were never installed on this machine; a stale, already-running dev server on port 3000 was serving pre-Dashboard-v2 compiled code (root-caused via a raw vs. mapped error-message mismatch, not guessed); two duplicate stale UI assertions (`tests/e2e/auth.setup.ts` and this spec's own test 1) expected a literal "Dashboard" `<h1>`, which the real Dashboard v2 redesign replaced with the org name — both fixed to assert the main content container instead, matching `e2e/smoke.spec.ts`'s existing pattern; and a one-time `beta1@benavora-test.com` account-creation race across parallel workers (self-resolved once the account existed). **Real result, not a clean pass**: 10/17 passed — chromium 4/5, firefox 4/5, webkit 0/5. WebKit failed all 5 tests on a reproducible, previously-undocumented navigation race (`page.goto()` immediately after login is interrupted by a still-in-flight `router.refresh()` redirect to `/dashboard`) — not a flaky one-off, it reproduced on every WebKit test this run. Separately, test 3 ("creating an application... adds a row to /applications/list") failed on both chromium and firefox with different symptoms, a real cross-browser application-flow flakiness, unresolved this session. See `CROSSBROWSER_TEST_RESULTS_20260813.md` for full detail. |

---

## UI Redesign Status

| Page/Component | Status | Notes |
|---|---|---|
| Dashboard | IN PROGRESS | Hero banner, HUD, Action Items working. Layout complete. Illustration positioning needs refinement. |
| FlightPathHUD | BUILT | 6 colored cards, flip animation, stage accent colors. |
| Sidebar | PARTIAL | Deep navy #1A2B3C. Active state needs confirmation. |
| Opportunities page | BUILT | Reconciled 2026-08-07: duplicate/stale entry — this is the same feature as row #105 ("Probability Badges on Opportunities," BUILT — UNVERIFIED), which this row's leftover "PLANNED/Tonight's queue" text contradicted. Confirmed by direct read of `src/app/(dashboard)/opportunities/page.tsx` this session: real probability badges (`probabilityTone()`, sourced from `opportunity_probability_scores.overall_score`) **and** a real sort control (`SortOption` including `probability-desc`/`probability-asc`, genuinely wired to the row sort via `useMemo`) both exist — not just the badges row #105 already covered. Cross-reference row #105 rather than treating this as a separate, still-open item. |
| All other pages | PLANNED | One component per CC session. Post-dashboard queue. |

**The One UI Rule:** All colors, backgrounds, shadows, borders must use inline `style={{}}` with hardcoded hex values. Never CSS variables or Tailwind color classes.

---

## Summary

| Category | Total | Built | Partial | In Build | Planned |
|---|---|---|---|---|---|
| Phase 1 MVP | 18 | 18 | 0 | 0 | 0 |
| Tier 1-3 Enhancements | 21 | 21 | 0 | 0 | 0 |
| Tier 4 Browser Automation | 7 | 7 | 0 | 0 | 0 |
| Tier 5 SaaS Layer | 6 | 6 | 0 | 0 | 0 |
| Tier 6 Full Autonomous | 26 | 22 | 3 | 0 | 1 |
| Platform Vision Pillars | 94 | 38 | 3 | 19 | 34 |
| Data Pipeline | 7 | 4 | 2 | 0 | 1 |
| Scraper (Directive 1) | 5 | 5 | 0 | 0 | 0 |
| Universal Scraper (uscraper-001-007) | 7 | 3 | 4 | 0 | 0 |
| Testing | 8 | 8 | 0 | 0 | 0 |
| **TOTAL** | **199** | **132** | **12** | **19** | **36** |

**2026-08-15 addendum:** row #99 (Signal Monitoring) moved Planned→Built this session — AG-43
Funder Signal Monitor, news + 990 halves only (LinkedIn deliberately excluded, see its row and
`AGENT_VERIFICATION_LOG.md`) — Platform Vision Pillars 37→38 Built / 35→34 Planned, TOTAL 131→132
Built / 37→36 Planned.

**2026-08-08 addendum:** row #144 (Narrative Gap Analysis) moved Planned→Built this session (see its
row for detail) — Platform Vision Pillars 28→29 Built / 44→43 Planned, TOTAL 114→115 Built / 55→54
Planned. Row T6 (DB Migration Tests) also moved Planned→Built this session — Testing 3→4 Built /
5→4 Planned, TOTAL 122→123 Built / 45→44 Planned. Not otherwise re-tallied against the note below.

**2026-08-08 queue-37 addendum:** rows #121/#122/#124/#221/#226 moved Planned→Built (5), #123/#66
moved Planned→Partial (2), row D4 stayed conceptually Planned/NOT-BUILT (no column change) — see
`AGENT_VERIFICATION_LOG.md`'s queue-37 entry for full evidence, including a real RLS bug and two
real `safe-fetch.ts` bugs found and fixed live during this pass. Platform Vision Pillars: Built
29→34, Partial 2→3, Planned 43→37. Tier 6: Partial 2→3, Planned 2→1. TOTAL: Built 117→122, Partial
10→12, Planned 52→45.

**2026-08-08 addendum (second):** row #139 (Plain Language Financials) moved Planned→Built this
session (see its row for detail — a new `plainLanguageFinancials` key on
`board_meeting_packets.packet_content`, board-packet-agent.ts) — Platform Vision Pillars 29→30 Built
/ 43→42 Planned, TOTAL 115→116 Built / 54→53 Planned. Not otherwise re-tallied against the note below.

**2026-08-11 addendum:** rows #82 (Path Finder), #151 (Auto-Monitor on Add), and #153 (Real-Time
Panel Updates) moved Planned→Built during a governance-doc verification pass — all three were
actually built and live-verified back on 2026-08-07/08 (commits `a8e18ff`/`4662e02`, `11dd99e`, and
`1fc87fa`/`764df7b`) but the registry rows themselves were never flipped; row #151's own
STATE_OF_THE_BUILD.md session entry explicitly flagged this as out-of-scope-at-the-time and asked a
future session to close it. Note row #153 is BUILT — BLOCKED (VERIFIED), not a clean BUILT — the code
is correct but the `supabase_realtime` publication has zero member tables in production, so no
realtime event has ever actually fired there; counted as Built here for consistency with how this
table already treats other BUILT-tier Platform Vision Pillar rows (e.g. the 2026-08-08 queue-37
addendum above), not because it fully works end-to-end. Platform Vision Pillars: 30→33 Built,
42→39 Planned. TOTAL: 116→119 Built, 53→50 Planned.

**2026-08-14 addendum:** row #229 (Global Feature Search / command palette) added — a genuinely new
feature (Ctrl+K search over ~90 reachable routes, role-filtered client-side), not a status flip on an
existing row. Counted BUILT — UNVERIFIED (browser-level): both quality gates (`tsc`, `build`) pass
clean and the code/wiring was read and confirmed real, but no live Playwright run could be completed
this session (sandbox denied the `npx playwright test` invocation) to confirm the palette actually
renders/matches/navigates or that role-filtering holds against a real non-owner account — see the row
itself for detail. Platform Vision Pillars: 93→94 total, 34→35 Built. TOTAL: 198→199 total, 127→128
Built.

**2026-08-14 addendum:** row D7 (DATAOCEAN Backup) moved from an un-tallied "CRITICAL" label (counted
in the Planned bucket) to PARTIAL this session — `scripts/backup-enrichment-output.ts` built and wired
into all 6 real enrichment-output writers, copy logic live-verified against a local stand-in; the real
D:\ destination itself remains unverified (still unreachable from this sandbox). Row #171 (RAG
Integration) and row #66 (990-PF Giving History) also received real dated updates this session but use
the finer-grained BUILT/PARTIAL agent-verification tiers already excluded from this table's plain
columns (see the Status Key note above) — #171's tier changed (BUILT — BLOCKED → BUILT — UNVERIFIED)
but its plain-column bucket (Built) does not change; #66 stayed PARTIAL, no bucket change. Data
Pipeline: Partial 2→3, Planned 2→1. TOTAL: Partial 12→13, Planned 40→39. Built/In Build unchanged by
this addendum (127/19).

**2026-08-14 addendum (second, later session):** row D7 (DATAOCEAN Backup) reclassified from PARTIAL
to MOOT — Reid confirmed directly that the `enrichment-output/` data this row's backup script exists
to protect is old, was already scraped once, and is already ingested into the live app, so a fresh
D:\ backup of it is no longer operationally relevant. This is not a new build or a reversal of the
prior addendum's technical findings (the script's copy logic, its wiring into all 6 writers, and the
still-unverified real-D:\-write status are all unchanged) — it only changes the risk framing from an
open CRITICAL/unresolved item to a non-blocking one. Counted under Built (script is real, wired, and
working against a verified stand-in) rather than Partial for tally purposes. `scripts/backup-enrichment-output.ts`
is left in place, not deleted. Data Pipeline: Built 3→4, Partial 3→2. TOTAL: Built 128→129, Partial
13→12. Planned/In Build unchanged by this addendum.

**2026-08-13 addendum:** row T8 (Cross-Browser Tests) moved Planned→Built this session — a real
Firefox/WebKit run (`CROSSBROWSER_TEST_RESULTS_20260813.md`) exercised `e2e/critical-paths.spec.ts`
across all three engines for the first time in this repo's history; result is a genuine partial pass
(10/17), not a clean one — see the T8 row itself for the real per-browser breakdown and the
WebKit-specific navigation-race finding. Row T7 (Soak Tests) stayed Built —
`SOAK_TEST_AUTOAPPLY_RESULTS_20260813.md` is the same 2026-08-13 AutoApply queue processor overnight
run already described in the T7 row above, re-saved under a dated filename to satisfy a queue's
`file_exists` gate check, not a second, separate execution. That run was honestly partial (0/50 items
reached terminal status inside the 130-minute cap, root-caused to the queue's own unconditional
60-120s per-item rate limiter, not a hang), so it does not change T7's Built/Planned category but does
change its notes and tier label. Testing: 5→6 Built, 3→2 Planned. TOTAL: 124→125 Built, 43→42 Planned.

**2026-08-13 addendum (second):** rows T4 (E2E Tests) and T5 (Visual Regression Tests) moved
Planned→Built this session — `e2e/critical-paths.spec.ts` and `e2e/visual-regression.spec.ts` were
confirmed to exist on disk and be committed (`git log`: `acb16ff`, `critical-paths.spec.ts` further
touched in `b46e135`), correcting a stale Planned status left over from before both files were
authored. Both already passed real compile/build/test gates in a FORGE queue earlier today, and T8's
own row already relies on `critical-paths.spec.ts` as pre-existing (its `test:e2e:all-browsers` run
executes this same spec across chromium/firefox/webkit). T4 is plain BUILT, not BUILT — VERIFIED — no
session has yet reported dated pass/fail numbers for running this spec on its own (as opposed to T8's
cross-browser numbers, which are for the multi-browser run, not this row). T5 is likewise plain BUILT,
not yet executed/verified by any session — flagged for a future session to run it against a baseline
and report real results. Testing: 6→8 Built, 2→0 Planned. TOTAL: 125→127 Built, 42→40 Planned.

**2026-08-15 addendum:** row #106 (Factor Breakdown UI) moved Planned→Built — VERIFIED this session.
The registry row was stale, not the code: the real `ProbabilityBreakdown` component (commit `cac32f3`,
already on `main`) had shipped in a prior session but the row itself was never flipped from PLANNED.
Live-verified this session via `scripts/verify-factor-breakdown-ui.ts`: an independent, unmodified
re-run of `computeGrantProbability()` against a real Faith Foundation opportunity matched the
already-persisted `opportunity_probability_scores` row (the one the UI actually renders) on every
displayed field, including all 4 factors' weight/value/contribution individually. Platform Vision
Pillars: 35→36 Built, 37→36 Planned. TOTAL: 129→130 Built, 39→38 Planned.

**2026-08-15 addendum (second):** row #160 (Agent Log Viewer) moved Planned→Built — VERIFIED this
session — same recurring pattern as row #106 above: the code (`src/app/(dashboard)/agents/marketplace/
[agentId]/page.tsx` + `GET /api/agents/registry/[agentId]/runs`) was already built and committed
2026-08-07 (`2ed3983`), reachable from every row #159 Agent Marketplace card, but the registry row
itself was never flipped. One real gap found and fixed this session: the API's `cursor`/`limit`
pagination was never used by the page — added a "Load More" control matching row #214's existing
Decision Log pattern. Live-verified against real production data via an authenticated Playwright
session (admin-issued magic link, no password touched): AG-17 Discovery's 5 rendered rows matched a
direct `psql` read of `agent_runs` exactly (same ids/order/fields); pagination separately verified
against AG-02 Eligibility Scoring's 124 real runs (page 1 = 50 rows + Load More, page 2 appends 50
more with zero id overlap). Platform Vision Pillars: 36→37 Built, 36→35 Planned. TOTAL: 130→131
Built, 38→37 Planned.

**Note on the July 30 → August 7, 2026 agent-verification updates:** the AG-15–AG-42 rows above (and
their Post-Launch Vision cross-references, #217/#218/#220/#225) use the finer-grained BUILT — VERIFIED
/ BUILT — UNVERIFIED / BUILT — BLOCKED / NOT-BUILT tiers defined in the Status Key rather than the
plain five-category scheme this Summary table counts against; those finer tiers were **not**
recomputed into this table's plain columns (doing so precisely would require re-auditing every
BUILT-tier row in the Autonomous Agents / Post-Launch Vision sections, not just the ones this pass
touched). **The Platform Vision Pillars row above (93 total) *was* precisely recomputed this
session**, unlike the rest of this table: all 15 stale "IN BUILD/Tonight" rows (#79, #98, #135, #136,
#140, #152, #156–159, #161–165) and both PLANNED→BUILT corrections (#137 AG-27, #141 AG-41) were
individually re-tallied — 12 IN BUILD→Built, 3 IN BUILD→Planned (#98, #157, #159 — a real, not
directionally-uniform, mixed result), 2 Planned→Built, net Built 11→25, In Build 34→19, Planned
46→47, Partial unchanged at 2 (25+2+19+47=93, reconciles exactly). A later pass moved row #133
(Forecast Dashboard) Planned→Built independently of this note (25→26, 47→46 — not individually
narrated above), and this same 2026-08-07 session moved row #142 (Simulator UI) Planned→Built too
(26→27, 46→45; 27+2+19+45=93, reconciles exactly). The grand TOTAL row reflects the matching deltas
(Built 97→113, In Build 34→19, Planned 57→56). The Autonomous Agent Infrastructure /
Autonomous Agents / Autonomous UI / Post-Launch Vision sections (rows #187–228) remain outside this
Summary table's count entirely (a pre-existing gap, not introduced this pass — 42 rows across those
four sections are not reflected in any Summary row, including the TOTAL) and are still not
individually recomputed here — treat those sections' aggregate standing as approximate. The row-level
detail above, and `NOT_BUILT_MASTER_INVENTORY.md`'s Section 2, are the authoritative, current sources
for AG-01 through AG-42's real status; this table is not.

**Infrastructure:**
- Database tables: 67 (097 migrations applied or queued)
- AI Agents: 30 designed (7 Phase 1, 7 Phase 2, 16 Phase 3 new)
- API routes: 208+
- Pages: 99+
- FORGE prompts executed: 130+
