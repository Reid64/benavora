-- 113_revoke_anon_truncate_batch.sql
--
-- Closes ANON_GRANT_AUDIT.md's Category B finding (2026-08-03): all 95 tables below have RLS
-- enabled with real policies (mostly organization_id-scoped, a few service_role-only or
-- auth.role()='authenticated'-scoped -- see the audit doc for per-table detail), but every one of
-- them still carries the anon TRUNCATE grant from this project's public-schema default privileges.
-- Postgres RLS policies never govern TRUNCATE (privilege-gated only, like DROP TABLE), so this was
-- a real bypass on every one of these 95 tables regardless of how correct their other policies are.
--
-- Scope deliberately matches ANON_GRANT_AUDIT.md's own scope: anon TRUNCATE only. authenticated
-- also holds TRUNCATE on all 95 of these (confirmed live, same audit) and is NOT touched here --
-- flagged as a separate, not-yet-addressed item, consistent with this table's existing
-- organization-scoped write policies which assume a real authenticated session, not blanket trust.
-- This migration does not touch any other grant or policy on any of these tables.
--
-- List generated by a live query (RLS enabled AND anon holds TRUNCATE), not hand-transcribed from
-- the audit doc, to avoid a copy error on a 95-table list.

REVOKE TRUNCATE ON
  agent_decisions,
  agent_queue,
  agent_runs,
  alerts,
  application_documents,
  applications,
  audit_logs,
  autoapply_follow_ups,
  automation_notifications,
  automation_queue,
  automation_screenshots,
  automation_sessions,
  automation_steps,
  autonomous_triggers,
  board_meeting_packets,
  board_meetings,
  board_members,
  calendar_connections,
  calendar_events,
  campaign_sends,
  campaign_steps,
  community_need_signals,
  competitor_tracking,
  contacts,
  corporate_intent_signals,
  corporate_monitoring_events,
  corporate_relationships,
  custom_api_connections,
  dd_prospect_requests,
  deadlines,
  documents,
  donor_discovery_connectors,
  donor_discovery_prospects,
  donor_discovery_requests,
  donor_discovery_taxonomy_aliases,
  draft_automation_config,
  draft_queue,
  draft_versions,
  email_activity,
  email_campaign_sequences,
  email_campaigns,
  email_connections,
  email_sequence_enrollments,
  email_sequence_steps,
  email_templates,
  email_thread_links,
  fundability_scores,
  funder_dna_profiles,
  funder_giving_history,
  funder_intelligence,
  funder_relationship_events,
  funder_relationship_scores,
  funders,
  funding_forecasts,
  historical_awards,
  impact_simulations,
  integration_keys,
  integrations,
  invoices,
  knowledge_base,
  notes,
  onboarding_steps,
  opportunities,
  opportunity_keywords,
  org_autonomous_config,
  org_documents,
  org_usage_summary,
  organizations,
  outcomes,
  outreach_contacts,
  pig_edges,
  pig_nodes,
  pipeline_history,
  platform_config,
  profiles,
  programs,
  proven_narratives,
  renewals,
  request_profiles,
  research_cache,
  roi_insights,
  scraping_targets,
  search_profiles,
  simulation_scenarios,
  strategic_recommendations,
  submission_variables,
  subscriptions,
  success_probability_scores,
  synced_email_messages,
  synced_email_threads,
  team_activity_log,
  usage_metrics,
  usage_tracking,
  user_invitations,
  validations
FROM anon;
