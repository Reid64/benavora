# benavora — INTERACTION MAPS

> Generated by FORGE 2.0 Phase 2 (Governance Generator) on 2026-06-12T18:24:56.449Z.
> GOVERNANCE DOCUMENT — read-only during Phase 3 execution (BEHAVIORAL_CONTRACTS Contract 3).
> Interaction-level granularity per BEHAVIORAL_CONTRACTS Contract 18: every interactive
> element specifies user action → frontend reaction → API call → backend processing →
> database write → side effects → success/error responses → tracking event.

## Feature: Feature 1 – Grant Source Categorization

### 1. Source Type Dropdown on Grant Detail Page
- **User action:** User clicks the Source Type dropdown and selects a value
- **Frontend reaction:** Renders <Select> with all source_type enum values as human-readable labels; applies optimistic update to badge color and label immediately
- **API call:** `PATCH /api/grants/[id]`
- **Backend processing:** Validates source_type against enum values; verifies organization_id ownership via Supabase RLS; updates grants row
- **Database write:** `grants`
- **Side effects:** Toast: 'Source type updated.' on success, Optimistic update rolls back on network failure with toast: 'Failed to save. Check your connection.', Toast: 'You don't have permission to edit this grant.' on 403, Toast: 'Invalid source type selected. Please try again.' on 400
- **Success response:** HTTP 200 with updated grant object including new source_type
- **Error response:** HTTP 400 for invalid value; HTTP 403 for permission failure; HTTP 500 for server error
- **Tracking event:** `grant_source_type_updated`

### 2. SourceTypeBadge Component on Grant List Rows
- **User action:** User views any grant list (Dashboard, Pipeline, Search Results, Analytics)
- **Frontend reaction:** Renders <SourceTypeBadge source_type={...} /> pill badge with deterministic Tailwind CSS color mapping per source_type value; 'Uncategorized' gray badge for NULL
- **API call:** `GET /api/grants`
- **Backend processing:** Returns grant rows with source_type column included; RLS scopes results to organization_id
- **Database write:** `none`
- **Side effects:** —
- **Success response:** Grant list with source_type field populated per row
- **Error response:** Toast: 'Failed to load grants. Please refresh.' on fetch error
- **Tracking event:** `grant_list_viewed`

### 3. Source Type Filter Panel on Grant List Views
- **User action:** User selects one or more source types from the filter panel
- **Frontend reaction:** Updates URL query params (e.g. ?source_type=federal_government,private_foundation); refetches grant list with filter applied; displays active filter chips above list
- **API call:** `GET /api/grants`
- **Backend processing:** Filters Supabase query with .in('source_type', [...]) scoped by organization_id; returns matching grants
- **Database write:** `none`
- **Side effects:** Empty state displayed: 'No grants match the selected source types. Try adjusting your filters.' when result set is empty, Toast: 'Failed to load grants. Please refresh.' on fetch error
- **Success response:** HTTP 200 with filtered grants array
- **Error response:** HTTP 500 with error message
- **Tracking event:** `grant_list_filtered_by_source_type`

### 4. AI Auto-Assignment of source_type During Research Run
- **User action:** Automated — triggered when Research Agent saves a discovered grant
- **Frontend reaction:** No direct UI feedback during auto-assignment; source_type badge appears when user views results
- **API call:** `POST /api/research/run`
- **Backend processing:** Research agent prompt instructs model to classify each discovered grant into a source_type enum value; backend maps AI-returned string to enum; falls back to 'other' if unrecognized; logs warning to server console on fallback
- **Database write:** `grants`
- **Side effects:** Fallback to 'other' logged to server console if AI returns unrecognized string
- **Success response:** Grant INSERTed with source_type set
- **Error response:** source_type defaults to 'other' on any classification failure
- **Tracking event:** `grant_source_type_auto_assigned`

## Feature: Feature 2 – Research Agent Parallel Execution

### 1. Run Research Button on Research/Dashboard Page
- **User action:** User clicks 'Run Research' button
- **Frontend reaction:** Calls POST /api/research/run; displays per-agent status panel with cards showing Queued → Running → Completed/Failed states via Supabase Realtime subscription; button disabled while run is in progress
- **API call:** `POST /api/research/run`
- **Backend processing:** Fetches all active agent configurations for organization; INSERTs research_runs row with status='running' and research_run_agents rows with status='pending'; spawns all agent tasks simultaneously using Promise.allSettled; each agent calls AI provider with search profile parameters
- **Database write:** `research_runs`
- **Side effects:** research_run_agents rows INSERTed with status='pending' per agent, Supabase Realtime subscription established on research_run_agents for real-time status updates, HTTP 409 returned if run already in progress for organization
- **Success response:** HTTP 200 with research_run_id and initial status
- **Error response:** HTTP 409: 'A research run is already in progress. Please wait for it to complete.'
- **Tracking event:** `research_run_started`

### 2. Per-Agent Status Cards in Research Progress Panel
- **User action:** Automated — updates during active research run
- **Frontend reaction:** Supabase Realtime subscription on research_run_agents pushes updates; agent card badge flips from Queued to Running to green checkmark (Completed) or red X (Failed) in real-time
- **API call:** `GET /api/research/runs/[id]`
- **Backend processing:** As each agent Promise resolves or rejects, updates research_run_agents.status to 'completed' or 'failed'; sets completed_at; stores raw results in research_run_agents.raw_results JSONB
- **Database write:** `research_run_agents`
- **Side effects:** Supabase Realtime event triggers frontend badge update per agent card
- **Success response:** research_run_agents row updated with status and completed_at
- **Error response:** Agent marked failed with error='timeout' after 90 seconds; error='rate_limited' after two 429 responses
- **Tracking event:** `research_agent_status_updated`

### 3. Deduplication Summary Banner After Run Completion
- **User action:** Automated — displayed after all agents complete
- **Frontend reaction:** Dismisses loading state; refreshes grant list; displays summary banner: 'Research complete. Found [N] opportunities, removed [M] duplicates, saved [K] unique grants.'
- **API call:** `GET /api/research/runs/[id]`
- **Backend processing:** After Promise.allSettled: normalizes URLs, deduplicates by URL then by fuzzy funder+program match (Levenshtein ≤3); merges metadata from multiple sources; INSERTs only unique grants; updates research_runs.deduplication_stats JSONB; updates research_runs.status to 'completed' or 'partial'
- **Database write:** `grants`
- **Side effects:** research_runs.deduplication_stats updated with {total_found, duplicates_removed, unique_saved}, research_runs.status updated to 'completed' or 'partial', Toast: 'Research run complete. [K] new grants discovered.' on all-success, Toast warning: 'Research run finished with [X] agent errors. [K] grants saved from successful agents.' on partial failure, Toast error: 'Research run failed. No grants were saved. Check agent configuration.' on all-failed
- **Success response:** research_runs row updated with completed status and deduplication_stats
- **Error response:** research_runs.status set to 'failed' by cron job if stuck running >10 minutes
- **Tracking event:** `research_run_completed`

### 4. Concurrent Run Prevention – Disabled Run Research Button
- **User action:** User clicks 'Run Research' while a run is already in progress
- **Frontend reaction:** Button is disabled with tooltip: 'A research run is already in progress.'; frontend catches 409 from API and shows toast
- **API call:** `POST /api/research/run`
- **Backend processing:** Checks research_runs for row with status='running' scoped to organization_id; returns HTTP 409 if found
- **Database write:** `none`
- **Side effects:** Toast: 'A research run is already in progress. Please wait for it to complete.'
- **Success response:** N/A — concurrent run blocked
- **Error response:** HTTP 409 Conflict
- **Tracking event:** `research_run_concurrent_blocked`

## Feature: Feature 3 – Search Profile Configuration

### 1. Search Profile Page Load and Profile Fetch
- **User action:** User navigates to /settings/search-profile
- **Frontend reaction:** Fetches GET /api/search-profiles; renders loading skeleton during fetch; populates form with existing profile values or system defaults if 404 returned
- **API call:** `GET /api/search-profiles`
- **Backend processing:** Returns search_profiles row for organization_id scoped by RLS; returns 404 if none exists
- **Database write:** `none`
- **Side effects:** —
- **Success response:** HTTP 200 with search_profiles object or HTTP 404 triggering frontend default population
- **Error response:** Toast: 'Failed to load search profile. Please refresh.' on network error
- **Tracking event:** `search_profile_page_viewed`

### 2. Funding Type Toggles
- **User action:** User toggles on/off: Grants, Fellowships, Contracts, Loans, In-Kind, Other
- **Frontend reaction:** Updates local form state immediately; toggle animates on/off with 200ms CSS transition
- **API call:** `none`
- **Backend processing:** Stored on save via POST /api/search-profiles upsert
- **Database write:** `none`
- **Side effects:** —
- **Success response:** Local state updated
- **Error response:** Validation error on save if all funding types deselected: 'At least one funding type must be selected.'
- **Tracking event:** `search_profile_funding_type_toggled`

### 3. Source Type Filter Checkboxes
- **User action:** User checks/unchecks source type categories to include/exclude from research
- **Frontend reaction:** Multi-select checkbox list updates local form state immediately
- **API call:** `none`
- **Backend processing:** Stored on save via POST /api/search-profiles upsert into search_profiles.source_type_filters text array
- **Database write:** `none`
- **Side effects:** —
- **Success response:** Local state updated
- **Error response:** none
- **Tracking event:** `search_profile_source_filter_changed`

### 4. Focus Area Weight Sliders
- **User action:** User drags sliders or inputs numeric weights (0–100) for focus areas
- **Frontend reaction:** Running total updates in real-time as sliders move; displays sum below sliders; does not enforce sum to 100
- **API call:** `none`
- **Backend processing:** Stored on save via POST /api/search-profiles upsert into search_profiles.focus_area_weights JSONB
- **Database write:** `none`
- **Side effects:** —
- **Success response:** Local state updated with new weight values
- **Error response:** none
- **Tracking event:** `search_profile_focus_area_weight_changed`

### 5. Dollar Range Min/Max Inputs
- **User action:** User sets minimum and maximum grant award amounts
- **Frontend reaction:** Validates min ≤ max inline; formats as currency display (e.g. '$5,000 – $500,000'); shows inline error 'Minimum amount cannot exceed maximum amount.' if invalid; max=0 treated as no upper limit
- **API call:** `none`
- **Backend processing:** Stored on save via POST /api/search-profiles; amount_min stored as integer cents; amount_max stored as integer cents or NULL if 0
- **Database write:** `none`
- **Side effects:** —
- **Success response:** Local state updated with valid range
- **Error response:** Inline error: 'Minimum amount cannot exceed maximum amount.'
- **Tracking event:** `search_profile_amount_range_changed`

### 6. Geographic Scope Selector and State Multi-Select
- **User action:** User selects geographic scope; if State-specific, selects states from multi-select dropdown
- **Frontend reaction:** Renders scope selector; conditionally shows 50-state multi-select dropdown when State-specific chosen; selected states appear as dismissible chips
- **API call:** `none`
- **Backend processing:** Stored on save via POST /api/search-profiles into search_profiles.geographic_scope JSONB ({type, states[]})
- **Database write:** `none`
- **Side effects:** —
- **Success response:** Local state updated
- **Error response:** Save blocked if State-specific scope selected with no states chosen: 'Please select at least one state for state-specific scope.'
- **Tracking event:** `search_profile_geographic_scope_changed`

### 7. Eligibility Pre-filter Form Section
- **User action:** User configures 501(c)(3) status, years in operation, annual budget range, staff size range, served population tags
- **Frontend reaction:** Renders structured form section; updates local form state immediately
- **API call:** `none`
- **Backend processing:** Stored on save via POST /api/search-profiles into search_profiles.eligibility_filters JSONB
- **Database write:** `none`
- **Side effects:** —
- **Success response:** Local state updated
- **Error response:** none
- **Tracking event:** `search_profile_eligibility_filter_changed`

### 8. Negative Filters Tag-Input Field
- **User action:** User enters keyword/funder/region to exclude and presses Enter or comma
- **Frontend reaction:** Tag appears immediately as dismissible chip; duplicate entry highlighted briefly and not added; remove button on chip deletes tag from local state; shows warning if >100 tags
- **API call:** `none`
- **Backend processing:** Stored on save via POST /api/search-profiles into search_profiles.negative_filters text array
- **Database write:** `none`
- **Side effects:** Warning displayed if tag count >100: 'You have [N] exclusions. Very long lists may slow research.'
- **Success response:** Tag added to local state as chip
- **Error response:** Duplicate tag not added; brief highlight animation on duplicate attempt
- **Tracking event:** `search_profile_negative_filter_added`

### 9. Save Search Profile Button
- **User action:** User clicks 'Save Search Profile' button
- **Frontend reaction:** Validates form (min ≤ max, at least one funding type, state selection if scope is state-specific); calls POST /api/search-profiles; shows loading state on button
- **API call:** `POST /api/search-profiles`
- **Backend processing:** UPSERTs search_profiles row by organization_id with all fields; sets updated_at=now(); RLS enforces organization_id scoping
- **Database write:** `search_profiles`
- **Side effects:** Toast: 'Search profile saved. Your next research run will use these settings.' on success, Toast: 'Failed to save. Please try again.' on network error, Inline errors on failing validation fields
- **Success response:** HTTP 200 with upserted search_profiles object
- **Error response:** HTTP 400 for validation errors; HTTP 500 for server error
- **Tracking event:** `search_profile_saved`

### 10. Reset to Defaults Link
- **User action:** User clicks 'Reset to Defaults' link
- **Frontend reaction:** Opens confirmation modal: 'This will reset all search profile settings to system defaults. Continue?'; on confirm, resets local form state to hardcoded defaults without persisting
- **API call:** `none`
- **Backend processing:** No backend call until user explicitly saves after reset; then POST /api/search-profiles with default values
- **Database write:** `none`
- **Side effects:** Modal opens with backdrop blur; on cancel, no change; on confirm, form fields reset to defaults with 200ms transition
- **Success response:** Local form state reset to system defaults
- **Error response:** none
- **Tracking event:** `search_profile_reset_to_defaults`

## Feature: Feature 4 – Analytics Dashboard

### 1. Analytics Page Load and Date Range Selector
- **User action:** User navigates to /analytics and selects date range preset (Last 30 days, Last 90 days, Last 12 months, All Time)
- **Frontend reaction:** Fetches GET /api/analytics/summary with date params; renders loading skeletons for each chart panel; updates URL query params for shareable links
- **API call:** `GET /api/analytics/summary`
- **Backend processing:** Runs aggregation queries against grants and research_runs scoped by organization_id and date range; returns summary metrics
- **Database write:** `none`
- **Side effects:** HTTP 301 redirect from /outcomes to /analytics
- **Success response:** HTTP 200 with analytics summary object
- **Error response:** Toast: 'Failed to load analytics. Please refresh.' on fetch error
- **Tracking event:** `analytics_dashboard_viewed`

### 2. Pipeline Funnel Chart Panel
- **User action:** User views the Pipeline Funnel chart panel
- **Frontend reaction:** Renders Recharts FunnelChart (dynamic import with ssr:false) with stages Discovered→Researching→Eligible→Applied→Under Review→Awarded/Rejected; shows count labels and percentage drop-off; empty state if no grants
- **API call:** `GET /api/analytics/funnel`
- **Backend processing:** Returns counts per grants.status value scoped by organization_id and date range
- **Database write:** `none`
- **Side effects:** —
- **Success response:** HTTP 200 with array of {stage, count} objects
- **Error response:** Chart panel shows error state with retry button
- **Tracking event:** `analytics_funnel_viewed`

### 3. Success Rate Metric Card
- **User action:** User views the Success Rate card
- **Frontend reaction:** Renders large percentage metric (awarded/applied*100) with trend arrow vs previous period; shows '—' if no applications exist
- **API call:** `GET /api/analytics/summary`
- **Backend processing:** Returns {awarded, applied, previous_awarded, previous_applied} counts from grants table scoped by organization_id and date range
- **Database write:** `none`
- **Side effects:** —
- **Success response:** HTTP 200 with success rate metrics
- **Error response:** Card shows '—' with note on data unavailability
- **Tracking event:** `analytics_success_rate_viewed`

### 4. Dollars Requested vs Awarded Bar Chart
- **User action:** User views the financial comparison chart
- **Frontend reaction:** Renders Recharts BarChart (dynamic import, ssr:false) with grouped bars per month; dollar-formatted Y-axis; hover tooltip shows exact values
- **API call:** `GET /api/analytics/financials`
- **Backend processing:** Aggregates SUM(amount_requested) and SUM(amount_awarded) grouped by month scoped by organization_id and date range; skips NULL amounts
- **Database write:** `none`
- **Side effects:** Note '(some amounts not recorded)' displayed if >20% of grants lack amounts
- **Success response:** HTTP 200 with array of {month, total_requested, total_awarded}
- **Error response:** Chart renders with empty state placeholder bars at zero
- **Tracking event:** `analytics_financials_viewed`

### 5. Source Category Pie Chart
- **User action:** User views the Source Category pie chart; clicks a slice to filter grant table
- **Frontend reaction:** Renders Recharts PieChart (dynamic import, ssr:false) using same color palette as SourceTypeBadge; clicking slice updates shared filter state and refetches grant table below
- **API call:** `GET /api/analytics/by-source`
- **Backend processing:** Returns {source_type, count, total_amount}[] scoped by organization_id and date range
- **Database write:** `none`
- **Side effects:** Grant table below re-fetches with source_type filter applied on slice click
- **Success response:** HTTP 200 with source breakdown array
- **Error response:** Chart panel shows error state
- **Tracking event:** `analytics_source_pie_viewed`

### 6. Deadline Heatmap Panel
- **User action:** User views the Deadline Heatmap; clicks a day cell to filter grant table
- **Frontend reaction:** Renders calendar heatmap grid (Recharts ScatterChart, dynamic import ssr:false) for next 90 days; darker color = more deadlines; clicking day cell updates shared filter state; empty state if no upcoming deadlines
- **API call:** `GET /api/analytics/deadlines`
- **Backend processing:** Returns {date, count}[] for grants with deadline in next 90 days scoped by organization_id
- **Database write:** `none`
- **Side effects:** Grant table below filters to grants due on clicked date
- **Success response:** HTTP 200 with deadline density array
- **Error response:** Heatmap panel shows empty grid with error message
- **Tracking event:** `analytics_deadline_heatmap_viewed`

### 7. Agent Activity Line Chart Panel
- **User action:** User views the Agent Activity chart
- **Frontend reaction:** Renders Recharts LineChart (dynamic import, ssr:false) with multi-line: runs per day and grants discovered per run; legend; hover shows run count and discovery count; empty state if no runs yet
- **API call:** `GET /api/analytics/agent-activity`
- **Backend processing:** Returns {date, runs, grants_found, duplicates_removed}[] scoped by organization_id and date range
- **Database write:** `none`
- **Side effects:** —
- **Success response:** HTTP 200 with agent activity time series
- **Error response:** Chart shows empty state: 'Run your first research session to see agent activity here.'
- **Tracking event:** `analytics_agent_activity_viewed`

### 8. Filterable Grant Table Below Charts
- **User action:** User interacts with charts (clicks pie slice or heatmap day cell)
- **Frontend reaction:** Shared filter state updated from chart interaction; grant table re-fetches with applied filters; active filter chip displayed above table
- **API call:** `GET /api/grants`
- **Backend processing:** Standard grants query with filter params (source_type or deadline date) scoped by organization_id via RLS
- **Database write:** `none`
- **Side effects:** —
- **Success response:** HTTP 200 with filtered grants array
- **Error response:** Toast: 'Failed to load grants. Please refresh.'
- **Tracking event:** `analytics_grant_table_filtered`

## Feature: Feature 5 – Automated Eligibility Enhancement

### 1. High Match Badge on Grant List Rows and Grant Detail Header
- **User action:** User views grant list or grant detail page
- **Frontend reaction:** Renders eligibility badge inline: 'High Match' green badge for high_match; yellow '~ Moderate Match' for moderate_match; gray 'Low Match' if verbose mode enabled; 'Scoring...' spinner if unscored and scoring pending; 'Unscored' gray chip if no profile configured
- **API call:** `GET /api/grants`
- **Backend processing:** Returns grants with match_percentage, eligibility_flag, eligibility_notes columns; RLS scopes to organization_id
- **Database write:** `none`
- **Side effects:** —
- **Success response:** HTTP 200 with grants including eligibility fields
- **Error response:** Badge shows 'Unscored' on data unavailability
- **Tracking event:** `grant_eligibility_badge_viewed`

### 2. Eligibility Score Detail Panel (Accordion/Modal on Grant Detail)
- **User action:** User clicks match percentage badge or 'View Eligibility Details' link on Grant Detail page
- **Frontend reaction:** Expands inline accordion panel showing: circular progress indicator for score, AI rationale text, scoring timestamp, and Re-score button; animates open with 200ms transition
- **API call:** `GET /api/grants/[id]`
- **Backend processing:** Returns full grant object including match_percentage, eligibility_flag, eligibility_notes, eligibility_scored_at
- **Database write:** `none`
- **Side effects:** —
- **Success response:** HTTP 200 with grant detail including eligibility fields
- **Error response:** Panel shows 'Eligibility details unavailable' on error
- **Tracking event:** `grant_eligibility_detail_viewed`

### 3. Re-score Button in Eligibility Detail Panel
- **User action:** User clicks 'Re-score' button on the eligibility detail panel
- **Frontend reaction:** Calls POST /api/grants/[id]/rescore; disables button and shows spinner during processing; updates score and rationale on success
- **API call:** `POST /api/grants/[id]/rescore`
- **Backend processing:** Re-runs eligibility scoring job for specific grant: fetches organization's search_profiles.eligibility_filters and grant description; calls Anthropic Claude API; parses {score, rationale} from response; updates match_percentage, eligibility_flag, eligibility_notes, eligibility_scored_at; sets flag based on score thresholds (≥80 high_match, 50-79 moderate_match, <50 low_match)
- **Database write:** `grants`
- **Side effects:** Toast: 'Eligibility re-scored.' on success, Toast: 'Re-scoring failed. Please try again.' on error, Supabase Realtime update pushes new score to any open sessions
- **Success response:** HTTP 200 with updated grant eligibility fields
- **Error response:** HTTP 500; button re-enabled; toast error shown
- **Tracking event:** `grant_eligibility_rescored`

### 4. Eligibility Flag Filter on Grant List
- **User action:** User selects eligibility filter: All / High Match / Moderate / Low / Unscored
- **Frontend reaction:** Updates URL params with eligibility_flag value; refetches grant list; displays active filter chip above list
- **API call:** `GET /api/grants`
- **Backend processing:** Filters Supabase query by eligibility_flag column scoped by organization_id via RLS
- **Database write:** `none`
- **Side effects:** —
- **Success response:** HTTP 200 with filtered grants array
- **Error response:** Toast: 'Failed to load grants. Please refresh.'
- **Tracking event:** `grant_list_filtered_by_eligibility`

### 5. Automated Eligibility Scoring on Grant Discovery
- **User action:** Automated — triggers after research agent INSERTs new grant
- **Frontend reaction:** No immediate UI feedback; grant card updates match_percentage badge when user refreshes or via Supabase Realtime push
- **API call:** `POST /api/grants/[id]/rescore`
- **Backend processing:** Triggered async after deduplication INSERT: fetches search_profiles.eligibility_filters; constructs AI prompt for 0-100 score and 2-3 sentence rationale; calls Anthropic Claude; parses response; updates grants.match_percentage, eligibility_flag, eligibility_notes; if high_match and grant <24h old, INSERTs alert into alerts table with alert_type='high_match_grant'
- **Database write:** `grants`
- **Side effects:** alerts row INSERTed if high_match and grant <24h old, Sidebar Alerts icon badge count increments via Supabase Realtime, eligibility_flag set to 'unscored' with note if no search profile configured
- **Success response:** grants updated with eligibility fields
- **Error response:** eligibility_flag remains 'unscored' on AI call failure; error logged to server console
- **Tracking event:** `grant_eligibility_auto_scored`

### 6. Sidebar Alert Badge for New High Match Grants
- **User action:** Automated — triggered after scoring sets eligibility_flag='high_match' on a newly discovered grant
- **Frontend reaction:** Supabase Realtime pushes alerts table INSERT to frontend; red badge count on sidebar 'Alerts' nav icon increments
- **API call:** `GET /api/alerts`
- **Backend processing:** alerts row INSERTed with alert_type='high_match_grant', organization_id, grant_id, created_at; Supabase Realtime broadcasts to subscribed clients
- **Database write:** `alerts`
- **Side effects:** Sidebar Alerts icon badge count updates in real-time via Supabase Realtime
- **Success response:** Alert INSERTed; Realtime event broadcast to clients
- **Error response:** Badge count stale until next manual refresh if Realtime subscription drops
- **Tracking event:** `high_match_alert_created`
