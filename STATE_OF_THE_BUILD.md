# State of the Build

nav-rename-autoapply completed — "Automation" nav item renamed to "AutoApply", route moved from /automation to /autoapply across all dashboard pages and links.
fix-opportunity-detail-display completed — OverviewTab restructured: description now uses whitespace-pre-wrap; eligibility_requirements and required_documents moved into the Details card (scrollable and list rendering respectively); standalone conditional cards for those fields removed; Funding card amount_available uses explicit null check; Keywords card spans full width.
tighten-grants-gov-keywords completed — HOUSING_KEYWORD_QUERIES replaced with 12 mission-focused terms (homeless housing, transitional housing, emergency shelter, reentry housing, recovery housing, rural housing, CDBG, HOME Investment Partnership, Emergency Solutions Grant, Continuum of Care, faith-based community, substance abuse recovery housing). Added scoreRelevance() post-fetch filter: scores 0–100 on 10 relevance keywords; rejects on 13 REJECT_LIST terms (disability, veteran education, criminal alien, stem, defense, agriculture, farmers, fisheries, nih, cdc, nasa, dod research, clinical trial); only inserts opportunities with score >= 40 and no reject-list match; logs rejected grants to console with reason.
nofa-pdf-parsing-pipeline completed — NofaParserAgent (government_research type) downloads PDFs from opportunity_documents jsonb column, parses text via pdf-parse, extracts structured fields with Claude (description, eligibility_requirements, amount_available, amount_min, amount_max, deadline, geographic_restrictions, application_method, required_documents, recurrence), and updates opportunity rows for null/empty fields only. POST route at /api/agents/nofa-parser with maxDuration=300. Batch function runNofaBatch() processes all opportunities with documents but no description. pdf-parse added to serverComponentsExternalPackages in next.config.mjs.
opportunity-documents-ui completed — Added NOFA Documents card to OverviewTab listing all PDF URLs from opportunity_documents as clickable links (filename extracted from URL, opens in new tab). Added Parse NOFA button in opportunity detail header (alongside Edit/Delete) that POSTs to /api/agents/nofa-parser, shows isLoading spinner during processing, refreshes page data on success, and displays inline error alert on failure. Sparkles icon used for Parse NOFA; empty-state message shown when opportunity_documents is null or empty.
autoapply-form-analyzer completed — FormAnalyzerAgent at src/lib/agents/form-analyzer.ts: visits funder giving_portal_url via Playwright chromium (headless), sends full page HTML to Claude for form structure extraction (fields, isMultiStep, requiresLogin, requiresFileUpload, formAction), maps extracted fields to Benavora KB columns (organizations.name/ein/mission_statement/email/phone/address_line1/city/state/zip/website, request.amount, request.description; unmatched fields flagged as manual_review_required), inserts result into form_templates table. API route at src/app/api/agents/form-analyzer/route.ts (maxDuration=120, writer role required). TypeScript types added: form_analyzer added to AgentType union (agents.ts) and agent_type enum (database.ts); form_templates, autoapply_submissions, submission_queue table types added to Database.Tables (database.ts). Playwright already in serverComponentsExternalPackages (next.config.mjs). NOTE: Playwright binary not available in Vercel serverless — production analysis runs on dedicated worker.
autoapply-schema completed — Migration file created at supabase/migrations/045_autoapply_tables.sql with three tables: form_templates (reusable field mappings per funder portal; fk: organizations, funders), autoapply_submissions (full submission attempt log with 8-value status enum; fk: organizations, funders, form_templates), submission_queue (AutoApply worker queue with priority + 5-value status enum; fk: organizations, funders, autoapply_submissions). All three tables have RLS enabled with organization_id-scoped SELECT/INSERT/UPDATE/DELETE policies via organization_members join. Performance indexes added on organization_id, funder_id, status, and priority/scheduled_for. NOTE: Remote application via Supabase Management API and MCP tools was blocked by permission approval gates — migration SQL at supabase/migrations/045_autoapply_tables.sql requires manual application via Supabase dashboard SQL editor or: supabase db push (with Supabase CLI configured).
autoapply-form-filler completed — FormFillerAgent at src/lib/agents/form-filler.ts: accepts funderId + optional requestAmount/requestDescription; queries form_templates for stored field mapping (throws "Form template not found. Run form analysis first." if absent); queries organizations for org profile; generates 150-200 word donation request via Claude API if requestDescription not provided; launches Playwright chromium (headless); navigates to portal_url; fills each non-manual-review field using resolveFieldValue() mapping kbMapping strings to org profile fields and request data (page.fill for text/email/tel, page.selectOption for select, page.check for checkbox, page.setInputFiles for file — downloads from org-{orgId} Supabase Storage bucket to temp file, cleans up after); takes pre-submission screenshot and uploads to autoapply-screenshots bucket at {orgId}/autoapply/screenshots/{funderId}_pre.png; clicks submit button (typed fallback to text-match); waits 5s; takes post-submission screenshot; extracts confirmation number via regex from page body text; creates autoapply_submissions record (status=submitted, screenshots, confirmation, description, amount, retry_count=0); updates funder.last_contacted_at; logs via BaseAgent agent_runs. API route at src/app/api/agents/form-filler/route.ts (maxDuration=120, writer role required, no rate limiter — rate-limited upstream by enforceLimit). form_filler added to AgentType union (agents.ts) and agent_type enum (database.ts). NOTE: Playwright binary not available in Vercel serverless — production form filling runs on dedicated worker. DB migration needed to add form_filler to the agent_type PostgreSQL enum before agent_runs logging will succeed.
autoapply-dashboard-ui completed — Replaced src/app/(dashboard)/autoapply/page.tsx with a three-section dashboard: (A) QUEUE section shows submission_queue rows joined with funders(name, giving_portal_url); bulk checkboxes; "Analyze Forms" action button (POST /api/agents/form-analyzer with selected funderIds); "Run Selected" button (POST /api/agents/form-filler, enabled only when selected items have form_templates); (B) SUBMISSIONS section shows autoapply_submissions ordered desc, with color-coded Badge per status (green=submitted, red=failed, yellow=captcha_blocked, gray=queued), expandable rows showing pre_submit_screenshot_url and confirmation_screenshot_url thumbnails plus request_description text; (C) TEMPLATES section shows form_templates joined with funders(name), displays field count computed from field_mapping JSON, last_verified_at, last_used_at, per-row "Re-analyze" button (POST /api/agents/form-analyzer). "Add to Queue" button opens a Modal listing funders where giving_portal_url is not null with bulk checkbox selection that inserts into submission_queue. All styling matches existing dark theme using navy/teal/ink color tokens, Card/Badge/Button/Modal/EmptyState components. No mocks, no placeholders — all data from real Supabase queries.
