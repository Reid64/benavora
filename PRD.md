```json
{
  "prd": "# Executive Summary\n\nBenavora is an AI-powered nonprofit funding discovery and management platform. This PRD covers Tier 2 feature enhancements that transform Benavora from a basic grant-tracking app into a sophisticated, intelligent funding operations hub. The seven Tier 2 features add: structured grant source taxonomy with visual categorization, parallelized AI research with deduplication, a fully configurable search profile system, a rich analytics dashboard replacing the empty Outcomes page, automated eligibility scoring with smart flagging, a comprehensive alert and notification center, and multi-model AI consensus validation for discovered opportunities.\n\n(assumption: existing Benavora schema includes tables for `grants`, `organizations`, `users`, `research_runs`, and `agents` based on Tier 1 build; this PRD layers on top of that foundation)\n(assumption: multi-tenant architecture scoped by `organization_id` on all tables)\n(assumption: authentication via Supabase Auth with Row-Level Security policies)\n(assumption: deployment target is Vercel with Next.js App Router, TypeScript strict mode, pnpm package manager)\n(assumption: existing Tier 1 integrations include Anthropic Claude as primary AI provider; Tier 2 adds a second provider for consensus validation — defaulting to OpenAI GPT-4o)\n\n---\n\n## User Personas\n\n### Persona 1: Grants Manager (Primary)\n- **Name:** Maria Chen\n- **Role:** Director of Development at a mid-size nonprofit (5–50 staff)\n- **Goals:** Discover new funding sources efficiently, track grant pipeline, submit on time, report outcomes to board\n- **Pain Points:** Manual grant research is time-consuming; misses deadlines; unsure which AI-found grants are real; no centralized notification system\n- **Tech Comfort:** Moderate — comfortable with SaaS dashboards, not a developer\n- **Usage Pattern:** Daily active user, checks dashboard each morning, reviews alerts, initiates research runs weekly\n\n### Persona 2: Executive Director (Secondary)\n- **Name:** James Okafor\n- **Role:** Executive Director of a small nonprofit (<10 staff)\n- **Goals:** Maximize funding ROI, understand pipeline health at a glance, ensure compliance\n- **Pain Points:** No time for deep research; needs summary views; wants confidence that AI results are accurate\n- **Tech Comfort:** Low-to-moderate — prefers visual dashboards, minimal configuration\n- **Usage Pattern:** Weekly review of analytics dashboard, acts on high-priority alerts\n\n### Persona 3:** Grant Researcher / Associate (Tertiary)\n- **Name:** Sofia Reyes\n- **Role:** Development Associate at a large nonprofit (50+ staff)\n- **Goals:** Run targeted research for specific program areas, configure search profiles per initiative, validate eligibility quickly\n- **Pain Points:** AI results are too broad; needs to filter by focus area and geography; wants to see which model validated each grant\n- **Tech Comfort:** High — power user who configures advanced settings\n- **Usage Pattern:** Power user of search profile config and research runs; reviews consensus validation details\n\n---\n\n## Feature Specifications\n\n### Feature 1: Grant Source Categorization\n\n**Overview:** Add a `source_type` enum to the `grants` table and display color-coded badges throughout the UI so users instantly recognize grant origin category.\n\n#### Interaction-Level Decomposition\n\n**1.1 – Enum Definition and Schema Migration**\n- **User Action:** (System/admin action triggered by deployment migration)\n- **System Action (Backend):** Supabase migration adds `source_type` enum type with values: `federal_government`, `state_government`, `local_government`, `private_foundation`, `corporate_foundation`, `community_foundation`, `international`, `faith_based`, `individual_donor`, `other`. Adds `source_type` column (nullable initially, then backfilled) to `grants` table.\n- **Data Change:** `grants.source_type` column added; existing rows backfilled to `other` by default.\n- **Feedback:** Migration runs silently; no user-facing feedback during migration.\n\n**1.2 – Manual Source Type Assignment**\n- **User Action:** On the Grant Detail page, user clicks a dropdown labeled \"Source Type\" currently showing the badge or \"Uncategorized.\"\n- **System Action (Frontend):** Renders a `<Select>` component with all enum values formatted as human-readable labels (e.g., `federal_government` → \"Federal Government\"). Submits `PATCH /api/grants/[id]` with `{ source_type: selectedValue }`.\n- **System Action (Backend):** Next.js API route validates `source_type` is a valid enum value, verifies `organization_id` ownership via Supabase RLS, updates `grants` row.\n- **Data Change:** `grants.source_type` updated for the specific row.\n- **Feedback (Success):** Badge instantly updates color and label (optimistic update). Toast: \"Source type updated.\"\n- **Feedback (Error – Invalid Value):** Toast: \"Invalid source type selected. Please try again.\"\n- **Feedback (Error – Permission):** Toast: \"You don't have permission to edit this grant.\"\n- **Feedback (Error – Network):** Optimistic update rolls back; toast: \"Failed to save. Check your connection.\"\n\n**1.3 – AI Auto-Assignment During Research**\n- **User Action:** (Automated — triggered when Research Agent saves a discovered grant)\n- **System Action (Backend):** Research agent prompt instructs model to classify each discovered grant into one of the `source_type` enum values based on funder name and description. Classification included in structured JSON response.\n- **System Action (Backend):** API route that saves research results maps AI-returned `source_type` string to enum; falls back to `other` if unrecognized.\n- **Data Change:** `grants.source_type` set on INSERT of new grant records from research runs.\n- **Feedback:** No direct user feedback for auto-assignment; visible immediately when user views results.\n\n**1.4 – Color-Coded Badge Component**\n- **User Action:** User views any grant list (Dashboard, Pipeline, Search Results, Analytics).\n- **System Action (Frontend):** `<SourceTypeBadge source_type={...} />` component renders a pill badge with deterministic color mapping: `federal_government`=blue, `state_government`=indigo, `local_government`=violet, `private_foundation`=emerald, `corporate_foundation`=teal, `community_foundation`=cyan, `international`=amber, `faith_based`=orange, `individual_donor`=pink, `other`=gray. Uses Tailwind CSS color classes.\n- **Data Change:** Read-only; no data change.\n- **Feedback:** Visual badge visible inline in all grant list rows and grant detail header.\n\n**1.5 – Filter by Source Type**\n- **User Action:** On grant list views, user selects one or more source types from a filter panel.\n- **System Action (Frontend):** Updates URL query params (e.g., `?source_type=federal_government,private_foundation`). Refetches grant list with filter applied.\n- **System Action (Backend):** `GET /api/grants?source_type=...` filters Supabase query with `.in('source_type', [...])` scoped by `organization_id`.\n- **Data Change:** Read-only query.\n- **Feedback (Success):** List updates to show only matching grants; active filter chips displayed above list.\n- **Feedback (Empty):** Empty state: \"No grants match the selected source types. Try adjusting your filters.\"\n- **Feedback (Error):** Toast: \"Failed to load grants. Please refresh.\"\n\n#### Edge Cases – Feature 1\n- **Empty source_type:** Grants with `NULL` source_type display an \"Uncategorized\" gray badge with a prompt to classify.\n- **AI returns unrecognized string:** Backend falls back to `other`; logs warning to server console.\n- **Concurrent edit:** If two users edit `source_type` simultaneously, last-write-wins (Supabase default); no conflict UI needed for this field.\n- **Bulk import:** If grants are bulk-imported via CSV, `source_type` column is optional; missing values default to `other`.\n- **Enum extension:** Adding new enum values requires a migration; UI dropdowns are generated from a shared constant array so no code change needed beyond migration and constant update.\n\n---\n\n### Feature 2: Research Agent Parallel Execution\n\n**Overview:** Execute all configured research agents simultaneously (instead of sequentially) using Promise.allSettled, then deduplicate discovered grants by URL and funder name before saving.\n\n#### Interaction-Level Decomposition\n\n**2.1 – Trigger Parallel Research Run**\n- **User Action:** User clicks \"Run Research\" button on the Research or Dashboard page.\n- **System Action (Frontend):** Calls `POST /api/research/run` with `{ organization_id, search_profile_id (optional) }`. Displays a loading state with per-agent status indicators (running / completed / failed).\n- **System Action (Backend):** API route fetches all active agent configurations for the organization. Spawns all agent tasks simultaneously using `Promise.allSettled([agent1(), agent2(), agent3(), ...])`. Each agent task is an async function calling the AI provider with the appropriate prompt and search profile parameters.\n- **Data Change:** `research_runs` table: INSERT new row with `status='running'`, `started_at=now()`, `organization_id`, `search_profile_id`. `research_run_agents` table: INSERT one row per agent with `status='pending'`.\n- **Feedback:** UI shows a progress panel: each agent card transitions from \"Queued\" → \"Running\" → \"Completed\" or \"Failed\" in real-time via Supabase Realtime subscription on `research_run_agents`.\n\n**2.2 – Per-Agent Status Updates**\n- **User Action:** (Automated during run)\n- **System Action (Backend):** As each agent Promise resolves or rejects, backend updates `research_run_agents.status` to `completed` or `failed`, sets `completed_at`, stores raw results in `research_run_agents.raw_results` (JSONB).\n- **Data Change:** `research_run_agents` rows updated.\n- **Feedback:** Supabase Realtime pushes update to frontend; agent card badge flips to green checkmark or red X.\n\n**2.3 – Deduplication Logic**\n- **User Action:** (Automated after all agents complete)\n- **System Action (Backend):** After `Promise.allSettled` resolves, backend aggregates all results from all agents. Deduplication pipeline:\n  1. Normalize grant URLs (strip UTM params, trailing slashes, lowercase).\n  2. Group by normalized URL — keep first occurrence.\n  3. For entries without URLs, group by `(funder_name_normalized, program_name_normalized)` using fuzzy match (Levenshtein distance ≤ 3 characters).\n  4. For each surviving unique grant, merge metadata from multiple agent sources (union of tags, highest confidence score).\n- **Data Change:** Only unique grants are INSERTed into `grants` or `research_results` staging table. `research_runs.deduplication_stats` JSONB column updated: `{ total_found: N, duplicates_removed: M, unique_saved: K }`.\n- **Feedback:** After run completes, summary banner: \"Research complete. Found [N] opportunities, removed [M] duplicates, saved [K] unique grants.\"\n\n**2.4 – Run Completion**\n- **User Action:** (Automated)\n- **System Action (Backend):** Updates `research_runs.status` to `completed` (or `partial` if some agents failed), sets `completed_at`.\n- **System Action (Frontend):** Dismisses loading state, refreshes grant list, shows completion toast.\n- **Data Change:** `research_runs` row updated.\n- **Feedback (All Success):** Toast: \"Research run complete. [K] new grants discovered.\"\n- **Feedback (Partial Failure):** Toast (warning): \"Research run finished with [X] agent errors. [K] grants saved from successful agents.\"\n- **Feedback (All Failed):** Toast (error): \"Research run failed. No grants were saved. Check agent configuration.\"\n\n**2.5 – Concurrent Run Prevention**\n- **User Action:** User clicks \"Run Research\" while a run is already in progress.\n- **System Action (Frontend):** Button is disabled with tooltip: \"A research run is already in progress.\"\n- **System Action (Backend):** `POST /api/research/run` checks for `research_runs` row with `status='running'` for this `organization_id`; returns HTTP 409 if found.\n- **Data Change:** None.\n- **Feedback:** Frontend catches 409; toast: \"A research run is already in progress. Please wait for it to complete.\"\n\n#### Edge Cases – Feature 2\n- **Single agent configured:** Parallel execution degrades gracefully to single-promise execution; no UX change.\n- **Agent times out:** Each agent promise has a 90-second timeout; on timeout, agent marked `failed` with `error='timeout'`.\n- **AI provider rate limit:** If provider returns 429, agent retries once after 5 seconds; if still 429, marks agent `failed` with `error='rate_limited'`.\n- **Empty results from all agents:** Run completes with `status='completed'`, deduplication shows `{ total_found: 0, duplicates_removed: 0, unique_saved: 0 }`; UI shows empty state: \"No new grants found. Try adjusting your Search Profile.\"\n- **Duplicate with existing saved grant (not just within-run):** Before INSERT, backend checks `grants` table for matching URL or funder+program combo scoped to `organization_id`; skips if already exists, increments `already_tracked` counter in stats.\n- **Network failure mid-run:** If server crashes, `research_runs.status` stays `running`; a cron job (Vercel cron, every 10 min) marks runs as `failed` if `started_at` is >10 min ago and `status='running'`.\n\n---\n\n### Feature 3: Search Profile Configuration\n\n**Overview:** A dedicated `/settings/search-profile` page where users configure their grant discovery parameters: funding type toggles, source filters, focus area weights, dollar range, geographic scope, eligibility pre-filters, and negative filters.\n\n#### Interaction-Level Decomposition\n\n**3.1 – Page Load and Profile Fetch**\n- **User Action:** User navigates to `/settings/search-profile`.\n- **System Action (Frontend):** Fetches `GET /api/search-profiles?organization_id=...`. If no profile exists, renders form with system defaults. If profile exists, populates form fields.\n- **System Action (Backend):** Returns `search_profiles` row for `organization_id`; if none, returns 404 (frontend uses defaults).\n- **Data Change:** Read-only.\n- **Feedback:** Form renders with current or default values. Loading skeleton shown during fetch.\n\n**3.2 – Funding Type Toggles**\n- **User Action:** User toggles on/off: Grants, Fellowships, Contracts, Loans, In-Kind, Other.\n- **System Action (Frontend):** Updates local form state. Each toggle is a styled checkbox/switch component.\n- **Data Change:** Stored in `search_profiles.funding_types` (text array).\n- **Feedback:** Toggle animates on/off immediately (local state).\n\n**3.3 – Source Type Filters**\n- **User Action:** User checks/unchecks source type categories (same enum as Feature 1) to include/exclude from research.\n- **System Action (Frontend):** Multi-select checkbox list.\n- **Data Change:** Stored in `search_profiles.source_type_filters` (text array of enum values).\n- **Feedback:** Checkboxes update immediately.\n\n**3.4 – Focus Area Weights**\n- **User Action:** User drags sliders or inputs numeric weights (0–100) for focus areas: Education, Health, Environment, Arts, Housing, Food Security, Workforce, Youth, Seniors, Disability, Immigration, Other. Total weight displays as a percentage sum.\n- **System Action (Frontend):** Renders a weight editor with sliders. Computes and displays total. Does NOT enforce that weights sum to 100 (weights are relative priorities, not percentages).\n- **Data Change:** Stored in `search_profiles.focus_area_weights` (JSONB: `{ \"Education\": 80, \"Health\": 60, ... }`).\n- **Feedback:** Running total updates in real-time as sliders move.\n\n**3.5 – Dollar Range Configuration**\n- **User Action:** User sets minimum and maximum grant award amounts using two number inputs or a range slider.\n- **System Action (Frontend):** Validates min ≤ max; formats as currency display.\n- **Data Change:** Stored in `search_profiles.amount_min` (integer, cents) and `search_profiles.amount_max` (integer, cents).\n- **Feedback (Valid):** Range display updates: \"$5,000 – $500,000\".\n- **Feedback (Invalid – min > max):** Inline error: \"Minimum amount cannot exceed maximum amount.\"\n- **Feedback (Zero max):** Max of 0 treated as \"no upper limit\" — stored as `NULL`.\n\n**3.6 – Geographic Scope**\n- **User Action:** User selects geographic scope: National, Regional, State-specific (multi-select state picker), Local/County, International.\n- **System Action (Frontend):** Renders scope selector; if \"State-specific\" chosen, shows a multi-select dropdown of all 50 US states.\n- **Data Change:** Stored in `search_profiles.geographic_scope` (JSONB: `{ \"type\": \"state\", \"states\": [\"CA\", \"NY\"] }`).\n- **Feedback:** Selected states shown as chips below the dropdown.\n\n**3.7 – Eligibility Pre-filters**\n- **User Action:** User configures organization eligibility attributes: 501(c)(3) status toggle, years in operation (min), annual budget range, staff size range, served population tags (multi-select free-text).\n- **System Action (Frontend):** Renders structured form section. Values used by research agent prompts to filter out ineligible grants.\n- **Data Change:** Stored in `search_profiles.eligibility_filters` (JSONB).\n- **Feedback:** Form updates locally.\n\n**3.8 – Negative Filters (Exclusions)**\n- **User Action:** User adds keywords, funder names, or geographic regions to exclude. Enters text into a tag-input field and presses Enter or comma.\n- **System Action (Frontend):** Renders tag-input component; each tag displayed as a dismissible chip.\n- **Data Change:** Stored in `search_profiles.negative_filters` (text array: `[\"religious\", \"capital projects\", \"Acme Foundation\"]`).\n- **Feedback:** Tag appears immediately as a chip. Remove button on each chip deletes it.\n- **Feedback (Duplicate tag):** Duplicate entry highlighted briefly, not added again.\n\n**3.9 – Save Profile**\n- **User Action:** User clicks \"Save Search Profile\" button.\n- **System Action (Frontend):** Validates form (min ≤ max, at least one funding type selected). Calls `POST /api/search-profiles` (upsert by `organization_id`).\n- **System Action (Backend):** UPSERT into `search_profiles` with all fields. Sets `updated_at=now()`.\n- **Data Change:** `search_profiles` row created or updated.\n- **Feedback (Success):** Toast: \"Search profile saved. Your next research run will use these settings.\"\n- **Feedback (Validation Error):** Inline errors on failing fields; save button remains disabled.\n- **Feedback (Network Error):** Toast: \"Failed to save. Please try again.\"\n\n**3.10 – Reset to Defaults**\n- **User Action:** User clicks \"Reset to Defaults\" link.\n- **System Action (Frontend):** Confirmation modal: \"This will reset all search profile settings to system defaults. Continue?\" On confirm, resets local form state to hardcoded defaults.\n- **System Action (Backend):** On save after reset, UPSERT with default values.\n- **Data Change:** `search_profiles` updated to defaults.\n- **Feedback:** Modal closes; form shows default values; save still required to persist.\n\n#### Edge Cases – Feature 3\n- **No profile exists:** All form fields show system defaults; saving creates new row.\n- **All funding types deselected:** Save blocked with error: \"At least one funding type must be selected.\"\n- **Amount_max set to 0:** Treated as unlimited; stored as NULL.\n- **Focus area weights all zero:** Research agents receive equal-weight prompt; no error.\n- **Negative filter list too long (>100 tags):** UI shows warning: \"You have [N] exclusions. Very long lists may slow research.\" Still allows save.\n- **Concurrent save from two tabs:** Last write wins (upsert); no conflict UI.\n- **State-specific scope with no states selected:** Save blocked: \"Please select at least one state for state-specific scope.\"\n\n---\n\n### Feature 4: Analytics Dashboard\n\n**Overview:** Replace the empty Outcomes page with a rich analytics dashboard built with Recharts, displaying pipeline funnel, success rate, dollars requested vs. awarded, source category pie chart, deadline heatmap, and agent activity.\n\n#### Interaction-Level Decomposition\n\n**4.1 – Page Load and Date Range Selection**\n- **User Action:** User navigates to `/analytics` (formerly `/outcomes`). Selects date range from presets: Last 30 days, Last 90 days, Last 12 months, All Time.\n- **System Action (Frontend):** Fetches `GET /api/analytics/summary?organization_id=...&from=...&to=...`. Renders loading skeletons for each chart panel.\n- **System Action (Backend):** Runs aggregation queries against `grants` and `research_runs` tables scoped by `organization_id` and date range.\n- **Data Change:** Read-only.\n- **Feedback:** Charts render after data loads. Date range selector updates URL query params for shareable links.\n\n**4.2 – Pipeline Funnel Chart**\n- **User Action:** User views the Pipeline Funnel chart panel.\n- **System Action (Frontend):** Renders a vertical Recharts `FunnelChart` with stages: Discovered → Researching → Eligible → Applied → Under Review → Awarded/Rejected.\n- **System Action (Backend):** `GET /api/analytics/funnel` returns counts per `grants.status` value.\n- **Data Change:** Read-only.\n- **Feedback:** Funnel renders with count labels and percentage drop-off between stages.\n- **Feedback (Empty):** Placeholder: \"No grants in pipeline yet. Start a research run to discover opportunities.\"\n\n**4.3 – Success Rate Metric Card**\n- **User Action:** User views the Success Rate card.\n- **System Action (Frontend):** Renders a large percentage metric with trend arrow (vs. previous period). Formula: `awarded / applied * 100`.\n- **System Action (Backend):** Returns `{ awarded: N, applied: M, previous_awarded: P, previous_applied: Q }` from `grants` table counts.\n- **Data Change:** Read-only.\n- **Feedback:** Metric card shows \"XX% Success Rate\" with green ↑ or red ↓ trend vs. prior period.\n- **Feedback (No applications):** Shows \"—\" with note: \"No applications submitted yet.\"\n\n**4.4 – Dollars Requested vs. Awarded Chart**\n- **User Action:** User views the financial comparison chart.\n- **System Action (Frontend):** Renders a Recharts `BarChart` (grouped bars) with months on X-axis, two bars per month: total `amount_requested` and total `amount_awarded`.\n- **System Action (Backend):** `GET /api/analytics/financials` aggregates `SUM(amount_requested)` and `SUM(amount_awarded)` grouped by month, scoped to org and date range.\n- **Data Change:** Read-only.\n- **Feedback:** Chart renders with dollar-formatted Y-axis. Tooltip on hover shows exact values.\n- **Feedback (No data):** Empty state with placeholder bars at zero.\n\n**4.5 – Source Category Pie Chart**\n- **User Action:** User views the Source Category breakdown.\n- **System Action (Frontend):** Renders a Recharts `PieChart` with slices per `source_type`, using the same color palette as Feature 1 badges. Clicking a slice filters the grant list below.\n- **System Action (Backend):** `GET /api/analytics/by-source` returns `{ source_type: string, count: number, total_amount: number }[]`.\n- **Data Change:** Read-only.\n- **Feedback:** Pie chart with legend. Hover tooltip shows source type name, count, and total dollars.\n- **Feedback (All uncategorized):** Pie shows single \"Other\" slice with note prompting categorization.\n\n**4.6 – Deadline Heatmap**\n- **User Action:** User views the Deadline Heatmap panel.\n- **System Action (Frontend):** Renders a calendar heatmap (custom Recharts `ScatterChart` or a lightweight calendar grid) showing grant deadline density per day for the next 90 days. Darker color = more deadlines.\n- **System Action (Backend):** `GET /api/analytics/deadlines` returns `{ date: string, count: number }[]` for grants with `deadline` in next 90 days.\n- **Data Change:** Read-only.\n- **Feedback:** Clicking a day cell filters grant list to show grants due that day.\n- **Feedback (No upcoming deadlines):** Shows empty calendar grid with message: \"No upcoming deadlines in the next 90 days.\"\n\n**4.7 – Agent Activity Chart**\n- **User Action:** User views the Agent Activity panel.\n- **System Action (Frontend):** Renders a Recharts `LineChart` showing research runs per day and grants discovered per run over time.\n- **System Action (Backend):** `GET /api/analytics/agent-activity` returns `{ date: string, runs: number, grants_found: number, duplicates_removed: number }[]`.\n- **Data Change:** Read-only.\n- **Feedback:** Multi-line chart with legend. Hover shows run count and discovery count per day.\n- **Feedback (No runs yet):** Empty state: \"Run your first research session to see agent activity here.\"\n\n**4.8 – Filterable Grant Table Below Charts**\n- **User Action:** User interacts with charts (click pie slice, click heatmap day); grant table below filters to match.\n- **System Action (Frontend):** Chart interactions update a shared filter state. Grant table re-fetches with applied filters.\n- **System Action (Backend):** Standard `GET /api/grants` with filter params.\n- **Data Change:** Read-only.\n- **Feedback:** Table updates; active filter chip displayed above table.\n\n#### Edge Cases – Feature 4\n- **Organization with zero grants:** All charts show empty states simultaneously; page-level message: \"Your analytics dashboard is ready. Discover your first grants to see data here.\"\n- **amounts are NULL:** Financial charts skip NULL rows; metric cards note \"(some amounts not recorded)\" if >20% of grants lack amounts.\n- **Very large dataset (>10,000 grants):** Backend aggregations use Supabase SQL aggregation (not row-level JS); response time target <2s.\n- **Date range with no data:** Charts render with empty state, not broken.\n- **Route change from /outcomes:** Redirect `/outcomes` → `/analytics` with HTTP 301.\n- **Recharts SSR:** All Recharts components wrapped in `dynamic(() => import(...), { ssr: false })` to avoid hydration mismatch.\n- **Export:** Out of scope for Tier 2 (see Scope Boundaries).\n\n---\n\n### Feature 5: Automated Eligibility Enhancement\n\n**Overview:** Add `match_percentage` column to the `grants` table, compute it via AI analysis, and auto-flag grants scoring above 80% as \"High Match\" with visual indicators.\n\n#### Interaction-Level Decomposition\n\n**5.1 – Schema Addition**\n- **User Action:** (Migration)\n- **System Action (Backend):** Add `match_percentage` (integer 0–100, nullable) and `eligibility_flag` (enum: `high_match`, `moderate_match`, `low_match`, `unscored`) columns to `grants`. Add `eligibility_notes` (text) column for AI rationale.\n- **Data Change:** `grants` table schema updated; existing rows get `eligibility_flag='unscored'`.\n- **Feedback:** None (migration).\n\n**5.2 – Automated Scoring on Grant Discovery**\n- **User Action:** (Automated — runs after research agent saves a new grant)\n- **System Action (Backend):** After deduplication and INSERT, an eligibility scoring job is triggered (via Supabase Edge Function or Next.js API route called async). The job:\n  1. Fetches the organization's `search_profiles.eligibility_filters` and the grant's full description.\n  2. Constructs a prompt asking the AI to score eligibility 0–100 and provide 2-3 sentence rationale.\n  3. Calls primary AI provider (Anthropic Claude).\n  4. Parses `{ score: number, rationale: string }` from response.\n  5. Sets `match_percentage=score`, `eligibility_notes=rationale`.\n  6. Sets `eligibility_flag` based on score: ≥80 → `high_match`, 50–79 → `moderate_match`, <50 → `low_match`.\n- **Data Change:** `grants.match_percentage`, `grants.eligibility_flag`, `grants.eligibility_notes` updated.\n- **Feedback:** No immediate user feedback (background job). Grant card updates when user refreshes or via Realtime.\n\n**5.3 – High Match Auto-Flag Visual Indicator**\n- **User Action:** User views grant list or grant detail.\n- **System Action (Frontend):** Grants with `eligibility_flag='high_match'` display a green \"⭐ High Match\" badge. `moderate_match` shows yellow \"~ Moderate Match\" badge. `low_match` shows no badge (or gray \"Low Match\" if user enables verbose mode). `unscored` shows \"Scoring...\" spinner if scoring is pending, or \"Unscored\" gray chip if no profile is configured.\n- **Data Change:** Read-only.\n- **Feedback:** Badges visible inline in all grant list rows and grant detail header.\n\n**5.4 – Eligibility Score Detail Panel**\n- **User Action:** User clicks on the match percentage badge or a \"View Eligibility Details\" link on Grant Detail page.\n- **System Action (Frontend):** Expands an inline panel (accordion or modal) showing: score as a circular progress indicator, AI rationale text, scoring timestamp, and \"Re-score\" button.\n- **Data Change:** Read-only (for display).\n- **Feedback:** Panel animates open; rationale text displayed.\n\n**5.5 – Manual Re-score**\n- **User Action:** User clicks \"Re-score\" button on the eligibility detail panel.\n- **System Action (Frontend):** Calls `POST /api/grants/[id]/rescore`. Disables button; shows spinner.\n- **System Action (Backend):** Re-runs scoring job for the specific grant with current search profile.\n- **Data Change:** `grants.match_percentage`, `eligibility_flag`, `eligibility_notes`, `eligibility_scored_at` updated.\n- **Feedback (Success):** Score and rationale update; toast: \"Eligibility re-scored.\"\n- **Feedback (Error):** Toast: \"Re-scoring failed. Please try again.\"\n\n**5.6 – Filter by Eligibility Flag**\n- **User Action:** User selects eligibility filter on grant list: All / High Match / Moderate / Low / Unscored.\n- **System Action (Frontend):** Updates URL params and refetches.\n- **System Action (Backend):** `GET /api/grants?eligibility_flag=high_match` filters by column.\n- **Data Change:** Read-only.\n- **Feedback:** List filters instantly; active filter chip shown.\n\n**5.7 – Sidebar Alert for New High Match Grants**\n- **User Action:** (Automated — see Feature 6 for alert system integration)\n- **System Action (Backend):** After scoring, if `eligibility_flag='high_match'` and grant is newly discovered (<24 hours old), INSERT into `alerts` table with `alert_type='high_match_grant'`.\n- **Data Change:** `alerts` table INSERT.\n- **Feedback:** Red badge on sidebar \"Alerts\" icon increments (Feature 6 integration).\n\n#### Edge Cases – Feature 5\n- **No search profile configured:** Scoring job skips; sets `eligibility_flag='unscored'` with note: \"Configure a Search Profile to enable eligibility scoring.\"\n- **AI