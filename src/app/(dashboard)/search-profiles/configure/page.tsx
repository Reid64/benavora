// Route entry for the Search Profile Configuration page. The implementation
// lives in the sibling SearchConfiguration module so it can also be embedded as
// a tab on the Research page (a page.tsx may not have extra named exports).
export { default } from "./SearchConfiguration";
